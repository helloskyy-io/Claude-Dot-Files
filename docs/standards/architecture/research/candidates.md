# Action candidates — the running list

**This file is the durable home for research action candidates. `synthesis.md` is rewritten every cycle; this is not.**

## Why it exists

A candidate surfaced in `synthesis.md` disappeared on the next research cycle, so its disposition — and the reasoning behind a rejection — went with it. Two consequences, both observed: candidates already ruled on were re-proposed in later cycles, and seven of them were parked on the standup tracker because no other surface would hold them, which the tracker's own rules forbid.

## The rule

> **Research creates and appends. Planning dispositions.**

- **Research** adds new candidates with a **stable ID**, never reused, and never renumbers an existing one.
- **Planning** sets `decision`; a later process sets `status`. Both carry reasoning in the Note.
- **A carried-forward candidate REUSES its original ID.** When a later cycle restates a live candidate, it is the same candidate — do not mint a new ID. (Cycle 4 restated C-007 and this file briefly carried it twice before the duplicate was removed.)
- **Nobody deletes a row.** A rejected candidate stays visible so it is not re-proposed — that is the whole point.

**The nine `reject` rows below were closed BY HAND on 2026-08-10.** They had been reporting as outstanding work on a surface whose to-do bit is `status`. **No automated writer closed them and none exists** — what should close a `reject`, and what closes the six unclosable `requires review` rows, is [issue #69](https://github.com/helloskyy-io/Claude-Dot-Files/issues/69), which stays open. This is a correction to the data, not a change to the model.

## Two flags, orthogonal — do not collapse them

| Flag | Values | Who sets it |
|---|---|---|
| **`decision`** | `ship` · `requires review` · `reject` · **blank = not yet triaged** | **`plan-sprint`, and only `plan-sprint`.** This is its triage output |
| **`status`** | `open` · `closed` | **A later process.** `plan-feature` when the item lands in a phase doc; the build that completes it |

## The three dispositions

Every candidate ends at exactly one of these. There is no fourth, and **leaving a row blank is not a disposition** — it means triage has not happened yet.

### `ship` — we have decided to do this

The work is **understood well enough to schedule.** Somebody could pick it up and know what "done" looks like without another decision being made first.

**`ship` does NOT mean done.** A shipped candidate stays `open` until something actually implements it, and neither `plan-sprint` nor `plan-tech-stack` does detailed phase design, so neither can close one on its own.

### `requires review` — only the operator can rule on this

The candidate is **not ready to be scheduled, and no amount of further automated work makes it ready.** An open question, an unresolved trade-off, a ruling that changes what the project believes. Shipping it would put a question mark in the plan; rejecting it would throw away a real finding.

**Where it goes:** `plan-sprint` writes a `D-NNN` row into [`direction.md`](direction.md) — the operator's file — and sets `decision` here to `requires review`. The row stays in this file as the pointer, and **`status` stays `open`** because the decision is outstanding, just outstanding with a human.

**It then leaves the automation's working set entirely.** A non-blank decision is not re-triaged, so it never comes back around. It surfaces at `/standup` as an open direction decision, the operator rules, and only then does the disposition change to `ship` or `reject`.

> **This is the release valve.** Without it, `ship` and `reject` are the only doors out, and an open question that deserves neither gets forced through one of them. That is exactly what happened on the first `plan-sprint` run: eleven unresolved questions were shipped into the sprint plan as milestones, which produced two sprints that build nothing.

### `reject` — we are not doing this

State why in the Note. **The reasoning is the point** — a rejection without it gets re-proposed on the next cycle by a run that cannot see why it was refused.

## Where a shipped decision lands depends on its size

A candidate large enough to need its own sprint section gets one, added by `plan-sprint` — that is the **entire** extent of `plan-sprint`'s implementation. **Most shipped candidates are not that size.** They belong inside an *existing* sprint or phase doc, placed by `plan-feature`, and `plan-sprint` does nothing with them beyond setting `decision`.

**A blank decision is not the same as `open`.** Blank means nobody has triaged it; `open` means the work is outstanding. Collapsing the two is what turns this file into a to-do list nobody agreed to — the failure that put seven untriaged candidates on the standup tracker.

**Every workflow that touches this file states its own portion in its prompt:** *the decision was made — implement your portion only.*

## Provenance note — read before trusting cycle-4 rows

Cycle-3 rows come from `synthesis.md` on `main` and are settled. **Cycle-4 rows come from PR #33, which is `HOLD - redispatch` and unmerged.** Its held item concerns a currency-tier marker that C-024's costing rests on, so cycle-4 entries are **provisional** and this file gets revised if that assessment finds the evidence thin.

---

## Cycle 3 — 2026-08-04

| ID | Candidate | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|
| C-001 | Heartbeat clause on `python_sdk_long_activities.md` — heartbeats free at the SDK layer, billable on Cloud | `temporal.md` | `reject` | `closed` | billing is moot on self-hosted. The *ceiling* survives for a different reason: every heartbeat is a persistence write on our own cluster |
| C-002 | Schedule the self-host-vs-Cloud decision | `temporal.md` | `reject` | `closed` | decided 2026-07-12, self-hosted. Recorded in `system-overview.md` § Deployment target |
| C-003 | Decide shard capacity before the first self-hosted workflow runs | `temporal.md` | `ship` | `open` | build-time one-way door, and now *more* relevant since we self-host. **Ship:** a parameter of the server stand-up, not a question — Temporal Integration phase-doc detail |
| C-004 | Override the default retry policy on every activity wrapping a paid API — Temporal defaults to unlimited attempts | `temporal.md` | `ship` | `open` | **Ship:** a concrete rule for every activity wrapper; Temporal Integration Stage B phase-doc detail |
| C-005 | Amend the Serverless Workers reading — Lambda caps an activity at 15 min | `dedicated_edge_routing.md` | `reject` | `closed` | k3s pods, not serverless |
| C-006 | Record that no first-party Claude ↔ Temporal runtime integration exists | `temporal.md` | `ship` | `open` | **Ship:** scopes the hand-rolled `claude_cli` integration as permanent rather than temporary. Natural home is `stack_reference.md`, which is human-in-the-loop — Temporal Integration phase doc meanwhile |
| C-007 | Correct differentiator #1 in `problem-statement.md` | `bernstein_capability_mining.md` §0.1 | `ship` | `closed` | `b9710d5` |
| C-008 | Replace differentiator #2 with the credential version | `dedicated_edge_routing.md` §7 | `ship` | `closed` | `b9710d5` |
| C-009 | Add the trust-domain claim — stronger than any scheduling-model difference | `bernstein_capability_mining.md` §0.2 | `ship` | `closed` | `b9710d5`, promoted to differentiator #1 |
| C-010 | Resolve the queue-axis conflict before Temporal Integration is planned | `dedicated_edge_routing.md` §4.1 | `requires review` | `open` | gates that sprint; addendum §A3. **`D-001`** — the resolution is a commitment against a vendored standard, and C-022's spike is the evidence it needs first |
| C-011 | Ship three cheap guards: credential expiry, false completion, safety-hook wiring test (~9 h) | `fleet_failure_modes.md` §7 | `ship` | `open` | **Ship:** three things built with a stated cost. Placed as a *Fleet Reliability* milestone |
| C-012 | Do NOT build an operator dashboard; build the blocked-work notifier | `operator_interface.md` §0, §6 | `ship` | `open` | the negative *is* the finding. **Ship:** both halves are concrete. Placed as a *Fleet Reliability* milestone |
| C-013 | Close the "evaluate Paperclip after Phase 4" gate and rewrite the item | `paperclip_assessment.md` §7 | `ship` | `closed` | `b9710d5` |
| C-014 | Adopt the eight cost-S, dependency-free interface/doctrine items | `bernstein_capability_mining.md` §5 | `ship` | `open` | case-by-case, not a bundle. **Ship:** §5 already names a home for each of the eight (rows 2/4/6/8/9/10/11/12) across five existing sprints plus one problem-statement amendment — phase-doc detail, not a sprint change |
| C-015 | Fix the missed-window assumption in the sprint plan — backwards, verified against the code | `fleet_failure_modes.md` §5.2 | `ship` | `closed` | `b9710d5` |
| C-016 | Design the stalled predicate as a three-way conjunction before workers are written | `paperclip_assessment.md` §4.4 | `reject` | `closed` | claims the failure mode is live here today; **unverified**. **Reject:** folded into C-029 — the three-legged taxonomy supersedes the two-way framing, and one design does not need two homes |
| C-017 | Decide dedupe granularity as a ruling, not a build | `paperclip_assessment.md` §4.3, §6 | `requires review` | `open` | explicitly not a pair to build both of. **ALREADY TRACKED at issue #41**, whose *Proposed next action* is this exact ruling — worktree scan vs comment recency vs a real lock, decided once for both the bash and Python paths. No `D-` row: one item, one home |
| C-018 | Drop any uniqueness framing on subscription-auth-at-the-edge | `paperclip_assessment.md` §4.6 | `ship` | `closed` | `b9710d5` |
| C-019 | Reconsider giving up cross-machine failover for *all* work | `dedicated_edge_routing.md` §5, §7 | `ship` | `closed` | **RULED via `D-002` 2026-08-07** — pin to the initiating edge; the reconsideration is closed |

## Cycle 4 — 2026-08-06 · PROVISIONAL (PR #33 unmerged)

| ID | Candidate | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|
| C-020 | Restate differentiator #1 on the credential, not the topology — state both halves together | cycle-4 pool | `ship` | `open` | **Ship — already applied** at `f2b80a6`; `problem-statement.md` §1 states both halves together |
| C-021 | Cost differentiator #1 with the self-hosted-CI-runner warning; rule on the laptop trust boundary | cycle-4 pool | `requires review` | `open` | **`D-003`** — the costing is a `problem-statement.md` edit and the boundary is a ruling; neither is automation's |
| C-022 | Settle whether a Temporal worker can be prevented from polling a queue it should not serve — **before** the pinned-edge design | cycle-4 pool | `ship` | `open` | bears directly on C-010. **Ship:** an empirical spike with an artifact, not a preference — run it before `D-001` is ruled. Temporal Integration phase-doc detail |
| C-023 | Record that self-hosted Temporal ships `noopAuthorizer` by default; the namespace is the only credential boundary offered | cycle-4 pool | `ship` | `open` | security-relevant to the two-server split. **Ship:** natural home is `stack_reference.md` § *What we do NOT use*, which is human-in-the-loop — Temporal Integration phase doc meanwhile |
| C-024 | Split the sprint plan's *Tools to Evaluate* into backbone comparators and edge runtimes | cycle-4 pool | `ship` | `open` | the row whose evidence the held item concerns. **Ship — already applied**; the sprint plan carries both categories |
| C-025 | Give "nearest neighbour" an axis: bernstein by architecture, OpenClaw by thesis | cycle-4 pool | `ship` | `open` | **Ship — already applied** at `f2b80a6`; `problem-statement.md` § *The nearest neighbor* carries the axis table |
| C-026 | Add OpenClaw as ASSESSED-and-closed, not as an evaluation gate | cycle-4 pool | `ship` | `open` | **Ship — already applied**; listed under *Edge runtimes*, ASSESSED with no evaluation gate |
| C-027 | Add Hermes under a new edge-runtimes heading | cycle-4 pool | `ship` | `open` | an addition, not a rewrite. **Ship — already applied**; listed under *Edge runtimes* |
| C-028 | Plan the three pre-worker recovery items as ONE design session | cycle-4 pool | `ship` | `open` | **Ship:** one session producing one restart-recovery contract. Placed as a *Fleet Reliability* milestone |
| C-029 | Adopt the three-legged liveness taxonomy: stalled / looping / stranded | cycle-4 pool | `ship` | `open` | supersedes C-016's two-way framing. **Ship:** placed as a *Fleet Reliability* milestone |
| C-030 | Unblock quota-headroom — derivable from observed cap-errors, no provider telemetry needed | cycle-4 pool | `ship` | `open` | **Ship:** the telemetry transfers, the rotation does not — we hold one subscription. Placed as a *Fleet Reliability* milestone |
| C-031 | No fallback queue: an unresolvable assignee PARKs with a typed event, never silently falls back | cycle-4 pool | `ship` | `open` | a negative design decision. **Ship:** it is queue topology, so Temporal Integration owns it — phase-doc detail |
| C-032 | Amend `workflow-scripts.md` § Composition — it justifies two mechanisms with an argument supporting only the first | cycle-4 pool | `requires review` | `open` | **`D-004`** — a standards amendment against two binding documents, and `architectural_standard.md` §3's `author ≠ judge` seam restates the same over-claim. Agents surface, humans write |
| C-033 | Withdraw or downgrade `case_against.md`'s D7 — contradicted by its own primary source | cycle-4 pool | `ship` | `open` | **Ship:** a bounded correction to one paper with a named source. Needs a `research-refresh` dispatch against `case_against.md`, not a sprint item |
| C-034 | Switch `review-pr` to a cross-family judge — self-preference bias is causally linked to self-recognition | cycle-4 pool | `requires review` | `open` | **`D-005`** — every genuinely cross-family option needs a second provider credential, which is a thesis-level commitment, not a one-line change |
| C-035 | Run E1b first — classify 30 PRs' disposition items to read out the judge's marginal yield | cycle-4 pool | `ship` | `open` | cheap, and it sizes C-034. **Ship:** reads existing logs and PR threads, no new dispatches. Added as a Continuous Process Improvement milestone |
| C-037 | Cross-machine failover — **a third option**: pin the credential, not the work | amends C-019 | `ship` | `closed` | **RULED via `D-002` 2026-08-07** — the proxy option excludes `claude-cli` by name, so it does not apply here |

## Evicted from the sprint plan — 2026-08-06

Nine ideas that lived in the sprint plan under *Future Ideas (Not Yet Committed)*, some since April. **None was ever committed to and none had been triaged** — a candidates list wearing a plan's clothes, which is exactly the shape this file exists to hold. Moved verbatim in substance; the plan file is not the place to park an idea.

| ID | Candidate | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|
| C-038 | Cross-project intelligence — aggregate CPI analysis across repos so a pattern in one informs another | sprint plan, Future Idea A | `reject` | `closed` | needs centralized log collection or report aggregation. **Reject: delivered.** Continuous Process Improvement's *Cross-repo reporting* milestone centralises with source-repo metadata, so patterns spanning repos are already visible |
| C-039 | Workflow composition / chaining — an orchestrator running a pipeline of workflows end to end | sprint plan, Future Idea B | `reject` | `closed` | **largely overtaken** — parent/child composition ships today. **Reject:** two live sprints cover it — Workflow Decomposition ships the composition and its standard, and a driver above parents is Autonomous Operation |
| C-040 | Project templates for `plan-new` — stack preferences and boilerplate decisions pre-made per project type | sprint plan, Future Idea C | `reject` | `closed` | **Reject:** presupposes a settled stack reference, and `stack_reference.md` is explicitly seeded-not-complete pending a research pass. Re-propose once it lands and two projects want the same boilerplate |
| C-041 | Team scaling — per-user config overrides, aggregated CPI, role-based workflow access, onboarding | sprint plan, Future Idea D | `requires review` | `open` | bears on the SkyyNet multi-participant question. **`D-006`** — the four items cannot be sized until the repo/SkyyNet boundary is ruled |
| C-042 | Metrics dashboard over the JSONL logs — cost trends, efficiency, failure types, agent utilization | sprint plan, Future Idea E | `reject` | `closed` | **tension**: cycle-4 evidence argues a blocked-work notifier over a dashboard. **Reject:** the tension resolves against it — `operator_interface.md` argues explicitly for the notifier, which C-012 carries |
| C-043 | `/rollback-cpi` — revert the last CPI PR and mark that pattern tried-and-failed | sprint plan, Future Idea F | `reject` | `closed` | grows in value as CPI automation increases. **Reject:** CPI changes are human-ruled and already reversible by `git revert` plus an append to `cpi-decisions.md`, and the operation has never been performed once. Re-propose the first time a CPI change is reverted and the log fails to record why |
| C-044 | SkyyCommand AI decision engine — the lean-agent + rich-skill pattern applied to VM placement | sprint plan, Future Idea G | `reject` | `closed` | out of this repo's scope; belongs to SkyyCommand. **Reject:** out of scope by the problem statement's own frame — this repo is the Jarvis edge, and VM placement is SkyyCommand's |
| C-045 | Prompt pattern library — capture phrasings that measurably produce better output | sprint plan, Future Idea H | `reject` | `closed` | **Reject:** *measurably* is the blocker — nothing here can read out a phrasing's marginal yield, and `cpi-decisions.md` already records shipped prompt changes with their reasoning. Re-propose once C-035 shows the fleet can measure a prompt change at all |
| C-046 | `plan-new` greenfield — handle `git init`, initial commit and remote setup rather than requiring a repo | sprint plan, Future Idea I | `reject` | `closed` | found during the 1Password vault manager test, 2026-04-11. **Reject: delivered.** `scripts/helpers/init-project.sh` runs `git init --initial-branch=main`, makes the initial commit and creates the GitHub remote; `plan-new` correctly requires an existing repo |

---

## Re-homed from GitHub Issues — 2026-08-09

Five open issues that were **proposals, not defects**, re-homed under [Architecture Standard § 4 Memory](../architectural_standard.md) — *"a PROPOSAL — capability that does not exist yet and would be added — goes to `candidates.md` and is NEVER an Issue, however clean its done-state looks."*

**Why they were filed as issues:** the selection rule in force when they were filed routed on *"did something change?"* and *"does it have a done-state?"* — and a proposal answers **no** and **yes**. `candidates.md` was explicitly excluded as a destination, so a proposal had nowhere else to go. That paragraph was corrected the same day this section was written.

**`decision` is deliberately blank on every row: these have been re-homed, not triaged.** Blank means triage has not happened, and `decision` is `plan-sprint`'s output alone. **Two of the five were filed by the operator's own session**, so this is not a dispatch-behaviour problem.

| ID | Candidate | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|
| C-047 | Gate `vendor-standards.sh --check` on the merge path so a local edit to a vendored MIRROR standard cannot merge green | issue #55 | | `open` | Nothing is broken — the check works and is correct; this ADDS a gate. Split ruled: the committed-checksum half (local-edit detection) needs no upstream and is shippable; the upstream-divergence half is blocked on a read-only credential for a private repo, and the Testing Standard's freshness clause forbids gating a check that cannot establish its own baseline |
| C-048 | Per-file granularity in `install.sh`'s symlink targets, so a `tests/` dir beside a hook does not land in the live `~/.claude/hooks/` | issue #63 | | `open` | Nothing is broken — PR #58 placed the tests successfully elsewhere and documented the divergence. Changes the documented 7-target strategy on every machine, so it is an architecture decision. Constrained by C-049; rule them together |
| C-049 | Point `~/.claude/*` at a worktree pinned to a ref instead of at the git working tree, making `git pull` the deploy step | issue #64 | | `open` | Today the working tree IS production: whatever branch is checked out is live, and an edit takes effect before commit. **The value is not mainly safety** — it is that when CDF grows to server + multiple edges, adding rings becomes a config change rather than a re-architecture. Costs instant prompt iteration. Same architecture surface as C-048 |
| C-050 | Derive declared roster and inventory counts rather than restating them in prose | issue #70 | | `open` | **The live instance is already fixed** (`README.md` said 10 workflows, there are 9 — corrected 2026-08-09); what remains is the check, which is capability. `architectural_standard.md:46` already states the principle — *"a constant restated in two places diverges silently"* — and nothing enforces it. **THIRD OCCURRENCE, with its cost measured, added by PR #71's refine pass — this is the recurrence the row was left open to collect.** The first was this file's own summary block (see the note below §Where things stand); the second, `exit-protocol.md` §4's *"nine rules"* prose beside a table whose reason COLUMN is machine-gated while its rule COUNT is hand-typed; the third, `file_structure.txt` restating that same count as *"the nine-rule fail-safe contract"* and **going stale for a whole pass** after R0 landed, in the file `CLAUDE.md` calls authoritative. Note the pattern the three share: in every case the *contents* had a gate and the *count* did not, so each divergence was invisible to a green suite. **The cost is now known: three hand-corrections across two passes on one PR, each found by a reader rather than a check.** **FOURTH OCCURRENCE, 2026-08-10, and this one was caught BY A CHECK — which is the most useful thing the row has collected yet.** The run closing [Phase 3](../../../development/memory-management-framework/phase3_typed_exit_record.md) restated that phase's fresh agreement figures in `roadmap.md`, and `testing/scripts/tests/unit/test_measurement_figures_are_cited.py` went red. **It fired by accident, not by scope:** that gate is scoped to *Phase 5* lines, and it only saw these because the roadmap's status header names both phases on one line — the figures the same run put on the **Phase 3 checkbox line were invisible to it**, and were removed only because the accidental catch prompted a re-read. So the gate proves the mechanism is real, gateable and cheap to check, **and simultaneously proves the hand-kept-scope defect this file keeps recording**: a check whose subject is one enumerated phase retires itself the moment the next phase produces figures. **That run's OWN two instances are fixed — both roadmap mentions are now pointers — but the roadmap is NOT clean, and saying so is the point of this entry.** Running the gate's own regex over the whole file instead of its Phase-5 scope returns **two pre-existing restatements it has never seen**: `roadmap.md:149` carries Phase 1 E3(b)'s `6 of 7` and `31 of 38`, and `:170` carries candidate 6's `8 archived PRs`. Both predate that PR (verified at branch point `799e0f9`) and both were left unfixed as outside a measurement-recording task's scope at the time this paragraph was written — **recorded here so the count is honest rather than implied clean.** **Both are now fixed, by this same PR's second correction pass: both roadmap mentions are pointers, and the widened gate (sixth occurrence, below) is green over the whole file.** They were the standing evidence for the candidate when this paragraph was written; that evidence is now historical rather than live, which does not weaken the candidate — the sixth occurrence's argument is that the mechanism keeps recurring, not that these two offenders remain, and it recurred twice more in this same PR after this paragraph was first committed. The candidate is unchanged and better evidenced — **derive or cite, never restate, and scope the check to the SHAPE rather than to a list of phases someone remembered.** **FIFTH OCCURRENCE, same PR, one pass later, and it is the one that should decide this row: the restatements were left behind by the FIX for the fourth.** PR #79's review pass found that flipping Phase 3's status had left copies contradicting the flip — `roadmap.md`'s trailing Phase-3 paragraph still read *"BUILT, NOT YET PROVEN … seven of eight"* four lines below the checkbox flipped to *"both halves done"*, and the phase doc's close-out still justified leaving standards-candidate 1 alone because *"requirement 6 has not yet done"* it, two bullets under the box asserting requirement 6 was met. **Both are fixed; neither was reachable by any gate.** They carry no denominator figure, so the shape-matching check this row proposes would not have caught them either — **a completion VERDICT is restated exactly like a count and nothing in the proposal covers it.** That is new information about the candidate's scope, not just another tally mark: *three independent reviewers each found one of these by reading, in a PR whose own diff was expanding this row about that exact mechanism.* **The candidate should be ruled to cover restated STATUS as well as restated COUNTS, or it will ship a check that leaves the more consequential half uncovered.** *(That paragraph originally reported the population as "three of the five places"; the true population is **nine** assertion sites — four in the phase doc, three in `roadmap.md`, one in `file_structure.txt`, one in `phase5_convergence_stopping.md` — and the count was itself a restatement of something nobody derived, inside this row. Corrected 2026-08-10.)* **SIXTH OCCURRENCE, and it is the fifth's own fix leaving survivors: the pass that WROTE the fifth-occurrence paragraph above missed two sites two files over.** `file_structure.txt:97` still read *"Phase 3 BUILT-NOT-PROVEN … 7 of 8 requirements"* and `phase5_convergence_stopping.md:155` still read *"Requirement 6 of Phase 3 is still open"* — the second being a **gate condition that the same PR fired**. All four review lenses on PR #79's second correction pass found them independently; none was reachable by any check. Both are now fixed. **Two things this row did not have before, and they are what a ruling needs:** (1) **the roadmap half of the proposal is now BUILT and shipped in PR #79** — `test_measurement_figures_are_cited.py` no longer scopes its roadmap sweep to *Phase 5* but enumerates every `phase*.md` on disk, so a figure restated beside any phase's name fails; measured on the tree at the time, it found exactly the two pre-existing offenders named above and **zero** false positives. (2) **The STATUS half is NOT gateable the same way, and this is measured rather than assumed.** A prototype keyed on line co-occurrence — a line naming a phase whose doc declares itself COMPLETE, plus a not-done token — returned **9 hits of which only 2 were real**: it fires on `roadmap.md:3` (which correctly says Phase 4 is unstarted), on a candidate note quoting historical text, and on a line saying a *gated item* still does not land. **So "restate a status" has no syntactic signature the way "restate a count" does**, and ruling this row `ship` on the assumption that one mechanism covers both would produce a gate people disable. The two halves should be ruled separately: the count half is done, the status half needs a different mechanism (most likely a declared canonical status field per phase doc, compared rather than pattern-matched).
**FOURTH THROUGH SEVENTH OCCURRENCE, and the first one where the mechanism was measured to be NON-CONVERGENT — PR #75.** Four consecutive passes over that PR each headlined this same defect and each fixed the instances it could see: the draft pass wrote *"a summary line is a copy, and a correction that lands in the source without landing in its copies has not landed"* in its own reflection and then violated it five times in the same PR; the refine pass fixed those five and re-introduced it wherever ITS OWN corrections had copies; the review pass found four more, including two in the roadmap — the file a later run reads to learn what is proven — and one where a document contradicted itself twelve lines apart; and the correction pass after that found four again, one of them a **withdrawn argument** rather than a stale number (a justification the phase doc explicitly deleted as motivated reasoning, still standing in the roadmap as support). **The frontier moved every time and never shrank, which is the evidence this row was left open to collect: fix-the-instances does not converge on this class, and that is now measured rather than argued.** **What DID close it, and it is the shape this candidate should be ruled on:** a check keyed on the class instead of the instances — figures with a denominator may appear only in the section that takes the measurement, and every other mention is a citation. Two such gates now ship for one corpus (`testing/scripts/tests/unit/test_measurement_figures_are_cited.py`), both mutated in both directions. They cost about a hundred lines and they close the mechanism for the documents they cover; **the candidate is whether that shape generalises to the roster and inventory counts this row was originally about**, which is a decision and not a task. Note also that the check does NOT ask any prose to be correct — no test can check that — only that a derived figure live in one place, which is what makes it implementable at all |
| C-051 | A marketing workflow — outward positioning, with inward feedback into the problem statement and roadmap | issue #28 | | `open` | Filed 2026-08-05 and parked ever since. Wholly unbuilt capability; it was never a defect on any reading |

---

### Added 2026-08-09 — re-homed from issue #36

| ID | Candidate | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|
| C-052 | Close the V2 port's ranked coverage gaps — eight named functions, ranked by risk, produced by PR #31 as its *"state the untested surface, do not build it"* deliverable | issue #36 | | `open` | **A proposal, not a defect: "we have no test for X" is not a defect in X** — nothing claims any of these eight is wrong. **It had already decayed unnoticed**, which is why it left the issue queue: item 2 (`render()`) is now covered by `test_shared_render_catches_a_digit_bearing_placeholder`, landed via unrelated work; item 8 (`pr_number_from_url`) has a happy-path test but still none for its deliberate raise; the issue's own caveat *"without closing #30 none of it runs on the merge path"* is moot — **#30 is closed and the gate ships**. Still at zero: **item 1 `paper_currency`** and **item 4 `pass_numbers`**. Items 5-7 remain blocked on an integration tier that does not exist; building the one `tmp_path` git-repo fixture unblocks all three at once. **TRIAGE NOTE — item 1 is separable and stronger than the rest, and should probably be ruled `ship` on its own merits:** `paper_currency` computes staleness verdicts a prompt instructs the model to obey without recomputing, and its own docstring records why it exists — *"A model once marked four of eight papers past window when one was… Correct arithmetic, wrong anchor, and the error was invisible because the reasoning looked sound."* Documented past failure, authoritative output, zero tests, and nothing is likely to touch `research_activities.py` soon — which is precisely the case where a targeted one-off beats opportunistic coverage. The other seven are better taken at the point of change |

---

### Added 2026-08-09/10 — placed by producing runs on PR #71, PR #75 and PR #74, per [`finding-routing.md`](../../finding-routing.md) §4

**All of them were surfaced by their PR's build passes, classified as proposals by its review pass, and had no permitted writer until `finding-routing.md` §4 landed.** §4's rule — *a producing run MAY place its own proposals, in its own PR* — is what makes these rows placeable, and the reasoning is that the disposal-chute argument reserving defect filing to the reviewer **does not reach a proposal**: capability that does not exist was never the run's scope, so it cannot be scope the run is offloading. `decision` is blank because triage has not happened; that is `plan-sprint`'s output alone.

**Each was re-verified as a proposal rather than a defect at placement time, not merely inherited from the review.** *(This heading and the two paragraphs above it named PR #71 and "all three" while the section had grown to six rows from two PRs — a count restated in prose beside the rows that derive it, which is C-050's own mechanism occurring in the file that holds C-050. It was then rewritten to state ID RANGES instead, and **that correction failed the same way one row later**: the ranges said C-053–C-056 and C-057–C-059 while the table ran to C-060, so the newest row sat outside its own section's stated scope — invisible to exactly the reader who trusts the heading. A range is still a restated count. The ID lists and the row count are now gone from the heading entirely, which is the only version of this sentence that a new row cannot falsify.)* The distinction that governs them: *"we have no check for X" is not a defect in X* — none of them names anything that behaves wrongly today.

> **A fourth candidate was considered and is DISSOLVED rather than placed; the verdict is recorded here so a later pass does not re-derive it.** The review that surfaced these three also proposed *a proposal-sweep step in the pipeline*, on the grounds that the only measured route from "a run surfaces a proposal" to "`candidates.md` holds it" was the operator sweeping GitHub Issues by hand — a route that does not exist for a proposal correctly **not** filed as an Issue. **`finding-routing.md` §4 closed that gap while the review was being written.** The end-to-end mechanism now exists and this PR is its first exercise: a decide-only reviewer surfaces a proposal, holds for redispatch, and the redispatched **producing** run places it, because that run has the commit-and-push the reviewer structurally lacks. The one residue — a reviewer with a proposal and no redispatch to hand it to — is already ruled by that document's own §8: *"a run declining to place a proposal because no actor is permitted to — that is a defect in this standard, and it is filed against this standard."* **A sweep would be machinery for a gap that has an owner.**

| ID | Candidate | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|
| C-053 | A dependency manifest for the repo | PR #71 | | `open` | **Re-verified at placement:** no `requirements.txt`, `pyproject.toml`, `setup.py` or `Pipfile` exists anywhere in the tree, and `temporalio`, `pytest` and `jsonschema` all import on this workstation while being declared nowhere. **Proposal, not defect: nothing is currently broken** — `exit_record.py` is stdlib-only *because its author checked*, which is the point. The failure mode is the next module that does not check: it works here and strands a worktree on the VM. Related to C-052 only in that both are "a check that does not exist"; the mechanism is different (undeclared runtime dependency vs untested function) so they are **not** one entry |
| C-054 | A version pin or startup probe for the CLI-shape facts the V2 path depends on | PR #71 | | `open` | Two facts are depended on and declared nowhere: **`result.permission_denials` exists**, and **`--json-schema` moves prose out of `.result`**. **Partially mitigated by PR #71 and explicitly not closed by it** — that PR split R1 so an absent-or-mistyped `permission_denials` reports `denials_unreadable` rather than `permission_denied`, which means a CLI shape change is now *visible in the per-reason rate* instead of masquerading as a fleet-wide safety trip. It is still detected only after the fact, once per run, by an operator reading reasons. A startup probe would catch it once, before the run. **Proposal: the probe is capability that does not exist**; the mis-binning it would have caused was the defect, and that half is fixed |
| C-055 | A format check on `docs/file_structure.txt` | PR #71 | | `open` | The map is authoritative per `CLAUDE.md`, and its only structural signal is a column-1 tree gutter — nothing parses it and nothing validates it. **PR #71's draft pass broke it on five lines and nothing noticed**; its refine pass repaired them, and the map is clean as of this commit (re-verified at placement: every body line starts with a gutter character). **So the instance is closed and the check is the candidate** — proposal, not defect. Same mechanism as C-050 (*derive declared counts rather than restating them*): both are "an authoritative artifact with no gate," and if either is ever ruled `ship` the other should be re-read alongside it. **AMENDED 2026-08-12 (PR #84, research-verify correction pass) — the premise above is REVERSED, and what remains of the candidate is now measured rather than argued.** `testing/scripts/tests/unit/test_file_structure_map_covers_the_tree.py` landed in PR #82 (`e7fadd9`, 2026-08-12) and **does** parse the map, so *"nothing parses it and nothing validates it"* no longer holds. It derives its population from `git ls-files` rather than from the map itself, parses the tree's indentation back into real paths rather than matching leaf names, and enforces two rules — where the map ENUMERATES it must enumerate completely, and where it ROLLS UP every tracked file must still be reachable through some named ancestor. The first half found **27 missing files** when it landed. **The RESIDUAL gap is DIRECTORY-shaped, and PR #84 measured it on itself rather than reasoning about it:** the gate is scoped to FILES, so its roll-up rule is satisfied by *any* named ancestor — and `docs/development/` is always one. The suite was **GREEN on that branch** while `docs/development/persistent-memory-protocol/`, an entire new component directory, appeared nowhere in the map, and while `docs/development/github-actions-integration/` was still named despite having had zero tracked files since `e397632` folded it into `autonomous-execution`. Both were corrected by hand in that PR, which is the finding: nothing would have caught either, and the gate reported success throughout. **So the map's `development/` section is a second hand-kept enumeration — of DIRECTORIES — nested inside the one the gate already derives, and nothing derives it.** The remaining capability is directory-level set-equality wherever the map enumerates directories, and the machinery already exists since the test reconstructs paths from indentation today. **The candidate is NARROWER than when it was filed, not closed** — and its pairing with C-050 still holds, now as the half-closed instance of the same mechanism |
| C-056 | A routing policy for a verification that could not be performed — should an unperformed check be DOWNGRADED, RECORDED, or only annotated? | PR #71 | | `open` | **The half of `flaky-gh-discards-a-completed-review` that is genuinely a policy question, separated from the half that was a defect.** PR #71 fixed the defect: the two `gh` reads behind the render↔record invariant are read-only, so they are retried, and on exhaustion the run completes on the already-persisted route with the check reported as UNPERFORMED. **What is left is unruled and is capability, not a bug** — today the verdict stands unchanged and the operator gets a loud note saying *"verify the posted block by hand."* The alternative is that a route which could not be verified is downgraded to `HOLD - needs-assistance` automatically. **Consequence of leaving it: an operator can act on a `MERGE` whose render↔record invariant never ran**, and nothing but a note distinguishes that from one that ran and agreed. **DURABILITY IS THE SECOND HALF OF THE SAME RULING, added by PR #71's refine pass and deliberately NOT a separate row** — the note lives in `ReviewResult.notes`, which reaches an operator watching the run and nothing else, while every other computed-arm signal (`routed_outcome`, `undetermined_reason`, `channels_agree`) is written by `append_parent_route` and is countable offline. So **no replay can say how often this invariant degraded**, which is the per-reason rate the phase exists to produce; the docstring was corrected from *"recorded"* to *"reported"* rather than left claiming a durability the code does not have. Recording it durably means a new stratum in `exit-protocol.md` §2, and whether to add one is the same decision as whether an unperformed check should downgrade — one ruling, not two, which is why this expands the row instead of opening C-057. **It has no phase home** — `grep -E 'retry\|flaky\|transient' phase4_fleet_migration.md` returns only an unrelated `api_retry` enum checkbox, re-verified at placement. It generalises past this one check: the same question applies to every parent-side verification the fleet grows, which is why it is a policy row rather than a checkbox on one function. Note that **Temporal's retry is the wrong granularity here and would make it worse** — an activity retry is a new attempt, i.e. a second 40-minute review |
| C-057 | An independent check that a finding's `disposition` is TRUE — the one false-convergence mode Phase 5 could not defend against | PR #75 | | `open` | **Phase 5 enumerated five ways a convergence signal can be fooled and built a check for four; this is the fifth, named as unmitigated rather than smoothed over.** `review-pr` authors `findings[].disposition`; Phase 5's predicate reads it; a reviewer that marks `fixed` what is not fixed makes the predicate agree, silently. **Re-verified as a PROPOSAL at placement, not a defect:** nothing behaves wrongly today — the computed signal agrees with the incumbent flag on every assessable block, and no fire has been observed to be early. **Both figures and their denominators are in [Phase 5 § Measurement](../../../development/memory-management-framework/phase5_convergence_stopping.md), and the second one must not be quoted bare: the self-reported early-fire counter is CIRCULAR** — it is scored on a verdict the same pass wrote, so it cannot move for the very mode this candidate is about — and the observed counter has an empty denominator. This row's proposal-not-defect classification rests on *nothing behaves wrongly*, which the agreement figure supports; it does **not** rest on the early-fire zero, and an earlier version of this Note quoted that zero as though it did — and *"we have no check for X" is not a defect in X*, the distinction the C-053/C-055 placements already turn on. **Consequence of leaving it:** the convergence signal stays permanently ungateable. Phase 5 declines to route on it *for this reason*, so the mode is not a risk today — it is the thing standing between the signal and the exit-criteria milestone [Autonomous Operation](../../../development/autonomous-operation/autonomous-operation.md) is waiting on. **Why it is not C-056 expanded:** C-056 asks what to do when a verification COULD NOT RUN. This asks for a verification that DOES NOT EXIST. Same shape at a glance, different mechanism and different remedy — and C-056's own Note draws exactly this line when it separates the policy half from the defect half. **No done-state today and no phase owns it — and the first version of this sentence was WRONG, which is recorded rather than quietly fixed.** It read *"`grep -n 'disposition' phase4_fleet_migration.md` returns nothing"*; the grep actually returns **three** lines, and the claim was written from expectation instead of from the command. Run at placement, the three are: `:55` naming the `disposition.md` prompt file as a migration surface, `:77` on selecting a `pr_review:` block by run nonce, and `:104` on a parity table. **None is a home for this** — all three are about *addressing and migrating* the record, none about *verifying that a disposition claim is true* — so the conclusion stands and its evidence did not. The lesson is the one this repo keeps re-learning: a grep quoted without being run is a citation to nothing, and a correct conclusion resting on a false premise is one edit from becoming a wrong one. The remedy is a decision, not a task — the only known mechanism is a second actor re-checking a sample of `fixed` claims, which is a second review with its own cost, and whether that is worth buying is an operator call **Lens verdicts (`finding-routing.md` §5 gate 4).** `/decide`: BUILD-LATER — the root cause is that one actor both writes and reads `disposition`, and the only remedy that touches the root is a second actor; every cheaper variant (a heuristic on finding text, a diff check) verifies the artifact rather than the claim and would be a fifth thing to trust. `/best-practices`: independent verification of a self-reported completion is standard where the reporter has an incentive (audit sampling, four-eyes release sign-off), and the standard form is a SAMPLE rather than a re-review of everything — which is what makes it priceable at all. Both point the same way: real, not urgent while nothing routes on the signal, and the sizing question is the sample rate. |
| C-058 | A durable store for Kind 2 records, so a typed record can outlive its invocation | PR #75 | | `open` | **Phase 5 is the first consumer that needs one, and it works around the absence rather than solving it.** Its predicate compares this pass's findings against every prior pass's; the current pass comes from the TYPED exit record, but a prior pass's typed record no longer exists — a Kind 2 record's lifetime is one parent invocation ([`exit-protocol.md` §1](../../exit-protocol.md), and Phase 3's to-do-bit ruling). So prior terms are parsed out of the durable `pr_review:` block as prose, inside the component built to stop routing on parsed prose. **Re-verified as a PROPOSAL:** the workaround is SOUND, not broken — the render↔record invariant makes this pass's block and this pass's record carry identical `(id, disposition)` pairs, so the prose term is a faithful copy of what the typed term was. Nothing is wrong today. **Consequence of leaving it:** every future cross-pass consumer re-implements the same prose parse, and each one re-decides which fields survive the round trip. Phase 5 needed two of the eleven envelope fields; the third consumer may need one the block does not carry, and will discover that after building. **Not an expansion of C-054 or C-056:** those are about a record that arrives wrong or a check that could not run; this is about a record that arrived correctly and then ceased to exist. **No done-state today, deliberately:** a durable Kind 2 store is a component-sized decision — where it lives, how it is keyed, how long it is kept, whether Temporal's event history already is one — and [Temporal Integration](../../../development/temporal-integration/temporal-integration.md) is gated on this component rather than the reverse, so it cannot be settled here. It is a candidate precisely because nobody should build it from inside a phase **Lens verdicts (`finding-routing.md` §5 gate 4).** `/decide`: DEFER-TO-TEMPORAL — the five-whys bottoms out at *no component owns durable Kind 2 storage*, and Temporal Integration's event history may already be one; deciding this from inside a phase whose own component gates that integration would be choosing a store before knowing whether one exists. `/best-practices`: durable execution engines persist activity results as event history precisely so a later step can read an earlier one's output, so the industry answer is *do not build a second store beside the workflow engine*. Both say: do not build now, re-ask when Temporal Integration is planned — which is the trigger this row is waiting on. |
| C-059 | A reader for the run-log `convergence` events, so the LIVE path has a denominator and not only the `gh` archive | PR #75 | | `open` | **Phase 5 emits a `{"type": "convergence"}` event on every `review-pr` dispatch and nothing reads it.** Its § Measurement figures all come from `replay_convergence_predicate.py` over the GitHub `pr_review:` block archive — a corpus that accrued at the same rate before this phase shipped, so the emission contributes nothing to the gate's denominator. The run-log events are a SECOND corpus carrying facts the replay structurally cannot produce: the `pass_not_evaluable` and `history_unreadable` rates (the replay hardcodes `pass_evaluable=True`, because no archived block has a typed record), and the typed term the live predicate actually reads. **Proposal, not defect** — nothing behaves wrongly; the phase's stated conditions are answerable from the archive today, and this would answer a different and better question. The shape already exists: `replay_completion_predicate.py` reads `.claude/logs/*.jsonl` the same way, and both event types carry `run_id` so `convergence` joins to `parent_route`. **Not filed as an issue** — it is capability that does not exist, and per `finding-routing.md` § 4 that makes it a candidate whatever its done-state looks like. **Lens verdicts (`finding-routing.md` §5 gate 4).** `/decide`: BUILD-WHEN-NEEDED — the five-whys bottoms out at *the emission was added a phase before its reader*, which was correct (a metric over a field nothing writes is a plan), so the remedy is the reader and the trigger is the first question only it can answer. `/best-practices`: instrument-then-read is the normal order and a write-only observable is a known smell only when it stays that way across releases; the guard is that the events accrue in the meantime and are joinable, which they are. |
| C-067 | A per-leg count of tests that actually EXECUTED, so a leg cannot report green having run nothing | PR #74 | | `open` | **`mutate.sh` accepts pytest's exit 0 as GREEN on all three legs, and an all-skipped run exits 0 having executed no test.** On leg 2 that is harmless — both readings land on "THE GUARD DID NOT FIRE", a refusal to certify. On legs 1 and 3 it is not. Leg 1 exists to establish *the guard is green before it is meaningful* and leg 3 to prove *green again, so the tree restored*; an all-skipped leg establishes neither and is accepted anyway. **Measured on this PR, not reasoned**: a subject whose tests skip while it is pristine and run once mutated yields leg 1 = nothing ran, leg 2 = RED, leg 3 = nothing ran, and the harness prints `✓ MUTATION DEMONSTRATED` over two legs that executed no test at all. **Proposal, not defect** — the behaviour is unchanged from `main` and predates the exit-2 work entirely; what PR #74 added was an exit-code table asserting this ambiguity was already closed, and that sentence is corrected in the same PR. What does not exist is the mechanism: **no pytest exit code carries an executed-test count**, so closing it means leaving the exit-code channel for a structured one (`--junitxml`'s `tests`/`skipped` attributes are the obvious candidate) and changing `run_leg`, which the PR #74 disposition placed explicitly out of scope. **Not filed as an issue** — it is capability that does not exist and would be added, and per [`finding-routing.md`](../../finding-routing.md) § 4 that makes it a candidate whatever its done-state looks like. **Not an expansion of an existing row**: no row covers `mutate.sh`, and the nearest neighbour — the `cpi-decisions.md:1125` entry on `mutate.sh` being pytest-only — is about which *frameworks* a leg can drive, not about whether a leg that ran can be told from one that did not. **Cost of leaving it:** the harness's own headline verdict rests on two legs it does not verify ran, in the one tool whose entire premise is refusing to certify what did not run.  *(Renumbered from C-060 on 2026-08-11 — that ID was taken by an unrelated proposal on `main` while this branch was open.)* |
| C-068 | A precheck that the RESOLVED pytest configuration is compatible with `mutate.sh`'s exit-code table, covering the `addopts` channel no environment variable can clear | PR #74 | | `open` | **`mutate.sh` classifies pytest's exit codes, and a target repo's own configuration can change which code pytest returns for the same event.** The table's load-bearing premise is *"pytest interrupts to 2 on a collection error unless `--continue-on-collection-errors`"*; with that flag in force a collection error returns **1**, which the harness reads as RED without ever consulting the exit-2 discriminator — issue #72's false certification, reached through configuration rather than through code. **Measured on this PR, not reasoned:** with `PYTEST_ADDOPTS=--continue-on-collection-errors` set, mutating the module under test into a `SyntaxError` printed `✓ MUTATION DEMONSTRATED` and exited 0 over a test that never ran. **The DEFECT half was fixed in PR #74 and is not this row**: `run_leg` now clears `PYTEST_ADDOPTS`, the false *"which this harness never passes"* justification is corrected, and `test_an_inherited_PYTEST_ADDOPTS_cannot_reopen_issue_72` pins it. **What remains is capability that does not exist:** `addopts` in the target's `pytest.ini` / `pyproject.toml` / `tox.ini` supplies the same flag and **cannot be cleared by an environment variable** — closing it means resolving the effective configuration before leg 1 (`--override-ini`, or parsing what `pytest --help`/`--co -q` reports) and refusing or adjusting when it conflicts. Nothing like that exists, and there is **no reproduction of the ini channel today** — this repo's own config sets no `addopts`, which is what keeps this a proposal rather than a second defect. That reproduction test is the discriminator used to split the two halves. **Not an expansion of C-060:** that row is about a leg that RAN but executed nothing, read out of a channel pytest does not expose; this is about the exit code itself meaning something different than the table assumes. Different mechanism, different remedy — but if either is ruled `ship` the other should be read alongside it, because both end at *the exit-code channel is too narrow to carry what this harness needs* and a `--junitxml` structured channel would answer both at once. **No done-state today and no phase owns it.** `mutate.sh` is repo tooling, not phase work; the harness's header markets it as portable (*"four independent runs on a sibling repo each hand-rolled this identical loop"*), so the ini channel becomes reachable the moment it is used outside this repo — which is the trigger, and it has not fired. **Cost of leaving it:** the one tool that decides whether every other guard is trustworthy can be silently mis-calibrated by a config file it does not read, and the failure is a false certification rather than an error.  *(Renumbered from C-061 on 2026-08-11 — that ID was taken by an unrelated proposal on `main` while this branch was open.)* |

### Added 2026-08-10 — placed by the producing run that closed [Phase 3](../../../development/memory-management-framework/phase3_typed_exit_record.md), per [`finding-routing.md`](../../finding-routing.md) §4

**Surfaced while recording Phase 3's run set, and it is the reason that section can state what it states.** The run set was briefed as eight dispatches; reconciling the logs found a **ninth** that the instrument never recorded, and the gap it exposes is capability rather than breakage.

| ID | Candidate | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|
| C-060 | Recording the shadowed pair when the completion gate fails, so the agreement denominator is not conditioned on the prose channel succeeding | PR #79 | | `open` | **Phase 3's run set is nine live dispatches and the instrument recorded eight.** The ninth emitted a valid typed record and no prose `VERDICT:` line, so `run_claude`'s completion gate raised **before `route()` and before `append_parent_route`** and the pair was never compared. **The run's identity, turn count, exact last text block and root cause live in [step 7b's row-1 account](../../../development/memory-management-framework/phase3_typed_exit_record.md#step-7b--the-run-set-nine-live-dispatches-and-the-three-numbers-it-yields) and are deliberately not retyped here** — an earlier version of this Note carried its own copy of them, which is this file's own C-050 mechanism occurring in the file that holds C-050, and it went stale the moment the row-1 cause was corrected. **Consequence:** `channels_agree` can only ever be written on runs where the incumbent prose channel already succeeded, so the instrument built to weigh prose against the typed record is **structurally blind to prose's own failure mode**, and its agreement figure is conditioned on the very thing it is measuring. One dispatch in nine hit this, so the condition is not vacuous — **though that is not an incidence rate**, since the proximate cause was removed inside the measurement window and did not recur — and **no larger N removes the blindness itself** — [Phase 4](../../../development/memory-management-framework/phase4_fleet_migration.md) widening the denominator inherits the same blindness, which is what makes this worth placing rather than noting. **Re-verified as a PROPOSAL, not a defect — and the supporting sentence needed correcting to survive that re-verification.** It read *"nothing behaves wrongly"*, which is false about the motivating instance: **a real defect produced it — Phase 3's own Stage 6 ordering put the record's tool call last, and a tool call is a terminal action — and it was fixed on `main` by `fb85c3e` thirteen minutes later.** *What makes this a proposal is that the OBSERVATION GAP survives that fix.* The completion gate can raise for any reason — an early stop, a truncated turn, a model that stops after any terminal-feeling action — and on every such path `append_parent_route` structurally cannot write a route the parent has not computed. The fail-loud contract itself worked exactly as designed: the run failed, an operator saw it, the work was redone on the next dispatch. **So the row is stronger than the version it replaces, not weaker:** it no longer rests on the claim that the instance was causeless, which was the one claim in it that a reader could falsify. The remedy is a **new observation point** on the gate-failure path, recording that the child produced a record while the prose channel did not; that is capability that does not exist, and *"we have no measurement of X" is not a defect in X*, the same line the C-053/C-055/C-057 placements turn on. **Not C-056 expanded, and the distinction is the stage:** C-056 asks what to do with a verification that could not run **after** the route was persisted; this is a comparison that never started because the route was never computed. Different mechanism, and C-056's remedy vocabulary (annotate / record / downgrade an existing note) does not produce this one. **No done-state today:** what a parent should record when its child fails the *incumbent* contract is unruled, and making it durable needs a stratum for *"the run did not get far enough to route"* that [`exit-protocol.md` §2](../../exit-protocol.md) does not have — the same shape as C-056's blocker, which is why both wait on the same ruling. **Lens verdicts (`finding-routing.md` §5).** `/decide`: **BUILD-WITH-PHASE-4** — the five-whys bottoms out at *the incumbent channel's emission is a prompt instruction with no schema enforcement* (step 8 already names it as the weaker half), and the instrument that would measure that weakness sits **downstream of the gate that enforces it**; the remedy only earns its stratum where the denominator is meant to become a rate, which Phase 3 explicitly declines to be. `/best-practices`: this is **survivorship bias** in a channel A/B comparison — a named measurement-design defect — and the standard remedy is to record at the point of **assignment** rather than of **completion** (the intention-to-treat discipline), which maps directly onto recording the pair when the dispatch returns rather than when it clears the incumbent's gate. Both say the same thing: real, cheap in principle, not urgent while nothing reports a rate, and **load-bearing the moment Phase 4 states one**. **Nor is it step 4's already-fixed gap wearing a new name, and the phase doc's [step 7b](../../../development/memory-management-framework/phase3_typed_exit_record.md#step-7b--the-run-set-nine-live-dispatches-and-the-three-numbers-it-yields) now rules that distinction rather than leaving it to a reader:** step 4's was reachable by moving an append that already ran on the path, so it was repaired on the spot; this one sits upstream of any route being computed, so there is nothing to move. Same principle, different reach — which is what separates a repair from a proposal, and the reason a reader who spots the resemblance should not read this placement as motivated. |
| C-061 | Research and design a memory PROTOCOL — the record types, their stores, their wire formats and the rule for adding a fourth — as one designed set rather than three arrived at separately | operator, 2026-08-10 | | `open` | **FEATURE / RESEARCH REQUEST, not a defect — nothing is broken.** The framework works and is shipping. But it was researched as a TWO-channel design (`dual_channel_outcome_records`), a third type is being added by inference, and the durable record alone now spans **five surfaces, two substrates and two different to-do mechanics** — each defensible alone, none designed as a set. **The research already cites OpenTelemetry but only for record SHAPE (§2.5, attributes-vs-body); its metrics model — the industry's actual answer to where measurement data lives — was never opened.** Measurement is not in the *deliberately NOT commissioned* list either, so it was never considered and rejected; nobody asked. Operator's framing: *a select well-designed few that cover the bases and allow for future growth*. Ship the current design at 80% first, then refine through this. |
| C-062 | Extending the measurement-figures gate to cover QUANTITIES, so a memory or size figure restated in the roadmap is caught the way a count already is | PR #80 | | `open` | **The repo already built the right-shaped remedy for restated figures and keyed it one class too narrowly.** `testing/scripts/tests/unit/test_measurement_figures_are_cited.py` sweeps every roadmap line mentioning a phase and fails when a figure is restated instead of cited; its own docstring records that four consecutive passes each fixed what they could see while the frontier never shrank, and concludes *"Changing what the check keys on does."* **Its `FIGURE` pattern (`:45-51`) matches `N of M`, `~N%`, mutation counts and `N assessable/scorable/archived` — and no quantity with a unit.** **Measured on the run that placed this row:** `roadmap.md` carried `666 MiB` on a line that also mentions Phase 6, so it sat squarely inside the gate's sweep and passed clean; the restatement was caught by a human reviewer instead. **Consequence of leaving it:** the gate reads as covering restated figures and covers only the shapes someone happened to enumerate, which is worse than an absent gate — a green check is taken as evidence the class is handled, so the next quantity restated into a summary is found the same way this one was, by luck. **PROPOSAL, not a defect, and the line matters here:** nothing behaves wrongly — the gate does exactly what its pattern says, and every figure shape it was written for is still caught. This asks for coverage that has never existed, which per [`finding-routing.md` § 4](../../finding-routing.md) makes it a candidate whatever its done-state looks like. **Deliberately scoped to quantities and NOT to the general class.** The same run also found retracted *reasoning* restated in a summary (a roadmap Key Decision stating a growth rule its phase doc rules wrong), which is the same failure one abstraction up — but a regex cannot key on that, and proposing a gate for it would be proposing a thing nobody knows how to build. **The narrow half is a units alternation on an existing pattern; the wide half is an open question and is named here rather than bundled in.** **Done-state today: yes** — extend `FIGURE` with `\b\d+(\.\d+)?\s*(MiB|GiB|MB|GB|KiB)\b`, then fix whatever the newly-red sweep surfaces. That second half is the unbounded part and is why this is a candidate rather than a one-line fix folded into the PR that found it. **Lens verdicts (gate 5): `/decide` — not dissolved.** The upstream question is *why does a figure get restated at all*, and the answer is that prose cannot derive; the gate is the only place that catches it. **`/best-practices` — SURVIVES.** Assert-on-derived-values is standard practice and the repo already has the right-shaped instrument for counts. **Not resolved into a known fix:** extending the gate requires deciding what counts as a quantity in prose, which is design work rather than a line change — the producing run flagged that a reviewer could reasonably say it should have just fixed it, and this is the adjudication: it should not have. |
| C-064 | A bounded wait and an auth discriminator on the CLI invocation, so an authentication failure is distinguishable from a kill and does not sit through ten retries unobserved | PR #81 (Phase 4) |  | `open` | **Phase 4 step 3 asks a run to record what the `system/api_retry` error enum is used for, or state that nothing reads it. Nothing reads it — `grep -rn "api_retry\|rate_limit_event" scripts/ config/ --include=*.py --include=*.sh` returns ZERO on 2026-08-10, unchanged since Phase 1 — and stating that honestly means stating what it costs.** [Phase 1 E1(e)](../../../development/memory-management-framework/phase1_measure_the_channel.md) measured auth failure and `SIGTERM` as **indistinguishable in the result envelope**: both give `subtype: error_during_execution`, `terminal_reason: aborted_streaming`, `stop_reason: null` and the same opaque `[ede_diagnostic]` string in `errors[]`. The **only** discriminator is the `system/api_retry` stream events (`error_status: 401`, `error: "authentication_failed"`), which every dispatch already writes to `$LOG_FILE` and no code opens. **The second half is the one with teeth:** neither auth run exited on its own — **both returned 124, which was the measurement's own `timeout(1)` wrapper, after 9 retries against `max_retries: 10`** — and **nothing in `run-claude.sh` wraps the CLI in a timeout**. So the fleet's behaviour on an auth failure is a long unattended wait whose upper bound is the CLI's retry policy rather than anything this repo chose. **PROPOSAL, not a defect, and the distinction is which sentence you read.** Nothing behaves *wrongly*: the run does terminate, the completion gate fails it loud, and no wrong route is taken. What does not exist is (a) any reader of the discriminator and (b) any bound this repo owns on the wait. Both are capability that has never existed, which per [`finding-routing.md` § 4](../../finding-routing.md) makes this a candidate whatever its done-state looks like. **Deliberately NOT sized as one item, and the split is the useful part.** The timeout is small and has a done-state today — a `timeout` around the `claude` invocation in `run-claude.sh`, its duration taken from the observed retry envelope rather than guessed. Reading `api_retry` is larger and touches the fail-safe contract: it needs a stratum for *why the process died* that [`exit-protocol.md` §2](../../exit-protocol.md) does not have, which is the same blocker C-056 and C-060 both wait on. **A run shipping the timeout alone does the valuable half; a run shipping the reader alone adds an observable nobody routes on, which is the admission failure Phase 6 exists to stop.** **Not an expansion of C-060:** that is a comparison that never started because the gate raised; this is a failure mode the envelope cannot name at all. **Not filed as an issue** — the default is against filing, and its second half has no done-state today. **Why it surfaces HERE rather than in Phase 1, which measured it:** E1(e) surfaced it *to the operator in the PR body*, which `engineering-quality.md` names as the one placement that is never valid, and it has sat in a completed phase doc since 2026-08-08 with nothing pointing at it. |



### Added 2026-08-11

| ID | Candidate | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|
| C-065 | A stated rule for load-bearing PROSE: a comment or prompt sentence asserting how ANOTHER file behaves either names the check that holds it, or is not written | PR #81 (Phase 4, refine pass) |  | `open` | **DISTINCT FROM C-050 AND THE SPLIT IS THE USEFUL PART. C-050 is about a figure RESTATED in two places; this is about a claim ASSERTED once and false.** Measured on one PR: four wrong load-bearing prose claims, and they split cleanly into the two classes. C-050's class — the phase doc's `is_error` and `parse_verdict` citations, dead line numbers and counts moved by the PR's own diff; and `review_pr_workflow`'s docstring naming FIVE deferred pure functions with nothing enforcing the set. **The other class is what this row is for, and neither a restatement gate nor a resolve-check can see it:** (1) `review_pr_helper.completion_ref_mismatch_note` told an operator, at the moment of the failure, that the field's number *"flows into `gh pr view`, `gh pr comment` and `--pr`"* — nothing in the tree reads `completion_ref` for anything but that comparison, so a reader would have believed a cross-PR WRITE was averted and stopped looking, while the derivation that does reach `gh` was unguarded; (2) `disposition.md` told the child that a quoted prior block *"is now correctly ignored either way"* — `thread_snapshot` keeps only the LAST block per comment, so a quoted block placed below the child's own displaces it entirely and no nonce rescues it. **Both were syntactically fine, both cited real files, and both were false about what those files do.** **Consequence, and it is why the class is worse than the restatement class: a false statement in a file readers are trained to trust actively STOPS the next person checking** — a docstring is read instead of the code it describes, and a prompt sentence is followed instead of the parser it describes. **PROPOSAL, not a defect.** Both instances are fixed in the PR that found them; what does not exist is any rule or check covering the class. **Done-state, and it is deliberately the small half: a stated convention in the documentation standard** — *a comment asserting the behaviour of code in another file names the test that holds the claim, or states the claim as of a date, or is not made* — plus the observation that the repo ALREADY applies this well where it applies it at all (`SINGLE_CONSUMER_PARENT_MODULES`, the AST gates, the negative controls) and has simply never decided the boundary between prose-that-is-a-control and prose-that-records-a-measurement. **NOT proposed as a regex gate, and that is a finding rather than a gap:** C-062's row records that a prototype for the adjacent status class returned 9 hits of which 2 were real, and a cross-file behavioural claim has strictly less syntactic signature than a status. Proposing a gate here would be proposing a thing nobody knows how to build. **Not an expansion of C-050:** C-050's remedy is *derive rather than restate*, which is unavailable here — there is no second copy to derive from, only a claim about another file. Applying C-050's remedy to this class would produce nothing.  **AMENDED FROM PR #83 (research_minor, refine pass) — three more instances, and they identify a SUB-CLASS with a cheaper remedy than the general rule.** All three are **stale CROSS-REFERENCES**: a sentence naming a location in another document that was correct when written and was falsified by a later edit somewhere else. (1) `research_activities.upstream_block()` ended with *"Your sizing in Stage 2 must state which topics upstream already covers"* — true while `research_write` was its only consumer, false the moment `research_write_minor` injected the same block into a prompt with no sizing stage, which then ordered an assessment that prompt separately forbids. (2) `research_verify/prompts/verify.md` opened Stage 1 with *"For each paper written or updated in Stage 3"* — a reference to a DIFFERENT workflow's stage numbering, which the minor cycle does not share. (3) Two new workflow docstrings cited *"Research Standard §3"* for the sizing rubric, which lives in **§2** — and the same PR's own C-066 row cited §2 correctly, so one artifact disagreed with itself. **Why this sharpens rather than merely corroborates C-065:** the general class ("a claim about how another file behaves") was judged un-gateable — the row itself records that a prototype for the adjacent status class returned 9 hits of which 2 were real. **This sub-class is different, because both halves are syntactically distinctive and mechanically checkable:** a `§N` citation resolves against the target document's own headings, and a `Stage N` reference resolves against the cited prompt's `## Stage N:` headings. Neither needs to understand behaviour — both are name-resolution, the same shape as the link-checking this repo already does. **That makes a gate proposable here where it was not proposable for the parent class**, and it is offered as a narrowing of C-065's done-state rather than as a separate row. All three instances were fixed in PR #83. |
| C-066 | A differential baseline the mutation probe can establish even when the pre-mutation import fails, so it answers instead of abstaining | PR #74 | | `open` | **The probe resolves exit 2 DIFFERENTIALLY — `HARNESS_ERROR` only when `$FILE` imported cleanly BEFORE the mutation and does not now. When it cannot establish that baseline it abstains to RED and says so, rather than asserting a cause.** That abstention is correct and is deliberately not a defect: asserting a cause it cannot establish is how #72's false certification happened. **What has no home is the PROPOSAL for removing the ambiguity** — its only record was prose inside a 728-line shell script, which `plan-sprint` does not read, so it died at merge. `testing/README.md` claimed all three deliberately-open ambiguities had a candidate; two did (C-060, C-061) and this one did not. **Lens verdicts (gate 5): `/decide` — not dissolved**, the upstream question is whether a baseline can be established at all without importing, which is a real unknown rather than a framing error. **`/best-practices` — SURVIVES**: differential mutation testing is standard, and an abstention rate is a known quality metric for one. **Not resolved into a known fix** — nobody knows the mechanism yet, which is precisely why it is research rather than a task. |
| C-069 | A COARSE RESOURCE SERIES beside the peak and the mean, so a memory spike has a timestamp and can be attributed to something | PR #82 (Phase 6) |  | `open` | **The largest measurement this fleet has ever taken cannot be attributed to anything, and the instrument is why.** `_Sampler` polls every 2 s and keeps a running max, a running mean and nothing else — the series is discarded as it is read. **Measured 2026-08-11 by `python3 scripts/helpers/measure/replay_run_resources.py` over 17 fleet records** — a dated reading; [Phase 6 § Measurement](../../../development/memory-management-framework/phase6_read_what_it_writes.md) is the live one and this row does not track it:** one `build-draft` run peaked at **12.891 GiB** of anonymous memory against a **0.614 GiB** mean over 1761 samples, while **neither knob explains it** — it spawned **0** sub-agents and pulled **0.61 MiB** of tool results, both at or below the corpus median, and the next-highest peak in the corpus is **0.552 GiB**, a 23× gap. **Consequence:** figure 3 exists to separate sub-agent COUNT from content VOLUME, and the one run where the answer would matter most is a run where the answer is *neither*, with no way to ask *when*. A 21× peak-to-mean ratio says the spike was brief; nothing says which minute, which tool call, or whether it recurs. **The next occurrence produces exactly the same unattributable number**, which is the shape of the 2026-08-10 livelock one level down — an event nobody could locate. **PROPOSAL, not a defect:** the sampler does precisely what it says, every emitted field is correct, and no figure this phase built is wrong because of it. What does not exist is retention, which per [`finding-routing.md` § 4](../../finding-routing.md) makes it a candidate whatever its done-state looks like. **PRICED, because the phase brief asked for a price rather than a silent design-around.** The cheap arm has a done-state today and is genuinely small: keep a bounded ring of `(offset_seconds, anon, total, pids)` at the existing 2 s cadence and emit it only when `peak_anon` exceeds some multiple of `mean_total` — a 30-minute run is ~900 samples, and at four small integers that is well under 30 KB of JSONL for the rare record that earns it, with every other record unchanged. **The expensive arm is the one that makes it a candidate rather than a fix: WHERE the series lives.** A per-sample series is a different retention class from a per-run record — it is the first thing on this surface whose size scales with run LENGTH rather than with run COUNT, and the run log is machine-local, gitignored and pruned by nobody. **That is a Kind 3 retention question the surface has no rule for, and inventing one inside a build PR is exactly the "arrived at separately" failure C-061 was raised to stop.** **Not an expansion of C-061:** that asks for the record types and their stores to be designed as a set; this is one concrete retention question that C-061's design would have to answer, and it is recorded so that design has a real instance to answer rather than a principle. **Not a defect against `resource_telemetry`:** its own docstring already states the limitation it has (`peak_anon` is sampling-dependent), and this is the adjacent limitation it does not state — recorded here, and now stated at the module, in the same change. |
| C-071 | Naming research cycle SHAPE in the Research Standard, so §2's sizing rubric can route a question to the one-paper cycle instead of only telling a run how many topics to pick | PR #83 (research_minor) |  | `open` | **PROPOSAL against a VENDORED (MIRROR) standard — it is placed here rather than made, because `docs/standards/research/` must not be edited in this repo and an amendment goes upstream to MDC-Master-Planning then re-vendors.** **The consequence, which is the reason this is a finding and not a note:** §2's rubric (`research_standard.md:53`) describes topic COUNTS and nothing else — Small is *1–2 topics*. It has no vocabulary for the SHAPE of a cycle, so a run reading it to decide how much research to do can size a question correctly at Small and still emit `topics.md`, a fan-out, a synthesis and a verify pass over all of it. **That is not hypothetical: it is exactly what happened.** A portfolio-direction question was assessed as Medium, produced five papers and a synthesis at ~3.5 hours, and the operator called it *"mass overkill"* — and the important half is that **sizing it correctly would not have fixed it**, because the architecture below the rubric does not change with the topic count. **PR #83 built the missing shape (`research_minor`: one paper, no `topics.md`, no synthesis, same `research_verify` and same `research-critic` gate) and deliberately did NOT touch the standard.** So the capability now exists and the document that a run consults to choose it does not mention it — a run following the standard faithfully will never reach the cheaper cycle. **Done-state today: yes, and it is small** — a row or a sentence in §2 stating that a single-concern question with one destination is answered by a one-paper cycle, alongside the existing count rubric. **The bounded part is deliberately excluded from the proposal:** whether the bands themselves should be re-cut is a separate question with its own evidence requirement, and §2 already says a single overrun is an observation rather than evidence. **NOT a defect:** nothing behaves wrongly — the standard is silent, not incorrect, and every §3 per-paper obligation is preserved in full by the new cycle, so a paper it produces is standard-conforming today. **Not an expansion of an existing row:** no candidate concerns the Research Standard's own sizing text. **Not filed as an issue** — it is capability that would be ADDED to a document, which per [`finding-routing.md` § 4](../../finding-routing.md) makes it a candidate whatever its done-state looks like, and the default is against filing.  *(Renumbered from C-066 on 2026-08-11 — that ID went to an unrelated proposal on `main` while this branch was open.)*  *(Renumbered from C-069 on 2026-08-12 — PR #82 merged first and took that ID.)* |
| C-072 | Promote the research family's shared RULES footer to one fragment, so the four prompts that carry it stop drifting apart | PR #83 (research_minor, refine pass) |  | `open` | **The consequence, and it is already happening rather than predicted: the four copies have DIVERGED.** `research_write/prompts/write.md`, `research_verify/prompts/verify.md`, `research_refresh/prompts/refresh.md` and the new `research_write_minor/prompts/write_minor.md` each end in a near-identical RULES block — Bash-CWD, Re-Read-before-Edit, large-file reading, parallel gather, the git-status check. They are not identical: `verify.md`'s relative-paths bullet is one line where `write.md`'s is a paragraph, and `refresh.md` carries an abbreviated set. **So the four workflows are being given materially different operating rules for the same hazards, and nothing anywhere records that this was a decision.** **The rule is already binding and already crossed:** [`temporal_standard.md` §10.1 rule 3](../../temporal/temporal_standard.md) — *"promoted OUT of a workflow's folder to the parent level if and only if MORE THAN ONE workflow uses it — consumer count decides, never taste"* — echoed for `.md` fragments specifically in [`claude-dot-files-addendum.md` §A4](../../temporal/claude-dot-files-addendum.md). The threshold was crossed at three copies, before PR #83 existed; that PR added the fourth. **The mechanism and the precedent both already exist:** `act.shared_prompt()` loads module-level fragments, and the build family already made exactly this migration — `build_draft`, `build_draft_minor`, `build_refine` and `build_refine_minor` all inject `${RULES}` from one `modules/assistant/prompts/rules.md`. **Why it was not simply done in PR #83, stated so the deferral can be judged rather than taken on trust:** the remedy is not an edit to the file that PR authored. Unifying four drifted copies means choosing which wording wins and then rewriting three prompts belonging to workflows that PR does not own — a change to what three live, verified workflows receive at dispatch, made incidentally inside a PR whose stated design principle is that the full research cycle stays byte-unaffected. **Done-state today: yes.** A `research`-purpose-level fragment plus the plumbing to load one (`shared_prompt()` currently resolves only against `modules/assistant/prompts/`, so a purpose-level fragment needs either a second loader or promotion all the way to module level — that choice is part of the work, not a blocker on it). **PROPOSAL, not a defect:** nothing behaves wrongly today, and each prompt is individually coherent. **Not an expansion of C-050 or C-065:** both concern a claim that is FALSE — a restated figure and a cross-file assertion. Every sentence in all four footers is true; the defect is that there are four of them.  *(Renumbered from C-067 for the same reason.)*  *(Renumbered from C-070 on 2026-08-12 — PR #82 merged first and took that ID.)* |

### Added 2026-08-12

| ID | Candidate | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|
| C-073 | A declared NORMALISATION convention in Research Standard §3, so a paper's verbatim claims are mechanically checkable instead of re-derived by every verifier that reads it | PR #84 (research_minor, verify pass) |  | `open` | **PROPOSAL against a VENDORED (MIRROR) standard — placed here rather than made, because `docs/standards/research/` must not be edited in this repo and an amendment goes upstream to MDC-Master-Planning then re-vendors. Same mechanism as C-071.** **The consequence, and it is why this is a finding rather than a note:** §3's verbatim rule (`research_standard.md:106`) says *"A span may be labelled or presented as a quotation only if its exact character sequence was returned by a fetch"* and stops there. It never says how a paper DECLARES the transformations that unavoidably sit between a source's bytes and the paper's rendering of them — and they are unavoidable: markdown and asciidoc inline markup inside a quoted span (`[share-nothing](http://…)`, `<<def_SHA1,SHA-1>>`, `_30 days_`), source line-wrap and sentence-boundary double spaces, typographic apostrophes and quotes in `pdftotext` output, and whether an elision is marked at the end of a span as well as inside it. **Undeclared, a verifier re-derives the whole transformation set from scratch and reports a FALSE MISS on every span whose deviation it has not yet guessed.** **Measured on this PR, and the shape of the measurement is the argument:** `cross_node_memory_protocol.md` took **five** independent critic passes, and **after round 1 not one blocking evidence defect existed** — no fabricated source, no fabricated span, no miscitation, no inflated mark, no wrong count, across 28 sources and 90 quoted spans, each git-hosted span verified by clone-and-`grep -F`. **Rounds 2, 3 and 4 each found a defect in the CONVENTION, never in the evidence.** The paper's original blanket claim (*"the exact characters returned — the two exceptions are marked"*) was over-absolute; its four-rule replacement was incomplete by two whole classes (PDF typographic marks, and local-file spans); that replacement's rule 4 was then over-absolute in exactly the way the blanket claim had been, with 19 counter-examples; fixing rule 4 left rule 2 asserting a universal its own new text falsified. **The cost is not hypothetical:** round 2's sweep reported sound spans as deviations, and the analyst's own truncation detector returned 39, then 23, before reaching the true 19. **So nearly the entire correction budget on a paper whose evidence was clean went to inventing a convention no standard asked for — and the next paper reinvents it or goes without.** **Done-state today: yes, and small** — a clause in §3 beside the verbatim rule requiring a paper to state the normalisation between its sources' bytes and its own rendering, each rule scoped to what was measured, its exceptions named, and its counts' populations stated. **This PR's §6 is a worked exemplar to draw from, including its failure mode, which is the half worth codifying: a normalisation stated as a UNIVERSAL generates false findings and is worse than none.** **NOT a defect:** nothing behaves wrongly — §3 is silent, not incorrect, the paper conforms today, and its `Critic:` verdict is `PASS`. **Not an expansion of C-071:** that concerns §2's sizing rubric and cycle SHAPE; this concerns §3's sourcing rules. Different section, disjoint remedy, neither subsumes the other. **Not an expansion of C-065:** that class is a claim that is FALSE about another file. Every span here was TRUE — what was missing was any stated way to CHECK it. **Not filed as an issue** — capability that would be ADDED to a document, which per [`finding-routing.md` § 4](../../finding-routing.md) makes it a candidate whatever its done-state looks like, and the default is against filing. |
| C-074 | Rule whether the persistent memory protocol is its OWN COMPONENT or a PHASE of the Memory Management Framework | PR #84 (cross-node memory prior art) | | `open` | **The research explicitly could not settle it, and said so.** §5.1: no source bears on component-vs-phase, and the paper names the trap — mechanism-list length reads as an argument for a dedicated component while being equally consistent with a phase. **It decides where the roadmap and phase docs get written**, so it gates planning rather than building, and four in-progress sprints consume the answer (CPI, Workflow Decomposition, Memory Management, Temporal Integration). **Placed as a candidate rather than written straight into `direction.md`**: a `D-` row requires a `Source: C-NNN` and is `plan-sprint`'s write, so placing one directly would skip the convention and self-grant that authority. **Lens verdicts (gate 5): `/decide` — not dissolved**, the upstream question is genuinely open and the evidence is absent rather than unread. **`/best-practices` — SURVIVES**: component boundaries are ratified by an owner, not derived from a mechanism count. |
| C-075 | Scope a correction round's re-verification to the spans that CHANGED, so `research_verify` stops re-downloading a corpus it has already verified | PR #86 (research_minor, verify pass) |  | `open` | **`verify.md` already NAMES this defect and deliberately does not fix it** — *"The real defect is that a correction round re-verifies everything rather than what changed — `author != judge` needs a fresh JUDGMENT, not a fresh DOWNLOAD, and this stage conflates them. Until that is rebuilt, 3 bounds the damage."* The 3-round ceiling is therefore a **damage bound standing in for a capability**, and the capability is what this row proposes. **The cost the bound was set against, from `verify.md`'s own measurement of PR #84:** five critic passes over 28 sources re-fetched from scratch every round, **$58.62 and 135 minutes in this stage alone — six times the $9.14 the paper cost to write** — at 441 Bash calls against 1 WebFetch. **Measured on THIS PR, which is the new half:** round 1 ran the full pass (18 external sources, every git-hosted span by shallow-clone + `grep -F`); **rounds 2 and 3 were scoped to the repaired spans only, re-cloning nothing**, on the basis Research Standard §3 already states — *"A repaired span is a new claim"*, carrying an original's full sourcing burden. **Scoping did not cost detection: round 2 found a blocking defect round 1 had missed** (four internal cross-references reading `§4.3.3` where the content is `§4.3.4`), and round 3 closed at PASS with zero blocking. One paper, closed inside the ceiling — one data point toward `verify.md`'s own stated 90%-inside-3-rounds target. **Why this is a finding and not a note: the scoping existed only in the orchestrator's hand-written dispatches.** Nothing in `verify.md` asks for it, so the next run either reinvents it or re-downloads the corpus, and the ceiling keeps bounding a cost that no longer has to be paid. **Done-state today: yes, and small** — a clause in Stage 1 stating that round 1 is the full pass and rounds 2+ verify the repaired spans and their dependents, plus the guard this run had to write by hand: the analyst's span list is **a claim to check against the diff, never a map to trust** (round 3's critic found one genuine edit the analyst's own inventory omitted, and mis-attributed its round because no intermediate commit existed to separate them). **NOT a defect:** nothing behaves wrongly — the stage is correct and expensive, and `verify.md` chose the bound knowingly. **Not an expansion of C-073:** that proposes a normalisation convention in Research Standard §3, a vendored document, so a verifier stops re-deriving what counts as verbatim; this proposes a scope rule in this repo's own `verify.md` so a verifier stops re-fetching what it already checked. Different document, different owner, disjoint remedy. **Not filed as an issue** — capability that would be ADDED, which per [`finding-routing.md` § 4](../../finding-routing.md) makes it a candidate whatever its done-state looks like. *(Id assigned as the next free one on this branch; if a PR merges first and takes C-075, this row renumbers on the file's existing rule that ids are never reused.)* |

## Where things stand

**Twenty-seven rows are untriaged, and they are the next `plan-sprint` pass's working set: C-047 through C-075.** Five (C-047–C-051) were re-homed out of the GitHub issue queue on 2026-08-09, C-052 came from issue #36, C-053–C-056 were placed from PR #71, C-057–C-059 from PR #75, C-060 from PR #79, C-061 from the operator, C-062 from PR #80, **C-064 and C-065 from PR #81 (Phase 4)**, the second placed by that PR's refine pass, **C-066, C-067 and C-068 from PR #74**, **C-069 from PR #82 (Phase 6)**, **C-071 and C-072 from PR #83 (research_minor)**, the second placed by that PR's refine pass, **C-073 and C-074 from PR #84 (research_minor — the first from its verify pass)**, and **C-075 from PR #86 (research_minor, verify pass)** — the numbering skips C-063 because that id was used and withdrawn, and this file's own rule is that ids are never reused and never renumbered. *(A C-063 was placed and then WITHDRAWN in the same PR — `/decide` dissolved it: it proposed a cross-fleet ownership rule for `activities/run-claude.sh`, and the tension it describes is between a frozen fleet and a migrating one. The operator has ruled V1 is deleted when unused, so the condition expires on its own; a rule written for a transitional period costs more than the period. Recorded here so a later pass does not re-derive it.)* Their blank `decision` is the truth, not an omission — but blank is only honest while this paragraph says how many there are. **C-048 and C-049 must be ruled together**, per C-048's own Note.

The 45 rows that predate them carry a decision from the 2026-08-06 `plan-sprint` pass: **27 `ship`**, **6 `requires review`**, **12 `reject`**.

> **Every number in this section is a restatement of something nothing derives, and each one was wrong.** It read *"Nothing is untriaged… All 45 rows… 25 `ship`, 8 `requires review`"* while **nine** rows were blank and the split was **27/6**; the next paragraph said **seven** `requires review` rows were filed as `D-001`–`D-006` when **five** are, under non-contiguous ids. The consequence was not cosmetic: five proposals re-homed out of the GitHub issue queue on 2026-08-09 were sitting invisibly in this one, because the summary `plan-sprint` builds its working set from said four rows were untriaged. **This is exactly the mechanism C-050 names** — *derive declared counts rather than restating them* — occurring in the file that holds C-050, and this is its second recorded occurrence. A third correction is the wrong response; ruling C-050 is the right one.

**The `requires review` rows are the live queue, and there are six: C-010, C-017, C-021, C-032, C-034, C-041.** Five are filed in [`direction.md`](direction.md) — C-010 as `D-001`, C-021 as `D-003`, C-032 as `D-004`, C-034 as `D-005`, C-041 as `D-006`. **C-017** is not, because issue #41 already tracks that ruling and one item does not get two homes. (`D-002` and `D-007` are sourced from rows and papers outside this queue, so they do not belong to this count.) Nothing here moves until the operator rules.

**Six `ship` rows were already applied before triage** — C-020, C-024, C-025, C-026 and C-027 landed in `f2b80a6` and the sprint-plan comparator split, and C-007/C-008/C-009/C-013/C-015/C-018 in `b9710d5`. A candidate can be delivered before anyone gets round to ruling it; recording that is cheaper than re-deriving it.

**12 rejected, and the reasoning is the point.** Three assumed **Temporal Cloud**, settled 2026-07-12 and unwritten at the time, so a research cycle costed out a product ruled out three weeks earlier — now recorded in `stack_reference.md`. Four more were **already delivered** by shipped work. The rest name what would have to change for them to be re-proposed.

**Known gap in this file's own model:** `status` is set by "a later process" — `plan-feature` on landing, or the build that completes it. A `reject` never lands in a phase doc and no build completes it, so nothing will ever close one. Nine of the twelve 2026-08-06 rejects therefore sat at `status: open`, reading as outstanding work when they were not — corrected by hand on 2026-08-10 (see the note above). The gap itself remains: nothing will close the next `reject` the same way.
