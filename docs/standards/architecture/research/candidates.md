# Action candidates — the running list

**This file is the durable home for research action candidates. `synthesis.md` is rewritten every cycle; this is not.**

## Why it exists

A candidate surfaced in `synthesis.md` disappeared on the next research cycle, so its disposition — and the reasoning behind a rejection — went with it. Two consequences, both observed: candidates already ruled on were re-proposed in later cycles, and seven of them were parked on the standup tracker because no other surface would hold them, which the tracker's own rules forbid.

## The rule

> **Research creates and appends. Planning dispositions.**

- **Research** adds new candidates with a **stable ID**, never reused, and never renumbers an existing one.
- **Planning** sets `decision`; a later process sets `status`. Both carry reasoning in the Note.
- **A carried-forward candidate REUSES its original ID.** When a later cycle restates a live candidate, it is the same candidate — do not mint a new ID. (Cycle 4 restated C-007 and this file briefly carried it twice before the duplicate was removed.)
- **Nobody deletes a row.** A rejected candidate stays visible so it is not re-proposed — that is the whole point.

**The nine `reject` rows below were closed BY HAND on 2026-08-10.** They had been reporting as outstanding work on a surface whose to-do bit is `status`. **No automated writer closed them and none exists** — what should close a `reject`, and what closes the six unclosable `requires review` rows, is [issue #69](https://github.com/helloskyy-io/Claude-Dot-Files/issues/69), which stays open. This is a correction to the data, not a change to the model.

## Two flags, orthogonal — do not collapse them

| Flag | Values | Who sets it |
|---|---|---|
| **`decision`** | `ship` · `requires review` · `reject` · **blank = not yet triaged** | **`triage-candidates`, and only `triage-candidates`.** This is its whole output |
| **`status`** | `open` · `closed` | **A later process.** `plan-feature` when the item lands in a phase doc; the build that completes it |

> **The writer changed on 2026-08-12 and the rule did not.** `decision` was `plan-sprint`'s while `plan-sprint` triaged; triage is now its own workflow and the column went with it. **`plan-sprint` may no longer write this column at all** — it reads the file to know which rows are `ship`, and its runner fails the run if any ruling changed while it held the file. One writer per surface, as before.

## `component` — where a shipped candidate gets scaffolded

**`component` is NOT a third flag**, which is why it sits above rather than inside the table. It carries no disposition: it names a place.

| Column | Value | Who sets it |
|---|---|---|
| **`component`** | the `docs/development/<name>/` this candidate belongs to — an **existing** one if it extends something already planned, a **new** name if it does not · **blank = not yet named** | **Whoever FILES the candidate, at the moment they file it.** |

**The writer is the filer because only the filer knows.** They have just written the proposal and know which component it belongs to; anything downstream would be guessing at it from a one-line summary. Every prompt that files a row carries the instruction, and **the prohibition is mechanical rather than requested**: `triage-candidates` and `plan-sprint` both snapshot this column either side of their run and fail the run if it moved on a row that already existed. A row the run APPENDED is exempt, because filing a proposal requires naming where it goes. *(The guard exists because a guess here does not stay a cell — see What reads it, below.)*

**A blank is an unanswered question, not an error.** Nothing is scaffolded for a blank row and nothing fails because of one — it simply waits for someone who knows. A cell that is not blank but yields no folder name — an EN dash, `--`, anything punctuation-only — is treated the same way and reported in the run's notes, because it is a typo rather than an answer.

**The rows that predate this column are all blank, and the OPERATOR is who names them.** Backfilling is a triage judgement rather than a migration, and it is the one judgement `triage-candidates` is explicitly forbidden from making — *"you never decide where a shipped candidate goes. Not which sprint, not which phase, not which component."* **That leaves no automated actor, which is the point rather than an oversight**: the column's whole argument is that only the filer knows, and for a row filed before the column existed there is no filer to ask. They surface at `/standup` as `ship` + `open` rows with a blank cell, and nothing scaffolds until a human names one.

**What reads it:** `plan-candidates` — an activity, not a child, so no model is involved. For each row that is `ship` **and** `open` **and** names a component **and** whose directory does not exist yet, it creates `docs/development/<name>/research/` and seeds `synthesis.md` from the candidate's own summary. **A named component whose directory already exists is left alone**: the name means the candidate extends something already planned, and there is nothing to scaffold. **The one exception is a directory this pipeline seeded and never researched** — a run that died between the two — which is resumed rather than skipped forever.

**This is why the column has a guard and the other two have one for a weaker reason.** `decision` and `status` guess wrongly into a cell somebody later reads. `component` is read by `plan-candidates` in the very next step of the same parent, on the same branch, and becomes a committed directory plus a `research-write` and a `research-verify` dispatch. A guess here is not a bad cell; it is built.

## The three dispositions

Every candidate ends at exactly one of these. There is no fourth, and **leaving a row blank is not a disposition** — it means triage has not happened yet.

### `ship` — we have decided to do this

The work is **understood well enough to schedule.** Somebody could pick it up and know what "done" looks like without another decision being made first.

**`ship` does NOT mean done.** A shipped candidate stays `open` until something actually implements it, and none of `triage-candidates`, `plan-sprint` or `plan-tech-stack` does detailed phase design, so none can close one on its own.

### `requires review` — only the operator can rule on this

The candidate is **not ready to be scheduled, and no amount of further automated work makes it ready.** An open question, an unresolved trade-off, a ruling that changes what the project believes. Shipping it would put a question mark in the plan; rejecting it would throw away a real finding.

**Where it goes:** `triage-candidates` writes a `D-NNN` row into [`direction.md`](direction.md) — the operator's file — and sets `decision` here to `requires review`. The row stays in this file as the pointer, and **`status` stays `open`** because the decision is outstanding, just outstanding with a human.

**It then leaves the automation's working set entirely.** A non-blank decision is not re-triaged, so it never comes back around. It surfaces at `/standup` as an open direction decision, the operator rules, and only then does the disposition change to `ship` or `reject`.

> **This is the release valve.** Without it, `ship` and `reject` are the only doors out, and an open question that deserves neither gets forced through one of them. That is exactly what happened on the first triage run (then part of `plan-sprint`): eleven unresolved questions were shipped into the sprint plan as milestones, which produced two sprints that build nothing.

### `reject` — we are not doing this

State why in the Note. **The reasoning is the point** — a rejection without it gets re-proposed on the next cycle by a run that cannot see why it was refused.

## Where a shipped decision lands depends on its size

A candidate large enough to need its own sprint section gets one, added by `plan-sprint` — that is the **entire** extent of `plan-sprint`'s implementation. **Most shipped candidates are not that size.** They belong inside an *existing* sprint or phase doc, placed by `plan-feature`. `triage-candidates`, which set the `decision` in the first place, does nothing with any of them beyond that: it never places, and it never touches `sprint.md`.

**A blank decision is not the same as `open`.** Blank means nobody has triaged it; `open` means the work is outstanding. Collapsing the two is what turns this file into a to-do list nobody agreed to — the failure that put seven untriaged candidates on the standup tracker.

**Every workflow that touches this file states its own portion in its prompt:** *the decision was made — implement your portion only.*

## Provenance note — read before trusting cycle-4 rows

Cycle-3 rows come from `synthesis.md` on `main` and are settled. **Cycle-4 rows come from PR #33, which is `HOLD - redispatch` and unmerged.** Its held item concerns a currency-tier marker that C-024's costing rests on, so cycle-4 entries are **provisional** and this file gets revised if that assessment finds the evidence thin.

---

## Cycle 3 — 2026-08-04

| ID | Candidate | `component` | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|---|
| C-001 | Heartbeat clause on `python_sdk_long_activities.md` — heartbeats free at the SDK layer, billable on Cloud | | `temporal.md` | `reject` | `closed` | billing is moot on self-hosted. The *ceiling* survives for a different reason: every heartbeat is a persistence write on our own cluster |
| C-002 | Schedule the self-host-vs-Cloud decision | | `temporal.md` | `reject` | `closed` | decided 2026-07-12, self-hosted. Recorded in `system-overview.md` § Deployment target |
| C-003 | Decide shard capacity before the first self-hosted workflow runs | | `temporal.md` | `ship` | `open` | build-time one-way door, and now *more* relevant since we self-host. **Ship:** a parameter of the server stand-up, not a question — Temporal Integration phase-doc detail |
| C-004 | Override the default retry policy on every activity wrapping a paid API — Temporal defaults to unlimited attempts | | `temporal.md` | `ship` | `open` | **Ship:** a concrete rule for every activity wrapper; Temporal Integration Stage B phase-doc detail |
| C-005 | Amend the Serverless Workers reading — Lambda caps an activity at 15 min | | `dedicated_edge_routing.md` | `reject` | `closed` | k3s pods, not serverless |
| C-006 | Record that no first-party Claude ↔ Temporal runtime integration exists | | `temporal.md` | `ship` | `open` | **Ship:** scopes the hand-rolled `claude_cli` integration as permanent rather than temporary. Natural home is `stack_reference.md`, which is human-in-the-loop — Temporal Integration phase doc meanwhile |
| C-007 | Correct differentiator #1 in `problem-statement.md` | | `bernstein_capability_mining.md` §0.1 | `ship` | `closed` | `b9710d5` |
| C-008 | Replace differentiator #2 with the credential version | | `dedicated_edge_routing.md` §7 | `ship` | `closed` | `b9710d5` |
| C-009 | Add the trust-domain claim — stronger than any scheduling-model difference | | `bernstein_capability_mining.md` §0.2 | `ship` | `closed` | `b9710d5`, promoted to differentiator #1 |
| C-010 | Resolve the queue-axis conflict before Temporal Integration is planned | | `dedicated_edge_routing.md` §4.1 | `requires review` | `open` | gates that sprint; addendum §A3. **`D-001`** — the resolution is a commitment against a vendored standard, and C-022's spike is the evidence it needs first |
| C-011 | Ship three cheap guards: credential expiry, false completion, safety-hook wiring test (~9 h) | | `fleet_failure_modes.md` §7 | `ship` | `open` | **Ship:** three things built with a stated cost. Placed as a *Fleet Reliability* milestone |
| C-012 | Do NOT build an operator dashboard; build the blocked-work notifier | | `operator_interface.md` §0, §6 | `ship` | `open` | the negative *is* the finding. **Ship:** both halves are concrete. Placed as a *Fleet Reliability* milestone |
| C-013 | Close the "evaluate Paperclip after Phase 4" gate and rewrite the item | | `paperclip_assessment.md` §7 | `ship` | `closed` | `b9710d5` |
| C-014 | Adopt the eight cost-S, dependency-free interface/doctrine items | | `bernstein_capability_mining.md` §5 | `ship` | `open` | case-by-case, not a bundle. **Ship:** §5 already names a home for each of the eight (rows 2/4/6/8/9/10/11/12) across five existing sprints plus one problem-statement amendment — phase-doc detail, not a sprint change |
| C-015 | Fix the missed-window assumption in the sprint plan — backwards, verified against the code | | `fleet_failure_modes.md` §5.2 | `ship` | `closed` | `b9710d5` |
| C-016 | Design the stalled predicate as a three-way conjunction before workers are written | | `paperclip_assessment.md` §4.4 | `reject` | `closed` | claims the failure mode is live here today; **unverified**. **Reject:** folded into C-029 — the three-legged taxonomy supersedes the two-way framing, and one design does not need two homes |
| C-017 | Decide dedupe granularity as a ruling, not a build | | `paperclip_assessment.md` §4.3, §6 | `requires review` | `open` | explicitly not a pair to build both of. **ALREADY TRACKED at issue #41**, whose *Proposed next action* is this exact ruling — worktree scan vs comment recency vs a real lock, decided once for both the bash and Python paths. No `D-` row: one item, one home |
| C-018 | Drop any uniqueness framing on subscription-auth-at-the-edge | | `paperclip_assessment.md` §4.6 | `ship` | `closed` | `b9710d5` |
| C-019 | Reconsider giving up cross-machine failover for *all* work | | `dedicated_edge_routing.md` §5, §7 | `ship` | `closed` | **RULED via `D-002` 2026-08-07** — pin to the initiating edge; the reconsideration is closed |

## Cycle 4 — 2026-08-06 · PROVISIONAL (PR #33 unmerged)

| ID | Candidate | `component` | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|---|
| C-020 | Restate differentiator #1 on the credential, not the topology — state both halves together | | cycle-4 pool | `ship` | `open` | **Ship — already applied** at `f2b80a6`; `problem-statement.md` §1 states both halves together |
| C-021 | Cost differentiator #1 with the self-hosted-CI-runner warning; rule on the laptop trust boundary | | cycle-4 pool | `requires review` | `open` | **`D-003`** — the costing is a `problem-statement.md` edit and the boundary is a ruling; neither is automation's |
| C-022 | Settle whether a Temporal worker can be prevented from polling a queue it should not serve — **before** the pinned-edge design | | cycle-4 pool | `ship` | `open` | bears directly on C-010. **Ship:** an empirical spike with an artifact, not a preference — run it before `D-001` is ruled. Temporal Integration phase-doc detail |
| C-023 | Record that self-hosted Temporal ships `noopAuthorizer` by default; the namespace is the only credential boundary offered | | cycle-4 pool | `ship` | `open` | security-relevant to the two-server split. **Ship:** natural home is `stack_reference.md` § *What we do NOT use*, which is human-in-the-loop — Temporal Integration phase doc meanwhile |
| C-024 | Split the sprint plan's *Tools to Evaluate* into backbone comparators and edge runtimes | | cycle-4 pool | `ship` | `open` | the row whose evidence the held item concerns. **Ship — already applied**; the sprint plan carries both categories |
| C-025 | Give "nearest neighbour" an axis: bernstein by architecture, OpenClaw by thesis | | cycle-4 pool | `ship` | `open` | **Ship — already applied** at `f2b80a6`; `problem-statement.md` § *The nearest neighbor* carries the axis table |
| C-026 | Add OpenClaw as ASSESSED-and-closed, not as an evaluation gate | | cycle-4 pool | `ship` | `open` | **Ship — already applied**; listed under *Edge runtimes*, ASSESSED with no evaluation gate |
| C-027 | Add Hermes under a new edge-runtimes heading | | cycle-4 pool | `ship` | `open` | an addition, not a rewrite. **Ship — already applied**; listed under *Edge runtimes* |
| C-028 | Plan the three pre-worker recovery items as ONE design session | | cycle-4 pool | `ship` | `open` | **Ship:** one session producing one restart-recovery contract. Placed as a *Fleet Reliability* milestone |
| C-029 | Adopt the three-legged liveness taxonomy: stalled / looping / stranded | | cycle-4 pool | `ship` | `open` | supersedes C-016's two-way framing. **Ship:** placed as a *Fleet Reliability* milestone |
| C-030 | Unblock quota-headroom — derivable from observed cap-errors, no provider telemetry needed | | cycle-4 pool | `ship` | `open` | **Ship:** the telemetry transfers, the rotation does not — we hold one subscription. Placed as a *Fleet Reliability* milestone |
| C-031 | No fallback queue: an unresolvable assignee PARKs with a typed event, never silently falls back | | cycle-4 pool | `ship` | `open` | a negative design decision. **Ship:** it is queue topology, so Temporal Integration owns it — phase-doc detail |
| C-032 | Amend `workflow-scripts.md` § Composition — it justifies two mechanisms with an argument supporting only the first | | cycle-4 pool | `requires review` | `open` | **`D-004`** — a standards amendment against two binding documents, and `architectural_standard.md` §3's `author ≠ judge` seam restates the same over-claim. Agents surface, humans write |
| C-033 | Withdraw or downgrade `case_against.md`'s D7 — contradicted by its own primary source | | cycle-4 pool | `ship` | `open` | **Ship:** a bounded correction to one paper with a named source. Needs a `research-refresh` dispatch against `case_against.md`, not a sprint item |
| C-034 | Switch `review-pr` to a cross-family judge — self-preference bias is causally linked to self-recognition | | cycle-4 pool | `requires review` | `open` | **`D-005`** — every genuinely cross-family option needs a second provider credential, which is a thesis-level commitment, not a one-line change |
| C-035 | Run E1b first — classify 30 PRs' disposition items to read out the judge's marginal yield | | cycle-4 pool | `ship` | `open` | cheap, and it sizes C-034. **Ship:** reads existing logs and PR threads, no new dispatches. Added as a Continuous Process Improvement milestone |
| C-037 | Cross-machine failover — **a third option**: pin the credential, not the work | | amends C-019 | `ship` | `closed` | **RULED via `D-002` 2026-08-07** — the proxy option excludes `claude-cli` by name, so it does not apply here |

## Evicted from the sprint plan — 2026-08-06

Nine ideas that lived in the sprint plan under *Future Ideas (Not Yet Committed)*, some since April. **None was ever committed to and none had been triaged** — a candidates list wearing a plan's clothes, which is exactly the shape this file exists to hold. Moved verbatim in substance; the plan file is not the place to park an idea.

| ID | Candidate | `component` | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|---|
| C-038 | Cross-project intelligence — aggregate CPI analysis across repos so a pattern in one informs another | | sprint plan, Future Idea A | `reject` | `closed` | needs centralized log collection or report aggregation. **Reject: delivered.** Continuous Process Improvement's *Cross-repo reporting* milestone centralises with source-repo metadata, so patterns spanning repos are already visible |
| C-039 | Workflow composition / chaining — an orchestrator running a pipeline of workflows end to end | | sprint plan, Future Idea B | `reject` | `closed` | **largely overtaken** — parent/child composition ships today. **Reject:** two live sprints cover it — Workflow Decomposition ships the composition and its standard, and a driver above parents is Autonomous Operation |
| C-040 | Project templates for `plan-new` — stack preferences and boilerplate decisions pre-made per project type | | sprint plan, Future Idea C | `reject` | `closed` | **Reject:** presupposes a settled stack reference, and `stack_reference.md` is explicitly seeded-not-complete pending a research pass. Re-propose once it lands and two projects want the same boilerplate |
| C-041 | Team scaling — per-user config overrides, aggregated CPI, role-based workflow access, onboarding | | sprint plan, Future Idea D | `requires review` | `open` | bears on the SkyyNet multi-participant question. **`D-006`** — the four items cannot be sized until the repo/SkyyNet boundary is ruled |
| C-042 | Metrics dashboard over the JSONL logs — cost trends, efficiency, failure types, agent utilization | | sprint plan, Future Idea E | `reject` | `closed` | **tension**: cycle-4 evidence argues a blocked-work notifier over a dashboard. **Reject:** the tension resolves against it — `operator_interface.md` argues explicitly for the notifier, which C-012 carries |
| C-043 | `/rollback-cpi` — revert the last CPI PR and mark that pattern tried-and-failed | | sprint plan, Future Idea F | `reject` | `closed` | grows in value as CPI automation increases. **Reject:** CPI changes are human-ruled and already reversible by `git revert` plus an append to `cpi-decisions.md`, and the operation has never been performed once. Re-propose the first time a CPI change is reverted and the log fails to record why |
| C-044 | SkyyCommand AI decision engine — the lean-agent + rich-skill pattern applied to VM placement | | sprint plan, Future Idea G | `reject` | `closed` | out of this repo's scope; belongs to SkyyCommand. **Reject:** out of scope by the problem statement's own frame — this repo is the Jarvis edge, and VM placement is SkyyCommand's |
| C-045 | Prompt pattern library — capture phrasings that measurably produce better output | | sprint plan, Future Idea H | `reject` | `closed` | **Reject:** *measurably* is the blocker — nothing here can read out a phrasing's marginal yield, and `cpi-decisions.md` already records shipped prompt changes with their reasoning. Re-propose once C-035 shows the fleet can measure a prompt change at all |
| C-046 | `plan-new` greenfield — handle `git init`, initial commit and remote setup rather than requiring a repo | | sprint plan, Future Idea I | `reject` | `closed` | found during the 1Password vault manager test, 2026-04-11. **Reject: delivered.** `scripts/helpers/init-project.sh` runs `git init --initial-branch=main`, makes the initial commit and creates the GitHub remote; `plan-new` correctly requires an existing repo |

---

## Re-homed from GitHub Issues — 2026-08-09

Five open issues that were **proposals, not defects**, re-homed under [Architecture Standard § 4 Memory](../architectural_standard.md) — *"a PROPOSAL — capability that does not exist yet and would be added — goes to `candidates.md` and is NEVER an Issue, however clean its done-state looks."*

**Why they were filed as issues:** the selection rule in force when they were filed routed on *"did something change?"* and *"does it have a done-state?"* — and a proposal answers **no** and **yes**. `candidates.md` was explicitly excluded as a destination, so a proposal had nowhere else to go. That paragraph was corrected the same day this section was written.

**`decision` is deliberately blank on every row: these have been re-homed, not triaged.** Blank means triage has not happened, and `decision` is `triage-candidates`'s output alone. **Two of the five were filed by the operator's own session**, so this is not a dispatch-behaviour problem.

| ID | Candidate | `component` | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|---|
| C-047 | Gate `vendor-standards.sh --check` on the merge path so a local edit to a vendored MIRROR standard cannot merge green | | issue #55 | | `open` | Nothing is broken — the check works and is correct; this ADDS a gate. Split ruled: the committed-checksum half (local-edit detection) needs no upstream and is shippable; the upstream-divergence half is blocked on a read-only credential for a private repo, and the Testing Standard's freshness clause forbids gating a check that cannot establish its own baseline |
| C-048 | Per-file granularity in `install.sh`'s symlink targets, so a `tests/` dir beside a hook does not land in the live `~/.claude/hooks/` | | issue #63 | | `open` | Nothing is broken — PR #58 placed the tests successfully elsewhere and documented the divergence. Changes the documented 7-target strategy on every machine, so it is an architecture decision. Constrained by C-049; rule them together |
| C-049 | Point `~/.claude/*` at a worktree pinned to a ref instead of at the git working tree, making `git pull` the deploy step | | issue #64 | | `open` | Today the working tree IS production: whatever branch is checked out is live, and an edit takes effect before commit. **The value is not mainly safety** — it is that when CDF grows to server + multiple edges, adding rings becomes a config change rather than a re-architecture. Costs instant prompt iteration. Same architecture surface as C-048 |
| C-050 | Derive declared roster and inventory counts rather than restating them in prose | | issue #70 | | `open` | **The live instance is already fixed** (`README.md` said 10 workflows, there are 9 — corrected 2026-08-09); what remains is the check, which is capability. `architectural_standard.md:46` already states the principle — *"a constant restated in two places diverges silently"* — and nothing enforces it. **THIRD OCCURRENCE, with its cost measured, added by PR #71's refine pass — this is the recurrence the row was left open to collect.** The first was this file's own summary block (see the note below §Where things stand); the second, `exit-protocol.md` §4's *"nine rules"* prose beside a table whose reason COLUMN is machine-gated while its rule COUNT is hand-typed; the third, `file_structure.txt` restating that same count as *"the nine-rule fail-safe contract"* and **going stale for a whole pass** after R0 landed, in the file `CLAUDE.md` calls authoritative. Note the pattern the three share: in every case the *contents* had a gate and the *count* did not, so each divergence was invisible to a green suite. **The cost is now known: three hand-corrections across two passes on one PR, each found by a reader rather than a check.** **FOURTH OCCURRENCE, 2026-08-10, and this one was caught BY A CHECK — which is the most useful thing the row has collected yet.** The run closing [Phase 3](../../../development/memory-management-framework/phase3_typed_exit_record.md) restated that phase's fresh agreement figures in `roadmap.md`, and `testing/scripts/tests/unit/test_measurement_figures_are_cited.py` went red. **It fired by accident, not by scope:** that gate is scoped to *Phase 5* lines, and it only saw these because the roadmap's status header names both phases on one line — the figures the same run put on the **Phase 3 checkbox line were invisible to it**, and were removed only because the accidental catch prompted a re-read. So the gate proves the mechanism is real, gateable and cheap to check, **and simultaneously proves the hand-kept-scope defect this file keeps recording**: a check whose subject is one enumerated phase retires itself the moment the next phase produces figures. **That run's OWN two instances are fixed — both roadmap mentions are now pointers — but the roadmap is NOT clean, and saying so is the point of this entry.** Running the gate's own regex over the whole file instead of its Phase-5 scope returns **two pre-existing restatements it has never seen**: `roadmap.md:149` carries Phase 1 E3(b)'s `6 of 7` and `31 of 38`, and `:170` carries candidate 6's `8 archived PRs`. Both predate that PR (verified at branch point `799e0f9`) and both were left unfixed as outside a measurement-recording task's scope at the time this paragraph was written — **recorded here so the count is honest rather than implied clean.** **Both are now fixed, by this same PR's second correction pass: both roadmap mentions are pointers, and the widened gate (sixth occurrence, below) is green over the whole file.** They were the standing evidence for the candidate when this paragraph was written; that evidence is now historical rather than live, which does not weaken the candidate — the sixth occurrence's argument is that the mechanism keeps recurring, not that these two offenders remain, and it recurred twice more in this same PR after this paragraph was first committed. The candidate is unchanged and better evidenced — **derive or cite, never restate, and scope the check to the SHAPE rather than to a list of phases someone remembered.** **FIFTH OCCURRENCE, same PR, one pass later, and it is the one that should decide this row: the restatements were left behind by the FIX for the fourth.** PR #79's review pass found that flipping Phase 3's status had left copies contradicting the flip — `roadmap.md`'s trailing Phase-3 paragraph still read *"BUILT, NOT YET PROVEN … seven of eight"* four lines below the checkbox flipped to *"both halves done"*, and the phase doc's close-out still justified leaving standards-candidate 1 alone because *"requirement 6 has not yet done"* it, two bullets under the box asserting requirement 6 was met. **Both are fixed; neither was reachable by any gate.** They carry no denominator figure, so the shape-matching check this row proposes would not have caught them either — **a completion VERDICT is restated exactly like a count and nothing in the proposal covers it.** That is new information about the candidate's scope, not just another tally mark: *three independent reviewers each found one of these by reading, in a PR whose own diff was expanding this row about that exact mechanism.* **The candidate should be ruled to cover restated STATUS as well as restated COUNTS, or it will ship a check that leaves the more consequential half uncovered.** *(That paragraph originally reported the population as "three of the five places"; the true population is **nine** assertion sites — four in the phase doc, three in `roadmap.md`, one in `file_structure.txt`, one in `phase5_convergence_stopping.md` — and the count was itself a restatement of something nobody derived, inside this row. Corrected 2026-08-10.)* **SIXTH OCCURRENCE, and it is the fifth's own fix leaving survivors: the pass that WROTE the fifth-occurrence paragraph above missed two sites two files over.** `file_structure.txt:97` still read *"Phase 3 BUILT-NOT-PROVEN … 7 of 8 requirements"* and `phase5_convergence_stopping.md:155` still read *"Requirement 6 of Phase 3 is still open"* — the second being a **gate condition that the same PR fired**. All four review lenses on PR #79's second correction pass found them independently; none was reachable by any check. Both are now fixed. **Two things this row did not have before, and they are what a ruling needs:** (1) **the roadmap half of the proposal is now BUILT and shipped in PR #79** — `test_measurement_figures_are_cited.py` no longer scopes its roadmap sweep to *Phase 5* but enumerates every `phase*.md` on disk, so a figure restated beside any phase's name fails; measured on the tree at the time, it found exactly the two pre-existing offenders named above and **zero** false positives. (2) **The STATUS half is NOT gateable the same way, and this is measured rather than assumed.** A prototype keyed on line co-occurrence — a line naming a phase whose doc declares itself COMPLETE, plus a not-done token — returned **9 hits of which only 2 were real**: it fires on `roadmap.md:3` (which correctly says Phase 4 is unstarted), on a candidate note quoting historical text, and on a line saying a *gated item* still does not land. **So "restate a status" has no syntactic signature the way "restate a count" does**, and ruling this row `ship` on the assumption that one mechanism covers both would produce a gate people disable. The two halves should be ruled separately: the count half is done, the status half needs a different mechanism (most likely a declared canonical status field per phase doc, compared rather than pattern-matched). **FOURTH THROUGH SEVENTH OCCURRENCE, and the first one where the mechanism was measured to be NON-CONVERGENT — PR #75.** Four consecutive passes over that PR each headlined this same defect and each fixed the instances it could see: the draft pass wrote *"a summary line is a copy, and a correction that lands in the source without landing in its copies has not landed"* in its own reflection and then violated it five times in the same PR; the refine pass fixed those five and re-introduced it wherever ITS OWN corrections had copies; the review pass found four more, including two in the roadmap — the file a later run reads to learn what is proven — and one where a document contradicted itself twelve lines apart; and the correction pass after that found four again, one of them a **withdrawn argument** rather than a stale number (a justification the phase doc explicitly deleted as motivated reasoning, still standing in the roadmap as support). **The frontier moved every time and never shrank, which is the evidence this row was left open to collect: fix-the-instances does not converge on this class, and that is now measured rather than argued.** **What DID close it, and it is the shape this candidate should be ruled on:** a check keyed on the class instead of the instances — figures with a denominator may appear only in the section that takes the measurement, and every other mention is a citation. Two such gates now ship for one corpus (`testing/scripts/tests/unit/test_measurement_figures_are_cited.py`), both mutated in both directions. They cost about a hundred lines and they close the mechanism for the documents they cover; **the candidate is whether that shape generalises to the roster and inventory counts this row was originally about**, which is a decision and not a task. Note also that the check does NOT ask any prose to be correct — no test can check that — only that a derived figure live in one place, which is what makes it implementable at all |
| C-051 | A marketing workflow — outward positioning, with inward feedback into the problem statement and roadmap | | issue #28 | | `open` | Filed 2026-08-05 and parked ever since. Wholly unbuilt capability; it was never a defect on any reading |

---

### Added 2026-08-09 — re-homed from issue #36

| ID | Candidate | `component` | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|---|
| C-052 | Close the V2 port's ranked coverage gaps — eight named functions, ranked by risk, produced by PR #31 as its *"state the untested surface, do not build it"* deliverable | | issue #36 | | `open` | **A proposal, not a defect: "we have no test for X" is not a defect in X** — nothing claims any of these eight is wrong. **It had already decayed unnoticed**, which is why it left the issue queue: item 2 (`render()`) is now covered by `test_shared_render_catches_a_digit_bearing_placeholder`, landed via unrelated work; item 8 (`pr_number_from_url`) has a happy-path test but still none for its deliberate raise; the issue's own caveat *"without closing #30 none of it runs on the merge path"* is moot — **#30 is closed and the gate ships**. Still at zero: **item 1 `paper_currency`** and **item 4 `pass_numbers`**. Items 5-7 remain blocked on an integration tier that does not exist; building the one `tmp_path` git-repo fixture unblocks all three at once. **TRIAGE NOTE — item 1 is separable and stronger than the rest, and should probably be ruled `ship` on its own merits:** `paper_currency` computes staleness verdicts a prompt instructs the model to obey without recomputing, and its own docstring records why it exists — *"A model once marked four of eight papers past window when one was… Correct arithmetic, wrong anchor, and the error was invisible because the reasoning looked sound."* Documented past failure, authoritative output, zero tests, and nothing is likely to touch `research_activities.py` soon — which is precisely the case where a targeted one-off beats opportunistic coverage. The other seven are better taken at the point of change |

---

### Added 2026-08-09/10 — placed by producing runs on PR #71, PR #75 and PR #74, per [`finding-routing.md`](../../finding-routing.md) §4

**All of them were surfaced by their PR's build passes, classified as proposals by its review pass, and had no permitted writer until `finding-routing.md` §4 landed.** §4's rule — *a producing run MAY place its own proposals, in its own PR* — is what makes these rows placeable, and the reasoning is that the disposal-chute argument reserving defect filing to the reviewer **does not reach a proposal**: capability that does not exist was never the run's scope, so it cannot be scope the run is offloading. `decision` is blank because triage has not happened; that is `triage-candidates`'s output alone.

**Each was re-verified as a proposal rather than a defect at placement time, not merely inherited from the review.** *(This heading and the two paragraphs above it named PR #71 and "all three" while the section had grown to six rows from two PRs — a count restated in prose beside the rows that derive it, which is C-050's own mechanism occurring in the file that holds C-050. It was then rewritten to state ID RANGES instead, and **that correction failed the same way one row later**: the ranges said C-053–C-056 and C-057–C-059 while the table ran to C-060, so the newest row sat outside its own section's stated scope — invisible to exactly the reader who trusts the heading. A range is still a restated count. The ID lists and the row count are now gone from the heading entirely, which is the only version of this sentence that a new row cannot falsify.)* The distinction that governs them: *"we have no check for X" is not a defect in X* — none of them names anything that behaves wrongly today.

> **A fourth candidate was considered and is DISSOLVED rather than placed; the verdict is recorded here so a later pass does not re-derive it.** The review that surfaced these three also proposed *a proposal-sweep step in the pipeline*, on the grounds that the only measured route from "a run surfaces a proposal" to "`candidates.md` holds it" was the operator sweeping GitHub Issues by hand — a route that does not exist for a proposal correctly **not** filed as an Issue. **`finding-routing.md` §4 closed that gap while the review was being written.** The end-to-end mechanism now exists and this PR is its first exercise: a decide-only reviewer surfaces a proposal, holds for redispatch, and the redispatched **producing** run places it, because that run has the commit-and-push the reviewer structurally lacks. The one residue — a reviewer with a proposal and no redispatch to hand it to — is already ruled by that document's own §8: *"a run declining to place a proposal because no actor is permitted to — that is a defect in this standard, and it is filed against this standard."* **A sweep would be machinery for a gap that has an owner.**

| ID | Candidate | `component` | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|---|
| C-053 | A dependency manifest for the repo | | PR #71 | | `open` | **Re-verified at placement:** no `requirements.txt`, `pyproject.toml`, `setup.py` or `Pipfile` exists anywhere in the tree, and `temporalio`, `pytest` and `jsonschema` all import on this workstation while being declared nowhere. **Proposal, not defect: nothing is currently broken** — `exit_record.py` is stdlib-only *because its author checked*, which is the point. The failure mode is the next module that does not check: it works here and strands a worktree on the VM. Related to C-052 only in that both are "a check that does not exist"; the mechanism is different (undeclared runtime dependency vs untested function) so they are **not** one entry |
| C-054 | A version pin or startup probe for the CLI-shape facts the V2 path depends on | | PR #71 | | `open` | Two facts are depended on and declared nowhere: **`result.permission_denials` exists**, and **`--json-schema` moves prose out of `.result`**. **Partially mitigated by PR #71 and explicitly not closed by it** — that PR split R1 so an absent-or-mistyped `permission_denials` reports `denials_unreadable` rather than `permission_denied`, which means a CLI shape change is now *visible in the per-reason rate* instead of masquerading as a fleet-wide safety trip. It is still detected only after the fact, once per run, by an operator reading reasons. A startup probe would catch it once, before the run. **Proposal: the probe is capability that does not exist**; the mis-binning it would have caused was the defect, and that half is fixed |
| C-055 | A format check on `docs/file_structure.txt` | | PR #71 | | `open` | The map is authoritative per `CLAUDE.md`, and its only structural signal is a column-1 tree gutter — nothing parses it and nothing validates it. **PR #71's draft pass broke it on five lines and nothing noticed**; its refine pass repaired them, and the map is clean as of this commit (re-verified at placement: every body line starts with a gutter character). **So the instance is closed and the check is the candidate** — proposal, not defect. Same mechanism as C-050 (*derive declared counts rather than restating them*): both are "an authoritative artifact with no gate," and if either is ever ruled `ship` the other should be re-read alongside it. **AMENDED 2026-08-12 (PR #84, research-verify correction pass) — the premise above is REVERSED, and what remains of the candidate is now measured rather than argued.** `testing/scripts/tests/unit/test_file_structure_map_covers_the_tree.py` landed in PR #82 (`e7fadd9`, 2026-08-12) and **does** parse the map, so *"nothing parses it and nothing validates it"* no longer holds. It derives its population from `git ls-files` rather than from the map itself, parses the tree's indentation back into real paths rather than matching leaf names, and enforces two rules — where the map ENUMERATES it must enumerate completely, and where it ROLLS UP every tracked file must still be reachable through some named ancestor. The first half found **27 missing files** when it landed. **The RESIDUAL gap is DIRECTORY-shaped, and PR #84 measured it on itself rather than reasoning about it:** the gate is scoped to FILES, so its roll-up rule is satisfied by *any* named ancestor — and `docs/development/` is always one. The suite was **GREEN on that branch** while `docs/development/persistent-memory-protocol/`, an entire new component directory, appeared nowhere in the map, and while `docs/development/github-actions-integration/` was still named despite having had zero tracked files since `e397632` folded it into `autonomous-execution`. Both were corrected by hand in that PR, which is the finding: nothing would have caught either, and the gate reported success throughout. **So the map's `development/` section is a second hand-kept enumeration — of DIRECTORIES — nested inside the one the gate already derives, and nothing derives it.** The remaining capability is directory-level set-equality wherever the map enumerates directories, and the machinery already exists since the test reconstructs paths from indentation today. **The candidate is NARROWER than when it was filed, not closed** — and its pairing with C-050 still holds, now as the half-closed instance of the same mechanism |
| C-056 | A routing policy for a verification that could not be performed — should an unperformed check be DOWNGRADED, RECORDED, or only annotated? | | PR #71 | | `open` | **The half of `flaky-gh-discards-a-completed-review` that is genuinely a policy question, separated from the half that was a defect.** PR #71 fixed the defect: the two `gh` reads behind the render↔record invariant are read-only, so they are retried, and on exhaustion the run completes on the already-persisted route with the check reported as UNPERFORMED. **What is left is unruled and is capability, not a bug** — today the verdict stands unchanged and the operator gets a loud note saying *"verify the posted block by hand."* The alternative is that a route which could not be verified is downgraded to `HOLD - needs-assistance` automatically. **Consequence of leaving it: an operator can act on a `MERGE` whose render↔record invariant never ran**, and nothing but a note distinguishes that from one that ran and agreed. **DURABILITY IS THE SECOND HALF OF THE SAME RULING, added by PR #71's refine pass and deliberately NOT a separate row** — the note lives in `ReviewResult.notes`, which reaches an operator watching the run and nothing else, while every other computed-arm signal (`routed_outcome`, `undetermined_reason`, `channels_agree`) is written by `append_parent_route` and is countable offline. So **no replay can say how often this invariant degraded**, which is the per-reason rate the phase exists to produce; the docstring was corrected from *"recorded"* to *"reported"* rather than left claiming a durability the code does not have. Recording it durably means a new stratum in `exit-protocol.md` §2, and whether to add one is the same decision as whether an unperformed check should downgrade — one ruling, not two, which is why this expands the row instead of opening C-057. **It has no phase home** — `grep -E 'retry\|flaky\|transient' phase4_fleet_migration.md` returns only an unrelated `api_retry` enum checkbox, re-verified at placement. It generalises past this one check: the same question applies to every parent-side verification the fleet grows, which is why it is a policy row rather than a checkbox on one function. Note that **Temporal's retry is the wrong granularity here and would make it worse** — an activity retry is a new attempt, i.e. a second 40-minute review |
| C-057 | An independent check that a finding's `disposition` is TRUE — the one false-convergence mode Phase 5 could not defend against | | PR #75 | | `open` | **Phase 5 enumerated five ways a convergence signal can be fooled and built a check for four; this is the fifth, named as unmitigated rather than smoothed over.** `review-pr` authors `findings[].disposition`; Phase 5's predicate reads it; a reviewer that marks `fixed` what is not fixed makes the predicate agree, silently. **Re-verified as a PROPOSAL at placement, not a defect:** nothing behaves wrongly today — the computed signal agrees with the incumbent flag on every assessable block, and no fire has been observed to be early. **Both figures and their denominators are in [Phase 5 § Measurement](../../../development/memory-management-framework/phase5_convergence_stopping.md), and the second one must not be quoted bare: the self-reported early-fire counter is CIRCULAR** — it is scored on a verdict the same pass wrote, so it cannot move for the very mode this candidate is about — and the observed counter has an empty denominator. This row's proposal-not-defect classification rests on *nothing behaves wrongly*, which the agreement figure supports; it does **not** rest on the early-fire zero, and an earlier version of this Note quoted that zero as though it did — and *"we have no check for X" is not a defect in X*, the distinction the C-053/C-055 placements already turn on. **Consequence of leaving it:** the convergence signal stays permanently ungateable. Phase 5 declines to route on it *for this reason*, so the mode is not a risk today — it is the thing standing between the signal and the exit-criteria milestone [Autonomous Operation](../../../development/autonomous-operation/autonomous-operation.md) is waiting on. **Why it is not C-056 expanded:** C-056 asks what to do when a verification COULD NOT RUN. This asks for a verification that DOES NOT EXIST. Same shape at a glance, different mechanism and different remedy — and C-056's own Note draws exactly this line when it separates the policy half from the defect half. **No done-state today and no phase owns it — and the first version of this sentence was WRONG, which is recorded rather than quietly fixed.** It read *"`grep -n 'disposition' phase4_fleet_migration.md` returns nothing"*; the grep actually returns **three** lines, and the claim was written from expectation instead of from the command. Run at placement, the three are: `:55` naming the `disposition.md` prompt file as a migration surface, `:77` on selecting a `pr_review:` block by run nonce, and `:104` on a parity table. **None is a home for this** — all three are about *addressing and migrating* the record, none about *verifying that a disposition claim is true* — so the conclusion stands and its evidence did not. The lesson is the one this repo keeps re-learning: a grep quoted without being run is a citation to nothing, and a correct conclusion resting on a false premise is one edit from becoming a wrong one. The remedy is a decision, not a task — the only known mechanism is a second actor re-checking a sample of `fixed` claims, which is a second review with its own cost, and whether that is worth buying is an operator call **Lens verdicts (`finding-routing.md` §5 gate 4).** `/decide`: BUILD-LATER — the root cause is that one actor both writes and reads `disposition`, and the only remedy that touches the root is a second actor; every cheaper variant (a heuristic on finding text, a diff check) verifies the artifact rather than the claim and would be a fifth thing to trust. `/best-practices`: independent verification of a self-reported completion is standard where the reporter has an incentive (audit sampling, four-eyes release sign-off), and the standard form is a SAMPLE rather than a re-review of everything — which is what makes it priceable at all. Both point the same way: real, not urgent while nothing routes on the signal, and the sizing question is the sample rate. |
| C-058 | A durable store for Kind 2 records, so a typed record can outlive its invocation | | PR #75 | | `open` | **Phase 5 is the first consumer that needs one, and it works around the absence rather than solving it.** Its predicate compares this pass's findings against every prior pass's; the current pass comes from the TYPED exit record, but a prior pass's typed record no longer exists — a Kind 2 record's lifetime is one parent invocation ([`exit-protocol.md` §1](../../exit-protocol.md), and Phase 3's to-do-bit ruling). So prior terms are parsed out of the durable `pr_review:` block as prose, inside the component built to stop routing on parsed prose. **Re-verified as a PROPOSAL:** the workaround is SOUND, not broken — the render↔record invariant makes this pass's block and this pass's record carry identical `(id, disposition)` pairs, so the prose term is a faithful copy of what the typed term was. Nothing is wrong today. **Consequence of leaving it:** every future cross-pass consumer re-implements the same prose parse, and each one re-decides which fields survive the round trip. Phase 5 needed two of the eleven envelope fields; the third consumer may need one the block does not carry, and will discover that after building. **Not an expansion of C-054 or C-056:** those are about a record that arrives wrong or a check that could not run; this is about a record that arrived correctly and then ceased to exist. **No done-state today, deliberately:** a durable Kind 2 store is a component-sized decision — where it lives, how it is keyed, how long it is kept, whether Temporal's event history already is one — and [Temporal Integration](../../../development/temporal-integration/temporal-integration.md) is gated on this component rather than the reverse, so it cannot be settled here. It is a candidate precisely because nobody should build it from inside a phase **Lens verdicts (`finding-routing.md` §5 gate 4).** `/decide`: DEFER-TO-TEMPORAL — the five-whys bottoms out at *no component owns durable Kind 2 storage*, and Temporal Integration's event history may already be one; deciding this from inside a phase whose own component gates that integration would be choosing a store before knowing whether one exists. `/best-practices`: durable execution engines persist activity results as event history precisely so a later step can read an earlier one's output, so the industry answer is *do not build a second store beside the workflow engine*. Both say: do not build now, re-ask when Temporal Integration is planned — which is the trigger this row is waiting on. |
| C-059 | A reader for the run-log `convergence` events, so the LIVE path has a denominator and not only the `gh` archive | | PR #75 | | `open` | **Phase 5 emits a `{"type": "convergence"}` event on every `review-pr` dispatch and nothing reads it.** Its § Measurement figures all come from `replay_convergence_predicate.py` over the GitHub `pr_review:` block archive — a corpus that accrued at the same rate before this phase shipped, so the emission contributes nothing to the gate's denominator. The run-log events are a SECOND corpus carrying facts the replay structurally cannot produce: the `pass_not_evaluable` and `history_unreadable` rates (the replay hardcodes `pass_evaluable=True`, because no archived block has a typed record), and the typed term the live predicate actually reads. **Proposal, not defect** — nothing behaves wrongly; the phase's stated conditions are answerable from the archive today, and this would answer a different and better question. The shape already exists: `replay_completion_predicate.py` reads `.claude/logs/*.jsonl` the same way, and both event types carry `run_id` so `convergence` joins to `parent_route`. **Not filed as an issue** — it is capability that does not exist, and per `finding-routing.md` § 4 that makes it a candidate whatever its done-state looks like. **Lens verdicts (`finding-routing.md` §5 gate 4).** `/decide`: BUILD-WHEN-NEEDED — the five-whys bottoms out at *the emission was added a phase before its reader*, which was correct (a metric over a field nothing writes is a plan), so the remedy is the reader and the trigger is the first question only it can answer. `/best-practices`: instrument-then-read is the normal order and a write-only observable is a known smell only when it stays that way across releases; the guard is that the events accrue in the meantime and are joinable, which they are. |
| C-067 | A per-leg count of tests that actually EXECUTED, so a leg cannot report green having run nothing | | PR #74 | | `open` | **`mutate.sh` accepts pytest's exit 0 as GREEN on all three legs, and an all-skipped run exits 0 having executed no test.** On leg 2 that is harmless — both readings land on "THE GUARD DID NOT FIRE", a refusal to certify. On legs 1 and 3 it is not. Leg 1 exists to establish *the guard is green before it is meaningful* and leg 3 to prove *green again, so the tree restored*; an all-skipped leg establishes neither and is accepted anyway. **Measured on this PR, not reasoned**: a subject whose tests skip while it is pristine and run once mutated yields leg 1 = nothing ran, leg 2 = RED, leg 3 = nothing ran, and the harness prints `✓ MUTATION DEMONSTRATED` over two legs that executed no test at all. **Proposal, not defect** — the behaviour is unchanged from `main` and predates the exit-2 work entirely; what PR #74 added was an exit-code table asserting this ambiguity was already closed, and that sentence is corrected in the same PR. What does not exist is the mechanism: **no pytest exit code carries an executed-test count**, so closing it means leaving the exit-code channel for a structured one (`--junitxml`'s `tests`/`skipped` attributes are the obvious candidate) and changing `run_leg`, which the PR #74 disposition placed explicitly out of scope. **Not filed as an issue** — it is capability that does not exist and would be added, and per [`finding-routing.md`](../../finding-routing.md) § 4 that makes it a candidate whatever its done-state looks like. **Not an expansion of an existing row**: no row covers `mutate.sh`, and the nearest neighbour — the `cpi-decisions.md:1125` entry on `mutate.sh` being pytest-only — is about which *frameworks* a leg can drive, not about whether a leg that ran can be told from one that did not. **Cost of leaving it:** the harness's own headline verdict rests on two legs it does not verify ran, in the one tool whose entire premise is refusing to certify what did not run.  *(Renumbered from C-060 on 2026-08-11 — that ID was taken by an unrelated proposal on `main` while this branch was open.)* |
| C-068 | A precheck that the RESOLVED pytest configuration is compatible with `mutate.sh`'s exit-code table, covering the `addopts` channel no environment variable can clear | | PR #74 | | `open` | **`mutate.sh` classifies pytest's exit codes, and a target repo's own configuration can change which code pytest returns for the same event.** The table's load-bearing premise is *"pytest interrupts to 2 on a collection error unless `--continue-on-collection-errors`"*; with that flag in force a collection error returns **1**, which the harness reads as RED without ever consulting the exit-2 discriminator — issue #72's false certification, reached through configuration rather than through code. **Measured on this PR, not reasoned:** with `PYTEST_ADDOPTS=--continue-on-collection-errors` set, mutating the module under test into a `SyntaxError` printed `✓ MUTATION DEMONSTRATED` and exited 0 over a test that never ran. **The DEFECT half was fixed in PR #74 and is not this row**: `run_leg` now clears `PYTEST_ADDOPTS`, the false *"which this harness never passes"* justification is corrected, and `test_an_inherited_PYTEST_ADDOPTS_cannot_reopen_issue_72` pins it. **What remains is capability that does not exist:** `addopts` in the target's `pytest.ini` / `pyproject.toml` / `tox.ini` supplies the same flag and **cannot be cleared by an environment variable** — closing it means resolving the effective configuration before leg 1 (`--override-ini`, or parsing what `pytest --help`/`--co -q` reports) and refusing or adjusting when it conflicts. Nothing like that exists, and there is **no reproduction of the ini channel today** — this repo's own config sets no `addopts`, which is what keeps this a proposal rather than a second defect. That reproduction test is the discriminator used to split the two halves. **Not an expansion of C-060:** that row is about a leg that RAN but executed nothing, read out of a channel pytest does not expose; this is about the exit code itself meaning something different than the table assumes. Different mechanism, different remedy — but if either is ruled `ship` the other should be read alongside it, because both end at *the exit-code channel is too narrow to carry what this harness needs* and a `--junitxml` structured channel would answer both at once. **No done-state today and no phase owns it.** `mutate.sh` is repo tooling, not phase work; the harness's header markets it as portable (*"four independent runs on a sibling repo each hand-rolled this identical loop"*), so the ini channel becomes reachable the moment it is used outside this repo — which is the trigger, and it has not fired. **Cost of leaving it:** the one tool that decides whether every other guard is trustworthy can be silently mis-calibrated by a config file it does not read, and the failure is a false certification rather than an error.  *(Renumbered from C-061 on 2026-08-11 — that ID was taken by an unrelated proposal on `main` while this branch was open.)* |

### Added 2026-08-10 — placed by the producing run that closed [Phase 3](../../../development/memory-management-framework/phase3_typed_exit_record.md), per [`finding-routing.md`](../../finding-routing.md) §4

**Surfaced while recording Phase 3's run set, and it is the reason that section can state what it states.** The run set was briefed as eight dispatches; reconciling the logs found a **ninth** that the instrument never recorded, and the gap it exposes is capability rather than breakage.

| ID | Candidate | `component` | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|---|
| C-060 | Recording the shadowed pair when the completion gate fails, so the agreement denominator is not conditioned on the prose channel succeeding | memory-management-framework | PR #79 | `ship` | `closed` | **FIXED 2026-08-14 by hand: the pair is now recorded on the failure path too, so the denominator is no longer conditioned on the prose channel succeeding. Three tests, mutation-checked.** **Phase 3's run set is nine live dispatches and the instrument recorded eight.** The ninth emitted a valid typed record and no prose `VERDICT:` line, so `run_claude`'s completion gate raised **before `route()` and before `append_parent_route`** and the pair was never compared. **The run's identity, turn count, exact last text block and root cause live in [step 7b's row-1 account](../../../development/memory-management-framework/phase3_typed_exit_record.md#step-7b--the-run-set-nine-live-dispatches-and-the-three-numbers-it-yields) and are deliberately not retyped here** — an earlier version of this Note carried its own copy of them, which is this file's own C-050 mechanism occurring in the file that holds C-050, and it went stale the moment the row-1 cause was corrected. **Consequence:** `channels_agree` can only ever be written on runs where the incumbent prose channel already succeeded, so the instrument built to weigh prose against the typed record is **structurally blind to prose's own failure mode**, and its agreement figure is conditioned on the very thing it is measuring. One dispatch in nine hit this, so the condition is not vacuous — **though that is not an incidence rate**, since the proximate cause was removed inside the measurement window and did not recur — and **no larger N removes the blindness itself** — [Phase 4](../../../development/memory-management-framework/phase4_fleet_migration.md) widening the denominator inherits the same blindness, which is what makes this worth placing rather than noting. **Re-verified as a PROPOSAL, not a defect — and the supporting sentence needed correcting to survive that re-verification.** It read *"nothing behaves wrongly"*, which is false about the motivating instance: **a real defect produced it — Phase 3's own Stage 6 ordering put the record's tool call last, and a tool call is a terminal action — and it was fixed on `main` by `fb85c3e` thirteen minutes later.** *What makes this a proposal is that the OBSERVATION GAP survives that fix.* The completion gate can raise for any reason — an early stop, a truncated turn, a model that stops after any terminal-feeling action — and on every such path `append_parent_route` structurally cannot write a route the parent has not computed. The fail-loud contract itself worked exactly as designed: the run failed, an operator saw it, the work was redone on the next dispatch. **So the row is stronger than the version it replaces, not weaker:** it no longer rests on the claim that the instance was causeless, which was the one claim in it that a reader could falsify. The remedy is a **new observation point** on the gate-failure path, recording that the child produced a record while the prose channel did not; that is capability that does not exist, and *"we have no measurement of X" is not a defect in X*, the same line the C-053/C-055/C-057 placements turn on. **Not C-056 expanded, and the distinction is the stage:** C-056 asks what to do with a verification that could not run **after** the route was persisted; this is a comparison that never started because the route was never computed. Different mechanism, and C-056's remedy vocabulary (annotate / record / downgrade an existing note) does not produce this one. **No done-state today:** what a parent should record when its child fails the *incumbent* contract is unruled, and making it durable needs a stratum for *"the run did not get far enough to route"* that [`exit-protocol.md` §2](../../exit-protocol.md) does not have — the same shape as C-056's blocker, which is why both wait on the same ruling. **Lens verdicts (`finding-routing.md` §5).** `/decide`: **BUILD-WITH-PHASE-4** — the five-whys bottoms out at *the incumbent channel's emission is a prompt instruction with no schema enforcement* (step 8 already names it as the weaker half), and the instrument that would measure that weakness sits **downstream of the gate that enforces it**; the remedy only earns its stratum where the denominator is meant to become a rate, which Phase 3 explicitly declines to be. `/best-practices`: this is **survivorship bias** in a channel A/B comparison — a named measurement-design defect — and the standard remedy is to record at the point of **assignment** rather than of **completion** (the intention-to-treat discipline), which maps directly onto recording the pair when the dispatch returns rather than when it clears the incumbent's gate. Both say the same thing: real, cheap in principle, not urgent while nothing reports a rate, and **load-bearing the moment Phase 4 states one**. **Nor is it step 4's already-fixed gap wearing a new name, and the phase doc's [step 7b](../../../development/memory-management-framework/phase3_typed_exit_record.md#step-7b--the-run-set-nine-live-dispatches-and-the-three-numbers-it-yields) now rules that distinction rather than leaving it to a reader:** step 4's was reachable by moving an append that already ran on the path, so it was repaired on the spot; this one sits upstream of any route being computed, so there is nothing to move. Same principle, different reach — which is what separates a repair from a proposal, and the reason a reader who spots the resemblance should not read this placement as motivated. |
| C-061 | Research and design a memory PROTOCOL — the record types, their stores, their wire formats and the rule for adding a fourth — as one designed set rather than three arrived at separately | | operator, 2026-08-10 | | `open` | **FEATURE / RESEARCH REQUEST, not a defect — nothing is broken.** The framework works and is shipping. But it was researched as a TWO-channel design (`dual_channel_outcome_records`), a third type is being added by inference, and the durable record alone now spans **five surfaces, two substrates and two different to-do mechanics** — each defensible alone, none designed as a set. **The research already cites OpenTelemetry but only for record SHAPE (§2.5, attributes-vs-body); its metrics model — the industry's actual answer to where measurement data lives — was never opened.** Measurement is not in the *deliberately NOT commissioned* list either, so it was never considered and rejected; nobody asked. Operator's framing: *a select well-designed few that cover the bases and allow for future growth*. Ship the current design at 80% first, then refine through this. |
| C-062 | Extending the measurement-figures gate to cover QUANTITIES, so a memory or size figure restated in the roadmap is caught the way a count already is | | PR #80 | | `open` | **The repo already built the right-shaped remedy for restated figures and keyed it one class too narrowly.** `testing/scripts/tests/unit/test_measurement_figures_are_cited.py` sweeps every roadmap line mentioning a phase and fails when a figure is restated instead of cited; its own docstring records that four consecutive passes each fixed what they could see while the frontier never shrank, and concludes *"Changing what the check keys on does."* **Its `FIGURE` pattern (`:45-51`) matches `N of M`, `~N%`, mutation counts and `N assessable/scorable/archived` — and no quantity with a unit.** **Measured on the run that placed this row:** `roadmap.md` carried `666 MiB` on a line that also mentions Phase 6, so it sat squarely inside the gate's sweep and passed clean; the restatement was caught by a human reviewer instead. **Consequence of leaving it:** the gate reads as covering restated figures and covers only the shapes someone happened to enumerate, which is worse than an absent gate — a green check is taken as evidence the class is handled, so the next quantity restated into a summary is found the same way this one was, by luck. **PROPOSAL, not a defect, and the line matters here:** nothing behaves wrongly — the gate does exactly what its pattern says, and every figure shape it was written for is still caught. This asks for coverage that has never existed, which per [`finding-routing.md` § 4](../../finding-routing.md) makes it a candidate whatever its done-state looks like. **Deliberately scoped to quantities and NOT to the general class.** The same run also found retracted *reasoning* restated in a summary (a roadmap Key Decision stating a growth rule its phase doc rules wrong), which is the same failure one abstraction up — but a regex cannot key on that, and proposing a gate for it would be proposing a thing nobody knows how to build. **The narrow half is a units alternation on an existing pattern; the wide half is an open question and is named here rather than bundled in.** **Done-state today: yes** — extend `FIGURE` with `\b\d+(\.\d+)?\s*(MiB\|GiB\|MB\|GB\|KiB)\b`, then fix whatever the newly-red sweep surfaces. That second half is the unbounded part and is why this is a candidate rather than a one-line fix folded into the PR that found it. **Lens verdicts (gate 5): `/decide` — not dissolved.** The upstream question is *why does a figure get restated at all*, and the answer is that prose cannot derive; the gate is the only place that catches it. **`/best-practices` — SURVIVES.** Assert-on-derived-values is standard practice and the repo already has the right-shaped instrument for counts. **Not resolved into a known fix:** extending the gate requires deciding what counts as a quantity in prose, which is design work rather than a line change — the producing run flagged that a reviewer could reasonably say it should have just fixed it, and this is the adjudication: it should not have. |
| C-064 | A bounded wait and an auth discriminator on the CLI invocation, so an authentication failure is distinguishable from a kill and does not sit through ten retries unobserved | | PR #81 (Phase 4) |  | `open` | **Phase 4 step 3 asks a run to record what the `system/api_retry` error enum is used for, or state that nothing reads it. Nothing reads it — `grep -rn "api_retry\|rate_limit_event" scripts/ config/ --include=*.py --include=*.sh` returns ZERO on 2026-08-10, unchanged since Phase 1 — and stating that honestly means stating what it costs.** [Phase 1 E1(e)](../../../development/memory-management-framework/phase1_measure_the_channel.md) measured auth failure and `SIGTERM` as **indistinguishable in the result envelope**: both give `subtype: error_during_execution`, `terminal_reason: aborted_streaming`, `stop_reason: null` and the same opaque `[ede_diagnostic]` string in `errors[]`. The **only** discriminator is the `system/api_retry` stream events (`error_status: 401`, `error: "authentication_failed"`), which every dispatch already writes to `$LOG_FILE` and no code opens. **The second half is the one with teeth:** neither auth run exited on its own — **both returned 124, which was the measurement's own `timeout(1)` wrapper, after 9 retries against `max_retries: 10`** — and **nothing in `run-claude.sh` wraps the CLI in a timeout**. So the fleet's behaviour on an auth failure is a long unattended wait whose upper bound is the CLI's retry policy rather than anything this repo chose. **PROPOSAL, not a defect, and the distinction is which sentence you read.** Nothing behaves *wrongly*: the run does terminate, the completion gate fails it loud, and no wrong route is taken. What does not exist is (a) any reader of the discriminator and (b) any bound this repo owns on the wait. Both are capability that has never existed, which per [`finding-routing.md` § 4](../../finding-routing.md) makes this a candidate whatever its done-state looks like. **Deliberately NOT sized as one item, and the split is the useful part.** The timeout is small and has a done-state today — a `timeout` around the `claude` invocation in `run-claude.sh`, its duration taken from the observed retry envelope rather than guessed. Reading `api_retry` is larger and touches the fail-safe contract: it needs a stratum for *why the process died* that [`exit-protocol.md` §2](../../exit-protocol.md) does not have, which is the same blocker C-056 and C-060 both wait on. **A run shipping the timeout alone does the valuable half; a run shipping the reader alone adds an observable nobody routes on, which is the admission failure Phase 6 exists to stop.** **Not an expansion of C-060:** that is a comparison that never started because the gate raised; this is a failure mode the envelope cannot name at all. **Not filed as an issue** — the default is against filing, and its second half has no done-state today. **Why it surfaces HERE rather than in Phase 1, which measured it:** E1(e) surfaced it *to the operator in the PR body*, which `engineering-quality.md` names as the one placement that is never valid, and it has sat in a completed phase doc since 2026-08-08 with nothing pointing at it. |



### Added 2026-08-11

| ID | Candidate | `component` | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|---|
| C-065 | A stated rule for load-bearing PROSE: a comment or prompt sentence asserting how ANOTHER file behaves either names the check that holds it, or is not written | | PR #81 (Phase 4, refine pass) |  | `open` | **DISTINCT FROM C-050 AND THE SPLIT IS THE USEFUL PART. C-050 is about a figure RESTATED in two places; this is about a claim ASSERTED once and false.** Measured on one PR: four wrong load-bearing prose claims, and they split cleanly into the two classes. C-050's class — the phase doc's `is_error` and `parse_verdict` citations, dead line numbers and counts moved by the PR's own diff; and `review_pr_workflow`'s docstring naming FIVE deferred pure functions with nothing enforcing the set. **The other class is what this row is for, and neither a restatement gate nor a resolve-check can see it:** (1) `review_pr_helper.completion_ref_mismatch_note` told an operator, at the moment of the failure, that the field's number *"flows into `gh pr view`, `gh pr comment` and `--pr`"* — nothing in the tree reads `completion_ref` for anything but that comparison, so a reader would have believed a cross-PR WRITE was averted and stopped looking, while the derivation that does reach `gh` was unguarded; (2) `disposition.md` told the child that a quoted prior block *"is now correctly ignored either way"* — `thread_snapshot` keeps only the LAST block per comment, so a quoted block placed below the child's own displaces it entirely and no nonce rescues it. **Both were syntactically fine, both cited real files, and both were false about what those files do.** **Consequence, and it is why the class is worse than the restatement class: a false statement in a file readers are trained to trust actively STOPS the next person checking** — a docstring is read instead of the code it describes, and a prompt sentence is followed instead of the parser it describes. **PROPOSAL, not a defect.** Both instances are fixed in the PR that found them; what does not exist is any rule or check covering the class. **Done-state, and it is deliberately the small half: a stated convention in the documentation standard** — *a comment asserting the behaviour of code in another file names the test that holds the claim, or states the claim as of a date, or is not made* — plus the observation that the repo ALREADY applies this well where it applies it at all (`SINGLE_CONSUMER_PARENT_MODULES`, the AST gates, the negative controls) and has simply never decided the boundary between prose-that-is-a-control and prose-that-records-a-measurement. **NOT proposed as a regex gate, and that is a finding rather than a gap:** C-062's row records that a prototype for the adjacent status class returned 9 hits of which 2 were real, and a cross-file behavioural claim has strictly less syntactic signature than a status. Proposing a gate here would be proposing a thing nobody knows how to build. **Not an expansion of C-050:** C-050's remedy is *derive rather than restate*, which is unavailable here — there is no second copy to derive from, only a claim about another file. Applying C-050's remedy to this class would produce nothing.  **AMENDED FROM PR #83 (research_minor, refine pass) — three more instances, and they identify a SUB-CLASS with a cheaper remedy than the general rule.** All three are **stale CROSS-REFERENCES**: a sentence naming a location in another document that was correct when written and was falsified by a later edit somewhere else. (1) `research_activities.upstream_block()` ended with *"Your sizing in Stage 2 must state which topics upstream already covers"* — true while `research_write` was its only consumer, false the moment `research_write_minor` injected the same block into a prompt with no sizing stage, which then ordered an assessment that prompt separately forbids. (2) `research_verify/prompts/verify.md` opened Stage 1 with *"For each paper written or updated in Stage 3"* — a reference to a DIFFERENT workflow's stage numbering, which the minor cycle does not share. (3) Two new workflow docstrings cited *"Research Standard §3"* for the sizing rubric, which lives in **§2** — and the same PR's own C-066 row cited §2 correctly, so one artifact disagreed with itself. **Why this sharpens rather than merely corroborates C-065:** the general class ("a claim about how another file behaves") was judged un-gateable — the row itself records that a prototype for the adjacent status class returned 9 hits of which 2 were real. **This sub-class is different, because both halves are syntactically distinctive and mechanically checkable:** a `§N` citation resolves against the target document's own headings, and a `Stage N` reference resolves against the cited prompt's `## Stage N:` headings. Neither needs to understand behaviour — both are name-resolution, the same shape as the link-checking this repo already does. **That makes a gate proposable here where it was not proposable for the parent class**, and it is offered as a narrowing of C-065's done-state rather than as a separate row. All three instances were fixed in PR #83. |
| C-066 | A differential baseline the mutation probe can establish even when the pre-mutation import fails, so it answers instead of abstaining | | PR #74 | | `open` | **The probe resolves exit 2 DIFFERENTIALLY — `HARNESS_ERROR` only when `$FILE` imported cleanly BEFORE the mutation and does not now. When it cannot establish that baseline it abstains to RED and says so, rather than asserting a cause.** That abstention is correct and is deliberately not a defect: asserting a cause it cannot establish is how #72's false certification happened. **What has no home is the PROPOSAL for removing the ambiguity** — its only record was prose inside a 728-line shell script, which `plan-sprint` does not read, so it died at merge. `testing/README.md` claimed all three deliberately-open ambiguities had a candidate; two did (C-060, C-061) and this one did not. **Lens verdicts (gate 5): `/decide` — not dissolved**, the upstream question is whether a baseline can be established at all without importing, which is a real unknown rather than a framing error. **`/best-practices` — SURVIVES**: differential mutation testing is standard, and an abstention rate is a known quality metric for one. **Not resolved into a known fix** — nobody knows the mechanism yet, which is precisely why it is research rather than a task. |
| C-069 | A COARSE RESOURCE SERIES beside the peak and the mean, so a memory spike has a timestamp and can be attributed to something | | PR #82 (Phase 6) |  | `open` | **The largest measurement this fleet has ever taken cannot be attributed to anything, and the instrument is why.** `_Sampler` polls every 2 s and keeps a running max, a running mean and nothing else — the series is discarded as it is read. **Measured 2026-08-11 by `python3 scripts/helpers/measure/replay_run_resources.py` over 17 fleet records** — a dated reading; [Phase 6 § Measurement](../../../development/memory-management-framework/phase6_read_what_it_writes.md) is the live one and this row does not track it:** one `build-draft` run peaked at **12.891 GiB** of anonymous memory against a **0.614 GiB** mean over 1761 samples, while **neither knob explains it** — it spawned **0** sub-agents and pulled **0.61 MiB** of tool results, both at or below the corpus median, and the next-highest peak in the corpus is **0.552 GiB**, a 23× gap. **Consequence:** figure 3 exists to separate sub-agent COUNT from content VOLUME, and the one run where the answer would matter most is a run where the answer is *neither*, with no way to ask *when*. A 21× peak-to-mean ratio says the spike was brief; nothing says which minute, which tool call, or whether it recurs. **The next occurrence produces exactly the same unattributable number**, which is the shape of the 2026-08-10 livelock one level down — an event nobody could locate. **PROPOSAL, not a defect:** the sampler does precisely what it says, every emitted field is correct, and no figure this phase built is wrong because of it. What does not exist is retention, which per [`finding-routing.md` § 4](../../finding-routing.md) makes it a candidate whatever its done-state looks like. **PRICED, because the phase brief asked for a price rather than a silent design-around.** The cheap arm has a done-state today and is genuinely small: keep a bounded ring of `(offset_seconds, anon, total, pids)` at the existing 2 s cadence and emit it only when `peak_anon` exceeds some multiple of `mean_total` — a 30-minute run is ~900 samples, and at four small integers that is well under 30 KB of JSONL for the rare record that earns it, with every other record unchanged. **The expensive arm is the one that makes it a candidate rather than a fix: WHERE the series lives.** A per-sample series is a different retention class from a per-run record — it is the first thing on this surface whose size scales with run LENGTH rather than with run COUNT, and the run log is machine-local, gitignored and pruned by nobody. **That is a Kind 3 retention question the surface has no rule for, and inventing one inside a build PR is exactly the "arrived at separately" failure C-061 was raised to stop.** **Not an expansion of C-061:** that asks for the record types and their stores to be designed as a set; this is one concrete retention question that C-061's design would have to answer, and it is recorded so that design has a real instance to answer rather than a principle. **Not a defect against `resource_telemetry`:** its own docstring already states the limitation it has (`peak_anon` is sampling-dependent), and this is the adjacent limitation it does not state — recorded here, and now stated at the module, in the same change. |
| C-071 | Naming research cycle SHAPE in the Research Standard, so §2's sizing rubric can route a question to the one-paper cycle instead of only telling a run how many topics to pick | | PR #83 (research_minor) |  | `open` | **PROPOSAL against a VENDORED (MIRROR) standard — it is placed here rather than made, because `docs/standards/research/` must not be edited in this repo and an amendment goes upstream to MDC-Master-Planning then re-vendors.** **The consequence, which is the reason this is a finding and not a note:** §2's rubric (`research_standard.md:53`) describes topic COUNTS and nothing else — Small is *1–2 topics*. It has no vocabulary for the SHAPE of a cycle, so a run reading it to decide how much research to do can size a question correctly at Small and still emit `topics.md`, a fan-out, a synthesis and a verify pass over all of it. **That is not hypothetical: it is exactly what happened.** A portfolio-direction question was assessed as Medium, produced five papers and a synthesis at ~3.5 hours, and the operator called it *"mass overkill"* — and the important half is that **sizing it correctly would not have fixed it**, because the architecture below the rubric does not change with the topic count. **PR #83 built the missing shape (`research_minor`: one paper, no `topics.md`, no synthesis, same `research_verify` and same `research-critic` gate) and deliberately did NOT touch the standard.** So the capability now exists and the document that a run consults to choose it does not mention it — a run following the standard faithfully will never reach the cheaper cycle. **Done-state today: yes, and it is small** — a row or a sentence in §2 stating that a single-concern question with one destination is answered by a one-paper cycle, alongside the existing count rubric. **The bounded part is deliberately excluded from the proposal:** whether the bands themselves should be re-cut is a separate question with its own evidence requirement, and §2 already says a single overrun is an observation rather than evidence. **NOT a defect:** nothing behaves wrongly — the standard is silent, not incorrect, and every §3 per-paper obligation is preserved in full by the new cycle, so a paper it produces is standard-conforming today. **Not an expansion of an existing row:** no candidate concerns the Research Standard's own sizing text. **Not filed as an issue** — it is capability that would be ADDED to a document, which per [`finding-routing.md` § 4](../../finding-routing.md) makes it a candidate whatever its done-state looks like, and the default is against filing.  *(Renumbered from C-066 on 2026-08-11 — that ID went to an unrelated proposal on `main` while this branch was open.)*  *(Renumbered from C-069 on 2026-08-12 — PR #82 merged first and took that ID.)* |
| C-072 | Promote the research family's shared RULES footer to one fragment, so the four prompts that carry it stop drifting apart | | PR #83 (research_minor, refine pass) |  | `open` | **The consequence, and it is already happening rather than predicted: the four copies have DIVERGED.** `research_write/prompts/write.md`, `research_verify/prompts/verify.md`, `research_refresh/prompts/refresh.md` and the new `research_write_minor/prompts/write_minor.md` each end in a near-identical RULES block — Bash-CWD, Re-Read-before-Edit, large-file reading, parallel gather, the git-status check. They are not identical: `verify.md`'s relative-paths bullet is one line where `write.md`'s is a paragraph, and `refresh.md` carries an abbreviated set. **So the four workflows are being given materially different operating rules for the same hazards, and nothing anywhere records that this was a decision.** **The rule is already binding and already crossed:** [`temporal_standard.md` §10.1 rule 3](../../temporal/temporal_standard.md) — *"promoted OUT of a workflow's folder to the parent level if and only if MORE THAN ONE workflow uses it — consumer count decides, never taste"* — echoed for `.md` fragments specifically in [`claude-dot-files-addendum.md` §A4](../../temporal/claude-dot-files-addendum.md). The threshold was crossed at three copies, before PR #83 existed; that PR added the fourth. **The mechanism and the precedent both already exist:** `act.shared_prompt()` loads module-level fragments, and the build family already made exactly this migration — `build_draft`, `build_draft_minor`, `build_refine` and `build_refine_minor` all inject `${RULES}` from one `modules/assistant/prompts/rules.md`. **Why it was not simply done in PR #83, stated so the deferral can be judged rather than taken on trust:** the remedy is not an edit to the file that PR authored. Unifying four drifted copies means choosing which wording wins and then rewriting three prompts belonging to workflows that PR does not own — a change to what three live, verified workflows receive at dispatch, made incidentally inside a PR whose stated design principle is that the full research cycle stays byte-unaffected. **Done-state today: yes.** A `research`-purpose-level fragment plus the plumbing to load one (`shared_prompt()` currently resolves only against `modules/assistant/prompts/`, so a purpose-level fragment needs either a second loader or promotion all the way to module level — that choice is part of the work, not a blocker on it). **PROPOSAL, not a defect:** nothing behaves wrongly today, and each prompt is individually coherent. **Not an expansion of C-050 or C-065:** both concern a claim that is FALSE — a restated figure and a cross-file assertion. Every sentence in all four footers is true; the defect is that there are four of them.  *(Renumbered from C-067 for the same reason.)*  *(Renumbered from C-070 on 2026-08-12 — PR #82 merged first and took that ID.)* |
| C-077 | A stated placement rule for a SHARED TEST HELPER, so the second test module that needs one stops choosing between copying it and inventing a home | | PR #85 (triage split, refine pass) |  | `open` | **PROPOSAL against a VENDORED (MIRROR) standard — placed here rather than made, because `docs/standards/testing/` must not be edited in this repo and an amendment goes upstream to MDC-Master-Planning then re-vendors. Same mechanism as C-071 and C-073.** **The consequence, and it is why this is a finding rather than a note: the standard's silence prices COPYING at zero and the correct answer at a design decision.** A test module needing a helper that a sibling already has faces three options with no rule between them — copy it (two definitions that drift), import the sibling test module (a module pytest COLLECTS rather than imports, which is the coupling `review_run_fakes.py`'s own `file_structure.txt` annotation records as having broken at collection, issue #72), or invent a non-`test_`-prefixed module in `tests/unit/` and hope that is sanctioned. **The drift arm is not hypothetical and this repo has paid for it twice**: `plan_activities.normalise_cell` exists because two hand-written normalisations of one cell diverged and a row read RULED to one reader and BLANK to the other, and this PR removed a second instance in the making. **Two independent instances now exist and neither was guided by a rule** — `review_run_fakes.py` (the review_pr fakes) and `observer_registry.py` (the shared registry-entry resolver) — each arriving at the same shape by reasoning it out from scratch. A standards audit of this PR classified the placement as *tolerated by precedent rather than codified*, which is exactly the state in which the third instance goes the other way. **Done-state today: yes, and it is one sentence** — a clause in the Testing Standard's tier section stating that a helper shared by two or more test modules in a `tests/` directory lives in a sibling module without the `test_` prefix, named for its purpose, and is entered in the repo map like any other file. **NOT a defect:** nothing behaves wrongly, both existing helpers are placed correctly, and every test passes. The standard is silent, not incorrect. **Not an expansion of C-060, C-061 or C-066:** those concern the mutation probe's exit semantics and telemetry retention — different document, disjoint remedy. **Not an expansion of C-072:** that is about four DRIFTED prompt copies under a rule that already binds and was already crossed; here no rule exists to cross. **Not filed as an issue** — capability that would be ADDED to a document, which per [`finding-routing.md` § 4](../../finding-routing.md) makes it a candidate whatever its done-state looks like, and the default is against filing. *(Allocated C-075, not C-073: PR #84 landed C-073 and C-074 on `main` while this branch was open — checked with `git show origin/main:…/candidates.md` immediately before writing this row, per this file's rule that an id is never reused.)* |

### Added 2026-08-12

| ID | Candidate | `component` | Source | `decision` | `status` | Note |
|---|---|---|---|---|---|---|
| C-073 | A declared NORMALISATION convention in Research Standard §3, so a paper's verbatim claims are mechanically checkable instead of re-derived by every verifier that reads it | | PR #84 (research_minor, verify pass) |  | `open` | **PROPOSAL against a VENDORED (MIRROR) standard — placed here rather than made, because `docs/standards/research/` must not be edited in this repo and an amendment goes upstream to MDC-Master-Planning then re-vendors. Same mechanism as C-071.** **The consequence, and it is why this is a finding rather than a note:** §3's verbatim rule (`research_standard.md:106`) says *"A span may be labelled or presented as a quotation only if its exact character sequence was returned by a fetch"* and stops there. It never says how a paper DECLARES the transformations that unavoidably sit between a source's bytes and the paper's rendering of them — and they are unavoidable: markdown and asciidoc inline markup inside a quoted span (`[share-nothing](http://…)`, `<<def_SHA1,SHA-1>>`, `_30 days_`), source line-wrap and sentence-boundary double spaces, typographic apostrophes and quotes in `pdftotext` output, and whether an elision is marked at the end of a span as well as inside it. **Undeclared, a verifier re-derives the whole transformation set from scratch and reports a FALSE MISS on every span whose deviation it has not yet guessed.** **Measured on this PR, and the shape of the measurement is the argument:** `cross_node_memory_protocol.md` took **five** independent critic passes, and **after round 1 not one blocking evidence defect existed** — no fabricated source, no fabricated span, no miscitation, no inflated mark, no wrong count, across 28 sources and 90 quoted spans, each git-hosted span verified by clone-and-`grep -F`. **Rounds 2, 3 and 4 each found a defect in the CONVENTION, never in the evidence.** The paper's original blanket claim (*"the exact characters returned — the two exceptions are marked"*) was over-absolute; its four-rule replacement was incomplete by two whole classes (PDF typographic marks, and local-file spans); that replacement's rule 4 was then over-absolute in exactly the way the blanket claim had been, with 19 counter-examples; fixing rule 4 left rule 2 asserting a universal its own new text falsified. **The cost is not hypothetical:** round 2's sweep reported sound spans as deviations, and the analyst's own truncation detector returned 39, then 23, before reaching the true 19. **So nearly the entire correction budget on a paper whose evidence was clean went to inventing a convention no standard asked for — and the next paper reinvents it or goes without.** **Done-state today: yes, and small** — a clause in §3 beside the verbatim rule requiring a paper to state the normalisation between its sources' bytes and its own rendering, each rule scoped to what was measured, its exceptions named, and its counts' populations stated. **This PR's §6 is a worked exemplar to draw from, including its failure mode, which is the half worth codifying: a normalisation stated as a UNIVERSAL generates false findings and is worse than none.** **NOT a defect:** nothing behaves wrongly — §3 is silent, not incorrect, the paper conforms today, and its `Critic:` verdict is `PASS`. **Not an expansion of C-071:** that concerns §2's sizing rubric and cycle SHAPE; this concerns §3's sourcing rules. Different section, disjoint remedy, neither subsumes the other. **Not an expansion of C-065:** that class is a claim that is FALSE about another file. Every span here was TRUE — what was missing was any stated way to CHECK it. **Not filed as an issue** — capability that would be ADDED to a document, which per [`finding-routing.md` § 4](../../finding-routing.md) makes it a candidate whatever its done-state looks like, and the default is against filing. |
| C-074 | Rule whether the persistent memory protocol is its OWN COMPONENT or a PHASE of the Memory Management Framework | | PR #84 (cross-node memory prior art) | | `open` | **The research explicitly could not settle it, and said so.** §5.1: no source bears on component-vs-phase, and the paper names the trap — mechanism-list length reads as an argument for a dedicated component while being equally consistent with a phase. **It decides where the roadmap and phase docs get written**, so it gates planning rather than building, and four in-progress sprints consume the answer (CPI, Workflow Decomposition, Memory Management, Temporal Integration). **Placed as a candidate rather than written straight into `direction.md`**: a `D-` row requires a `Source: C-NNN` and is `plan-sprint`'s write, so placing one directly would skip the convention and self-grant that authority. **Lens verdicts (gate 5): `/decide` — not dissolved**, the upstream question is genuinely open and the evidence is absent rather than unread. **`/best-practices` — SURVIVES**: component boundaries are ratified by an owner, not derived from a mechanism count. **2026-08-13 — the substance is now argued, and the `decision` is still the operator's.** A `plan-revision` run implemented the **own-component** reading and stated its reasoning against §0's test in [`persistent-memory-protocol/roadmap.md` § *Why this is a component and not a phase*](../../../development/persistent-memory-protocol/roadmap.md) — deliberately **not** from the mechanism count this row warns about, but from a comparison of two stated ownership sentences, with three further checks (it inverts authority over MMF's own outputs; MMF is six-of-six complete so a seventh phase would falsify its completion claim; its scope sentence names git, which synthesis §10 rules is the coding edge's binding rather than the protocol's). **`decision` left BLANK on purpose** — setting it is `plan-sprint`'s write and the ruling is the operator's. If the operator rules the other way the remedy is mechanical: the phase docs move into `memory-management-framework/` and renumber from 7. |
| C-075 | Scope a correction round's re-verification to the spans that CHANGED, so `research_verify` stops re-downloading a corpus it has already verified | | PR #86 (research_minor, verify pass) |  | `open` | **`verify.md` already NAMES this defect and deliberately does not fix it** — *"The real defect is that a correction round re-verifies everything rather than what changed — `author != judge` needs a fresh JUDGMENT, not a fresh DOWNLOAD, and this stage conflates them. Until that is rebuilt, 3 bounds the damage."* The 3-round ceiling is therefore a **damage bound standing in for a capability**, and the capability is what this row proposes. **The cost the bound was set against, from `verify.md`'s own measurement of PR #84:** five critic passes over 28 sources re-fetched from scratch every round, **$58.62 and 135 minutes in this stage alone — six times the $9.14 the paper cost to write** — at 441 Bash calls against 1 WebFetch. **Measured on THIS PR, which is the new half:** round 1 ran the full pass (18 external sources, every git-hosted span by shallow-clone + `grep -F`); **rounds 2 and 3 were scoped to the repaired spans only, re-cloning nothing**, on the basis Research Standard §3 already states — *"A repaired span is a new claim"*, carrying an original's full sourcing burden. **Scoping did not cost detection: round 2 found a blocking defect round 1 had missed** (four internal cross-references reading `§4.3.3` where the content is `§4.3.4`), and round 3 closed at PASS with zero blocking. One paper, closed inside the ceiling — one data point toward `verify.md`'s own stated 90%-inside-3-rounds target. **Why this is a finding and not a note: the scoping existed only in the orchestrator's hand-written dispatches.** Nothing in `verify.md` asks for it, so the next run either reinvents it or re-downloads the corpus, and the ceiling keeps bounding a cost that no longer has to be paid. **Done-state today: yes, and small** — a clause in Stage 1 stating that round 1 is the full pass and rounds 2+ verify the repaired spans and their dependents, plus the guard this run had to write by hand: the analyst's span list is **a claim to check against the diff, never a map to trust** (round 3's critic found one genuine edit the analyst's own inventory omitted, and mis-attributed its round because no intermediate commit existed to separate them). **NOT a defect:** nothing behaves wrongly — the stage is correct and expensive, and `verify.md` chose the bound knowingly. **Not an expansion of C-073:** that proposes a normalisation convention in Research Standard §3, a vendored document, so a verifier stops re-deriving what counts as verbatim; this proposes a scope rule in this repo's own `verify.md` so a verifier stops re-fetching what it already checked. Different document, different owner, disjoint remedy. **Not filed as an issue** — capability that would be ADDED, which per [`finding-routing.md` § 4](../../finding-routing.md) makes it a candidate whatever its done-state looks like. *(Id assigned as the next free one on this branch; if a PR merges first and takes C-075, this row renumbers on the file's existing rule that ids are never reused.)* |
| C-076 | Automate the fleet's deployment — server side, edge provisioning, and the git-push-redeploys path that cross-edge CPI depends on | | persistent-memory-protocol synthesis, 2026-08-12 | | `open` | **Surfaced while settling how CPI crosses the edge/server boundary, and deliberately NOT folded into that component.** The persistent-memory synthesis §13 settles CPI sequencing by having Edge1 make changes and push to git, **which redeploys the server side** — so an automated deployment path is a dependency of a decision already taken, not a nice-to-have. **This is delivery, not memory**, and putting it in a memory component would bury a whole domain inside a feature. **Sized as its own sprint** by the operator: server setup, edge provisioning, and the redeploy trigger are three separate build surfaces. **Placed rather than described in the synthesis**, because a proposal that lives only in a synthesis dies the next time a research cycle rewrites that file — which for this pool is expected rather than hypothetical. |
| C-079 | A full research cycle on journal format at volume, and on redaction/classification for records crossing a trust boundary — the second GATES cross-edge aggregation | | persistent-memory-protocol plan (PR for `plan-revision`, 2026-08-13) | | `open` | **The synthesis names both as *"genuine research questions"* and *"the strongest candidates for a full cycle on this topic"*, and neither has a home.** They are placed as ONE row because one research cycle answers both — same corpus, same pool, and the format question is what determines which fields a classification rule would even have to range over; splitting them would commission two sweeps of the same literature. **The consequence, and it is why this is a finding rather than a note:** the classification half is a **hard gate on [Phase 7](../../../development/persistent-memory-protocol/roadmap.md#phase-7--cross-machine-aggregation-writing-locally-firstphase7s3aggregationmd)** — shipping raw records to a store every edge reads crosses a trust boundary nobody has drawn, and the [heartbeat pollution paper](https://arxiv.org/pdf/2603.23064) measured pollution reaching durable memory at rates up to **91% without prompt injection**, ordinary misinformation sufficing. Unresearched, the gate is opened by whoever happens to be building at the time, from priors. **THE CLASSIFICATION HALF IS TWO QUESTIONS, AND COLLAPSING THEM IS HOW THE SECOND GETS SKIPPED — surfaced by this plan's security review and worth carrying into the cycle's scope.** *(a) EGRESS: what may leave this edge* — the per-field classification above. *(b) INGRESS: what may this edge BELIEVE* — how a reader treats another edge's records, whether they are origin-authenticated, and what a reader may *act on* versus merely display. **The heartbeat paper is evidence for (b), not (a):** pollution is an *integrity* threat, so a build could classify every field correctly and still ship a reader that treats a polluted record as fleet history — and under the plan's rebuild test the stores are **projections**, so that record replays straight into this file. A BagIt manifest does not close it either: it is regenerable by any writer, so it proves integrity against accident and transport corruption, not against a party with write access — which is exactly the party a shared store introduces. **One cycle answers both; the row names both so satisfying one cannot be mistaken for satisfying the other.** **The question is smaller than it first looked and that is itself a research input:** synthesis §11 records that §2 changed its shape — every byte in the journal was *deliberately* written to a durable surface and most is already public in a GitHub PR, so the question is **per-field, and most fields answer themselves**. It is still a decision. **Done-state today: yes** — a research cycle has one, and the pool exists (`docs/development/persistent-memory-protocol/research/`). **Not an expansion of C-076:** that is deployment automation, a delivery domain. This is evidence for a memory-component gate. Disjoint remedies, different consumers. **Not a defect:** nothing behaves wrongly; no record crosses a boundary today. **Not filed as an issue** — commissioning research is capability that would be ADDED, which per [`finding-routing.md` § 4](../../finding-routing.md) makes it a candidate whatever its done-state looks like. |
| C-078 | Promote Documentation Standard §0 (component vs phase) out of the excluded *Development Planning Files* section upstream, so a layout-independent binding rule stops being swallowed by a layout-specific exclusion | | persistent-memory-protocol plan (PR for `plan-revision`, 2026-08-13) | `ship` | `closed` | **RESOLVED 2026-08-14 upstream (`1ffbc27`): §0 is now its own top-level section, with the decision binding everywhere and the artifact shapes staying with the layout section.** This is the THIRD instance of one structural hazard in the same vendored document, and the first two are already recorded in [its own README](../../documentation/README.md).** §0 (`documentation_standard.md:69`, `### 0. Component vs phase — what gets a folder (binding)`) states a rule that depends on nothing about a repo's folder layout — *every planning doc is either a new component with a roadmap, or a phase of an existing one; there is no third category.* It sits inside `## Development Planning Files` (line 65), which `README.md:36` excludes wholesale: *"assumes the master-planning layout."* **So a rule marked `(binding)` does not bind here, by structure rather than by anyone's decision.** **The consequence, measured on this very run:** the dispatch brief asserted *"Documentation Standard §0 is binding"*, which is false as cited, and a run that took it at face value would have propagated a wrong binding-standard citation into a new component's roadmap — **the exact defect class the workflow's own Stage 1 warns about, and which it has already produced once in a phase's sizing.** This run caught it and cited `sprint.md` for authority instead, but the next one starts from the same false premise. **The precedent is exact and it is upstream's own:** `README.md:34` records that `§ Completion checkboxes` was structurally swallowed by the Sprint-Tracking exclusion, a producing run read it that way on 2026-08-09 and *"was not being careless"*, and **upstream promoted it to its own top-level section the same day**, retiring the hazard for every consumer. `README.md:38` records a third case where a carve-out *"swallowed the clauses beside it."* The same remedy applies unchanged. **PROPOSAL against a VENDORED (MIRROR) standard** — `docs/standards/documentation/` must not be edited here; the amendment goes upstream to MDC-Master-Planning and re-vendors, same mechanism as C-071 and C-073. **Done-state today: yes, and small** — move one subsection up one level. **Not an expansion of C-073:** that is the Research Standard's §3 sourcing rules. Different document, different section, disjoint remedy. **Not a defect:** §0 is correct and internally consistent; only its placement is wrong, and this repo has a local authority (`sprint.md`) that covers the same ground, so nothing behaves wrongly today. **Not filed as an issue** — capability added to a document, per [`finding-routing.md` § 4](../../finding-routing.md). |
| C-080 | Carry a candidate's Note EVIDENCE into the synthesis `plan-candidates` seeds, so the reasoning that justified the component does not stop at the folder it created | workflow-decomposition | PR for `plan-candidates` (build-draft, 2026-08-13) |  | `open` | **`plan-candidates` seeds a new component's `synthesis.md` from the `Candidate` cell — one line — and the `Note` cell, which is where this file actually keeps its evidence, does not travel.** Measured on the live file the day this was written: the median Note is a paragraph and several run to over 2,000 characters carrying measurements, rejected alternatives and explicit *not-an-expansion-of* rulings. **The consequence is a research cycle re-deriving what a triage pass already established**: `research-write` runs against the seeded pool immediately afterwards, is told the one-line summary is its brief, and has no access to the reasoning that made the candidate `ship` — so a rejected alternative already argued against in the Note is a live option again to the run that reads only the seed. **Why it was NOT done here, and this is the substantive half:** the row parser deliberately stops every cell at the next pipe and never reaches the Note, because **Note text carries unescaped pipes** — and that property is exactly what made inserting the `component` column a safe prefix-only transform. *(A tally was stated here and was falsified by this very row: the count was taken before the row was appended, so the denominator was stale the moment it was written. The load-bearing claim is that no row's first five cells contain a pipe, which no new row can falsify.)* Reaching the Note means parsing the one cell whose content is not pipe-safe, which is a design decision about the file's format, not a line of code. **Done-state today: yes** — either an escaping rule for the Note that the filing prompts enforce, or a last-cell parse that takes everything after the fifth pipe verbatim. **NOT a defect:** the seed behaves exactly as designed and as the operator specified — *"it just needs to move the info over to the correct place"* — and every test on it passes. This proposes carrying MORE than was asked for. **Not an expansion of C-050:** that concerns derived-versus-restated counts in prose. **`component` is `workflow-decomposition`, which already exists** — so `plan-candidates` correctly scaffolds nothing for this row, which is the extends-an-existing-component case working as intended. **Not filed as an issue** — capability that would be ADDED, which per [`finding-routing.md` § 4](../../finding-routing.md) makes it a candidate whatever its done-state looks like. *(Id taken as the next free one after re-reading the file at HEAD and comparing against `origin/main` immediately before writing — both carried 76 rows with highest C-079.)* |
| C-081 | Give a run FILING a candidate the list of existing `docs/development/` components, so a near-miss name forks an existing component instead of extending it | workflow-decomposition | PR for `plan-candidates` (build-refine, 2026-08-13) |  | `open` | **`plan-candidates` decides *extends an existing component* versus *is a new one* by EXACT SLUG EQUALITY** — `docs/development/<slug>/` either exists or it does not. A filer writing `memory-management` against the tree's `memory-management-framework/`, or `temporal` against `temporal-integration/`, therefore gets a SECOND directory beside the real one, plus a `research-write` and a `research-verify` dispatch into it. **That is two homes for one item, which this file's own § Why it exists names as the failure it was built to end.** **The filer cannot check, and that is the substantive half.** The filing instruction lives in `prompts/decision_log_and_reflection.md`, which is injected into every producing workflow, and it says *"an existing `docs/development/<name>/` if it extends one"* — to a run that is handed no enumeration of those names. `act.existing_work` already renders exactly that list, but only into `triage-candidates`, `plan-sprint` and `plan-revision`, none of which is where most rows are filed. So the instruction asks for a comparison against a set the reader cannot see. **The remedy is prompt-side, not code-side, and the distinction is the whole reason this is a candidate rather than a fix.** Rendering the component list into the shared filing fragment changes how a prompt shared by twelve workflows is composed — genuinely outside a `plan-candidates` PR. **A fuzzy-match guard in the activity was considered and REJECTED**: `difflib.get_close_matches` would make the scaffolder decline a legitimately new component whose name resembles an existing one, and the operator's stated basis for building this as code at all is that *"it needs no judgement, which is why it is code"*. Adding a similarity threshold gives it judgement, and a wrong skip is as expensive as a wrong fork. **Done-state today: yes, and small** — one `${EXISTING_COMPONENTS}` substitution in the shared fragment, populated from a directory listing. **NOT a defect:** every documented condition behaves as specified and every test passes; nothing has forked yet, because on the tree as of 2026-08-13 exactly one row names a component at all. This proposes closing a gap the design leaves open. **Not an expansion of C-080:** that carries the Note's EVIDENCE into the seeded synthesis — what the seed says once the component is chosen. This is about choosing the component in the first place. Disjoint remedies, different files, neither subsumes the other. **Not filed as an issue** — capability that would be ADDED, which per [`finding-routing.md` § 4](../../finding-routing.md) makes it a candidate whatever its done-state looks like. *(Id taken by re-reading this file at HEAD and comparing against `origin/main` immediately before writing: `origin/main` ran to C-079, this branch to C-080.)* |
| C-082 | Route `plan-project`'s loop-back by WHAT THE RUNWAY NAMES, so a runway against a component's research is not spent on the one child that cannot close it | workflow-decomposition | PR for `plan-candidates` (build-refine pass 2, 2026-08-14) |  | `open` | **`plan-project`'s loop-back can only reach `plan-sprint`, and the comment justifying that reasons entirely about TRIAGE** — *"every candidate already carries a decision, so re-triaging would re-litigate rulings"* — because when it was written the research step was inert by construction and no research artifact could appear in a plan-project PR. **`plan-candidates` is what changed that**, which is why this is filed from this PR rather than earlier. **The consequence:** a `review-pr` runway naming a component's `synthesis.md` (a thin citation, an unverified span — exactly what `research-verify` and `research-critic` exist to catch) is dispatched `MAX_LOOPS` times at `plan-sprint`, which cannot edit a research pool, and the run then reports *"The automated loop is SPENT"* over a defect nobody addressed. The operator pays three opus dispatches and reads notes pointing at the sprint plan. **Done-state today: yes** — `research_workflow._verify_then_dispose` already routes a research runway to `research_verify` with `correction_pass=True`, so the shape exists and the work is choosing the child by runway content. **NOT a defect in what this PR built:** every documented behaviour of `plan-candidates` is correct and tested; this is a routing capability the parent has never had. **Not an expansion of C-081:** that is about a filer choosing a component name. This is about which child a correction pass reaches. Disjoint remedies, different files. **Not filed as an issue** — capability that would be ADDED, per [`finding-routing.md` § 4](../../finding-routing.md). |
| C-083 | Let `research-verify` record its own success, so a component whose verify FAILED is re-verified on redispatch instead of reading as finished work | workflow-decomposition | PR for `plan-candidates` (build-refine pass 2, 2026-08-14) |  | `open` | **`plan-candidates` marks a fresh pool with `<!-- plan-candidates: seeded, no research yet -->` so an abandoned pool is RESUMED rather than skipped forever — but that marker can only recover the components a run had not reached yet.** `research-write` fully rewrites `synthesis.md` and commits before `research-verify` runs (its completion contract is a PR URL), so the marker is gone by the time verify can fail. **Measured by reproduction:** a pool whose synthesis was rewritten and never verified classifies as `extends`, is absent from `to_research`, and the parent prints *"the candidate extends it"* over an unverified synthesis that then reaches the planning PR. **The gap is not new and is not `plan-candidates`':** a sprint-section component whose verify failed was equally unrecoverable before this activity existed, because `new_sprint_sections` diffs from the redispatch's own base rather than from `origin/main`. What is new is that the seed marker made half the hole visible, and half a recovery reads as a whole one. **The remedy is a WRITE BY THE VERIFIER**, not a smarter reader: nothing downstream can distinguish *research that finished* from *research that was interrupted after the write* unless the step that finishes says so. **Done-state today: yes, and small** — one line in `research_verify` on the success path, plus reading it in `_is_unresearched`. **Out of scope here because `research-verify` is a CHILD TWO PARENTS SHARE** — `run_research` calls it as well, so changing its post-conditions from a `plan-candidates` PR changes the research parent without its tests in the diff. **Not an expansion of C-082:** that routes a correction pass once a runway exists; this is about a pool that never gets one. **Not filed as an issue** — capability that would be ADDED, per [`finding-routing.md` § 4](../../finding-routing.md). |
| C-084 | Gate QUOTATIONS the way figures and prose tallies are already gated: a quoted span in a docstring or comment that attributes a sentence to a named repo file must appear in that file | workflow-decomposition | PR for `plan-candidates` (build-refine pass 3, 2026-08-14) |  | `open` | **Three passes on one PR produced three quotation defects and every one was found by a human-equivalent read rather than by a gate.** Pass 2 fixed two — a sentence attributed to `candidates.md` that is not in it (twice), and *"you never decide where a shipped candidate goes"* attributed to `plan_sprint.md` when it exists only in `triage_candidates.md`, about the workflow that does NOT place. **The consequence is worse than a wrong comment: a false quotation attributed to a named file actively stops the next reader checking**, because it reads as already-verified. The plan-sprint instance told a maintainer a prose backstop existed on an authorization surface where the MAY NOT row is the whole of it. **Done-state today: yes, and the mechanism already exists three times over** — `test_candidates_prose_matches_the_table`, `test_loop_cap_prose_is_counted` and `test_measurement_figures_are_cited` are all the same shape (a claim in prose, re-derived from the artifact it names). This is the fourth and it is mechanical: find `*"..."*` spans in .py docstrings/comments whose surrounding text names a repo path, and grep the file. **Not an expansion of C-080/C-081/C-082/C-083** — those are about what `plan-candidates` carries, how a filer names a component, how the loop-back routes, and how verify records success; this is a repo-wide prose gate with no scaffolding in it. **Measured against this pass as a control: all 8 checkable quotations in the diff verified verbatim**, so the class is currently clean and the gate is to keep it that way rather than to fix a backlog. Proposed independently by quality-control and by pass 2's own reflection, where it would have died at merge. |
| C-085 | Give the planning family its fresh-context judge — `plan-verify`, the READ half of the split `plan-feature` is the write half of: it reads a component's `roadmap.md` and phase docs cold, sizes the phases, and holds the decomposition to `author != judge` | workflow-decomposition | PR for `plan-feature` (build-draft, 2026-08-14) |  | `closed` | **The consequence, and it is why this is a finding rather than a note about unfinished work: `plan-feature` ships today with NO sizing anywhere in the pipeline and NO fresh reader of its decomposition.** The other two families each have their judge and both state the same reason in their own docstrings — research is `research-write` -> `research-verify` because *"a separate fresh-context run verifies it... the run that wrote an artifact defends it"*, and build is `build-draft` -> `build-refine` because *"the fresh context is the point, not an implementation detail"*. Planning now has a producer with neither. **Two things are load-bearing rather than cosmetic.** (1) **Hours have no owner at all.** `plan-feature` deliberately estimates none — an author sizing their own decomposition is defending it — and its runner FAILS THE RUN on an hour figure, so the number does not exist anywhere until this lands. `plan-sprint` totals hours it is handed and derives none. (2) **`review-pr --type planning` is a PR reviewer, not a plan reviewer**: it judges the diff and the criteria in `criteria_planning.md`, and nothing in it reads a roadmap cold and asks whether the phase boundaries are the right ones. **What it would cover:** sizing per phase; whether each phase ends at one verifiable outcome; whether a producer shipped without its consumer; whether the evidence cited actually supports the phase; and the question `plan-feature`'s own report is required to ask and cannot answer about itself — *where is this plan weakest*. **Sized as its own workflow, not a stage**, on the same argument that made `triage-candidates` a separate run: a judge inside the producing dispatch shares the producer's context, which is the one property it exists not to have. **→ DELIVERED 2026-08-15** — `plan-verify` exists as a workflow, an entry script and a runner, is wired into `plan_project` between `plan-feature` and `plan-sprint`, and declares its own `MAY_NOT_OBSERVERS` / `DISAPPEARANCE_OBSERVERS` tables. `status` moves to `closed` because this row's work is BUILT; `decision` stays blank because it is `triage-candidates`' alone and nothing has ruled on it. **That justification originally cited "§ Where things stand already records six rows delivered before anyone got round to ruling them", and THAT CITATION WAS FALSE — corrected 2026-08-16 by this PR's refine pass.** § Where things stand records no such thing; the rows a reader would reach for as precedent (C-020, C-024–C-027) all carry `decision: ship` with `status: open`, which is the OPPOSITE combination; and `grep` over the table returns **this row as the only one in the file with `decision` blank and `status: closed`**. So this is the first instance, not an established pattern, and it is stated as such: the two flags are orthogonal by this file's own § Two flags model, so the state is defensible, but nothing here had done it before and a reader should not be told otherwise. Whether "built ahead of triage" should close a row at all is a ruling for `triage-candidates`, and this row does not pre-empt it. **One thing it does NOT deliver, stated here so the next reader does not assume it:** the estimates land in `<component>/roadmap.md`, and `plan-sprint` cannot see them — its prompt states it never opens a phase doc, the block it is handed lists no roadmap, and it contains no reader for an hour figure, so it still sizes against its 160-hour calibration. That is a change to `plan-sprint` and is filed separately. |
| C-086 | Require every `MAY_NOT_OBSERVERS` row to be EXERCISED by a test that makes the offence and reads the message, not merely to name a symbol that resolves | workflow-decomposition | PR for `plan-feature` (build-refine, 2026-08-14) |  | `open` | **The consequence, measured on this PR rather than predicted: four defects shipped past a registry that reported full coverage, and three of them were mechanisms that did not cover what their own row claimed.** `test_authorization_is_observed` proves a row names a symbol that RESOLVES; `test_disappearance_is_observed` proves a snapshot is CLAIMED. Neither invokes a mechanism against the row's own scenario, and the gap between *a mechanism exists* and *it fires on the case it names* is where all four sat. **What was actually found here:** `malformed_phase_docs` was sourced from a `^phase` sweep, so `the_run_bag.md` — the standard's first-named failure mode and the likeliest one — was invisible while the row said that function observed it; the hour and checkbox guards were scoped narrower than the write grant, leaving a permitted file nothing read; and the phase-identity message was unreachable because a broader guard raised first. **Done-state today: yes, and the shape already exists** — `observer_registry` discovers rows and mechanisms by AST, so the addition is a third assertion requiring each mechanism symbol to appear in a behavioural test module, the same way `test_the_two_candidate_READERS_ALWAYS_KEY_THE_SAME_ROWS` holds a coupling a registry entry merely asserts. **Not an expansion of C-084:** that gates QUOTATIONS in prose against the file they name. This gates a REGISTRY ENTRY against the behaviour it claims. Disjoint mechanisms, disjoint failure classes. **Not a defect in what this PR built** — every defect it names is FIXED in this PR; what does not exist is the gate that would have caught them, and would catch the next one on `plan-sprint` or `triage-candidates`, whose registries have the same property. **Not filed as an issue** — capability that would be ADDED, per [`finding-routing.md` § 4](../../finding-routing.md). |
| C-087 | Key `plan-project`'s resume on the PLANNING deliverable — a component with research complete and no `roadmap.md` needs `plan-feature` — instead of on a research marker that is gone by then | workflow-decomposition | PR for `plan-feature` (build-refine pass 2, 2026-08-14) |  | `open` | **`scaffold_candidate_components` decides *does this component still need work?* from the `<!-- plan-candidates: seeded, no research yet -->` marker in its `synthesis.md`, and `research-write` strips that marker as its first act — so the resume signal cannot see the planning step at all.** **The consequence, verified by reading the three files that form the chain:** a component whose research SUCCEEDED and whose `plan-feature` then failed reads as `extends` on every later `--pr` redispatch. It never re-enters `to_research`, never re-enters the `origin` map that drives the loop, and nothing anywhere in the pipeline asks whether a scaffolded component has a `roadmap.md`. `review-pr --type planning` judges a diff, not corpus completeness, so it is not the backstop either. The component leaves the automated pipeline silently, with fully-verified research and no plan. **What WAS fixed in this PR, and why that is not this:** the parent now catches a mid-loop failure and tells the operator, in the raised message, that the component is orphaned and what the standalone recovery command is. That closes the DEFECT — an operator inheriting a false impression of completeness — and leaves the capability gap: the pipeline still cannot recover on its own. **Done-state today: yes, and small** — the resume classifier reads `(<component>/roadmap.md).is_file()` alongside the marker it already reads, so *needs planning* stops being derived from *needs research*. **NOT an expansion of C-083**, and the distinction is the whole reason this is separate: C-083 has `research-verify` record its own success so an INTERRUPTED verify is re-verified. Its remedy is a marker written by the verifier — which, on this scenario, would correctly read *verified* and still skip the component, because the step that failed was the one after it. Different failing step, different signal, and C-083's fix does not reach this case. **Not filed as an issue** — capability that would be ADDED, per [`finding-routing.md` § 4](../../finding-routing.md). *(Id taken by re-reading this file at HEAD and comparing against `origin/main` immediately before writing: `origin/main` ran to C-084, this branch to C-086, and the merge of the two was resolved before this row was appended.)* |
| C-088 | Give the **build and research** families observer registries — the plan family's `MAY_NOT_OBSERVERS` / `DISAPPEARANCE_OBSERVERS` tables plus AST discovery exist in three files, all `plan`, so two thirds of the fleet states prohibitions in prose with nothing observing them | workflow-decomposition | PR #92 `plan-feature` reflections (3 passes, 2026-08-14/15) |  | `open` | **The run called this the single highest-leverage thing in the tree.** Writing the plan table forced eleven *what observes this?* answers and **two of the eleven mechanisms did not exist until the registry demanded them** — the apparatus creates coverage rather than reporting it. Adjacent to C-086, which asks that existing rows be EXERCISED; this asks that the other two families have rows at all. |
| C-089 | Add *ask what the guard does NOT look at* to the mutation instructions — negative controls structurally cannot find SCOPE defects, and every defect found on PR #92 across three passes was one | workflow-decomposition | PR #92 `plan-feature` reflections (3 passes, 2026-08-14/15) |  | `open` | A control mutates a guard and counts failures, which proves the tests discriminate against the code **as scoped**; mutating a narrow guard still fails the narrow tests written for it. The four defects were a regex crossing a sentence, a grant crossing a directory, a sweep missing a filename class, and an ordering hiding a message. Pass 1 named the blind spot and filed C-086; **pass 3 is the evidence that naming it was not enough.** The technique that actually found all four is the one to write down. |
| C-090 | Enforce *merge `origin/main` and re-run before pushing* in the parent's push step rather than as a prompt instruction — a gate that lands while a branch is open is a gate the branch has never run | workflow-decomposition | PR #92 `plan-feature` reflections (3 passes, 2026-08-14/15) |  | `open` | `main` is **not branch-protected** (verified 2026-08-15), so nothing stops a stale-green PR merging with an unmet gate. A new `test_prompt_budgets.py` landed mid-branch and `plan_feature.md` failed it at 17,821 bytes with no budget line; it was caught only because that run happened to merge `main` a second time before pushing. **Belongs in code, not a prompt** — the prompt-economy standard's test 3 asks whether the harness can enforce it, and here it can, deterministically and without a model turn. |
| C-091 | Derive the hand-maintained control sets instead of maintaining them — adding one workflow required five hand-edits across `test_authorization_is_observed`, `test_disappearance_is_observed` (x2), `test_pr_url_address` and the repo-root census | workflow-decomposition | PR #92 `plan-feature` reflections (3 passes, 2026-08-14/15) |  | `open` | Each set is deliberate and each says so in its own failure message — *a THIRD workflow arriving is expected to fail here once, on purpose* — and that design is right. **The cost is that it rises linearly with fleet size, and every edit is a place a future author widens the set without checking coverage.** The question is whether the sets can be derived while keeping the deliberate fail-on-arrival property. |
| C-092 | Harden four filters that test `path.parts` of an ABSOLUTE path — the same shape that made `test_relative_links_resolve.py` scan **0 files from a worktree and 354 from the main checkout at the same commit** | workflow-decomposition | PR #92 `plan-feature` reflections (3 passes, 2026-08-14/15) |  | `open` | **Not reachable today, and that is the finding rather than a reason to skip it.** The link test's `SKIP_PARTS` held `.claude` and `worktrees`, both of which appear in every worktree path, so the filter ate the entire scan and its assertion had never once run in a dispatch; only its vacuity guard surfaced it. The four remaining instances (`test_convergence.py:804`, `test_pr_url_address.py:344`, `test_exit_record.py:1072`, `test_prompt_budgets.py:76`) skip on `tests` / `prompts`, which appear in no path we use — so they are the identical latent shape awaiting a differently-named checkout. One line each: filter on `relative_to(ROOT).parts`. |
| C-093 | Give `plan-sprint` a reader for the per-phase hour estimates `plan-verify` now writes, so the sprint plan totals real numbers instead of judging every item against a 160-hour calibration | workflow-decomposition | PR for `plan-verify` (build-draft, 2026-08-15) |  | `open` | **`plan-verify` shipped the estimates and `plan-sprint` cannot see them, so the pipeline now produces a number nothing consumes.** Verified by reading the workflow rather than assumed: `plan_sprint.md` states *"You never open a phase doc"*; the `EXISTING_WORK` block it is handed enumerates component directories, their `research/synthesis.md`, the product pool's papers and the open GitHub issues, and **names no `roadmap.md` anywhere**; and `grep -rn 'hour\|hrs' plan_activities.py plan_sprint_workflow.py plan_project_workflow.py` returns only two PROSE mentions in docstrings and no reader. **The consequence is a decision made on the wrong input:** `plan_sprint.md` tells the run to *"measure it against the 160-hour calibration and say what you got"*, which was the honest instruction while no per-phase figure existed. One exists now, per component, in `<component>/roadmap.md` — so the sprint plan is sized by a model's read of a paragraph while a derived total sits one file away, and the operator cannot tell those two apart from the report. **Ordering is already correct and is NOT the gap:** `plan_project` runs `plan-verify` before `plan-sprint`, which is the defect `plan_sprint_workflow`'s own docstring records — *"the sprint plan used to be updated BEFORE anything estimated the work"* — and that had been latent rather than fixed, because until this PR there were no estimates for it to be ahead of. **Done-state today: yes, and it is two pieces** — a reader that totals `roadmap_hours` per component (`plan_verify_activities.roadmap_hours` already is one, and a second consumer promotes it to `plan_activities` under §10.1 rule 3), and a prompt change replacing *measure it against the calibration* with *here is the counted total, and the calibration is what you judge it against*. **NOT a defect in what this PR built:** every documented behaviour of `plan-verify` is correct and tested, and the estimates land exactly where the design says. This is capability `plan-sprint` has never had. **Deliberately NOT built here, and the brief for this PR says so:** `plan-sprint` is out of scope, and a silent edit to a neighbouring workflow is how one workflow's fix becomes another's regression — its own tests would not be in the diff. **Not an expansion of C-050** (derived-versus-restated counts in prose): this is a figure that is not restated anywhere and is not read anywhere either. **Not an expansion of C-085**, which asked for the sizer and is now delivered; this is its consumer. *(Id taken by re-reading this file at HEAD and comparing against `origin/main` immediately before writing: `origin/main` ran to C-087, this branch to C-092, and C-092 is the higher.)* **Filed TWICE, independently, from the two passes of this same PR — this row is the merge of both.** `origin/main`'s wording added two things kept here: the consequence has a measured precedent in [MMF Phase 6](../../../development/memory-management-framework/phase6_read_what_it_writes.md), where three phases shipped an emitter and none shipped a reader; and the *"You never open a phase doc"* instruction it rests on is at `plan_sprint.md:87`. Recorded rather than silently dropped because a duplicate id resolved by deleting one side loses whichever evidence was only on that side. |
| C-094 | Give the build family's review stage the BRANCH as its subject rather than the producing run's own diff — commits already on the branch when a dispatch starts pass through no review lens at all and merge with it | workflow-decomposition | PR #93 for `plan-verify` (build-refine correction pass, 2026-08-15) |  | `open` | **The consequence is that a majority of what a PR delivers to the default branch can reach it unreviewed, and nothing reports that.** Measured on PR #93: six commits — `df3c98c`, `a223ccb`, `8df6029`, `42ccfb6`, `5c03389`, `92e2671` — were on `build/plan-verify` before the draft ran, so `build-refine`'s four review lenses, which are dispatched against the draft's diff, never saw them. They are not incidental: they change a PreToolUse safety hook, both hook timeouts in `settings.json`, the CI-gate verdict enum and the shipped build prompts. **The gap is not theoretical — `review-pr` reviewed them from the other side and found a real defect** (`wait_for_ci`'s docstring documenting a `raises` outcome the function never produces, which a caller would turn back into the read-failure/gate-absence conflation that cost PR #92 three rebuilds). **What this run VERIFIED rather than assumed:** the six commits are on the branch and not on `origin/main` (`git log --oneline origin/main..HEAD`); `build-refine`'s Stage 2 dispatches name the draft's diff as the subject; and this correction pass reproduced the finding by execution — monkeypatching `subprocess.run` to make every `gh pr checks` reply unparseable and calling `wait_for_ci("1")`, which RETURNED `False`. **Nearest neighbour is C-090 and it is a different mechanism:** that one is about a gate that never RAN on the branch; this is about a review that ran with the wrong subject. **Shape of the remedy, not a design:** the reviewing stage takes `origin/main...HEAD` rather than the producing run's own commits, or the range is stated and the uncovered commits named in the PR body so the omission is at least visible. |
| C-095 | Make the research family say whether a pool it was pointed at is meant to EXIST, so a typo'd pool path costs a message instead of a full dispatch that researches into a new directory | workflow-decomposition | PR #93 `plan-verify` (build-draft, 2026-08-16) |  | `open` | **`run_research.py` and `run_research_minor.py` accept a pool path that does not exist and proceed, and nothing downstream can tell a new pool from a misspelled one.** Measured on this branch: `run_research.py docs/development/no-such-pool-yet --dry-run` exits 0 and prints `Due papers: 0` — the same output a correct empty pool produces. **The consequence is a whole dispatch spent wrong**: the run cuts a worktree, researches, and opens a PR that adds an entire pool one directory letter away from the real one, and the operator finds out at review rather than at the second the command was typed. That is the orphaned-worktree class (#48/#49) reached through an argument `preflight` did not previously see. **Why it is NOT fixed in this PR, and this is the substantive half:** this PR brings both runners inside the containment mechanism, and containment is a different question from existence. Nothing in the research family `mkdir`s its pool — `plan-candidates` scaffolds them — so requiring existence today would refuse the one legitimate case the flag has, a genuinely new pool. The behaviour is therefore preserved exactly, with `must_exist=False` on the declaration and the reason stated at the call site. **Deciding which case the flag means is a change to the research family's CLI contract, whose tests are not in this diff.** **Done-state today: yes, and it is a choice not a build** — either require existence and give `plan-candidates` the sole right to create a pool, or keep the permission and make it explicit (`--new-pool`), so that the silent third state stops existing. **NOT a defect in what this PR built:** every documented behaviour is preserved and the escape it does close is proven by execution on both runners. **Not an expansion of C-091** (deriving the hand-maintained control sets): that is about test-side bookkeeping, this is an operator-facing CLI contract. **Not an expansion of C-087**, which resumes a component whose planning step failed — that is about recovering from a run that went wrong, this is about refusing to start one. *(Id taken by re-reading this file at HEAD immediately after resolving the `origin/main` merge, which itself carried a duplicate C-093 minted on the other branch; highest was C-094.)* |
| C-096 | Promote the seven runners' identical `try/except RuntimeError -> print(f"\n✗ {exc}") -> return 1` block, so the family's operator-facing failure message has ONE implementation instead of seven | workflow-decomposition | PR #93 `plan-verify` (build-refine, 2026-08-16) |  | `open` | **Seven consumers now carry it byte-identically, and this file's own promotion rule is a consumer count rather than taste** — `resolve_operator_paths` was promoted at TWO under §10.1 rule 3 (*"if and only if more than one workflow uses it… the consumer count decides, never taste"*), and this block is at seven. **The consequence is the drift `resolve_operator_paths`' own docstring names, one altitude down:** the next change to how a bad invocation is reported — a hint line, an exit code that distinguishes a refusal from a crash, routing to `logging` — lands in whichever runner the author had open, and the other six keep the old shape with nothing in any diff to show the divergence. **NOT a defect in what this PR built, and that is why it is a proposal rather than a fix:** the block predates this change (it wrapped the bare `preflight()` call in six of seven V2 entrypoints before `RepoPathParser` existed); this PR made it MORE uniform, not less. **Deliberately not extracted here** — `code-style.md` prefers three similar lines to a premature abstraction and `engineering-quality.md` § *Surgical changes* forbids refactoring adjacent working code inside a security fix; touching all seven runners would have put churn in the same diff as an authorization boundary, where a reviewer most needs to see only what changed. **Done-state today: yes, and it is one function** — a `parse_or_exit(parser, argv)` in `preflight.py`, which already owns `RepoPathParser`. **Raised by `refactoring-evaluator` at Medium on this PR, which recommended surfacing rather than doing it.** *(Id taken by re-reading this file at HEAD and confirming `git log 9e054ea..origin/main` was empty immediately before writing: `origin/main` had not moved since this branch's merge commit, and C-095 was the highest on either side.)* |
| C-097 | Give `workflow-scripts.md` a section naming `RepoPathParser.add_repo_path` as the required way a Python entrypoint declares an operator-supplied repo path, so the rule is discoverable before a test enforces it | workflow-decomposition | PR #93 `plan-verify` (build-refine, 2026-08-16) |  | `open` | **The mechanism is now mandatory and enforced — `test_no_runner_joins_an_UNRESOLVED_operator_path.py` fails any `run_*.py` that joins an argparse attribute onto a root — and no standards document mentions it.** Verified by reading `workflow-scripts.md` end to end, including its *Validate Inputs* section: `add_repo_path`, `RepoPathParser` and `parse_with_preflight` appear nowhere in `docs/standards/`. **The consequence is that the eleventh runner's author meets this rule as a red test rather than as a documented convention**, and the failure they see names a join in their own file rather than the mechanism they were supposed to use — the specific reason this PR chose a guard whose message spells out the remedy. That is a mitigation, not a substitute for the rule being findable by someone reading the standards before writing code. **Filed rather than fixed because `standards-governance.md` bars an autonomous run from writing standards content**, which is the correct rule and is being followed rather than worked around; `candidates.md` is the working surface that carries a proposal to the architecture session without a standards edit. **Done-state today: yes** — one section in `workflow-scripts.md`, plus a decision on whether `preflight`'s two other public helpers belong in the same section. **Not an expansion of C-091** (deriving hand-maintained control sets — that is test-side bookkeeping) **and not of C-088** (observer registries for the build and research families — that is a mechanism to build, this is a rule to write down). **Raised by `standards-auditor` at Info on this PR, which explicitly declined to propose the edit itself for the same governance reason.** |
| C-098 | Cross-reference OPEN issues against HEAD before a build brief is written — an issue whose fix landed in an unrelated PR stays open, and the next dispatch is briefed to fix a defect that no longer exists | continuous-process-improvement | PR #94 `build-draft` (issues #57/#65/#68) |  | `open` | **Measured on this dispatch, not hypothesised.** Issue #68 was filed 2026-08-08 against `review_pr_activities.py`'s unanchored `"pr_review:" in body` predicate. Its V2 half was fixed in PR #71 at `9fa69ff` (2026-08-09) — fence-anchored `PR_REVIEW_BLOCK`, seven covering tests, plus the `SHARED_KIND_ONE_PATTERNS` cross-tree gate — and the issue was never closed. On 2026-08-16 a build brief restated the defect verbatim, quoting a file:line that no longer holds that code, and Stage 1 spent a stage disproving a premise instead of building. **The failure mode is worse than the wasted turns**: the brief instructed the run to change code that is already correct and already gated, and a run that trusted its premise would have reverted a shipped fix. **This is a gap in the CPI loop, not in any one workflow** — the loop rules findings and files issues, and nothing reads the issue queue back against the tree afterwards. `gh issue list --state open` plus the fix's own test names is enough signal; the capability is the reading, which does not exist. Adjacent to C-050's shape (a declaration nobody derives) but a different surface: there the restatement is inside the repo, here it is the tracker.  **RENUMBERED from C-094 on 2026-08-16**: `origin/main` had already landed a different C-094 while this branch was open — the sixth such collision — so this row keeps its content and takes the next free id. |
| C-099 | Check what a PR's merge will ACTUALLY close, across all three channels that bind a closing keyword — `closingIssuesReferences` reports the body and title and is blind to commit messages | continuous-process-improvement | PR #94 `build-refine` (correction pass on `review-pr` F1) |  | `open` | **Two carriers found in one PR, by two different readers, and the second is reported by nothing.** A `review-pr` pass found that PR #94's body still bound `Closes` to #68 in three places — one in bare prose — after a build pass had deleted the `Closes #68` line and verified the deletion by grep. `gh pr view --json closingIssuesReferences` returned `[57,65,68]`: the PROPERTY, where the grep was a string standing in for it. **This correction pass then found a third carrier that `closingIssuesReferences` does not report at all** — commit `ea5c862`'s message body contains the literal `` `Closes #68` `` while explaining that the line was removed, and GitHub closes an issue from a closing keyword in any commit message that lands on the default branch. `origin/main`'s last 12 commits are all single-parent, so this repo lands branch commits individually and the message ships verbatim. **Backticks do not exempt a closing keyword in any of the three channels**, and prose written to explain that an issue is deliberately NOT being closed is the exact shape that closes it. The capability that does not exist is the reading: one command answers two channels, nothing answers the third, and the remedy for the third (rewriting a pushed message) needs an authority a dispatch does not have — so it has to be caught before the commit, not before the merge. Adjacent to but distinct from C-094: that one asks *is this issue still real?* before a brief is written; this asks *will merging shut the right issues?* before a branch is pushed  **RENUMBERED from C-095 on 2026-08-16**: `origin/main` had already landed a different C-095 while this branch was open — the sixth such collision — so this row keeps its content and takes the next free id. |
| C-100 | **Build our own CI/CD, initial deployment and its documentation** — the merge gate today runs post-hoc on `push: branches:[main]`, so a red commit lands and is reported afterwards. Branch protection is ruled out permanently (paid feature, `cpi-decisions.md` 2026-08-16). Candidate orchestrators discussed: **Temporal** (already the fleet's direction) or **Argo**. **Scope is wider than the merge gate**: continuous integration, continuous delivery, how a machine is stood up from nothing, and the documentation for both. **Its own feature and its own sprint**, gated on research the operator kicks off after a brainstorm | `temporal-integration` | operator ruling, restated across ~10 sessions and recorded on no surface until 2026-08-16 |  | `open` | Raised because the ruling existed only in conversation, so every fresh context re-derived the question and re-proposed branch protection. The rejection is logged; **this row is the affirmative half — what we build INSTEAD** |
| C-101 | `docs/file_structure.txt`'s guard cannot see a DUPLICATE entry, or a NEW SUBDIRECTORY under a mapped ancestor — two blind spots sharing one remedy, and the map's own set-equality claim is what hides the first | continuous-process-improvement | PR #96 `build-refine` (merge of `origin/main` `7d2a757`) |  | `open` | **Produced by a real merge on this branch, then confirmed by mutation — not hypothesised.** This branch added a `judge_marginal_yield.py` line to the map because `origin/main` lacked one at the branch point (`git show 9befe23:docs/file_structure.txt \| grep -c judge_marginal_yield` → `0`). `origin/main` then landed `7d2a757`, which added its own line for the same file with different annotation text. The merge **auto-resolved with no conflict** — the two lines sit at different positions under different neighbours — leaving the authoritative map asserting one file twice, with two different descriptions. **The guard is structurally blind to it:** `test_file_structure_map_covers_the_tree.py::_mapped_paths` returns a `set()`, so both lines collapse to one member and every assertion built on it is satisfied. Mutation run to confirm rather than infer: duplicating the surviving line and running the module gave **6 passed**, predicted green and observed green. **The consequence is the map's standing, which is the thing its own docstring says every silent miss spends** — `CLAUDE.md` calls the map authoritative, so a reader who greps a path and gets two contradictory annotations cannot tell which is current, and a later `--check`-style set comparison would report set-equality and be right. The de-duplicating step is exactly the shape this repo's own guard-scoping rule names: *ask whether one case stands in for two things that fail separately.* **This is the second blind spot found in the same guard in one pass** — the other being that a whole NEW subdirectory under an already-mapped ancestor trips neither the enumeration check (which fires only on *partially*-listed directories) nor the reachability check (satisfied by any mapped ancestor); recorded together because one duplicate-and-population check would close both. The capability that does not exist is the reading: nothing compares the map's entries as a *multiset* against `git ls-files`. Adjacent to C-098/C-099's shape — a declaration nobody derives — on a fourth surface. **This row was placed as C-100 and renumbered to C-101 at the merge that found the collision:** `origin/main` landed its own C-100 in `66719e9` while this branch was open, and that one is merged and cited by issue #98, so it keeps the id. Same shape as C-070/C-072's renumbering note, and recorded the same way — the no-renumbering rule protects an id others already cite, and nothing outside this branch ever cited this one. **Lens verdicts (gate 5), recorded by PR #96's `build-refine` correction pass because the filing passes omitted them:** `/decide` — **not dissolved.** The upstream question is *why a hand-maintained map exists at all beside `git ls-files`*, and the answer is that it carries per-entry annotation the index cannot; the map is the artifact, so its agreement with the tree is a real property and not a framing error. `/best-practices` — **SURVIVES.** Deriving a declared inventory from the thing it declares is the same discipline C-050 states, and the repo already has the right-shaped instrument. **SPLIT FROM WHAT IS NOW C-105 at PR #96 disposition pass 8.** The two were filed together because they share a mechanism, and that bundling made the row unrulable: these two are measured and ready, while the third is explicitly NOT fit to ship as a gate. `triage-candidates` writes ONE `decision` per row, so no value was correct for both. One finding, one entry, one recommendation. |
| C-105 | `docs/file_structure.txt`'s ANNOTATION can be SEVERED from the leaf it describes, and the current format offers no structural signal to gate on — so the remedy is the map's FORMAT, not another guard | continuous-process-improvement | PR #96 `review-pr` pass 8 |  | `open` | **A THIRD blind spot in the same guard, found by PR #96's `build-refine` correction pass and recorded here rather than as a sibling row because the mechanism is the one this row is already about:** the guard derives the map's PATHS from `git ls-files` and asserts parse, enumeration and reachability, and says nothing about whether a continuation-annotation block is still ATTACHED to the leaf it describes. Four instances inside this PR's lifetime — one repaired at `66be084` (introduced by `d34caab`), and three live and repaired at this row's own pass, of which two were introduced by merges into this branch and one (`test_run_log.py`, introduced by `e7fadd9` for PR #82) has been wrong on `origin/main` since 2026-08-12. Same mechanism as the duplicate above — something inserts a leaf where nothing is looking. **The candidate tell is measured, not asserted, and its METHOD is stated so a triage pass can re-derive it rather than trust it:** over every line matching `[├└]──\s` that carries a `#`, take the annotation text after the `#` and flag the leaf when that text ends in a comma or in a word that cannot end an English sentence (`the a an and of to with from against into per via that whose which these those its their`) AND the NEXT line is another leaf rather than a continuation. Across all 680 lines that predicate returned **3 hits, all 3 true positives, zero false**; widening the list to sentence-capable words (`on it is are`) returned **6 hits for the same 3 true**, so the precision comes from the word list rather than from the structure. **It is NOT fit to ship as a gate as it stands** — validated on n=3 against a hand-curated English vocabulary that must grow as annotation phrasing drifts, with no bound on false negatives. A sound gate needs a STRUCTURAL signal, which the current format does not offer, and that is the real finding. **The `#`-column tell is not gateable at all, and that was measured too:** of the **294** annotated leaves, **34** carry `#` past column 53, and in **29** of those the filename itself already ends at or past column 53 — so the column measures name length, not displacement, and the remaining 5 are ordinary extra padding. A guard keyed on column would be 34 false positives deep before it caught anything. Consequence: `CLAUDE.md` calls the map authoritative, and `_map_entries()` in `test_retired_vocabulary_is_gone_from_live_surfaces.py` joins each leaf to its continuation lines, so a severed block silently re-attributes one guard's guarantees to another in that sweep's per-entry text — checked and NOT a live wrong answer from that gate, because all seven paths involved return `None` from `_is_record`, so no RECORD exemption is misapplied. **SPLIT OUT OF C-101 at PR #96 disposition pass 8**, because its verdict is the opposite of that row's other two: they are measured and shippable, this one says in its own words that it *"is NOT fit to ship as a gate as it stands"*. **The remedy is stated and has been homeless until now:** a PATH-KEYED annotation source of truth with the ASCII tree as a RENDERED VIEW, surfaced by `refactoring-evaluator` at pass 8, which makes displacement structurally impossible rather than merely detectable — the STRUCTURAL signal this row says the format does not offer. It was reported with no pointer because pass 7's dispatch barred adding a candidate row; that homelessness was the reviewer's and is corrected here rather than left to die at merge. |
| C-102 | Allocate a candidate id so that two branches cannot both take the next free one — the file's own *"ids are never reused and never renumbered"* invariant is false in practice, and ids are cited from outside the file, so a citation can silently come to name a different proposal | continuous-process-improvement | PR #96 `build-refine` (correction pass 4) |  | `open` | **Measured on this file, not estimated.** It records **9 renumbering events across 7 rows** — `C-060`→`C-067`, `C-061`→`C-068`, `C-066`→`C-071`, `C-069`→`C-071`, `C-067`→`C-072`, `C-070`→`C-072`, `C-094`→`C-098`, `C-095`→`C-099`, and this PR's `C-100`→`C-101` — plus two ids (`C-063` withdrawn, `C-070` vacated) that no row ever took. Every one is recorded honestly on the row, and every one breaches the invariant [`candidates.md`](candidates.md) states about itself. **THE MECHANISM: each branch allocates "the next free id" from its own snapshot, and the collision is invisible until the merge** — git sees two rows added at different positions and auto-resolves with no conflict, so nothing red says the same id was taken twice. `test_candidate_ids_are_unique.py` catches a duplicate only AFTER the merge lands, when the remedy has already become a renumber. **Ids are cited from outside this file** — issue #98 (closed) cites `C-100`, which is precisely why this branch's own C-100 had to yield rather than `main`'s — so id instability degrades citations that cannot be updated from here. **This is a PROPOSAL, not a defect: no id is wrong today.** What does not exist is an allocation mechanism, which is what keeps this out of the issue queue per [`finding-routing.md` § 8](../../finding-routing.md). **Done-state today: yes** — a rule plus a check, one section. The design choice is real and is triage's to rule, not this row's: allocate at merge rather than at write, use non-sequential ids, or add a freshness gate that fails when the id was free in the branch's base and is taken on `origin/main`. Adjacent to C-098/C-099/C-101's shape — a declaration nobody derives — but distinct: those are read-back gaps, this is an allocation gap, and the thing being allocated is the address every other row is cited by. **Lens verdicts (gate 5), recorded by PR #96's `build-refine` correction pass because the filing passes omitted them:** `/decide` — **not dissolved.** The upstream question is whether a sequential id should be the address at all; that is exactly what the row hands triage, and it is an open design choice rather than a mis-framing. `/best-practices` — **SURVIVES.** Collision-free identifier allocation under concurrent writers is a solved class with several standard answers, and the row names three of them. |
| C-103 | Extend the two GFM table-rendering gates beyond `candidates.md` — a stray line inside a table block, and a row whose cell count disagrees with its own header — to every markdown table the repo treats as authoritative | continuous-process-improvement | PR #96 `build-draft` (three-finding close) | | `open` | **Both gates were built on `candidates.md` and are scoped to it; the class is not.** Measured on this PR: a 1,981-character line sat inside the candidate table and rendered through GitHub's `POST /markdown` as a DETACHED row — its whole text in the ID column, six empty cells beside it — while C-050's Note was cut from 8,633 characters to 6,677, losing exactly the evidence a triage pass is meant to weigh. Nothing was red: the stray carried no `C-NNN`, so no derived count counted it and none missed it. **Then the same two predicates were run over all 228 tracked `.md` files: zero further stray lines, and TWO live cell-count mismatches that nothing gates** — `phase4_fleet_migration.md:69` (7 fields against a 5-field header) and `memory-management-framework/roadmap.md:269` (5 against 4). Both truncate a planning table's right-hand cell today. **UPDATE — PR #96's fifth correction pass (2026-08-17) fixed both instances AND shipped the gate, so the sentence that stood here saying they were "deliberately NOT fixed" is no longer true and has been struck rather than left to mislead a triage pass.** `testing/scripts/tests/unit/test_markdown_tables_render_whole.py` now checks every row of every table block in all **228** tracked `.md` files against its OWN header, importing `_table_blocks` and `_cells` from the sibling rather than copying them, because two definitions of *inside a table* is the drift this row is about. Both questions this row called open were answered by **measurement rather than ruling**: the population is every tracked `.md` **including** the vendored mirrors — 11 files, 10 carrying tables, all clean — whose remedy when red is upstream-then-re-vendor and is stated in the gate's own limits list; and fenced code blocks are excluded because `POST /markdown` returns **zero** `<table>` elements inside a fence, which makes any finding there a false positive by construction (two such tables exist today and both are internally consistent, so the exclusion is not what makes the gate green — proven by mutation: gutting it left the tree-wide check passing). **What remains for triage is narrower than this row was filed for:** whether the HTML-block residual should carry CommonMark's fixed tag list. Measured — `<div>` ends a table for GitHub and `<span>` does not, and **zero** tracked files have a `<`-opening line inside a table block today, so the residual is real and unexercised, and it is held red-able rather than left as prose. `decision` and `status` are deliberately untouched by that pass: reporting what shipped is not ruling on the candidate. **Lens verdicts (gate 5):** `/decide` — **not dissolved.** The upstream question is *whether a markdown table's RENDERING is a property worth gating anywhere but here*, and it is: two live truncations sit in files this repo treats as authoritative today, found by the same instrument and cited above. `/best-practices` — **SURVIVES.** Verifying a source artifact in the representation its consumer reads is ordinary practice (link checkers, schema validators, snapshot rendering), and the mechanism already exists — this is a population change, not a new instrument. **Not resolved into a known fix:** which files count as authoritative, and whether the scan must carry CommonMark's fixed HTML-block tag list (measured: `<div>` ends a table, `<span>` does not, so a blanket `<` test would invent failures), are design decisions rather than line changes. |
| C-104 | Give `review-pr` a convergence stopping rule keyed on the KIND of finding a pass returns, so a PR whose remaining findings are all about its own gate infrastructure is scoped to closing them rather than accumulating new ones | continuous-process-improvement | PR #96 `build-refine` pass 8, raised by `quality-control` | | `open` | **Measured on this PR: eight build passes and six `review-pr` dispositions, and the convergence is real rather than a treadmill — the KIND of finding shifts outward each pass.** Early passes closed content instances (six pipe-truncated rows in three files); middle passes closed coverage gaps in the class gate itself (the stray-line shape, the HTML-block residual); pass 8's findings were exclusively about the checking apparatus's own soundness — a duplicated derivation, two dead imports, an undisclosed fence asymmetry, a missing vacuity floor, a residual test asserting a property of `ast` rather than of its gate. Each pass audits a smaller and more introspective surface than the last, which is a recursion floor and not a loop. **The proposal is to make that observable rather than re-derived.** `quality-control`'s formulation: once a pass's findings are exclusively about gate infrastructure rather than about undetected instances of the original defect class, the next pass closes the open structural findings and adds no new checks, then ships. **Why this is not already covered by the existing convergence predicate:** that one counts passes and compares finding ids across them, so it cannot distinguish "pass 7 found three new things" from "pass 7 found three new things one abstraction level further out" — the second is convergence and reads to it as the first. **Cost of not having it:** each correction pass writes new tests minutes before shipping with no second reader, which is exactly where this pass's own surviving mutation was found (`stray_lines` was gutted with all 46 tests green), so an unbounded add-more-checks cycle manufactures the next pass's findings at the same rate it closes them. |
| C-106 | State a retry policy for external CLI calls in the workflow-script standard — two policies now COMPOSE MULTIPLICATIVELY on one path and nothing anywhere states the product, so the wait an operator actually sits through is a number nobody has written down and the next retry is re-derived from scratch | temporal-integration | PR #101 `build-draft` | | `open` | **The composition is live on this branch, not hypothetical.** `assistant_activities._GH_RETRY_BACKOFF_SECONDS` `(2.0, 6.0)` was added by PR #101 so a `gh` read survives a transient 5xx; `review_pr_workflow._THREAD_READ_BACKOFF_SECONDS` `(2.0, 8.0)` already retried `RuntimeError` on the thread read so a flaky read could not discard a completed review. The second calls the first, so the attempts **multiply rather than add**: 3 × 3 = **9 attempts, ~34s**, measured and pinned as a literal in `test_the_retry_under_the_review_threads_retry_stays_bounded`. Neither constant's own comment could have told you that, because each is correct about itself. **The consequence is that the bound is only ever knowable by reading both files at once** — an operator asking "why did this take half a minute" has no single number to look up, and whoever edits either tuple next moves the product without touching anything that says so. **Why this is a PROPOSAL and not a defect:** nothing behaves wrongly today; the composed bound is deliberate and asserted. What does not exist is the STANDARD — `docs/standards/workflow-scripts.md` says nothing about retrying an external CLI call. `grep -in retry` over it returns **exactly one** hit, line 429, and it is a different subject: *"every child boundary is a **retry/resume point**"*, about composing workflows for recoverability under durable execution. Checked rather than assumed, because "the standard is silent" is the kind of claim that is one grep from being wrong. So the three decisions PR #101 had to make were each made from first principles: retry only idempotent reads (a 5xx on a mutation may mean it landed and the reply was lost — issue #41 records duplicate comments as a real harm here), classify on the HTTP status rather than server-supplied prose, and state the composed bound wherever policies nest. **Done-state today: yes** — one section in an existing standard, with the two live policies as its worked example. **Surfaced rather than written because `docs/standards/` is human-in-the-loop**; this row is the placement, per `finding-routing.md` § 4, and the standards file itself is untouched by that run. **Adjacent to, and distinct from, the CPI rows about undeclared counts (C-098/C-099/C-101):** those are declarations nobody derives, this is a decision nobody wrote down, and the artifact is a standard rather than a gate. **And distinct from C-004, the one row sharing its exact keyword — checked by mechanism rather than by phrase, per [`finding-routing.md` § 5](../../finding-routing.md) gate 2.** C-004 is a *Temporal SDK* activity-level `RetryPolicy` on a future worker runtime, whose failure mode is Temporal's unlimited-attempts default re-charging a paid API; this is a hand-rolled `subprocess` backoff around the `gh` CLI in the pre-Temporal fleet, whose failure mode is a composed bound nobody has written down. They do not overlap today and they will not merge later: when workers land, C-004 governs the activity wrapper and this still governs what the wrapper calls. Recorded because the two read as duplicates from their titles alone, and the next reader should not have to re-derive that they are not. **Lens verdicts (gate 5), added by this PR's fourth correction pass after `standards-auditor` found them missing — the same omission a `build-refine` pass on PR #96 was already spent correcting on C-102, which is why it is recorded rather than quietly filled in.** `/decide` — **not dissolved.** The upstream question is *whether a retry policy belongs in a standard at all, or is a per-caller judgement*, and the composition answers it: two policies written independently, each correct about itself, multiply on one path to a bound neither states. That is precisely the class a standard exists for — a decision no single author is positioned to make. `/best-practices` — **SURVIVES.** Bounded retry with jittered backoff, an idempotency gate on which operations may repeat, and a STATED ceiling are settled industry practice (AWS Architecture Blog on exponential backoff and jitter; Google SRE Ch. 22 on cascading failure, whose whole subject is retries composing); what is not settled and is this candidate's actual content is *which bound a policy must state* — attempts, added latency, or wall clock. This PR's own comment stated added latency and was read as stating wall clock, which is the worked example. **Neither lens narrows the scope, and neither resolves it into a known fix** — the mechanism (a section in `workflow-scripts.md`) is obvious; what a human must rule on is whether a standard should mandate stating the composed product, which is a cost on every future retry author. |
| C-107 | Split `modules/assistant/assistant_activities.py` at the `gh` seam — 1307 lines and 27 module-level functions across seven concerns, and the fleet's whole GitHub surface is one of them | temporal-integration | PR #101 `build-refine` | | `open` | **Two independent reviewers converged on the same seam, in the same pass, without conferring.** `refactoring-evaluator` (High) and `quality-control` both scoped the split OUT of PR #101 and both named `modules/assistant/github.py` — the `gh` layer, one concern, a clean surface. Recorded here so the seam is not re-derived by whoever reads this file next. **Measured in this branch, not remembered:** `wc -l` → **1307** lines; an `ast` walk of the module's top level → **27** functions, **1** class, **11** module-level constants; **6** of those functions are the `gh` layer (`_gh_transient_reason`, `_gh_is_read_only`, `_gh_timed_out_line`, `gh_attempt`, `gh`, `gh_json`) together with **3** of the constants (`_READ_ONLY_GH_VERBS`, `_GH_RETRY_BACKOFF_SECONDS`, `GH_JSON_SHAPES`). `git diff --numstat origin/main...HEAD` → **+551/-20** on this file in PR #101 alone, so the growth is recent and attributable rather than ambient. **Why this is a PROPOSAL and not a defect:** nothing behaves wrongly. The module is correct, tested, and its `gh` layer is the most heavily guarded code in the tree. What does not exist is the extraction, and the reason it does not is a genuine gap rather than an oversight — **[Temporal Standard](../../temporal/) §10.1 rule 3 promotes a shared module to the PARENT MODULE LEVEL, and all three importers of `shared.run_bounded` (`assistant_activities`, `build/build_activities`, `plan/plan_activities`) are already under `modules/assistant/`, so the rule is satisfied where it stands and is SILENT on whether a 1307-line module should be split for size alone.** PR #101's producing run rejected the extraction on exactly that reasoning and the `review-pr` pass verified the reasoning as sound; the silence is what makes it a human question rather than a conformance failure. **Done-state today: yes** — one extraction, one new module, no behaviour change, with the six functions and three constants above as the worked boundary. **The trigger that would force it anyway** is a third non-assistant consumer of the `gh` layer, which is already recorded in PR #101 as the revisit condition for the sibling question about `run_bounded`'s home. **Distinct from C-106, the only adjacent row — checked by mechanism, per [`finding-routing.md` § 5](../../finding-routing.md) gate 2.** C-106 asks what a *standard* must say about retry policy for external CLI calls; this asks where the *code* implementing one should live. C-106's artifact is `docs/standards/workflow-scripts.md`, this one's is a Python module. Neither blocks the other and delivering either leaves the other untouched. **Placed rather than surfaced because the PR body is not a home.** PR #101's fourth pass recorded this under "Surfaced — no verified home", and `review-pr` pass 3 held the PR on it: a proposal left in a PR body dies at merge, and `candidates.md` is the home the same run had already used for C-106. **Why [`finding-routing.md`](../../finding-routing.md) § 5 gate 0 does not reach this, stated because the row is a precedent the next run will read.** Gate 0's closed list — `fixed`/`rejected`/`hold`, with `candidate` explicitly unreachable — governs a finding ABOUT THE WORK IN HAND, and PR #101 does touch this file. It does not apply because § 2's Question 0 comes first and answers PROPOSAL: `assistant_activities.py` is a pre-existing module that behaves correctly, the split is capability that does not exist, and § 3's selection rule and gate 0's list are both written for a DEFECT. § 4 then grants a producing run the right to place its own proposal in its own PR. The distinction that makes this checkable rather than arguable: a gate-0 finding names something WRONG in the diff; this names something ABSENT from the tree. **A run reaching for this precedent should test its own item against that sentence, not against this one.** **Lens verdicts (gate 5).** `/decide` — **not dissolved.** The five-whys bottoms out at *why is there no rule for when a module is too large*, and the answer is that [Temporal Standard](../../temporal/) §10.1 rule 3 answers a different question (which LEVEL a shared module is promoted to) and is satisfied here, so nothing in the corpus is positioned to say whether 1307 lines across seven concerns is a defect or a preference. That is an unresolved upstream question, not a dissolved one. `/best-practices` — **RESOLVES INTO A KNOWN FIX, and that is the finding.** Extract-module against a single-concern seam is settled practice with a named mechanism, an agreed safety property (no behaviour change) and an obvious verification (the suite). What is NOT settled by any external source is whether to spend it here and now, because the cost is a 1307-line file's import surface moving under four other modules mid-PR. **So the two lenses disagree in a way that is itself the argument for filing rather than doing:** the mechanism is known and the timing is a judgement, which is exactly the shape `triage-candidates` exists to rule on and the shape a correction pass must not rule on alone. |
| C-108 | A shipped check that a prompt PROMOTION leaves the assembled prompt byte-identical — the promoter's own before/after comparison is a throwaway script, and it is scoped to the moved text rather than to the render | workflow-decomposition | PR #100 `build-refine` (correction pass), raised independently by the draft's reflection and by `quality-control` |  | `open` | **Measured on this PR, by re-running the comparison the PR body claims: the claim was false and nothing in the tree could see it.** PR #100's body asserts *"Every one of the 8 affected prompts was snapshotted before any edit and reassembles byte-identical after promotion."* Rendering all eight at `origin/main` and at the branch head and diffing them gave **6 of 8 differing** — two draft prompts carried 447 bytes of `<!-- SHARED -->` editor commentary into the model-facing text, one of it as literal line 1, and seven promotion seams each gained a blank line. **The draft was not being careless: its check verified that the MOVED TEXT was unchanged, while its claim asserted that the ASSEMBLED PROMPT was unchanged, and nothing bridged the two.** That gap is the proposal. **The consequence is not this PR's headers — those are fixed here — it is the next promotion.** Seven duplicated consumer-sets remain, each a future PR that will reach for the same methodology, and `workflow-scripts.md` § *A prompt block with two consumers is promoted* names the mechanical recipe without naming any fidelity check at all. Every existing guard is blind to the class: the duplication test matches verbatim blocks, the budget test counts file bytes, `test_prompt_completeness` matches supplier names, and `test_promoted_fragments_render_for_every_consumer` (shipped by this PR) asserts a needle ARRIVED, never that nothing else did. **A promotion that silently rewords a sentence, flattens an indented literal, or drags commentary into the prompt passes all of them** — and the draft's own reflection records that it nearly shipped exactly the flattening defect, caught only by the throwaway script. **Done-state today: yes, and the mechanism already exists in the tree** — this pass's comparison drove the real entry points with a captured `run_claude` and normalised the tmpdir, which is the same shape `test_promoted_fragments_render_for_every_consumer.py` already uses; what it needs to become a gate is a stored pre-promotion snapshot and a rule for retiring one. **The design question is real and is triage's to rule, not this row's:** a committed snapshot per promotion PR, versus a helper the promoter runs against `origin/main` at authoring time and pastes the output of, versus retiring *"byte-identical"* as a claim a PR body may make without a reproducible check behind it. The third is free and the weakest. **PROPOSAL, not a defect:** no prompt is wrong today — this PR's two header instances are fixed in it, and the remaining seam blank lines are disclosed and inert. What does not exist is the check. **Raised twice independently, which is why it is filed rather than surfaced:** the draft's own reflection proposed *"a small helper that snapshots the affected prompts, promotes, and diffs the reassembly"* and deliberately did not place it, and `quality-control` reached the same remedy from the opposite direction — that an unreproducible check makes the surrounding rigour read as more trustworthy than it is. **Not an expansion of C-072** (promote the research family's RULES footer — that is one more promotion to DO; this is the gate that would make it safe) **and not of C-065** (a false claim about another file's behaviour — that class has no second copy to derive from, whereas this one is a diff of two renders and is mechanically checkable, which is what makes it gateable where C-065 was judged not to be). **EXPANDED 2026-08-17 by PR #100's fourth correction pass, with a measured second face of the same gap: the pool has no content guard AFTER promotion either.** That pass promoted `submit_and_push.md` and then mutated it — deleting the `RE-CHECK origin/main` bullet from the shared fragment outright — and **the entire suite stayed green (31 passed)**. Promotion converts a drift risk into a single-point-of-deletion risk: while text is duplicated, a one-sided edit is a divergence some detector can see; once shared, removing it takes it from every consumer at once and nothing diverges. This is not hypothetical maintenance worry — it is the same afternoon's measurement on the fragment that PR had just created to fix a missing-instruction defect. It applies to every fragment in the pool, not to that one — the count is deliberately not stated, because a bare figure here is the same race with the tree this file keeps losing. **THIS WIDENS C-106'S SCOPE and the widening is the point:** the row was opened about the ACT of promoting, and it now also covers the pool's content over its LIFETIME — a different trigger (any later edit, not a before/after snapshot) and a different mechanism (a standing content floor, not a one-time comparison). Ruling C-106 as a promotion-time check alone would leave the deletion half unsatisfied. **C-107's note distinguishes itself from C-106 on the pre-expansion reading**, which was accurate when written; the two rows are still distinct, but that discriminator should be re-read against this sentence rather than taken at face value. |
| C-109 | `workflow-scripts.md` § *A prompt block with two consumers is promoted* forbids a TEST from ruling on near-duplicates, and does not say whether a surfacing-only detector is a test in that sense — so the same sentence licenses and forbids the guard PR #100 shipped under it | workflow-decomposition | PR #100 `build-refine` (second correction pass), raised by `quality-control` |  | `open` | **The sentence, verbatim:** *"That half is a judgement about which differences are deliberate, and it belongs to the fork-vs-parameterize ruling, not to a test."* It is the closing paragraph of the section that created the shared prompt pool, and it is there to stop a test from deciding that two drifted paragraphs must be collapsed. **Why it needs a ruling rather than a reading.** PR #100's second correction pass shipped `test_tier_siblings_do_not_DRIFT_by_a_sentence.py`, which freezes today's seven near-duplicate pairs between a workflow and its own `_minor` sibling, rules on none of them, and fails only when the SET grows — with a failure message that offers reconciliation and *record-it-as-deliberate* as equal exits. `standards-auditor` read § 715 as forbidding a test that DECIDES and permitting one that SURFACES, citing that the same section sanctions exactly this shape two paragraphs earlier for the verbatim half (*"freezes the duplication that existed when the rule landed and ratchets both ways"*). `quality-control` agreed the reading is plausible and flagged that settling a binding standard's ambiguity inside the PR that ships the tooling is the wrong actor. **Both are right, which is the finding.** **The consequence is not this guard, it is the next one.** The near-duplicate half was named as a known blind spot and left unguarded for as long as the sentence has existed; the next author who wants to close it reads the same sentence and gets no answer, so the outcome depends on which agent reads it. **Measured on this PR:** the two drifts that guard now covers were live for the whole life of the branch, ran through a draft pass and a full correction pass, and were found by a review pass reading the two files side by side — which is the manual labour a detector exists to remove. **Done-state today: yes, and it is one sentence.** Either § 715 gains a clause permitting a surfacing ratchet and naming the two-exit failure message as the condition, or it gains one forbidding it and the shipped guard is withdrawn. **PROPOSAL, not a defect:** nothing is wrong today under either reading — what does not exist is the sentence that makes the reading unnecessary. **Not an expansion of C-106**, which proposes a fidelity check on the ACT of promoting; this is about whether a class of guard may exist at all. **This row is placed rather than surfaced because a standards amendment is a proposal and `standards-governance.md` § *research/ is a WORKING SURFACE* names this file as where an autonomous run puts one — the amendment itself is human-in-the-loop and is not made here.** |
| C-110 | **Nothing defines what a `_minor` tier's prompt is supposed to CONTAIN, so general discipline lands in one tier and not its sibling and every reconciliation is re-argued from scratch** — ten blocks of guidance that is not about review depth exist in `build_refine` and nowhere in `build_refine_minor` | workflow-decomposition | PR #100 `build-refine` (fourth correction pass), held by `review-pr` pass 3 |  | `open` | **Measured in this tree, not carried on a prior pass's account.** `build_refine` holds 30 substantive prompt blocks and `build_refine_minor` holds 7; **25 of the major tier's blocks have no counterpart in the minor tier** above a 0.55 similarity floor. Most are the documented lens-count axis — the Stage-2a/2b machinery, the refactoring-evaluator and standards-auditor briefs, the quality-control dispatch — and those SHOULD differ. **Ten are not**, and they are the finding: `MUTATE AN ASSERTION'S SCOPE`, `COMPARE THE CHECK SET`, `PRINT WHAT THE MUTATION ACTUALLY PRODUCED`, `SIZE THE CHANGE THEN SET THE BAR`, `ASK WHAT EACH GUARD DOES NOT LOOK AT`, `A MUTATION PROVES A GUARD DISCRIMINATES WITHIN ITS POPULATION`, *"If you have written the remedy, apply it"*, *"Rejecting is legitimate — with reasoning that holds"*, the treat-another-run's-*"pre-existing"*-as-a-claim paragraph, and the finding-disposition rule. None of those is about how many lenses a pass runs. **THE CLASS IS FLEET-WIDE, which no prior pass measured:** the same asymmetry holds in the other two tier pairs — `build_draft` 17 major-only against 27 minor-only, `research_write` 18 against 16 — so the minor tier is not simply a subset of the major one in either direction, and whatever rule is written has three consumers rather than one. **Why NO GUARD CAN REACH IT, which is what separates this from what PR #100 shipped.** Both block detectors in `test_tier_siblings_do_not_DRIFT_by_a_sentence` compare PAIRS, and an orphan block has no pair; the module's own *WHAT THIS DOES NOT LOOK AT* names it *"the LARGEST uncovered class, not a corner"*. The `_orphan_lines` detector that PR added reaches one granularity down — a whole LINE missing from a block that DOES have a counterpart — and is deliberately not this. **Why it is a PROPOSAL and not a defect:** every one of the 25 predates the branch, nothing about the promotion created them, and reconciling any of them requires first deciding what the minor tier is FOR — a contract stated today only in code comments and binding nowhere. **THE TEN DO NOT SHARE ONE BLOCKER, and triage should not be told they do.** Most wait on that contract. But at least two — *"If you have written the remedy, apply it"* and *"Rejecting is legitimate — with reasoning that holds"* — are disposition rules, a category this PR already treated as tier-invariant when it promoted five `resolve_*` fragments; their real obstacle is that each is already frozen with two consumers in `test_prompt_blocks_are_shared_not_copied.py`, so adding a third tier trips the spread check and the correct move is a cross-family promotion rather than a contract ruling. A contract ruling alone would leave those two exactly where they are. **Done-state today: yes**, and it is a paragraph in `workflow-scripts.md` saying which classes of prompt content are tier-invariant (operational safety, disposition rules, evidence discipline) and which are tier-scoped (review depth, agent count), after which the ten are a mechanical reconciliation and a guard becomes writable. **The cost of leaving it:** this is the third correction pass on one PR to meet the same question and the third to answer it differently, and the instance that motivated the hold — the minor refine tier never being told to re-check `origin/main` before pushing — shipped for the whole life of the branch behind a baseline entry that recorded the pair as looked-at. **Not an expansion of C-107, and they should be ruled together.** C-107 asks whether a surfacing-only GUARD may exist under that section at all; this asks what the two tiers must CONTAIN. Different questions, same section, and a clause written for either without reading the other is how one section grows two rules that disagree. **Not C-106 either** — that is about a promotion silently editing prose, which is a fidelity question rather than a content one. **Predicate, stated so triage can re-derive rather than trust:** blocks are the blank-line-separated runs of every `prompts/*.md` under a child, excluding the shared pool, keeping those of 120 bytes or more — the same split `test_tier_siblings_do_not_DRIFT_by_a_sentence.py` uses; a block is an orphan when its best `difflib` ratio against every block of the sibling is under 0.55. **Lens verdicts (gate 5).** `/decide`: the five-whys bottoms out at *nobody ever wrote down what the `_minor` tier is for*, so each reconciliation is argued from the arguer's model of it — which is why three passes on one PR reached three different answers. It does NOT dissolve: the ambiguity is real, load-bearing and cheap to settle, and settling it converts a recurring judgement into a lookup. `/best-practices`: a shared-partial system states a contract once and names which partials are invariant — the standard practice is an explicit inventory of what varies by tier, not a convention inferred from what happens to differ today; the drift here is what an unstated contract predicts. *(Id allocated by re-reading this file and `git show origin/main:docs/standards/architecture/research/candidates.md`, whose maximum is C-105; C-106 and C-107 are this PR's own earlier rows.)* |
| C-111 | A child prompt may hold a drifted near-copy of a shared pool fragment and NO guard can see it at any granularity, so promotion silently stops being the single source of truth the standard sells it as | workflow-decomposition | PR #100 `review-pr` pass 4 |  | `open` | **Both duplication guards exclude the pool BY CONSTRUCTION**, so the child↔pool axis has no detector: `test_prompt_blocks_are_shared_not_copied.py:118-119` skips it (`if p.parent == SHARED: continue`) and `test_tier_siblings_do_not_DRIFT_by_a_sentence.py:170` defines `SHARED` only to scan children against each other. **Live instance, measured in this branch's tree rather than inherited:** of the 12 blocks in `modules/assistant/prompts/mutation_discipline.md` (counted at the duplication guard's own `MIN_BLOCK = 120`), two survive in `build/build_refine/prompts/stages_2_to_4.md` as drifted copies — *SIZE THE CHANGE, THEN SET THE BAR* at **0.966** and *ASK WHAT EACH GUARD DOES NOT LOOK AT* at **0.925** — while the whole-file ratio is **0.117**, so a whole-file comparison hides both and a verbatim hash sees neither (**zero** blocks are byte-identical). `build_refine` does not load `${MUTATION_DISCIPLINE}` at all — only `build_draft` and `build_draft_minor` do (`build_draft_workflow.py:57`, `build_draft_minor_workflow.py:49`). **This is a PROPOSAL, not a defect this PR introduced:** the duplication predates the branch, and reconciling it is a content ruling rather than a mechanical edit — the two copies have each drifted by a clause, and `build_refine` would share only 2 of the fragment's 12 blocks, so loading the whole fragment is exactly the question **C-110** exists to ask. **Read with C-110:** those two blocks are also two of its named ten, so the rows are distinct mechanisms over the same text. **NOT an expansion of C-108**, which covers the pool's OWN content (promotion-time byte fidelity, and post-promotion deletion from a fragment); this is the child→pool DUPLICATION axis — a different detector and a different remedy. **Scored the way this PR's own guard scores** — `difflib`, `autojunk=False`, symmetric max, per `_pair_score` at `test_tier_siblings_do_not_DRIFT_by_a_sentence.py:423-447`. **THE SETTING IS THE WHOLE DISCREPANCY, and an earlier draft of this row got the diagnosis backwards.** It read *"the pass that surfaced this reported 0.925 and 0.117; re-measured here at 0.844 and 0.061"* — as though the earlier figures had failed to reproduce. Both pairs reproduce exactly, in this tree, today: 0.844/0.061 are `difflib`'s DEFAULT, and 0.966/0.925/0.117 are the same comparison with `autojunk` off. That default is the scorer artifact this very PR removed from `_pair_score` after it scored one line at 0.366 and 0.701 depending on argument order — so a row carrying the autojunk-ON numbers would re-import the artifact the same change was written to eliminate. The autojunk-free figures are the ones stated. (The first block's 0.966 is identical under both settings; only the second moves.) |
| C-112 | Trace a paper's corrections into the PR BODY — `research_verify` Stage 2 names the synthesis as the only dependent, so a merge-time reader can be handed the over-claim the correction rounds just removed |  | PR #102 (research_minor, verify correction pass) |  | `open` | **Stage 2 of `verify.md` binds the Research Standard's *a corrected fact traces to ALL its dependents*, and then enumerates the dependents as the draft SYNTHESIS only.** The PR body is not in that set and is a real dependent: it is written by the producing run BEFORE any correction round exists, and it is the first artifact a human reads when ruling on the merge. **Two instances survived three correction rounds and a full `review-pr` pass on this PR, in the same body.** (1) A source count of 13 against the paper's §8, which enumerates 15 — `[R10]` and `[R11]` were added by round 1 and the body was never re-counted; the breakdown beside it was stale in two of its five terms. (2) **The consequential one:** the body stated the negative finding as *"no prompt/LLM-engineering literature on divergence between copies of a prompt exists yet"* — the exact does-not-exist over-claim that round 3 removed from the paper's §3.5 heading and §8 after `[R6]` was found to be a vision paper rather than a systematic survey. So the paper says *not found by these methods on this date* and its own PR body said *does not exist*, and the reader ruling from the body carries forward a claim the gate specifically corrected. **Both were fixed in this PR by the verify pass; the row is for the missing trace, not the instances.** Remedy is one clause — name the PR body in Stage 2's dependent set, beside the synthesis, and require the body's derived figures be re-enumerated at the end of the last correction round. **The count half is ALSO a recurrence of C-050** (a figure restated where nothing derives it) and is reported here rather than added to that row, which has other filers; the over-claim half is not a count and C-050 does not reach it. **Distinct from C-073 and C-075**, the two other `research_verify` rows: C-073 is about normalisation of a verbatim span, C-075 about which spans a round re-downloads — neither is about which SURFACES a correction has to reach. **This row was placed as C-106 and renumbered to C-112 at the merge that found the collision:** `origin/main` landed a different C-106 (the retry-policy row) while this branch was open, and that one is merged, so it keeps the id. Same shape as C-101's renumbering note, and the third recorded instance of the allocation collision C-102 proposes fixing. |
| C-113 | **A planning workflow's write boundary excludes `docs/file_structure.txt`, which its own output makes stale — so every plan it produces ships a RED suite it is structurally forbidden to fix** — give the boundary the one map line, or let the map roll planning directories up |  | PR #111 (`plan-feature`, operator correction pass) |  | `open` | **Measured on this branch, and on the previous run's commit, so it is not an artifact of this pass.** `test_a_directory_the_map_ENUMERATES_is_enumerated_COMPLETELY` fails as soon as a phase doc is added to a directory the map enumerates file-by-file: `docs/file_structure.txt:155-158` lists `workflow-decomposition/` as `roadmap.md` + `research/`, so the four phase docs planned into it are holes. Reproduced at `6d71c75` (four docs) and at `a8846fb`, the previous run's tip (also four) — **that run's PR body asserted the opposite**, *"the map's coverage test passes (it checks the directory, not the phase list)"*, which is why this row states the command rather than the conclusion: `python3 -m pytest testing/scripts/tests/unit/test_file_structure_map_covers_the_tree.py`. **Why it is structural rather than one stale map.** `plan-feature`'s grant is markdown files sitting directly in the component directory, and the map is not one; its dispatch prompt names `docs/file_structure.txt` as out of scope explicitly. Writing a phase doc is the workflow's ENTIRE JOB, and the map enumerates at least two planning directories that way (`persistent-memory-protocol/` is the other, at `:108-113`). **So the next component `plan-feature` plans lands red for the same reason, and the run after that, until one of the two sides moves** — the failure is produced by the boundary, not by any run's carelessness, which is exactly the shape a single run cannot fix from inside. **Two remedies, and they are genuinely different rulings rather than one idea phrased twice.** (a) **Widen the boundary by one line** — permit the workflow to edit only its own component's block in the map, checked the way the phase-doc filename check already is. Keeps the map's per-file annotations, which are load-bearing prose a reader uses; costs a carve-out in a boundary whose value is that it is narrow, and hands an LLM producer a file every other component also lives in. (b) **Let the map roll planning directories up** — a `roadmap + N phase docs` summary line instead of per-file entries, which the test already supports (it only enforces completeness on directories the map chooses to enumerate). Cheaper and keeps the boundary intact; costs the per-phase annotations for those directories. **Not an expansion of C-101**, which is about a duplicate map entry a merge produced — a data defect in the map. This is about who may write it at all. **`component` left BLANK deliberately:** the finding sits between a workflow's write contract and a repo-wide doc test, and nothing scaffolded obviously owns that pair — `workflow-decomposition` owns the composition and invocation contracts but not a workflow's filesystem grant, and inventing a name here would commit `plan-candidates` to scaffolding a directory on a guess. **Done-state today: yes** — either remedy is a single small edit once the ruling is made, which is why this is a proposal awaiting a ruling rather than an issue awaiting a trigger. |

## Where things stand
**Sixty-three rows are untriaged, and they are the next `triage-candidates` pass's working set: C-047 through C-113.** Five (C-047–C-051) were re-homed out of the GitHub issue queue on 2026-08-09, C-052 came from issue #36, C-053–C-056 were placed from PR #71, C-057–C-059 from PR #75, C-060 from PR #79, C-061 from the operator, C-062 from PR #80, **C-064 and C-065 from PR #81 (Phase 4)**, the second placed by that PR's refine pass, **C-066, C-067 and C-068 from PR #74**, **C-069 from PR #82 (Phase 6)**, **C-071 and C-072 from PR #83 (research_minor)**, the second placed by that PR's refine pass, **C-073 and C-074 from PR #84 (research_minor, verify pass)**, **C-075 from PR #85 (the triage split, refine pass)**, **C-076, C-078 and C-079 from the persistent-memory-protocol plan**, **C-080 through C-084 from the `plan-candidates` build**, one per pass after the draft (C-081 by the refine pass, C-082 and C-083 by its second correction pass, C-084 by its third), **C-085 and C-086 from the `plan-feature` build** — the workflow that lands the write half of the planning split, proposing the read half, and its refine pass proposing the gate that would have caught the four defects it fixed — and **C-093, C-094, C-095, C-096 and C-097 from the `plan-verify` build**, which delivered C-085 and found that the estimates it writes have no consumer, the second placed by its correction pass after `review-pr` observed that six commits already on that branch had passed through no review lens — **C-100 from the operator's CI/CD ruling** and **C-101 and C-102 from PR #96 (`build-refine` on the memory-taxonomy rename)** — the first about the duplicate map entry that PR's own merge of `origin/main` produced, placed as C-100 and renumbered when the merge found the collision, and the second, placed by that PR's fourth correction pass, about the id-allocation mechanism whose absence is what made the renumber necessary — and **C-107 from PR #101 (`build-refine`, fifth pass)**, placed because `review-pr` held that PR on the proposal having been surfaced in a PR body and nowhere else, **and C-108, C-109, C-110 and C-111 from PR #100 (the prompt-block promotion)** — the first three placed by that PR's first, second and fourth `build-refine` correction passes, about the byte-fidelity check its own draft claimed to have passed, about whether the standard permits a surfacing-only near-duplicate guard at all, and about what a `_minor` tier's prompt is contractually supposed to contain; the fourth placed by `review-pr` pass 4, about the child↔pool duplication axis both duplication guards exclude by construction — **and C-112 from PR #102 (`research_minor`, verify correction pass)**, about a paper's corrections tracing into the PR body and not only into the synthesis, itself placed as C-106 and renumbered at its merge for the same reason — **and C-113 from PR #111 (`plan-feature`, the operator correction pass)**, about a planning workflow's write boundary excluding the one file its own output makes stale, whose `component` is deliberately blank. **They were placed as C-106, C-107 and C-108 and renumbered when this branch's merge of `origin/main` found those ids already taken by PR #101** — the second collision of this exact shape recorded in this paragraph, and C-102 is the row that proposes fixing the allocation mechanism whose absence causes it — the numbering skips C-063 and C-070, and this file's own rule is that ids are never reused and never renumbered. *(A C-063 was placed and then WITHDRAWN in the same PR — `/decide` dissolved it: it proposed a cross-fleet ownership rule for `activities/run-claude.sh`, and the tension it describes is between a frozen fleet and a migrating one. The operator has ruled V1 is deleted when unused, so the condition expires on its own; a rule written for a transitional period costs more than the period. Recorded here so a later pass does not re-derive it.)* *(C-070 was vacated by C-072's own renumbering note and no row ever took it — checked on both sides of this merge, `git show origin/main:…` and this branch, before writing this sentence.)* Their blank `decision` is the truth, not an omission — but blank is only honest while this paragraph says how many there are. **C-048 and C-049 must be ruled together**, per C-048's own Note.
> **Six declarations in this section are derived rather than restated — [`test_candidates_prose_matches_the_table.py`](../../../../testing/scripts/tests/unit/test_candidates_prose_matches_the_table.py) computes each from the table and fails when it drifts.** Those six are the untriaged total, the declared id range, the skipped ids, the decided-row count and its `ship`/`requires review`/`reject` split, the separately-stated rejected count, and the `requires review` queue as both a count and a list of ids — the same six `test_every_declaration_this_check_verifies_is_actually_FOUND` names, so this sentence goes red with the check rather than beside it. **A figure NOT in that list is not covered, and there are such figures below** — the applied-before-triage count is one, and it was wrong ("Six", against eleven ids) until PR #101's fifth pass corrected it. This sentence used to read "derives every one of them", which is the exact universal-with-no-failure-mode this repository keeps writing and then finding false. This paragraph was written by hand three times and was wrong twice; **the third occasion was a MERGE** — PR #85 and `origin/main` both rewrote the sentence, and a conflict resolution deletes rows with no diff to show for it. The check keys on the CLASS, not on the ids that happened to collide: any declared figure the table does not support goes red, and an *undeclared gap* — the shape a deleted row leaves behind — goes red on its own. Its sibling [`test_candidate_ids_are_unique.py`](../../../../testing/scripts/tests/unit/test_candidate_ids_are_unique.py) holds the other half of the same seam.

The 47 rows that predate them carry a decision: **29 `ship`**, **6 `requires review`**, **12 `reject`**.

> **Every number in this section is a restatement of something nothing derives, and each one was wrong.** It read *"Nothing is untriaged… All 45 rows… 25 `ship`, 8 `requires review`"* while **nine** rows were blank and the split was **27/6**; the next paragraph said **seven** `requires review` rows were filed as `D-001`–`D-006` when **five** are, under non-contiguous ids. The consequence was not cosmetic: five proposals re-homed out of the GitHub issue queue on 2026-08-09 were sitting invisibly in this one, because the summary `plan-sprint` builds its working set from said four rows were untriaged. **This is exactly the mechanism C-050 names** — *derive declared counts rather than restating them* — occurring in the file that holds C-050, and this is its second recorded occurrence. A third correction is the wrong response; ruling C-050 is the right one.

**The `requires review` rows are the live queue, and there are six: C-010, C-017, C-021, C-032, C-034, C-041.** Five are filed in [`direction.md`](direction.md) — C-010 as `D-001`, C-021 as `D-003`, C-032 as `D-004`, C-034 as `D-005`, C-041 as `D-006`. **C-017** is not, because issue #41 already tracks that ruling and one item does not get two homes. (`D-002` and `D-007` are sourced from rows and papers outside this queue, so they do not belong to this count.) Nothing here moves until the operator rules.

**Eleven `ship` rows were already applied before triage** — C-020, C-024, C-025, C-026 and C-027 landed in `f2b80a6` and the sprint-plan comparator split, and C-007/C-008/C-009/C-013/C-015/C-018 in `b9710d5`. A candidate can be delivered before anyone gets round to ruling it; recording that is cheaper than re-deriving it.

**12 rejected, and the reasoning is the point.** Three assumed **Temporal Cloud**, settled 2026-07-12 and unwritten at the time, so a research cycle costed out a product ruled out three weeks earlier — now recorded in `stack_reference.md`. Four more were **already delivered** by shipped work. The rest name what would have to change for them to be re-proposed.

**Known gap in this file's own model:** `status` is set by "a later process" — `plan-feature` on landing, or the build that completes it. A `reject` never lands in a phase doc and no build completes it, so nothing will ever close one. Nine of the twelve 2026-08-06 rejects therefore sat at `status: open`, reading as outstanding work when they were not — corrected by hand on 2026-08-10 (see the note above). The gap itself remains: nothing will close the next `reject` the same way.
