claude-dot-files/
├── config/                                          # Source of truth for synced Claude Code config (symlinked to ~/.claude/ by install.sh)
│   ├── CLAUDE.md                                    # Stub redirect — content lives in /rules
│   ├── settings.json                                # Global settings, permissions allowlist, hook config
│   │
│   ├── rules/                                       # Always-loaded global rules (one file per topic)
│   │   ├── claude-dot-files-governance.md           # Prevents uninstructed edits to this repo from project sessions
│   │   ├── code-style.md                            # Readability, early returns, match-existing-style
│   │   ├── communication.md                         # Don't add docstrings/comments to code you didn't change
│   │   ├── dependencies.md                          # Check existing tools, prefer stdlib
│   │   ├── engineering-quality.md                   # Senior-engineer quality bar, finding disposition, deferral placement, best-practices checkpoint
│   │   ├── git.md                                   # Commit format, push/amend discipline
│   │   ├── personal-tooling.md                      # Pointer to workflows guide + /get-started
│   │   ├── proactive-doc-management.md              # PM-side triggers for invoking doc-manager
│   │   ├── safety.md                                # No secrets, no force push, no destructive commands without confirmation
│   │   ├── standards-governance.md                  # Standards-not-ADRs, human-in-the-loop discipline, CPI log mechanics
│   │   └── terminal-output.md                       # Copy-paste-safe command formatting (no heredocs, single-line, --task-file)
│   │
│   ├── skills/                                      # Description-matched methodology (loaded on-demand)
│   │   ├── architecture-decisions.md                # ADR methodology (applied via standards docs per our convention)
│   │   ├── decision-methodology.md                  # Five-whys cascade + upstream check + BP-on-reframed-question for low/mid-confidence decisions
│   │   ├── documentation-management.md              # Doc-systems-engineer methodology (4 modes for doc-manager agent)
│   │   ├── documentation-structure.md               # Doc-bucket convention (development / standards incl. architecture / guide)
│   │   ├── planning-methodology.md                  # How to plan features, break down work, identify risks
│   │   ├── project-definition.md                    # Multi-stage process for defining new projects from scratch
│   │   ├── project-organization.md                  # Project shapes (multi-repo + master-planning OR single-repo /docs/), planning hierarchy
│   │   ├── quality-control-methodology.md           # Six-dimension senior-engineer integration test
│   │   ├── refactoring-methodology.md               # When and how to refactor safely
│   │   ├── standards-authoring.md                   # Timeless, rule-focused standards (bloat-pattern detection)
│   │   ├── standards-enforcement.md                 # How to check code conformance to standards
│   │   ├── testing-methodology.md                   # Testing principles, scoping, decision-making
│   │   ├── testing-scaffolding.md                   # Setting up test infrastructure in new projects
│   │   ├── test-suite-architecture.md               # Test suite layout, framework selection (incl. Ansible/Terraform)
│   │   ├── troubleshooting-methodology.md           # Divide-and-conquer + hypothesis-driven debugging with structured escalation
│   │   ├── workflow-analysis.md                     # CPI log analysis methodology
│   │   └── workflow-dispatch.md                     # How to choose and invoke autonomous workflows
│   │
│   ├── agents/                                      # Named subagents (invoked via Task tool)
│   │   ├── architect.md                             # System design & trade-off analysis (Opus, on-demand)
│   │   ├── code-reviewer.md                         # Code review with structured findings (Sonnet, on-demand)
│   │   ├── doc-manager.md                           # Documentation systems engineer — 4 modes (AUTHOR/COORDINATE/AUDIT/MAINTAIN)
│   │   ├── planner.md                               # Detailed implementation plans (Opus, on-demand)
│   │   ├── quality-control.md                       # Senior-engineer holistic lens — runs sequentially after parallel reviewers
│   │   ├── research-analyst.md                      # Deep-research mini-paper author (opus + web; raw-sources-over-rendered, gaps-are-findings)
│   │   ├── research-critic.md                       # Anti-hallucination gate — fetches every cited source, verifies claims match
│   │   ├── research-currency.md                     # Research refresh differ — fresh sweep vs existing paper, revalidation intervals
│   │   ├── security-auditor.md                      # Vulnerability detection & audit
│   │   ├── standards-architect.md                   # Audits standards corpus for coherence, bloat, drift
│   │   ├── test-writer.md                           # Generates & runs tests
│   │   └── workflow-analyst.md                      # CPI workflow-log analysis
│   │
│   ├── commands/                                    # Custom slash commands
│   │   ├── best-practices.md                        # /best-practices — industry-standard approach primer
│   │   ├── cleanup-merged-worktrees.md              # /cleanup-merged-worktrees — sweep worktrees with merged PRs
│   │   ├── create-claude.md                         # /create-claude — generate CLAUDE.md files for a project
│   │   ├── decide.md                                # /decide — fire five-whys decision methodology (loads decision-methodology skill)
│   │   ├── get-started.md                           # /get-started — session kickoff context
│   │   ├── review.md                                # /review — invoke code-reviewer agent
│   │   ├── standup.md                               # /standup — sweeps the five git memory surfaces → attention brief; WRITER on three of them (see docs/guide/memory-model.md §2.3)
│   │   ├── troubleshoot.md                          # /troubleshoot — fire systematic debugging methodology (loads troubleshooting-methodology skill)
│   │   ├── update-claude.md                         # /update-claude — sync CLAUDE.md standard references
│   │   └── update-file-structure.md                 # /update-file-structure — update this file
│   │
│   └── hooks/                                       # Hook scripts referenced by settings.json
│       ├── block-dangerous.sh                       # PreToolUse: deny destructive bash commands (threat model in header)
│       └── notify-done.sh                           # Stop: desktop notification via notify-send
│
├── docs/
│   ├── file_structure.txt                           # This file (annotated repo map)
│   │
│   ├── development/                                 # WHAT — active work tracking
│   │   ├── cpi-decisions.md                         # Append-only CPI decisions log (ship/defer/reject with watch-criteria)
│   │   ├── skyy-net-seed-handoff.md                 # Canonical handoff: Temporal/Skyy-Net-seed decisions, security surface, milestone 1 (read before resuming that work)
│   │   ├── sprint.md                                # The sprint plan — what gets built and in what order. NEVER a history lesson
│   │   │
│   │   │                                            # One FOLDER per sprint, directly under development/ (there is no phases/ level).
│   │   │                                            # Two shapes, both per the convention in sprint.md:
│   │   │                                            #   <name>/<name>.md                 fits in one phase (all but one today)
│   │   │                                            #   <name>/roadmap.md + phase{N}_*.md  outgrows one phase. Don't create to be tidy
│   │   │                                            # research/, where present, holds the component's evidence:
│   │   │                                            #   <name>/research/raw/<topic>.md   the pool — one mini-paper per topic
│   │   │                                            #   <name>/research/synthesis.md     what the pool means HERE. NON-BINDING
│   │   ├── autonomous-execution/                    # COMPLETE — the workflow fleet; skills-preloading fix, lean agents, log-as-evidence
│   │   ├── autonomous-operation/                    # NOT DESIGNED — the tier above parents; gated on typed memory + durable execution
│   │   ├── continuous-process-improvement/          # IN PROGRESS — two evidence sources; why the ruling is always a human's
│   │   ├── cross-device-sync/                       # COMPLETE — install.sh, idempotency as the enabling feature
│   │   ├── explore-claude-directory/                # COMPLETE — portable vs machine-local; projects/ is path-keyed
│   │   ├── local-ai-offloading/                     # NOTES PREDATING PLANNING (Apr 2026) — GPU offload; integration points now stale
│   │   ├── mcp-servers/                             # NOTES PREDATING PLANNING (Apr 2026) — scopes, transports, Docker reference
│   │   ├── memory-management-framework/             # Phases 1-2 COMPLETE (2026-08-08); Phase 3 COMPLETE (2026-08-10) — status lives in its phase doc, not restated here;
│   │   │                                            #   Phase 5 BUILT AND MEASURED and GATING NOTHING (2026-08-09); Phases 4 and 6 planned.
│   │   │                                            #   First component on the roadmap+phases shape sprint.md mandates
│   │   │   ├── roadmap.md                           # Kind 1 (git, human-read) vs Kind 2 (typed record, code-read); Key Decisions
│   │   │   ├── phase1_measure_the_channel.md        # COMPLETE — six experiments, thirteen rulings. Cancelled nothing; re-scoped Phase 5's predicate
│   │   │   ├── phase2_kind1_framework.md            # COMPLETE — Kind 1 as a substrate-free interface with GitHub as one binding. Found four of its own premises false
│   │   │   ├── phase3_typed_exit_record.md          # Arrangement A, split abstention, fail-safe contract, one pair proven
│   │   │   ├── phase4_fleet_migration.md            # V2-Python-tree rollout ONLY (re-cut 2026-08-10 by operator ruling — V1 is left in place, not
│   │   │   │                                        #   migrated); includes prompt text; `activities/run-claude.sh` is in scope as V2 infrastructure
│   │   │   ├── phase5_convergence_stopping.md       # The payoff — and NOT greenfield: it replaces a shipped model-asserted `converged` flag
│   │   │   │                                        #   with a computed one, and routes NOTHING. § Measurement is the single source of every
│   │   │   │                                        #   figure in the phase; a gate fails any restatement of one outside it
│   │   │   ├── phase6_read_what_it_writes.md        # Names the per-run JSONL log as a surface (Kind 3, proposed) and builds the readers three
│   │   │   │                                        #   phases of emitters never had; owns Phase 5's un-owned activation trigger
│   │   │   └── research/                            # 2 papers + synthesis, both PASS-WITH-FIXES 2026-08-06
│   │   ├── persistent-memory-protocol/              # PLANNED, NOT STARTED (2026-08-13) — ruled its OWN COMPONENT rather than a 7th phase
│   │   │                                            #   of memory-management-framework; the reasoning is in roadmap.md and C-074's
│   │   │                                            #   `decision` is deliberately left blank for the operator. Second component here
│   │   │   │                                        #   on the roadmap+phases shape. ALL EIGHT phases have a doc — a gate is a scheduling
│   │   │   │                                        #   fact, never a reason to leave a design unwritten (revised 2026-08-13)
│   │   │   ├── roadmap.md                           # Opens in plain words with a terms table; states the protocol WHOLE (nine
│   │   │   │                                        #   commitments) before any ordering; § The four kinds of record is the SINGLE
│   │   │   │                                        #   WRITER of the memory taxonomy and carries § Reading the old names, the
│   │   │   │                                        #   Kind 1/2/3 → working record/typed exit record/measurement samples map;
│   │   │   │                                        #   § Absorbed work records what came over when MMF retired; a 15-concept coverage
│   │   │   │                                        #   map; and the operator calls the plan carries WITHOUT deciding — storage budget,
│   │   │   │                                        #   snapshot cadence, what a machine is, and Phase 7's egress/ingress rulings
│   │   │   ├── phase1_the_run_bag.md                # Configurable root, folder per run keyed by run_id (NEVER by path), subfolder per
│   │   │   │                                        #   concurrent child, BagIt RFC 8493 manifest. A bag state is `lifecycle: open/sealed`
│   │   │   │                                        #   plus TWO INDEPENDENT FLAGS, `redacted` and `incomplete` — never one collapsed
│   │   │   │                                        #   label, or a bag that lost bytes reads like one a human redacted. No database — a
│   │   │   │                                        #   decision with a revisit trigger, not an omission
│   │   │   ├── phase2_content_store.md              # bernstein §4.6 — store the bytes, checksum them, re-verify quoted spans OFFLINE.
│   │   │   │                                        #   Tier 1, cost S, and it was ranked and then never placed once already
│   │   │   ├── phase3_the_emit_rule.md              # If ANY store gets it, the journal gets it VERBATIM. Destination is a field, not
│   │   │   │                                        #   a format. Stable edge_id — a rotating API key would orphan a machine's history.
│   │   │   │                                        #   OWNS THE WRITE-FAILURE ANSWER: a gap may exist, a silent gap may not. And it
│   │   │   │                                        #   rules that the journal event is a SEPARATE contract from the typed exit record,
│   │   │   │                                        #   because exit-protocol.md §2/§2.5 forbid the extension the draft promised
│   │   │   ├── phase4_rebuild_is_a_test.md          # Replay into a scratch dir and diff. What makes Phase 3 enforceable rather than a
│   │   │   │                                        #   promise; its own phase because a folded-in verification is a gate runs walk past.
│   │   │   │                                        #   Two arms — synthetic fixture on the merge path, live journal host-only; NO skip arm.
│   │   │   │                                        #   Also owns the RESTORE (the capability the roadmap sells) and the rule that a rebuilt
│   │   │   │                                        #   store carries the journal's provenance forward to every consumer
│   │   │   ├── phase5_snapshots_then_retention.md   # Snapshot FIRST, then delete — never past the last snapshot, because after Phase 4
│   │   │   │                                        #   a deletion rule is a decision about what can no longer be reconstructed. Split
│   │   │   │                                        #   retention: authored record never deleted, transcript on a schedule. GATED on the
│   │   │   │                                        #   Temporal server for the recurring half ONLY
│   │   │   ├── phase6_cpi_reads_the_journal.md      # The CONSUMER for phases 1-4, split from the poller at review so the only reader is
│   │   │   │                                        #   not gated behind an unbuilt Temporal server. Gated on `Port review-runs` instead
│   │   │   ├── phase7_s3_aggregation.md             # Ship bags to object storage under machine_id/run_id, writing locally first. THREE
│   │   │   │                                        #   gates and the last two are SEPARATE operator rulings — egress (what may leave)
│   │   │   │                                        #   and ingress (what may this machine believe); the heartbeat pollution paper is
│   │   │   │                                        #   evidence for the second only. Names its own split seams rather than deferring them
│   │   │   ├── phase8_the_poller.md                 # A scheduled workflow reads an existing to-do bit and starts a child with no human
│   │   │   │                                        #   trigger. No new cue surface. Fires ONCE per cue, and a failed child neither
│   │   │   │                                        #   retries forever nor vanishes. GATED on Temporal schedules
│   │   │   └── research/                          # 2 papers + synthesis. synthesis.md is an operator+PM DECISION RECORD (15 concepts
│   │   │                                            #   with provenance), NOT a research artifact — it says so and forbids citing it as
│   │   │                                            #   evidence. cross_node_memory_protocol.md is SUPERSEDED by its own header
│   │   ├── planning-and-agents/                     # COMPLETE — distinct-lens principle; Cursor-port abandoned for skills
│   │   ├── safety-and-guardrails/                   # COMPLETE — two-layer model; block-dangerous.sh is load-bearing
│   │   ├── workflow-decomposition/                  # 🟡 IN PROGRESS per sprint.md — roadmap + research pool; three phases, Phase 1 shipped, Phases 2 and 3 open
│   │   │   ├── roadmap.md                         # Three phases; written after Phase 1 shipped, so that phase is a record not requirements
│   │   │   └── research/                          # 2 papers + synthesis, both PASS-WITH-FIXES; fork_vs_parameterize_drift_signal backs the Phase 2 ruling,
│   │   │                                            #   invocation_contract is the Phase 3 evidence base a planner decomposes from
│   │   ├── temporal-integration/                    # IN PROGRESS — Stage A built; why Temporal, and why Python was forced.
│   │   │                                            #   Holds the 5-paper reliability pool moved here 2026-08-19 when Fleet
│   │   │                                            #   Reliability dissolved — durable dispatch id, liveness, the three guards
│   │   └── reviews/                                 # CPI review reports (review-runs.sh output)
│   │       ├── review-2026-04-10.md
│   │       ├── review-2026-04-24-mdc-master-planning.md
│   │       ├── review-2026-04-24-skyy-command.md
│   │       ├── review-desired-state-sturdy-wheat-pelican-2026-07-24.md
│   │       ├── review-mdc-ansible-collections-2026-07-24.md
│   │       ├── review-mdc-master-planning-2026-05-03.md
│   │       ├── review-mdc-master-planning-2026-05-09.md
│   │       ├── review-mdc-master-planning-2026-07-24.md
│   │       ├── review-skyy-command-2026-05-03.md
│   │       ├── review-skyy-command-2026-05-09.md
│   │       └── review-skyy-command-2026-07-24.md
│   │
│   ├── guide/                                       # USER-FACING — operating manuals
│   │   ├── deployment.md                            # DEPLOYMENT — install/sync model, prerequisites, local + VM + Ansible paths, services, troubleshooting
│   │   ├── operations.md                            # THE OPERATING MANUAL — memory model orientation, daily loop, and the single roster of every workflow/agent/command/skill/rule
│   │   ├── memory-model.md                          # THE MEMORY FRAMEWORK — the working record as a substrate-free interface, this fleet's five-surface binding, per-field consumer lists, the addressing convention
│   │   ├── workflows.md                             # Workflow architecture — the build split, model management, escalation ladder, MAX_TURNS table
│   │   ├── cpi-cycle.md                             # CPI cycle methodology (run-decide-log loop)
│   │   ├── claude_code_agents.md                    # PLATFORM BACKGROUND — what agents are, frontmatter, tool guardrails, dispatch patterns
│   │   ├── claude_code_headless.md                  # PLATFORM BACKGROUND — headless mode, the three outcomes, completion contracts, worktrees
│   │   ├── claude_code_orchestration.md             # PLATFORM BACKGROUND — orchestration options and which one this repo landed on
│   │   ├── claude_code_rules.md                     # PLATFORM BACKGROUND — rules vs CLAUDE.md, when to split
│   │   └── claude_code_skills.md                    # PLATFORM BACKGROUND — skills and context-aware methodology
│   │
│   └── standards/                                   # HOW — binding rules for how things are done
│       ├── architecture/                            # WHY the system is shaped this way — high-level overviews, not numbered ADRs
│       │   ├── research/                            # PRODUCT-LEVEL research — findings that could change WHAT we build, not how
│       │   │   │                                    #   A WORKING SURFACE, not curated product — an autonomous run MAY append here;
│       │   │   │                                    #   only the `decision` and `status` RULINGS stay human-only (standards-governance.md)
│       │   │   ├── README.md                        # What this altitude is for, and how it differs from a phase's own research/
│       │   │   ├── topics.md                        # SIZE memory — tier, topic list with destinations, named gaps. Rewritten each run, never appended
│       │   │   ├── raw/<topic>.md                   # The pool — 8 mini-papers carried over from the CSCI-6905.604 research project
│       │   │   ├── candidates.md                    # THE DURABLE HOME for action candidates (C-NNN). `synthesis.md` is rewritten every cycle;
│       │   │   │                                    #   this is not. A producing run places its own proposal here with `decision` BLANK
│       │   │   ├── direction.md                     # The operator's inbox — rulings only they can make. Binds nothing
│       │   │   └── synthesis.md                     # What the pool means for the product's direction. NON-BINDING
│       │   ├── README.md                            # What goes here vs the rest of standards/
│       │   ├── problem-statement.md                 # THE WHY — the trade-off being closed, and that this repo is iteration 1 with coding as the first edge
│       │   ├── stack_reference.md                   # WHAT WE RUN ON — settled stack decisions and what was ruled out. SEEDED, not complete: absence here is not a decision
│       │   └── architectural_standard.md            # THE MAP — layers, composition, memory, the seams and their reasons. Concise by design; detail lives in standards/roadmap
│       ├── research/                                # VENDORED (MIRROR) from MDC-Master-Planning — do not edit
│       │   ├── README.md                            # The two altitudes — product vs phase — and what each unblocks
│       │   └── research_standard.md                 # Mini-paper contract, source floor, confidence marking, revalidation
│       ├── documentation/                           # VENDORED (MIRROR) from MDC-Master-Planning — do not edit
│       │   ├── README.md                            # Applicability: what binds here, what is sprint-specific and ignored, and the CLAUDE.md-references-never-contains principle
│       │   └── documentation_standard.md            # Cross-references over repetition, cite-don't-re-list, standards state the rule not completion-state
│       ├── temporal/                                # VENDORED (MIRROR) from MDC-Master-Planning — do not edit; re-vendor via scripts/helpers/
│       │   ├── README.md                            # Applicability: what binds NOW (§3, §3.4, §7) vs what waits for workers
│       │   ├── claude-dot-files-addendum.md         # The ONLY locally-editable file here — rules genuinely ours (long LLM activities, a producer that can be confidently wrong)
│       │   ├── stateful_patterns.md                 # Persistence boundaries, idempotent writes, cleanup on failure
│       │   ├── temporal_standard.md                 # Three-layer architecture, ACTIVITY_MAP, directory structure
│       │   └── worker_deployment_standard.md        # Worker segmentation, queue naming, immutable images
│       ├── testing/                                 # VENDORED (MIRROR) — do not edit; re-vendor via scripts/helpers/. The README is ours and IS editable
│       │   ├── README.md                            # Applicability: which half binds HERE, how this repo diverges, and § The gate — the six checks tests.yml runs
│       │   └── testing_standard.md                  # The three-tier hierarchy this repo's testing/ implements, positive controls, test resource safety
│       ├── agents.md                                # Standards for writing custom agents
│       ├── exit-protocol.md                         # DRAFT, NOT BINDING — the child→parent handoff contract. Phase 3 wrote its answers in:
│       │                                            #   §2 the envelope's three strata, §3 the split abstention vocabulary, §4 the nine
│       │                                            #   ordered rules, §5 versioning. Still carries ⟨PHASE 4⟩ in §6 and is UNRATIFIED
│       ├── finding-routing.md                       # WHERE A FINDING GOES — defect vs proposal, the four gates, who may write which surface.
│       │                                            #   §4 is why a producing run places its own proposals in candidates.md
│       ├── hook-scripts.md                          # Standards for hook scripts (stdin JSON + jq, never env vars)
│       ├── rules.md                                 # Standards for writing global rules
│       ├── services.md                              # Standards for long-running services/daemons
│       ├── skills.md                                # Standards for writing skills
│       ├── slash-commands.md                        # Standards for slash commands
│       └── workflow-scripts.md                      # Standards for autonomous workflow scripts
│
├── scripts/                                         # ORGANIZING AXIS: *who invokes it?* — that one question explains every folder below
│   ├── helpers/                                     # YOU run these. Pure bash, no AI, zero tokens
│   │                                                #   (helpers/ = standalone executables you RUN; workflows/lib/ = bash you SOURCE. Different things, hence two folders)
│   │   ├── init-project.sh                          # Initialize new project (git + GitHub + scaffolding)
│   │   ├── lint-prompts.sh                          # Ship-gate (3 passes): backtick scan, sandbox-CONSTRUCTS every multi-line prompt fragment, verifies every MODEL_KEY resolves in config.yaml
│   │   ├── check-settings.sh                        # Ship-gate: validates config/settings.json — malformed JSON, hook paths that don't resolve to an executable config/hooks/*.sh
│   │   ├── check_settings.py                        # The importable validation logic check-settings.sh runs; also imported directly by its pytest suite
│   │   ├── vendor-standards.sh                      # Re-copy vendored standards from MDC-Master-Planning with MIRROR provenance; --check fails on local drift
│   │   ├── measure/                                 # Replay tools over the fleet's OWN archived artifacts. Read-only, off every merge path
│   │   │   │                                        #   Kept (not one-shots) because each answers a rate over a denominator that GROWS — see its README
│   │   │   ├── README.md                            # What each tool answers and which open decision reads it
│   │   │   ├── replay_completion_predicate.py       # How often the completion grep misses a real terminal outcome → direction row D-007, MMF Phase 1 E5
│   │   │   ├── replay_convergence_predicate.py     # Replays the SHIPPED convergence predicate over the archive → MMF Phase 5 step 6.
│   │   │   │                                       #   IMPORTS the predicate (unlike its pinning sibling above) — a copy would validate a rule nobody runs.
│   │   │   │                                       #   Re-sorts the shared dump by comment time: the predicate forbids the `pass:` label (issue #68)
│   │   │   ├── judge_marginal_yield.py               # Disposition findings classified ECHOED by the producing run's own reflection vs NEW to the judge — the marginal-yield measure
│   │   │   ├── replay_pr_review_blocks.py           # Finding-set delta between review passes + stable-id conformance → MMF Phase 1 E7, Phase 5
│   │   │   ├── run_log.py                           # NOT a replay tool — the ONE DECLARATION of the RUN LOG surface (MMF Phase 6): its member
│   │   │   │                                        #   event types, join key, publish classification and per-figure cutovers. A test compares the
│   │   │   │                                        #   declared set against `_append_run_event`'s call sites in BOTH directions
│   │   │   ├── replay_run_resources.py              # Four figures over `run_resources`: the unmeasured rate, peak_anon by workflow, the
│   │   │   │                                        #   subagent-vs-volume knob question, and the overlapping-run aggregate → MMF Phase 6 req 3
│   │   │   ├── replay_convergence_events.py         # The LIVE convergence corpus (candidate C-059) + Phase 5's gate conditions 1 and 2 with their
│   │   │   │                                        #   denominators. `--archive` adds condition 1; without it that half prints NOT TAKEN, never zero
│   │   │   └── replay_parent_route.py               # The computed abstention arm grouped by reason, and the prose-shadow agreement rate with
│   │   │                                            #   C-060's conditioning printed beside it → MMF Phase 3 step 4, Phase 4's shadow-removal box
│   │   └── tests/                                   # TIER 3 — this unit owns its tests (Testing Standard § per-unit ownership)
│   │       ├── conftest.py                          # Component-scoped import path (puts scripts/helpers/ on sys.path)
│   │       └── unit/
│   │           ├── test_check_settings.py           # NC-A..K negative controls for check_settings.py's hook-path validator (Testing Standard § Mutation evidence)
│   │           ├── test_lint_prompts_claims_only_what_ran.py  # lint-prompts.sh may claim only the passes that RAN (#57): with yq stripped from PATH the
│   │           │                                    #   summary makes no MODEL_KEY claim, with yq it claims a NON-ZERO count, STRICT makes a skip fatal
│   │           │                                    #   (and every spelling of OFF turns it off), a scan over an empty tree is a failure rather than a
│   │           │                                    #   tick, and a prompt block whose closing ')' is never found fails instead of vanishing
│   │           ├── test_replay_completion_predicate.py  # Guards the anchoring and strict-vs-loose gap E5's zero depends on (3 mutation-proven)
│   │           ├── test_replay_convergence_predicate.py  # Guards both early-fire counters — the OBSERVED one (a later pass reopened work)
│   │           │                                    #   and the circular self-reported one — plus comment-order sequencing, the pass-1
│   │           │                                    #   denominator, and absent-vs-false `converged`
│   │           ├── test_measure_readme_names_a_consumer.py  # This directory's own version of Phase 6's finding: every tool ON DISK has a README row
│   │           │                                    #   (read off disk, since a table checked against itself cannot see the tool never added to it)
│   │           │                                    #   and every row names a consumer in `Read by`. run_log.py excluded BY NAME, exclusion asserted
│   │           ├── test_replay_pr_review_blocks.py  # Guards block extraction + absent-vs-false `converged`, which E7's cross-tab depends on (2 mutation-proven)
│   │           ├── test_run_log.py                  # The run-log surface's declaration: declared members == actual writers (both directions), the
│   │           │                                    #   publish check firing on a MODEL-AUTHORED VALUE not a key name — including a slug JOINED into
│   │           │                                    #   a sentence or TRUNCATED for a column — the derived model→workflow map finding the one
│   │           │                                    #   ambiguous pair, and the two subagent parsers agreeing over the live archive
│   │           ├── test_nullable_fields_are_not_truth_tested.py  # A NUMERIC nullable may never be truth-tested: `not x` folds an unmeasured record into a measured zero. Reads USAGE, not the declaration — the declaration gate could not see seven instances of it
│   │           └── test_run_log_readers.py          # The three readers' OUTPUT: exclusions named by commit cutover, figure 1's denominator being
│   │                                                #   EVERY record, high/oom labelled not-applicable per record, the overlap sweep bounded from
│   │                                                #   both sides and naming any window it cannot place, an unmeasured record not killing the
│   │                                                #   report, and condition 1 printing NOT TAKEN rather than a zero
│   │
│   ├── services/                                    # Long-running background services
│   │   ├── gh-monitor.sh                            # Polls GitHub for @claude PR comments, dispatches workflows
│   │   ├── gh-monitor.service                       # systemd oneshot unit
│   │   └── gh-monitor.timer                         # systemd timer (5-min interval)
│   │
│   └── workflows/                                   # YOU run these. They call Claude headless (Workflow 2: walk-away dispatches)
│       ├── activities/                              # ACTIVITY LAYER (Temporal Standard §3.3b) — external I/O, workflow-agnostic,
│       │   │                                        #   single responsibility, idempotent. A parent may NOT inline any of this:
│       │   │                                        #   a workflow that does network I/O cannot replay. Sourced, never dispatched.
│       │   ├── require-environment.sh               # Tool presence, --repo validation, git-root resolution, formatter check (replaced ~36 duplicated lines × 10 workflows)
│       │   ├── paper-currency.sh                    # Compute which research papers are past their revalidation window, and render it for a prompt
│       │   ├── run-claude.sh                        # Invoke `claude -p` (model resolution, rate-limit probe, turn-cap + completion contract) + print_cycle_totals
│       │   └── wait-for-ci.sh                       # Poll a PR's head SHA until checks settle; guards push→fetch replication lag
│       │
│       ├── common/                                  # Shared TYPES/CONTENT, not activities — no I/O, nothing executes
│       │   ├── config-value.sh                      # Read ONE value out of a config.yaml map. INVOKED, not sourced — no caller setup to inherit
│       │   ├── format-stream.sh                     # Stream-json → human-readable formatter (pure transformation)
│       │   └── shared-prompts.sh                    # DECISION_LOG_AND_REFLECTION (incl. fetch-verified deferral rules) + HEADLESS_EXECUTION_GUARD
│       │
│       ├── build.sh                                 # BUILD — PARENT: pure-bash orchestration of build-draft then build-refine (no model of its own)
│       ├── build-minor.sh                           # BUILD-MINOR — PARENT: draft → refine (ONE lens) → review-pr. Same shape as build.sh, lighter middle child
│       ├── build-phase.sh                           # BUILD-PHASE — implement from a plan doc
│       ├── plan-new.sh                              # PLAN-NEW — greenfield project definition
│       ├── plan-revision.sh                         # PLAN-REVISION — revise existing planning docs
│       ├── research.sh                              # RESEARCH — create/extend component research pool + synthesis (--pr updates an existing research PR)
│       ├── research-refresh.sh                      # RESEARCH-REFRESH — revalidate due papers (bash-side gate, clean no-op; --pr gates on that PR's pool)
│       ├── review-runs.sh                           # REVIEW-RUNS — CPI log analysis, produces reports
│       ├── review-sprint.sh                         # REVIEW-SPRINT — comprehensive whole-repo end-of-sprint review
│       ├── children/                                # Child WORKFLOWS — full workflows (own model key, turn budget, worktree) INVOKED BY A PARENT, not dispatched by you.
│                                                    #   Children are SHARED, not owned: review-pr is called by every PR-producing parent. Not namespaced per parent.
│           ├── review-pr.sh                         # Disposition engine (decide-only) — every parent's last child. Verdict: MERGE | HOLD - redispatch | HOLD - needs-assistance
│           ├── build-draft.sh                       # Child 1 of build.sh — writes the change, opens an UNREVIEWED PR; holds NO review authority
│           ├── build-refine.sh                      # Child 2 of build.sh — FRESH context: fidelity, peer-review trio + quality-control, corrections
│           ├── build-draft-minor.sh                 # Child 1 of build-minor.sh — writes the scoped change (was the monolithic build-minor.sh)
│           └── build-refine-minor.sh                # Child 2 of build-minor.sh — FRESH context: fidelity, ONE lens (code-reviewer), corrections
│       │
│       └── temporal/                                # V2 PORT: the bash workflows re-expressed in Python against the Temporal
│           │                                        #   three-layer architecture — {name}_workflow.py orchestration,
│           │                                        #   {name}_helper.py pure compiler, {name}_activities.py I/O wrappers
│           ├── modules/assistant/                   # The assistant edge — build/, plan/, research/, review_pr/ families + shared activities
│           │                                        #   prompts/ is the SHARED PROMPT POOL — a .md fragment lives there iff more than one
│           │                                        #   child loads it, by §10.1's promotion rule applied to prose. Deliberately rolled up
│           │                                        #   here: a fragment is reached through shared_prompt("<stem>"), never through this map
│           │                                        #   plan/ holds plan_project (parent) and its wired children in order:
│           │                                        #   triage_candidates rules the candidates, plan_feature writes ONE component's
│           │                                        #   roadmap.md and numbered phaseN_*.md docs from its research, plan_sprint places
│           │                                        #   what they ruled — split apart so planning could sit between the first and last.
│           │                                        #   plan_feature is the WRITE half only: it sizes NOTHING and writes no sprint.md.
│           │                                        #   plan_verify is the READ half and runs immediately after it, per component: it
│           │                                        #   reads the roadmap and phase docs COLD, puts an hour estimate on every phase in
│           │                                        #   roadmap.md and nowhere else, and answers the question plan_feature's own report
│           │                                        #   must ask and cannot answer — where is this plan weakest. plan-sprint does NOT
│           │                                        #   read those estimates yet; it never opens a roadmap.
│           │                                        #   Ahead of the research step sits plan-candidates — an ACTIVITY, not a child: no
│           │                                        #   prompt, no model, it scaffolds a research pool per shipped candidate and is what
│           │                                        #   gave the research step a reachable input again. Also
│           │                                        #   plan_revision (ported, NOT yet wired into plan_project — see its docstring);
│           │                                        #   plan_revision/prompts/*.md are GENERATED from plan-revision.sh, never retyped
│           │                                        #   plan_activities.py holds ONLY what more than one of them calls; a helper with
│           │                                        #   one consumer lives in that workflow's own {name}_activities.py, per §10.1
│           │                                        #   rule 3 — consumer count decides, never taste
│           │                                        #   exit_record.py — the typed exit record: THE ONE DECLARATION of its
│           │                                        #   schema and vocabulary, and the ten-rule fail-safe contract a parent routes on
│           │                                        #   (docs/standards/exit-protocol.md). Dependency-free, like its sibling routing.py
│           │                                        #   convergence.py — MMF Phase 5's computed stopping predicate: the open/closed
│           │                                        #   partition over `disposition`, six ordered rules, a named recorded residual arm.
│           │                                        #   RECORDED, ROUTED ON BY NOTHING — MAX_LOOPS is still the only stopping authority
│           │                                        #   research/ holds TWO parents over a shared set of children: research (full pool —
│           │                                        #   research_write, then verify, then review-pr) and research_minor (ONE paper —
│           │                                        #   research_write_minor, then THE SAME verify and review-pr). The minor cycle drops
│           │                                        #   topics.md, the sizing assessment, the fan-out and the synthesis; it drops NONE of
│           │                                        #   Research Standard §3's per-paper rigor, and research-critic still gates it
│           ├── modules/journal/                    # PMP Phase 1 — WHERE a run's record goes and WHAT SHAPE it takes. One BagIt bag per
│           │                                        #   run under a configurable root, keyed by run_id and never by path (Claude Code keys
│           │                                        #   by mangled path, which makes every worktree its own project — the one place this
│           │                                        #   design deliberately does not copy it). root.py resolves and REFUSES; bag.py holds
│           │                                        #   the layout, manifest, lifecycle and the one sanctioned mutation (redaction);
│           │                                        #   validate.py re-hashes and reports all three state fields; journal_activities.py is
│           │                                        #   the activity every entrypoint invokes before its first side effect. Nothing writes
│           │                                        #   a run's CONTENT here — that is Phase 3. Imports no workflow module, so a
│           │                                        #   measurement helper can load it without dragging in temporalio
│           ├── scripts/                             # Kickoff entrypoints, until an SDK path replaces them — plus preflight.py and
│           │                                        #   validate_bag.py, which are NOT kickoffs: they are the two things a run (or an
│           │                                        #   operator) checks before or after, and both live here because they are launch
│           │                                        #   concerns rather than workflow concerns
│           │                                        #   research.sh / research_minor.sh are SIBLING entries, not one script with a flag —
│           │                                        #   different parents, different model keys, separately-budgeted turn caps
│           │                                        #   triage_candidates.sh / plan_feature.sh / plan_verify.sh / plan_sprint.sh are
│           │                                        #   siblings for the same reason: four jobs, four model keys, four turn caps. Each
│           │                                        #   runs standalone AND is called in order by plan_project.sh. plan_feature.sh and
│           │                                        #   plan_verify.sh take ONE component directory, which is also the cheap way to
│           │                                        #   exercise them; plan_verify.sh REFUSES a component with no roadmap.md rather
│           │                                        #   than dispatching against nothing, since writing the plan is not its job
│           └── tests/                               # TIER 3 — this code unit owns its tests (Testing Standard § per-unit ownership)
│               ├── conftest.py                      # Component-scoped import path (replaces the per-file sys.path preambles), AND the
│               │                                    #   session-wide autouse fixture that points every bag the suite opens at a temp
│               │                                    #   root — the FIX for the defect test_the_suite_never_writes_… guards, since five
│               │                                    #   unit modules drive an entrypoint main() and were writing real bags into the
│               │                                    #   operator's ~/.local/state/. Redirects CONFIG_PATH, not XDG_STATE_HOME, so a
│               │                                    #   configured root cannot silently lose the protection
│               ├── integration/                    # Runs against what the FLEET actually wrote, not against a constructed fixture
│               │   └── test_a_real_bag_validates.py # Every journal bag on THIS machine still validates. Skips — distinctly from passing —
│               │                                    #   where no dispatch has run, because a clone has no journal and there is nothing
│               │                                    #   honest to assert about that. Does NOT substitute for the entrypoint sweep: a fleet
│               │                                    #   where ten of eleven had stopped opening bags would still pass on the eleventh's output
│               ├── fixtures/                        # MEASURED inputs, not written ones
│               │   └── schema_declared_run.jsonl    # A real CLI 2.1.224 run with --json-schema declared. Carries the finding that
│               │                                    #   declaring a schema REPLACES .result with the structured output — the reason
│               │                                    #   run-claude.sh's completion gate reads assistant text blocks instead
│               └── unit/                            # Each assertion encodes a production failure that happened. No mocks anywhere;
│                   │                                #   the few tests that touch the OS do so because the property IS an OS property
│                   ├── test_build_helper.py         # Verdict parsing + fail-safe routing, arg compilation, handoff validation
│                   ├── test_ci_gate.py               # The PARENT reads the CI verdict, so MERGE is unreachable on a red tree — a
│                   │                                #   prompt-level instruction is a convention an agent can reason past
│                   ├── test_preflight.py             # Preconditions fail BEFORE anything is created, so a dead run leaves no
│                   │                                #   orphaned worktree behind it (#49)
│                   ├── test_render_opaque_operator_text.py # An operator's literal `${...}` reaches the model UNSUBSTITUTED — two
│                   │                                #   dispatches died on briefs that were merely DESCRIBING the placeholder mechanism
│                   ├── test_resource_telemetry.py    # The numbers are real, THERE IS NO CEILING, and an unmeasured run stays countable.
│                   │                                #   Guards the mechanism added after the 2026-08-10 livelock, whose cause is UNIDENTIFIED
│                   ├── test_v1_parity.py            # Turn caps are DERIVED from V1, never re-declared; plus the ruff F821 executability sweep
│                   ├── test_isolation_invariants.py # Worktree isolation is unconditional; a negative outcome is observed, never asserted
│                   ├── test_gh_retries_only_what_is_transient.py # `gh` rides out a 5xx or a throttle and refuses everything else — the
│                   │                                #   terminal twin of every transient case, the write gate that stops a retried mutation,
│                   │                                #   and the hang that is TERMINAL because retrying it turns a ~6min ceiling into ~18min
│                   ├── test_every_subprocess_the_fleet_launches_is_bounded.py # No `subprocess.run` in the production tree may omit
│                   │                                #   `timeout=`; `Popen` is exempt only by NAME, for the two streaming child dispatches
│                   ├── test_a_gh_reply_is_checked_for_SHAPE_not_only_parsed.py # Every `gh`-reply `json.loads` asks what SHAPE came
│                   │                                #   back — a decode guard alone lets `{"message": …}` through to a `TypeError`
│                   ├── test_a_census_guard_proves_its_own_predicate.py # A guard that walks the tree must exercise its PREDICATE on a
│                   │                                #   literal too — a floored walk over an unconditional predicate is a permanent green
│                   ├── test_a_bounded_reply_is_CHECKED_not_only_read.py # A launch reply's stdout may not be read as evidence unless
│                   │                                #   its return code was; per-BINDING, so a name rebound and read later stays a finding
│                   ├── test_a_prose_COUNT_of_a_collection_is_DERIVED.py # Prose here that counts a collection the same module defines
│                   │                                #   must match it — the fourth corpus to gate the figure-nobody-derives class
│                   ├── test_prompt_completeness.py  # Every ${VAR} in a shipped prompt has a supplier (data-driven over prompts/*.md)
│                   ├── test_delegated_contract.py   # The five env vars run-claude.sh asserts at SOURCE time, and their ordering
│                   ├── prose_number_words.py        # English number-words for the two prose-figure sweeps, in a HELPER module for the
│                   │                                #   same reason as review_run_fakes below. Each sweep had its own copy and they had
│                   │                                #   ALREADY diverged (twelve vs fourteen), so a figure written "thirteen X" was
│                   │                                #   invisible to one of them — their own defect class, in their own supporting data
│                   ├── review_run_fakes.py          # The shared review_pr fakes, in a HELPER module rather than a test module — a test
│                   │                                #   module is collected, not imported, and the coupling it replaced broke at COLLECTION,
│                   │                                #   which mutate.sh reads as a caught mutation (issue #72)
│                   ├── test_convergence.py          # Every false-convergence mode with its check AND a control on the same input;
│                   │                                #   the partition gated against CHILD_SCHEMA's enum; the finding scan anchored to
│                   │                                #   `findings:` and parametrised over every free-text key the prompt defines; ONE site
│                   │                                #   for the positional block inference, gated on the shape across review_pr/; and a
│                   │                                #   class-scoped assertion that NOTHING branches on the signal — an AST walk over every
│                   │                                #   conditional across modules/ AND scripts/, not a text scan of one package
│                   ├── test_exit_record.py          # The fail-safe contract: every outcome x hold_kind cell, one test per absence mode,
│                   │                                #   the shadow comparison firing, the render↔record invariant bound to THIS pass,
│                   │                                #   and FOUR one-declaration gates: the schema, the working-record shared parse (enumerated,
│                   │                                #   with a completeness check), `parse_verdict` by AST scan of the whole tree, and
│                   │                                #   exit-protocol §4's reason column compared against the shipped enum
│                   ├── test_build_prompt_variants_do_not_fork.py  # A stage-prompt variant must not silently lose its sibling's discipline (PR #99)
│                   ├── test_prompt_blocks_are_shared_not_copied.py  # §10.1 for prose: a block in 2 children must be SHARED — frozen baseline that may only shrink
│                   ├── test_promoted_fragments_render_for_every_consumer.py # A promoted fragment must RENDER for every
│                   │                                #   consumer, not merely have a supplier somewhere in its source. The static check
│                   │                                #   cannot see WHICH ARM supplies a key, so this drives the real entry points at both
│                   │                                #   research altitudes and both build tiers, and asserts the two draft tiers render
│                   │                                #   byte-identically — the property the deleted file-identity assertion used to hold
│                   ├── test_promotion_guard_prose_figures_are_DERIVED.py # A promotion guard may not state a figure
│                   │                                #   about the corpus it polices. Five counts in these three modules were falsified by
│                   │                                #   the change that shipped them, so every `<N> <noun>` in their prose must be BOUND
│                   │                                #   to a deriver or DECLARED historical with a reason. Exemptions are matched in a
│                   │                                #   local window — a docstring-wide lookup excused every figure in the docstring
│                   ├── test_tier_siblings_do_not_DRIFT_by_a_sentence.py # The blind spot the block guard NAMES and
│                   │                                #   cannot see: a copy that drifted by one word stops being verbatim and vanishes
│                   │                                #   from its baseline. Scoped to a workflow and its own _minor tier, where the axis
│                   │                                #   of difference is documented, so a near-match is an accident by definition. THREE
│                   │                                #   detectors: a similarity ratio, a one-sided-append predicate for the class the
│                   │                                #   ratio is LEAST likely to see the larger the accidental append gets, and a
│                   │                                #   line-orphan pass that looks INSIDE the pairs the first two surface, for a whole
│                   │                                #   line present on one side and absent from the other. It
│                   │                                #   SURFACES and never rules — the frozen lists may shrink, and may grow only with a
│                   │                                #   note saying the difference is deliberate
│                   ├── test_exit_record_transport.py # The measured seam — runs the jq filters EXTRACTED from run-claude.sh against the
│                   │                                #   fixture: the assistant filter finds the completion signal, the .result filter
│                   │                                #   does not (the finding, as a control), and neither accepts a non-final verdict.
│                   │                                #   Plus the permission_denials surface both fleets share: it names every denied
│                   │                                #   tool, never prints the command line, and ROUTES NOTHING (asserted structurally)
│                   ├── test_shim_usage_names_itself.py   # Every shim's `#   ./x.sh` usage line must name the shim it is in. Four documented a different workflow; copy-pasting one ran the wrong one
│                   ├── test_run_log_emission.py     # The EMISSION side of the run-log surface: all three member events agree on `run_id`,
│                   │                                #   with a control on the DEFECT'S OWN SHAPE (a stem that ENDS with the nonce, so a
│                   │                                #   suffix-match looks right and joins nothing); `workflow_key` required with no
│                   │                                #   default; and an AST check that finish() gets the bare `run_id` — the only guard
│                   │                                #   that can see the defect where it lived, since exercising run_claude needs the CLI
│                   ├── test_pr_url_address.py       # The PR-URL address as ONE declaration, gated on the class not the sites: an AST
│                   │                                #   scan for a second `def`/`re.compile`, an exact-set gate on string splitting
│                   │                                #   (three ad-hoc parses used to live there), the completion ERE checked against the
│                   │                                #   extractor, and BOTH parity bounds for the owner/repo narrowing
│                   ├── test_docstrings_do_not_promise_a_raise.py # A documented return-contract may not list an outcome
│                   │                                #   the function cannot produce. Keyed on the TABLE form (two or more indented True/False/raises
│                   │                                #   lines), never the word — a naive sweep flags seven correct functions. And the raise must be
│                   │                                #   able to ESCAPE: wait_for_ci raises inside a try its own except catches, so any(ast.Raise)
│                   │                                #   called the shipped defect correct
│                   ├── test_no_prose_claims_a_shipped_workflow_is_UNBUILT.py # No prose says a workflow does not exist
│                   │                                #   when its module is on disk. plan-feature said it in three places — a docstring, a prompt sent
│                   │                                #   to the model, an operator banner — and the PR that built plan-verify falsified all three and
│                   │                                #   updated none. The shipped set is derived from the tree, so a workflow landing retires every
│                   │                                #   claim about it that day; historical phrasings are exempted deliberately
│                   ├── test_memory_guardrail.py     # The root conftest's RLIMIT_AS cap actually binds the running process
│                   ├── test_master_runner_results.py # run-all.sh reports per-suite results truthfully — no silent PASS over a skipped tier
│                   ├── test_model_gets_the_worktree_path.py # The path RENDERED to the model is the worktree-anchored one, not the
│                   │                                #   main-checkout path. Two runs (#84, #86) wrote their paper outside the
│                   │                                #   worktree; asserts over the workflows found on disk, not a fixed list
│                   ├── test_model_keys_resolve.py   # Every declared MODEL_KEY has a config.yaml entry — a missing one cannot launch at all
│                   ├── test_authorization_is_observed.py # Every `You MAY NOT` row in a planning prompt names the code that observes it, or
│                   │                                #   says JUDGEMENT with a reason. Keyed on the CORRESPONDENCE between the rendered table
│                   │                                #   and a declared map, so a reworded or added row goes red — the split had a mechanism
│                   │                                #   for two rows and prose for the rest, while the prompt said all were enforced.
│                   │                                #   Also the second half: every guard built on a `*_this_run_had_no_right_to`
│                   │                                #   comparator must render `id 'before'->'after'`, found by AST rather than listed
│                   ├── test_a_grant_follows_its_flag.py # A write grant on an operator-supplied path is DERIVED from that
│                   │                                #   path, never named. Discovered from the RUNNERS — any run_*.py declaring --candidates must
│                   │                                #   reach a workflow whose grant honours it — and asserted by CALLING each grant with a pool
│                   │                                #   that is not this repo's, because a workflow can carry the literal in a comment and still
│                   │                                #   derive correctly. Three of four hard-coded it, which failed a correct --repo run at the
│                   │                                #   LAST guard and presented as "the flag is broken"
│                   ├── test_no_runner_joins_an_UNRESOLVED_operator_path.py # No run_*.py joins an argparse namespace
│                   │                                #   attribute onto a path. Gives the ABSENCE of an escape check the syntax test_preflight's
│                   │                                #   sweep says it lacks, by keying on the join the absence exists to permit rather than on
│                   │                                #   the missing call. Five of ten runners were in that state and ALL FIVE accepted
│                   │                                #   ../../../../tmp/… and read through it under --dangerously-skip-permissions
│                   ├── test_runner_module_attributes_EXIST.py # Every `<alias>.<name>` a runner calls exists on the
│                                       #   module it aliases. #115 shipped `act.branch_of`, which lives only on
│                                       #   research_activities — 1831 tests passed and the first live run died
│                   ├── test_an_entrypoint_REFUSES_an_escaping_operator_path.py # The same property proven by EXECUTION:
│                   │                                #   every runner declaring a repo path is driven with an escaping value and must refuse it
│                   │                                #   AS AN ESCAPE, then with an in-repo value and must not. Both arms, because a resolver
│                   │                                #   hard-wired to raise passes the first. argv synthesized from the parser's own registry
│                   ├── observer_registry.py         # ONE resolver for both observer registries — does this entry name code that EXISTS?
│                   │                                #   A helper module, not a test module. Shared rather than copied for the reason
│                   │                                #   normalise_cell exists: two copies drift, and the weaker one silently stops catching
│                   ├── test_disappearance_is_observed.py # Every before/after snapshot names what watches it for ABSENCE. Keyed on the
│                   │                                #   SNAPSHOTS, found by AST, because absence-blindness is a property of taking one:
│                   │                                #   every comparator in the family judges `before.keys() & after.keys()`. Four channels
│                   │                                #   returned a green run — a deleted operator ruling, a deleted sprint plan, one renamed
│                   │                                #   out of the tree, an erased tick. Plus an AST gate binding every declared write grant
│                   │                                #   to a deletion check, so a grant added later is covered without anyone remembering
│                   ├── test_dry_run_previews_the_dispatched_prompt.py # No runner hand-assembles the prompt values a
│                   │                                #   workflow already assembles. An AST sweep over scripts/run_*.py: the second argument
│                   │                                #   to render() may not be a dict literal, nor a runner-local call one indirection down.
│                   │                                #   Keyed on the SHAPE because the instance was fixed twice and reproduced twice — a
│                   │                                #   preview that is wrong looks exactly like one that is right
│                   ├── test_loop_cap_prose_is_counted.py # No workflow ASSERTS a loop cap; it reads one, or its own MAX_LOOPS really is 1.
│                   │                                #   An AST census over string literals, with the enclosing conditional's test inspected
│                   │                                #   so a counted sentence passes and a flat one does not. Five sites drifted when
│                   │                                #   routing.MAX_LOOPS went 1 -> 3, two of them inside a prompt sent to a model
│                   ├── test_plan_candidates.py      # plan-candidates, the one step in the planning chain that is an ACTIVITY and not a
│                   │                                #   child: no prompt, no model, so it is testable in-process at no dispatch cost.
│                   │                                #   The four skips (not `ship`, not `open`, blank component, directory already
│                   │                                #   exists), the resume path for a pool this pipeline seeded and abandoned, and
│                   │                                #   what the seeded synthesis has to carry. The shape check is parametrized over
│                   │                                #   the CLASS — every way a row is read wrongly or lost — against a nine-table
│                   │                                #   fixture, because a one-table fixture cannot see a whole-file check's scope
│                   │                                #   error, and one case reads the REAL candidates.md. The doors are NOT listed
│                   │                                #   here, and no longer in one docstring either — each lives in the helper that
│                   │                                #   raises for it, since the one list said "three ways" over four cases
│                   ├── test_plan_project_loop.py     # The plan parent's bounded loop-back and its fail-safe-to-human routing, plus the
│                   │                                #   Step 1b wiring that gave the research step a reachable input again, and that
│                   │                                #   every field of `Scaffolded` reaches the operator as its own note
│                   ├── test_plan_feature.py         # plan-feature's four OWN prohibitions, each exercised where the guards disagree: a
│                   │                                #   renumber is caught as a DELETION (both name guards pass it), a gap is not a free
│                   │                                #   number, and the hour detector does NOT fire on the three prose uses of "hours"
│                   │                                #   that exist in this tree — a word-keyed pattern would fail three correct runs
│                   ├── test_plan_verify.py          # plan-verify's own guards, and the TWO properties unique to a judge: the deliverable
│                   │                                #   is keyed on STATE where every prohibition in the family is a DELTA — a --pr
│                   │                                #   correction pass legitimately writes no new hours — and the re-planning guard
│                   │                                #   watches the INSIDE of the one file the grant opens, where boundary_crossings is
│                   │                                #   blind by construction. Its ordering fixture FOUND a defect: a dropped phase used
│                   │                                #   to raise UNSIZED, whose suggested remedy leaves the phase dropped
│                   ├── test_plan_revision.py        # plan-revision's pure logic (context block, completion URL) and its CLI contract
│                   ├── test_triage_candidates_split.py # The triage/sprint seam: the `decision` column moved to triage-candidates and
│                   │                                #   plan-sprint FAILS THE RUN if it wrote one. Six mutation shapes — four offend,
│                   │                                #   two are entitled (markup reformat, a proposal placed unruled), and that second
│                   │                                #   exemption is a defect the control found: diffing the id maps outright made the
│                   │                                #   SHARED placement instruction unfollowable
│                   ├── test_plan_revision_v1_parity.py # Prompt text is BYTE-IDENTICAL to plan-revision.sh — re-extracts the heredocs
│                   │                                #   and both PROMPT= strings on every run. Catches the ~935-byte drop-the-stages failure
│                   ├── test_research_minor.py       # The minor cycle is genuinely SMALLER, and a subtraction is what a green suite cannot
│                   │                                #   notice: every "absent from write_minor.md" check is paired with the SAME predicate
│                   │                                #   run against research_write's live prompt, which must still fire. Plus the reused
│                   │                                #   verify child's cycle-shape block RENDERED (not grepped) and an AST walk proving its
│                   │                                #   parameter reaches no `if` — a value, never a behavioural branch inside a prompt.
│                   │                                #   BOTH children's prompts are rendered end-to-end: the prompt FILE is not the prompt,
│                   │                                #   and ${CONTEXT_BLOCK} is assembled from pieces write_minor.md does not author — which
│                   │                                #   is exactly where the one live defect this suite's file-only checks all missed lived
│                   ├── test_runner_discovery.py     # An orphaned test file FAILS the suite — runs python.sh against a synthetic tree
│                   ├── test_turn_cap_banner_reads_the_worktree.py # The turn-cap banner reports the tree it can SEE (#65): worktree_delivery_state
│                   │                                #   extracted from run-claude.sh and run against real git fixtures under BOTH caller shapes —
│                   │                                #   V1's `set -euo pipefail` and the V2 fleet's optionless `bash -c` — covering dirty, unpushed,
│                   │                                #   fully-pushed, gone, no-upstream, detached, leftover-dir, and a FAILED push check (git shim)
│                   ├── test_testpaths_matches_discovery.py # pytest.ini's testpaths lists every tests/ dir python.sh discovers — a bare
│                   │                                    #   `pytest` must never silently cover less than run-all.sh while both report green
│                   ├── test_journal_root.py         # The journal root's resolution contract (PMP Phase 1 r1/r9): the config value wins,
│                   │                                #   each deployment shape's default is DOCUMENTED not silent, and resolution REFUSES a
│                   │                                #   non-0700 root, a foreign owner, a symlink, or a path inside any git working tree —
│                   │                                #   the last because a run can redirect the journal into its own worktree and push it.
│                   │                                #   Every refusal asserted on its MESSAGE: once the journal is mandatory, the refusal is
│                   │                                #   all an operator has instead of a working journal
│                   ├── test_journal_bag.py          # The bag's layout and the four writes that change it. bagit.txt is EXACTLY two lines
│                   │                                #   (RFC 8493 §2.1.1), which is why the schema version lives in bag-info.txt; reopening a
│                   │                                #   SEALED bag is refused; a redaction replaces, tombstones AND reseals, because any one
│                   │                                #   alone leaves the bag dishonest. The Oxum check came from RUNNING a redaction — the
│                   │                                #   first seal looked correct and the third reseal had three of a single-valued label
│                   ├── test_journal_validator.py    # r8's contract is the ALWAYS, not the fields: every report carries lifecycle AND redacted
│                   │                                #   AND incomplete. The 2x2x2 is enumerated rather than sampled, including sealed+redacted+
│                   │                                #   incomplete, because a validator checked at three of eight points looks exactly as green
│                   │                                #   as one checked at all eight. Missing / mismatched / unlisted are distinguished — the
│                   │                                #   third is a write landing AFTER the seal, invisible to any check that walks the manifest
│                   ├── test_the_suite_never_writes_to_the_operators_journal.py # Making bag-open structural made the SUITE a
│                   │                                #   dispatch: five modules drive an entrypoint main(), so every pytest run left a real bag
│                   │                                #   under ~/.local/state — 24 in one day, and the integration tier was grading them as
│                   │                                #   dispatch output. Keyed on the observable (did the real root change), not the mechanism
│                   ├── test_journal_containment.py  # The CLASS behind four escapes found one-per-review-pass: an externally-supplied string
│                   │                                #   composed onto a trusted base. Sweeps every `base / x` in the package and refuses one
│                   │                                #   whose right operand is neither a module constant nor contained_relpath() unless a row
│                   │                                #   declares why it cannot escape — so ADDING a join is what fails. The battery beside it
│                   │                                #   catches the other half: reverting a guard leaves the declaration standing and true of
│                   │                                #   nothing, which the sweep alone reads as covered
│                   ├── journal_entrypoint_facts.py  # WHAT THE ENTRYPOINT POPULATION IS, discovered once for the two guards that ask —
│                   │                                #   the bag-open sweep and the prose-figure guard. A helper and not a shared test
│                   │                                #   module because test_test_tree_hygiene refuses test-imports-test; two copies of
│                   │                                #   "which files are the parents" is the defect both guards exist to catch
│                   ├── conftest.py                  # The unit tier's shared `root` fixture — a journal root at DIR_MODE. Was pasted into
│                   │                                #   three modules with a fourth copy already drifted to a bare 0o700 literal. Here and
│                   │                                #   not in tests/conftest.py: the integration tier deliberately reads the REAL root
│                   ├── test_journal_prose_figures_are_DERIVED.py # THE RESTATED-FIGURE CLASS, which four passes closed one instance at a
│                   │                                #   time — "six of eleven" when it was five, "eight entrypoints" when only seven have a
│                   │                                #   --dry-run, "open/sealed/pruned" for a state named `redacted`. Sweeps this package's
│                   │                                #   prose (modules, its tests, the phase doc, this map) for entrypoint-population
│                   │                                #   figures; each must be BOUND to a deriver that computes it from the tree or DECLARED
│                   │                                #   with why none can — a historical or hypothetical count has no ground truth. An
│                   │                                #   unregistered figure FAILS, which is the part that converges. States what it cannot
│                   │                                #   see: "over half", a file outside the sweep, and any CAUSAL claim at all
│                   ├── test_journal_concurrent_writers.py # Sixteen threads released by a barrier all ask for ONE writer name and must get
│                   │                                #   sixteen directories. Sequential calls would pass against a check-then-create, which is
│                   │                                #   the implementation r3 exists to rule out. States its own limits: no global ORDER, no
│                   │                                #   reach into an external store, threads not processes
│                   ├── test_journal_state_is_derived_in_one_place.py # r8's three fields were derived TWICE — Bag and validate_bag
│                   │                                #   each turned a bag-info label into a flag and a manifest's existence into `sealed`.
│                   │                                #   Both copies were correct, which is why four review passes missed it: nothing breaks
│                   │                                #   until one side is edited, and then Bag.incomplete disagrees with BagReport.incomplete
│                   │                                #   about one bag — r8's collapse arriving by drift. Two AST sweeps over the package AND
│                   │                                #   every non-test module that IMPORTS it — resolved from the AST, so all eight spellings
│                   │                                #   of that import are in scope and Phase 3's emitters cannot be outside it by accident.
│                   │                                #   Two derivations swept: comparing a state label (through containers, so `in` counts),
│                   │                                #   and minting an `open`/`sealed` literal. Consumption stays legal or every caller is an
│                   │                                #   offender; a comparison that is genuinely not a derivation is DECLARED with its reason,
│                   │                                #   as test_journal_containment.py does for joins. Carries its own negative controls,
│                   │                                #   including the set-membership check that catches a sweep passing over nothing
│                   └── test_every_parent_opens_a_run_bag.py # r11's actual guarantee — the activity boundary does not make the call happen,
│                                                    #   and r9 cannot help because it fires only AFTER bag-open was invoked. Discovers every
│                                                    #   run_*.py by AST (never substring: the sibling sweep shipped green because a COMMENT
│                                                    #   satisfied its grep) and fails when one starts a run with no bag. Also checks ORDER —
│                                                    #   the bag precedes the first side effect — with a self-contained non-conforming fixture
│                                                    #   for both, and names the swept scope in its own failure message
│
├── testing/                                         # TIER 1 + TIER 2 of the Testing Standard's three-tier hierarchy
│   ├── README.md                                    # How to run the suite and how to add a test
│   ├── check-policy.yaml                            # Which CI checks GATE a merge and which are advisory. Read by the build parent
│   │                                                #   between refine and review-pr — the fact lives here because it is a fact about
│   │                                                #   THIS repo, and a generic parent holding it would silently give the next repo no gate
│   ├── run-all.sh                                   # Master runner — the single "run everything" entry point.
│   │                                                #   ./testing/run-all.sh [unit|integration|e2e] [component]
│   ├── suites/
│   │   └── python.sh                                # pytest suite runner. pytest is the ONLY adopted framework here, so it is the
│   │                                                #   only runner — a bash/bats runner gets added with the first .bats file, not before.
│   │                                                #   Refuses to run at all while a test_*.py sits outside tests/{unit,integration,e2e}/
│   ├── scripts/
│   │   ├── mutate.sh                                # Mutation harness — falsify a guard, show red, restore, show green. Gives every leg
│   │   │                                            #   AND every import probe its own PYTHONPYCACHEPREFIX; a length-preserving edit would
│   │   │                                            #   otherwise run stale .pyc. Exit 2 is ambiguous and resolved DIFFERENTIALLY — the file
│   │   │                                            #   is probed before and after, so only a mutation-caused import break is a harness error.
│   │   │                                            #   Its OWN emitted codes keep the same separation, and ENFORCE it: every termination the
│   │   │                                            #   script did not deliberately choose is classified as a harness error by its EXIT trap,
│   │   │                                            #   so a bare command aborting under set -e cannot return a verdict code. The codes
│   │   │                                            #   themselves are stated ONCE, in the file above classify_leg — not restated here
│   │   └── tests/unit/                              # TIER 3 for the harness AND the repo-wide test-tree properties that only matter
│   │                                                #   because of it — a collection failure exits 2, which pytest reports identically
│   │                                                #   whether the guard fired or nothing ran at all (issue #72)
│   │       ├── gfm_table_scan.py                    # NOT a test module — where a GFM table IS *and what is wrong with it*, owned
│   │       │                                        #   once and asked by both markdown gates below. Every boundary in it is
│   │       │                                        #   measured through GitHub's `POST /markdown` rather than read off
│   │       │                                        #   CommonMark, because the two disagree in both directions (`2.` interrupts
│   │       │                                        #   a table, `<span>` does not). Carries the four DERIVED checks too, not
│   │       │                                        #   just the primitives: an extraction that stopped at the primitives left
│   │       │                                        #   each caller re-typing the loops, and three reviewers filed four findings
│   │       │                                        #   that were that one omission wearing four hats — a duplicated loop, two
│   │       │                                        #   dead imports, an undisclosed fence asymmetry, a missing vacuity floor
│   │       ├── test_mutate.py                       # The harness's own guards
│   │       ├── test_test_tree_hygiene.py            # No test_*.py imports another test_*.py — anywhere in the repo, including a pair
│   │                                                #   nobody has written yet. The coupling breaks at collection, which mutate.sh cannot read.
│   │                                                #   TWO checks since PR #96: the import-statement one, and a second reading CALL ARGUMENTS,
│   │                                                #   because a path LOAD emits no ast.Import node and slipped past the first one unseen
│   │       ├── test_measurement_figures_are_cited.py # A figure with a denominator lives ONLY where the measurement is taken; every other
│   │       │                                        #   mention cites it. Keyed on the class after fix-the-instances was MEASURED
│   │       │                                        #   non-convergent over four passes on one PR — see candidates.md C-050
│   │       ├── test_file_structure_map_covers_the_tree.py # THIS map, checked against `git ls-files`: where it enumerates a directory it
│   │       │                                        #   enumerates it completely, and every tracked file is reachable through some
│   │       │                                        #   entry. Paths are reconstructed from the indentation — leaf-name matching scored
│   │       │                                        #   unrelated directories as enumerated and demanded the roll-ups be expanded
│   │       ├── test_the_on_disk_footprint_is_documented.py # State written OUTSIDE any checkout must be named where an OPERATOR reads
│   │       │                                        #   — CLAUDE.md, README.md, docs/guide/. Keyed on the journal's DEPLOYMENT_SHAPES
│   │       │                                        #   rather than a list of paths, so a fourth shape fails until the docs say where
│   │       │                                        #   the bytes land. A planning doc deliberately does not count: that is where the
│   │       │                                        #   journal root already was when review-pr found it undocumented
│   │       ├── test_candidate_ids_are_unique.py     # A `C-NNN` addresses ONE proposal. Three dispatches collided on the id space
│   │       │                                        #   within forty minutes on 2026-08-11 and nothing would have said so
│   │       ├── test_candidates_prose_matches_the_table.py # § Where things stand declares totals, an id range, a gap list and a
│   │       │                                        #   decision split ABOUT the table above it. Derived here, because the
│   │       │                                        #   paragraph was hand-written three times and wrong twice — the third time
│   │       │                                        #   in a MERGE, where a dropped row leaves no diff for anyone to read. Also
│   │       │                                        #   that every row RENDERS its flags the way § Two flags declares them, the
│   │       │                                        #   vocabulary read off that section rather than hard-coded here. Also
│   │       │                                        #   that every row splits into the header's CELL COUNT — an unescaped
│   │       │                                        #   `|` inside a code span truncated three rows on GitHub while every
│   │       │                                        #   gate stayed green, because the source was read and never the render
│   │       ├── test_markdown_tables_render_whole.py # The same cell-count property as the sibling above, over EVERY tracked
│   │       │                                        #   `.md` rather than one file — the defect is not file-shaped. Six
│   │       │                                        #   instances in three files were closed one at a time across four review
│   │       │                                        #   passes of one PR, and the sixth was live when the fifth was called
│   │       │                                        #   fixed. Takes its scanner from gfm_table_scan.py rather than copying it
│   │       │                                        #   — a first draft path-LOADED the sibling test module, which is the
│   │       │                                        #   coupling test_test_tree_hygiene.py forbids and did not then catch.
│   │       │                                        #   Fenced code blocks are excluded because GFM renders no table inside a
│   │       │                                        #   fence, so a finding there is a false positive by construction.
│   │       │                                        #   THREE shapes, not one: a row that misses its header's count, a non-row
│   │       │                                        #   line INSIDE a block, and a row SEVERED from one. The third uses a weaker
│   │       │                                        #   bar than the sibling's — three tracked files carry a deliberate one-line
│   │       │                                        #   row-shape illustration, and the strict bar fails all three
│   │       ├── test_prompt_budgets.py               # Every prompt carries a byte budget, so ADDING to one is a trade
│   │       │                                        #   rather than free. Prompts grew 224->317 KB in seven days under
│   │       │                                        #   review — adding is visible work, removing is nobody's job
│   │       ├── test_relative_links_resolve.py       # Every relative markdown link in a file this repo OWNS points at a
│   │       │                                        #   file that exists. 29 were broken and none had been caught; every
│   │       │                                        #   one was a `../` count off by one or two. Vendored standards,
│   │       │                                        #   research citations and skills' examples are excluded by name
│   │       ├── test_no_conflict_markers.py          # No git conflict marker reaches a commit — one did, and passed all four CI
│   │       │                                        #   checks, because every existing check was aimed at code rather than prose
│   │       ├── test_no_live_surface_advertises_a_superseded_contract.py # A guide row, a CLI string or a
│   │       │                                        #   prompt still describing a contract the code replaced. The largest
│   │       │                                        #   finding class measured — 33 across 12 PRs. PR #105 changed what
│   │       │                                        #   research-minor IS and left four surfaces stating the old one
│   │       └── test_retired_vocabulary_is_gone_from_live_surfaces.py # The retired Kind 1/2/3 labels survive only
│   │                                                #   where they are a RECORD. Reads every
│   │                                                #   tracked file as WHITESPACE-NORMALISED text — a different instrument than the
│   │                                                #   line-based grep that did the sweep and then verified it, which shared its every
│   │                                                #   blind spot. Four passes, five spellings: spaced, wrapped across a newline,
│   │                                                #   UPPERCASE, hyphenated, and the number as a word inside an identifier. The
│   │                                                #   record set is declared here ONCE; file_structure.txt is scoped by LINE, since
│   │                                                #   it annotates live and retired surfaces in one file
│   ├── config-hooks/                                # TIER 3 for config/hooks/ — the ONE component whose tests do not sit beside its code,
│   │   │                                            #   because install.sh symlinks config/hooks wholesale into ~/.claude/hooks. See its README
│   │   ├── README.md                                # The placement rationale, and why the runner was not changed to accommodate it
│   │   └── tests/unit/
│   │       ├── test_block_dangerous.py              # SPECIFICATION suite for the PreToolUse safety hook (issues #52, #61 — was
│   │                                                #   characterization until the operator ruled on the four defects it found). Attack corpus +
│   │                                                #   safe-lookalike corpus + the threat model's own gaps encoded as accepted behaviour.
│   │                                                #   Executes the MUST BLOCK / MUST ALLOW claims written beside each pattern IN THE HOOK, and
│   │                                                #   sweeps the separator class at BOTH positions: a shell separator appended to the end of
│   │                                                #   every dangerous command, and every internal separator respelt (tab, doubled space, the
│   │                                                #   space after `>` removed, a word split over a `\`-newline). A pattern added without a deny
│   │                                                #   case or without both claims FAILS here; so does one that enumerates a separator.
│   │                                                #   Also pins the SCRATCH-DELETE ELISION boundary from both sides — the commands measured
│   │                                                #   halting completed runs must pass, and the shapes that read like them must still deny — plus
│   │                                                #   a third sweep placing every dangerous command beside an elidable scratch delete, because
│   │                                                #   the elision MUTATES the string the patterns see and a hand-picked corpus cannot bound that.
│   │                                                #   Asserts every MUST BLOCK claim end-to-end, not only against its own regex. NO COUNTS ARE
│   │                                                #   RESTATED HERE — they live in the test module's docstring, per its sibling README
│   │       ├── test_hook_settings.py                # The SECOND guarantee about hooks, and deliberately a different shape
│   │                                                #   from the one above: that suite proves the hook decides CORRECTLY, this
│   │                                                #   one proves settings.json BOUNDS it. Asserts every configured hook
│   │                                                #   declares a `timeout`, and that none is so large it is not a bound.
│   │                                                #   Neither claim subsumes the other — a latency test cannot cover an
│   │                                                #   input nobody thought of, and a declared timeout cannot tell you a
│   │                                                #   hook got slower. Written after an unbounded PreToolUse hook ran 8m44s
│   │                                                #   at 99.9% CPU on ONE tool call and had to be killed by hand, holding a
│   │                                                #   live build (2026-08-14).
│   │       └── test_the_safety_hook_is_wired.py     # The hook is the ONLY control once the deny list and permissions are gone, so this
│   │                                                #   asks whether it is ever CONSULTED — not whether it decides right, which is its
│   │                                                #   sibling suite's question. Holds the THREE breakage shapes `workflow-scripts.md`
│   │                                                #   § The safety-layer invariant names, one test each: DECLARED on PreToolUse/Bash;
│   │                                                #   RESOLVES, with BOTH ends of install.sh's mapping read out of install.sh instead
│   │                                                #   of hardcoded; declared in the file install.sh puts at USER scope; and no runner
│   │                                                #   STRIPS --setting-sources. That last sweep's population is not filtered by
│   │                                                #   extension — an anchor test derives every claude dispatcher from `git ls-files`
│   │                                                #   and asserts the sweep contains it, after a Python-only glob left the one shell
│   │                                                #   runner it exists to watch unwatched (2026-08-16)
│   └── logs/                                        # Per-suite run output (gitignored — ephemeral)
│
├── .github/
│   └── workflows/
│       └── tests.yml                                # TIER ENFORCEMENT — the merge-path gate. `main` is branch-protected on its `suite` job.
│                                                    #   Runs the master runner, then the checks that exist in this repo but would otherwise
│                                                    #   be invoked by convention only: ruff F821, lint-prompts.sh, settings.json validity,
│                                                    #   bash -n, shellcheck --severity=error. Deliberately UNFILTERED (no `paths:`).
│                                                    #   vendor-standards.sh --check is the one control still off it — see issue #55.
│
├── .gitignore                                       # Git ignore rules
├── CLAUDE.md                                        # Project instructions (read by Claude Code when working in this repo)
├── config.yaml                                      # Centralized service/workflow configuration (gh-monitor, per-workflow models map, journal root + deployment shape)
├── conftest.py                                      # Repo-root pytest config — RLIMIT_AS memory guardrail (Testing Standard § Test Resource Safety, binding)
├── install.sh                                       # Creates symlinks from config/* into ~/.claude/
├── LICENSE                                          # MIT license
├── pytest.ini                                       # Pins pytest rootdir here, which is what guarantees conftest.py loads on every invocation
└── README.md                                        # Repo documentation

---

Symlink strategy (created by install.sh):

  config/settings.json  ──>  ~/.claude/settings.json
  config/CLAUDE.md      ──>  ~/.claude/CLAUDE.md
  config/agents/        ──>  ~/.claude/agents/
  config/commands/      ──>  ~/.claude/commands/
  config/hooks/         ──>  ~/.claude/hooks/
  config/rules/         ──>  ~/.claude/rules/
  config/skills/        ──>  ~/.claude/skills/

Machine-local (NOT synced):
  ~/.claude/.credentials.json    auth tokens
  ~/.claude/projects/            path-keyed project state
  ~/.claude/history.jsonl        session history
  ~/.claude/sessions/            session data
  ~/.claude/cache/               ephemeral cache
  ~/.claude/backups/             local backups (install.sh creates pre-install-<ts>/)
  ~/.claude/downloads/           temp downloads
  ~/.claude/file-history/        edit history
  ~/.claude/ide/                 IDE state
  ~/.claude/plans/               session plans
  ~/.claude/plugins/             managed by Claude Code
  ~/.claude/session-env/         session state
  ~/.claude/shell-snapshots/     shell state
  ~/.claude/telemetry/           telemetry data
