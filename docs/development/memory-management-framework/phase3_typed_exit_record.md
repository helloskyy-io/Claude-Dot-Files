# Phase 3 — The typed exit record: schema, fail-safe contract, one pair proven

**Component:** [Memory Management Framework](roadmap.md) · **Status: ✅ COMPLETE — BUILT AND PROVEN ON ONE PAIR (2026-08-10)**

> **All eight requirements are met and verifiable from the artifacts.** Requirement 6's second half — *both paths asserted to agree across a run set* — closed on **2026-08-10** with the [run set](#step-7--the-run-set-nine-live-dispatches-and-the-three-numbers-it-yields) recorded below: **nine live `review-pr` dispatches against four real PRs at real budget**, on merged Phase 3 code, every one a byproduct of ordinary work rather than staged for this measurement — so the measuring-an-artifact-you-are-inside-of defect [Phase 1](phase1_measure_the_channel.md) recorded three times does not apply. The comparator fired on eight of the nine and **raised twice**; every disagreement is causally explained from the logs, and **0 of 9 is attributable to the record, the schema or the transport**.
>
> **The ninth dispatch is the one worth reading for.** It emitted a valid typed record and no prose verdict, so the completion gate raised before the comparator ran and the pair was never compared. **The instrument can only observe runs the incumbent channel did not break** — a limit no larger N removes. It is stated with the numbers rather than under them, and placed as **C-060**.

The design phase, and the one that proves it works on exactly **one** parent/child pair with the incumbent prose channel still in place. Rolling it across the fleet is [Phase 4](phase4_fleet_migration.md) — deliberately separate, because a phase that both designs and migrates cannot tell you which of the two it got wrong.

The shape is settled by evidence and recorded in the [roadmap's Key Decisions](roadmap.md#key-decisions): **arrangement A** — the child writes a small typed record at exit to a channel the parent owns, and the human record is rendered from it. This phase specifies it, builds it once, and proves it — **including the channel's own properties**, which are not properties of the record's format and are where the incumbent's real protections live.

---

## Requirements for completion

Done when **all** of the following hold:

1. **The envelope is written down as its own contract** — the field list from [Phase 1](phase1_measure_the_channel.md) E6, each field with its named consumer and its **publish classification**, and the explicit statement that every field the parent does not read is not load-bearing.
2. **The record's authorship is ruled on** — model-authored under prompt instruction, or harness-derived from what the model wrote. These are different designs and only one of them is arrangement A.
3. **The abstention member is split in two** — a computed *could-not-check* arm and an asserted *needs-a-ruling* arm, each with its own emitter, reliability claim, and remedy.
4. **The fail-safe contract is total across all four absence modes** — absent, unparseable, **stale**, and **unknown `schema_version`** — each routing to the arm requiring a human, and each proven by its own test.
5. **The channel's properties are specified, not just the record's** — fresh per invocation, run-identity-bound, anchored outside the worktree.
6. **One parent routes on the typed record end-to-end**, with the prose VERDICT line still emitted, and both paths asserted to agree across a run set. Disagreement is a **loud failure** during this phase, not a preference.
7. **The disposition table is rendered from the record** (or reconciled to it by a write-time invariant), the archived copy carries only the publishable subset, and `/standup` is verified against the result.
8. **Three rulings are recorded:** the disagreement policy, the to-do-bit ownership, and the cross-language schema ownership.

**Out of scope, and explicitly so:** other children, other parents, retiring the prose channel. `build-minor.sh` is a **second** bash parent that greps the verdict (`:281`) — it is deliberately not migrated here, and [Phase 4](phase4_fleet_migration.md) owns it. Naming it prevents "one parent" from quietly meaning "the only parent."

---

## Dependencies

- **[Phase 1](phase1_measure_the_channel.md) — hard, but per-requirement rather than wholesale.** E6 supplies the field list; E1 supplies the transport ruling and decides whether the `is_error` composition rule has anything to compose; E2 decides whether the contract must defend against a *partial* record. **Only requirements 8's disagreement policy and to-do-bit ruling block on E3** — if E3 turns out to need prospective instrumentation, the rest of this phase proceeds and those two wait.
- **[Phase 2](phase2_kind1_framework.md) — hard.** Its enumeration of what the human record carries as prose is what tells this phase which fields the schema must model and which would be lost; its consumer list and retrievability convention are what requirement 7 is verified against.
- **Cites but does not re-derive:** `claude_code_integration_surface.md` §5 and §7. **Due 2026-08-22** — if this phase runs after that, state the staleness rather than relying on it silently.
- **Constrains:** [Temporal Integration](../temporal-integration/temporal-integration.md). The handoff shape stops moving at [Phase 4](phase4_fleet_migration.md), not here.

---

## §Runtime Verification

Adopted practice, not a binding local rule — see the [roadmap's note on doc shape](roadmap.md). Required in substance because this phase writes to and reads from the `claude` CLI's output surface.

- [x] Re-run the verification block and record the result here, with the date and host. **Verified 2026-08-09 on `puma-workstation-mint`, CLI 2.1.224 — not cited from Phase 1.** Three probes in the real child shape (`--model haiku --output-format stream-json --verbose --dangerously-skip-permissions --json-schema '<inline>'`), and the version happens to be unchanged from Phase 1's pin, which is a fact this pass measured rather than assumed
- [x] Record the CLI version this phase's design is built against — **2.1.224**. **And a correction to the checklist's own framing, because it asks for something the design does not have:** `schema_version` is **not** pinned to a CLI version and must not be. It versions the *record's shape*, which this repo owns; the CLI version governs the *transport*, which it does not. Tying them would bump the record's version on every CLI patch — and Phase 1 measured four patch versions adding ten envelope fields — routing every skewed worktree to a human for a change that touched no field of ours
- [x] Confirm the transport chosen by Phase 1 E1 still behaves as measured, in a worktree, under `--dangerously-skip-permissions`, at a real child's turn budget. **It does, and the confirmation found the thing this phase should be remembered for.** `structured_output` is present and validates; `permission_denials` is on the envelope; `session_id` and `uuid` are too. **What was not previously measured is the combination with `stream-json`, which is what the fleet actually runs** — it works, *and declaring a schema replaces `.result` with the serialised structured output*, which would have deleted the fleet's only write-time gate. Full account in step 7's finding; the measurement is preserved as a test fixture rather than as a paragraph
  > **Turn-budget honesty:** the probes ran at `--max-turns 2` and 3 on haiku, not at `review-pr`'s 120 on its configured model. The envelope's *shape* is model- and budget-independent — Phase 1 established that across nine forced modes — and what these probes tested is the shape. **What they do NOT establish is whether a 120-turn disposition pass reliably calls the tool**, which is a question about the model's behaviour under a long prompt and is answerable only by the run set that requirement 6 still owes

---

## Implementation steps

### 1. Specify the envelope

> **Amended by [Phase 1](phase1_measure_the_channel.md) E1 (2026-08-08) — three runtime-produced fields are ruled in or out by measurement, so this step does not re-litigate them.**
> - **`is_error` does NOT enter the envelope as a routing field.** Measured `is_error == (exit != 0)` on 8 of 8 forced modes; it carries nothing the propagated exit status does not.
> - **`num_turns` enters as telemetry only, never as a branch input.** The turn-cap run reported `num_turns: 2` against a cap of **1**; `subtype` states the same fact exactly and `run-claude.sh:167` already reads it.
> - **`permission_denials` is REQUIRED, and its consumer is named.** The forced-denial run exited **0** with `is_error: false` and `subtype: "success"` — every signal the fleet reads today said clean while the fleet's only in-run safety control had fired. The array and its stream event are the entire trace.
>
> **Amended by E2 (2026-08-08) — the schema is a tool the model chooses to call, so every required field must be one the child can ALWAYS fill.** A model that cannot satisfy the schema does not error; it silently omits `structured_output` on an otherwise-clean `success` run (measured). The split abstention vocabulary in step 4 is what makes "always fillable" achievable and is load-bearing for that reason, not decorative.

- [x] Write the field list from Phase 1 E6 into this doc as the contract, with each field's named consumer beside it. A field with no consumer does not enter the envelope — **DEVIATION, stated rather than silent: the contract is written into [`exit-protocol.md` §2.1–§2.3](../../standards/exit-protocol.md), not into this doc.** The protocol's own §2 row says *"⟨PHASE 3⟩ writes the contract"*, so copying the table here as well would put two normative field lists in the tree, which [Documentation Standard § Single-source codified fields](../../standards/documentation/documentation_standard.md) forbids and which is the exact defect [Phase 2](phase2_kind1_framework.md) spent three passes removing from `operations.md`. This doc records the **rulings and their evidence**; the protocol carries the fields
- [x] **Classify each field `publishable` or `internal`.** Done in the protocol's rightmost column. **One field is classified out of the record entirely rather than merely marked internal:** `permission_denials[].tool_input` carries literal command lines and absolute worktree paths, and `code_routed_control_flow.md` P13 (*definitive*) records redaction as the documented control. It is dropped at read time, so there is no copy to leak — publish the count, the tool name and the matched rule
- [x] Note the consequence the classification creates and do not paper over it — **and the consequence is smaller than the checklist assumed, because of the choice above.** With `tool_input` dropped at read time, the routing copy and the published copy differ in exactly one field, `run_id`, which is an opaque nonce. *"One author, two copies"* is still one author and two *derived* copies — but the filter is one field wide, not a redaction pass, and a reader comparing them will find them otherwise identical
- [x] Include `schema_version` and state the version-skew rule — [protocol §5](../../standards/exit-protocol.md). Ruled conservatively and to the letter of the requirement: **additive-only extension, and an unknown value routes to the human arm at rule R4.** No compatibility matrix, no negotiation, no general versioning framework
- [x] State plainly that the rich findings payload is **not** part of what the parent branches on — protocol §2.1: `findings[]` carries two short fields per finding, and the parent's ordered rules (§4) read `routed_outcome` and `undetermined_reason` only. **`findings[]` rides along for Phase 5 and for step 8's invariant; no rule in §4 reads it**
- [x] Declare the record's size posture — [protocol §2.5](../../standards/exit-protocol.md). Tekton's 4096 bytes, `findings[]` exempt, and **a second bound the checklist did not anticipate**: the schema is an argument value, so its own size is a build-time cost for every caller and is asserted by a test at the same 4096 bytes. The GitHub Actions figures are not cited anywhere in this component
- [x] State that aggregation stays in the producer, and why — [protocol §3](../../standards/exit-protocol.md) and `disposition.md` Stage 6. The child aggregates its per-finding `hold_kind` values into one `hold_kind` on the envelope. **The why, so a refactor does not move it:** a parent that re-derived the token would be a caller with no stake in the review making a judgement about the review, and it would have to re-implement the reviewer's precedence rule (any `needs_ruling` beats every `redispatch`) in a second place — the duplicated-vocabulary defect that produced `routing.py`, one level up

#### Step 1 — Rulings, and the two places the measured contract departs from Phase 1 E6

**The envelope is ELEVEN child-authored fields where E6 ruled nine, and both additions were forced by a requirement E6 was not looking at.** E6 enumerated the union of values *existing branch points read*; it was not asked what the channel itself requires. Neither addition serves a hypothetical consumer.

| Departure | Why | Consumer |
|---|---|---|
| **`run_id` added** | Step 3 requires the record to be **run-identity bound** and the parent to match it against the invocation it dispatched. No E6 field carries an invocation identity | The parent's rule R5 |
| **`completion_ref.substrate` added** | [`exit-protocol.md` §1](../../standards/exit-protocol.md) requires the Kind 1 reference to be **substrate-agnostic**, and a three-part ref with no substrate discriminator forces every reader to infer the binding from the shape of a string. [Phase 2](phase2_kind1_framework.md) §9 is what makes this concrete rather than theoretical: this fleet already runs two bindings | The resolver; and any non-GitHub binding |
| **`completion_ref.number: integer` re-typed to `completion_ref.id: string`** | **A measured conflict with E6, reported as a finding rather than absorbed.** E6 typed it `integer`; *both* consumers hold it as a string today — `routing.py:100` returns `match.group(1)`, and bash `${PR_URL##*/}` is a string. An integer type would make every consumer cast, and it presumes a substrate whose record ids are numeric | **B3, P4** |

**And `permission_denials` moves stratum without changing status.** E6 listed it alongside the child-authored fields; E1(f) had already established it is produced by the runtime and invisible to the child. The protocol states it as runtime-produced (§2.2). It is **still required, still has E1(f)'s named consumer, and still routes to the human arm** — nothing about the ruling weakens; the record just says who can physically write it.

### 2. Rule on who authors the record

This ruling comes before the mechanism, because the two answers are different architectures and only one of them is what this component decided to build.

- [x] **Rule it explicitly. RULED: MODEL-AUTHORED UNDER PROMPT INSTRUCTION**, and the mechanism forces it rather than merely permitting it. Phase 1 E2 measured that `--json-schema` is implemented as a **`StructuredOutput` tool the model must call** — there is no path by which the runtime fills those parameters, so a wrapper-parses design is not available under the chosen transport even if it were wanted. It is also arrangement B with extra steps, and arrangement B is what this component exists to replace. **The prompt is therefore part of the contract surface**, and §6's last bullet (prompt-borne emission is part of the conformance surface) is what keeps that honest
- [x] State the consequence for [Phase 4](phase4_fleet_migration.md)'s sizing — **and it is smaller than the checklist feared, because of a ruling that landed after the checklist was written.** The wire-format spec does live inside prompt strings in two places, `children/review-pr.sh` and the Python tree's `disposition.md`. But the bash fleet is **frozen** ([`exit-protocol.md` §7](../../standards/exit-protocol.md)), so `children/review-pr.sh`'s prompt is never edited: it keeps emitting prose and nothing else. **Phase 4's prompt-editing surface is one file per V2 child, not two per child** — the "twice over" is retired by the freeze, not by an argument
- [x] Note the composite case honestly — done, and the doc that says which fields come from which is [`exit-protocol.md` §2.1–§2.3](../../standards/exit-protocol.md). **Three strata, not two:** the child authors what only the child knows, the runtime produces what the child cannot see (`permission_denials`), and the parent computes what neither is entitled to decide (`routed_outcome`). The checklist named `is_error` and `num_turns` as the runtime half; **both were ruled OUT of the envelope entirely by Phase 1 E1(a) and E1(b)**, so the runtime stratum's actual content is `permission_denials` alone

#### Step 2 — What the ruling costs, stated so it is paid deliberately

**A model-authored record inherits the producer's error rate, and that is the whole reason §4 is total.** The record's *presence* is model-dependent, not transport-dependent (E2(c)): a schema the model finds unsatisfiable produces silence on a clean run rather than an error. Two consequences are load-bearing and are built, not noted:

1. **Every required field must be one the child can always fill.** The abstention vocabulary in step 4 is what makes that reachable — without an in-schema way to say *"a human must decide"*, a model facing a case it cannot resolve has no option but to not call the tool at all.
2. **The parent may not treat the record's own claims about itself as evidence.** `run_id` is model-echoed, so it proves nothing on its own — it is checked *against* the value the parent generated, which is why R5 is a comparison and not a presence test. A record cannot report its own absence, and it cannot vouch for its own identity either.

### 3. Specify the channel, not just the record

The incumbent's protections are properties of `mktemp` and an anchored regex, not of the prose format. A migration that carries the format across and leaves the channel unspecified is a **net weakening**, which this component's own bar forbids.

- [x] **Fresh per invocation. BUILT, not closed.** `assistant_activities.claude_log_path()` allocates a per-invocation path, **names it with the run nonce** so it is unique by construction, and **reserves it atomically** (`O_CREAT|O_EXCL`) so a residual collision raises rather than truncating. The scenario it defends is real in this pair: `build_workflow` calls `review_pr` twice in one run, either side of the loop-back — **and it is real BETWEEN pairs too**, which the first version missed: the log directory is the repo root's, shared across concurrent dispatches, while `MODEL_KEY` is a constant, so a name built from the model key and a second-granular stamp collided and one run truncated the other's log. Check-then-create reserved nothing across the process boundary to `run-claude.sh`'s `O_TRUNC` redirect; the name and the reservation are two independent fixes and both are present
- [x] **Run-identity bound. BUILT.** The parent generates a `run_id` nonce, puts it in the prompt, and rule R5 compares the record's echo against it; a mismatch routes to the human arm as `record_stale`. The two checks are independent as the checklist requires — path freshness is enforced by the filesystem, identity by the payload — and the second is what catches a record that arrived on a *correct* path from a *different* invocation
- [x] **Anchored outside the worktree. CLOSED by construction, and already enforced.** The child writes nothing: the record rides out in the CLI's own stdout, and the only thing that touches a filesystem is the parent's own redirect. `assistant_activities.run_claude` already **raises** when `repo_root` contains `.claude/worktrees` (`assistant_activities.py:270-274`), which is the same defect the checklist cites `run-claude.sh`'s docstring for. No write crosses the isolation boundary in either direction
- [x] The transport IS `structured_output` — **and the checklist's conclusion from that is measured HALF WRONG, which is this step's finding.** See below

#### Step 3 — Ruling: one of the three is closed by construction, two are not

The checklist's last bullet says that under `structured_output` all three requirements are *"satisfied by construction and closed rather than built — the record rides in the CLI's result envelope, with no path to own and no staleness class."* **The premise is true and the conclusion does not follow, because the fleet's plumbing materialises the envelope in a file before any parent sees it.**

`run-claude.sh:146,149` redirects the CLI's stdout — the stream carrying the `result` event, and with it `structured_output` — into `$LOG_FILE`. So there *is* a path to own. It is owned by the **parent**, not the child, which is what preserves the isolation argument; but "nobody owns a path" is false, and a design written on that sentence would allocate no path deliberately and inherit whatever the log naming happens to give it.

**What the log naming happens to give it, measured rather than assumed.** `assistant_activities.py:278-279` names the log `{model_key}-{%Y%m%d-%H%M%S}.jsonl`. That is **one-second granularity on a key that repeats within a single run** — `build_workflow` invokes `review-pr` twice. Truncation on redirect means a collision destroys the earlier log rather than leaving a stale record behind, so the *dangerous* direction is closed today by luck of a shell operator; the *lossy* direction is open. Neither is a property anyone chose.

**Ruling.** Requirement 3's third clause (anchored outside the worktree) is **closed by construction** and additionally guarded by an existing raise. Its first two clauses (fresh per invocation, run-identity bound) are **built**, because the transport removed the child's write and not the parent's. The two mechanisms cost eleven lines between them.

**The generalisable form, recorded because it will recur in [Phase 4](phase4_fleet_migration.md):** *"the transport has no staleness class"* is a claim about the transport, and the fleet's channel is transport **plus plumbing**. Phase 4 will read this bullet while migrating nine more call sites; each one has its own plumbing, and each one has to be asked the same question rather than inheriting this answer.

### 4. Specify the split abstention vocabulary

- [x] Define the **computed** arm — *could-not-check*. Written as `undetermined` + `undetermined_reason` in [`exit-protocol.md` §3](../../standards/exit-protocol.md). **What can emit it: the parent, from rules R1–R5, each of which reads a fact about a byte sequence or a process. What cannot: the child — the member is not in the schema it is given**, so the split is enforced by the schema rather than by convention
- [x] Define the **asserted** arm — *needs-a-ruling*. Written as `hold` + `hold_kind: needs_ruling`. **Renamed from today's `needs-assistance` deliberately** — the incumbent token names *who is called*, and the new one names *what is missing*, which is the distinction the split exists to draw. The prose token is unchanged on the wire and the mapping is stated in step 7's shadow table
- [x] State the different reliability of each — protocol §3, and the asserted arm's under-emission remains a **prediction, not a measurement**, which the doc says in those words rather than implying a number exists
- [x] State the different remedy for each — protocol §3: a *could-not-check* is a defect in the checker with a fix; a *needs-a-ruling* is a request for a person and has no fix. **Both route to the human arm; only one of them is a bug**
- [x] Record how each arm will be measured separately once emitted — below

#### Step 4 — How the two arms get measured separately, so the prediction becomes a number

The measurement is possible only because the two arms are **different fields**, not two values of one field. `routed_outcome == undetermined` is countable without reading `hold_kind`, and `hold_kind == needs_ruling` is countable without reading `routed_outcome`. Concretely, over the run logs the fleet already writes:

| Arm | Predicate over a run's `result` event | What a rise means |
|---|---|---|
| computed — *could-not-check* | `routed_outcome == "undetermined"`, grouped by `undetermined_reason` | **the machinery is failing**, and the reason names which part |
| asserted — *needs-a-ruling* | `structured_output.hold_kind == "needs_ruling"` | **the work is producing questions**, which is a statement about the work and not a defect |

**The denominator is the run count, not the hold count**, because the interesting number is the *rate at which a pass declines to rule*, and a hold-only denominator hides every clean pass. **Nothing in this phase reports either number**: one pair over one run set is not a rate, and [Phase 4](phase4_fleet_migration.md) is where the denominator becomes large enough to state one. Recording the predicate now is what stops Phase 4 inventing a different one and comparing it to nothing.

> **And one of the two predicates was not evaluable as the draft shipped it — the RELIABLE one.** The asserted arm reads `structured_output.hold_kind`, which the child writes into the log. The computed arm reads `routed_outcome` and `undetermined_reason`, which are **parent-computed** (protocol §2.3) and were persisted *nowhere*: they lived in a return value that dies with the process and in free-text operator notes. So the arm §3 calls reliable — *"its emitter has no incentive to guess"* — was the one Phase 4 could not count, and the sentence above about stopping Phase 4 inventing a predicate was exactly inverted for that half. **Fixed by writing the parent stratum into the run log** as one appended `{"type": "parent_route", …}` event, so both predicates evaluate over one artifact. It is appended **before** the shadow comparison, because a disagreement raises and a machinery failure that leaves no trace is the one most worth counting. **The generalisable form: a metric defined over a field nothing writes is a plan, not an instrument** — and the check is to name the artifact each predicate reads, not just the field.

### 5. Specify the fail-safe contract

- [x] Write the routing rules as an **ordered list, first match wins, with a documented default** — done, [`exit-protocol.md` §4](../../standards/exit-protocol.md), nine rules. Shape borrowed from Kubernetes `podFailurePolicy` and cited rather than designed. **It looks boring, and that was the goal**
- [x] Make the residual arm a **named state that is recorded** — `routed_outcome: undetermined` with `undetermined_reason: unmatched` at R9, carried in the record and surfaced to the operator like any other reason. R9 is reachable in practice, not decorative: R6–R8 do not exhaust `outcome` × `hold_kind`, and a widened supported-version set with un-widened rules lands there
- [x] Enumerate all four absence modes and route each to the human arm — R2 absent, R3 unparseable, R5 stale, R4 unknown `schema_version`. **R4 sits before R5 deliberately**, and the reason is written into the table: a record whose version is unknown has no guaranteed typing, so its `run_id` is not yet a value one may compare
- [x] **Absence must be reachable from `subtype: success` — written as its own ordered rule**, R2, with E2's measured tuple stated beside it and the population named to include *"the model declined to call the tool"*. **R2 is not conditioned on the exit status at all**, which is the strongest form of the requirement: it cannot accidentally acquire a `subtype` guard in a later edit, because it never had one to widen
- [x] **A non-empty `permission_denials[]` routes to the human arm and never to automatic redispatch** — R1, placed **first** so no later rule can reach past it. The ordering is the enforcement; a rule stated anywhere else in the list would be reachable only when the record happened not to match something above it
- [x] ~~If Phase 1 E2 found a partial record can exist, add a **consumer-side completeness check the parent enforces**~~ — **DROPPED by E2's ruling (2026-08-08).** 0 of 4 forced deaths (turn-cap, budget, `SIGTERM`, schema violation) produced a partial record: in every case the key is **absent entirely**, never truncated or invalid. There is no state between a validated object and no key, so neither the completeness check nor the producer-side atomic write is built. This is the transport's measured advantage over a file, and dropping the requirement is the measurement's whole point
- [x] Write the justification paragraph on the **stationary error rate**, not on producer exceptionalism — [`exit-protocol.md` §4](../../standards/exit-protocol.md) *"Why total rather than best-effort"* already carries it in those terms and this phase does not restate it. The one line worth adding here is what the phase measured: **R2's population is not a defect awaiting a fix.** E2's fourth row is a model correctly declining an unsatisfiable schema — the *right* behaviour producing an absent record. A contract written on "absence means something broke" would be waiting for a fix that is not coming

#### Step 5 — The fail-safe contract's own fail-safe: what happens when the parent cannot evaluate a rule

Every rule above reads a value. Three of them read values that can themselves be missing, and the contract is only total if that case is ruled rather than left to the language:

- **R1 reads `permission_denials` off the CLI result envelope.** If the envelope exists and has no such key — an older CLI, a changed shape — the parent must not read absence as an empty list. It routes `undetermined` / **`denials_unreadable`**: *"I could not check whether the safety control fired"* gets the **same arm** as *"the safety control fired"* and its **own reason**, because the arm is what protects the fleet and the reason is what the operator reads. **Sharing one bin has a named trigger and a measured consequence:** if a CLI change renames or drops the key, R1's unreadable branch fires on 100% of runs and reports a fleet-wide safety-control trip that never happened. *(Shipped as `permission_denied` for both branches and corrected 2026-08-09 — see the note below, which is the same defect one rule down.)*
- **R2 reads the `result` event.** If the log carries no `result` event at all, R2 fires: no event implies no key.
- **R4 reads `schema_version` out of a record that has already validated at R3**, so its presence is guaranteed by the schema's `required` list rather than by hope.
- **R1 also reads a list whose ELEMENTS it cannot type.** R1 can check that `permission_denials` is a list; it cannot check what is in it, and an entry that is not an object would raise from inside the routing contract — which the caller's handler does not catch, so the operator gets a traceback instead of a routed record. The redaction step tolerates it and keeps the count honest.

**This is the shape of the rule, not four special cases:** a total contract must also be total over *failures of its own inputs*, and the default direction is always the human arm.

> **The draft shipped the second bullet as prose and the opposite as code, and a refine pass caught it.** With no `result` event the implementation reached R1 first — an empty envelope has no `permission_denials` key — and reported `permission_denied`. **Routing was right and the reason was wrong, and the reason is the payload:** step 4 below defines the computed arm's rate as `undetermined` *grouped by* `undetermined_reason`, and a run killed mid-stream is the most frequent machinery failure there is, so every one of them was binned as a safety-control trip in the instrument this phase exists to build. **The test that should have caught it asserted only the arm**, not the reason, so it was green under both readings. Fixed in the code, in [protocol §4](../../standards/exit-protocol.md)'s R2 row, and in the test. **The generalisable form: where two artifacts of one phase can disagree, the test must assert the field they would disagree ON.**

### 6. Rule the three open questions

- [x] **Disagreement policy.** Record both the asserted verdict and the computed observable under distinct names — the raw observation is never overwritten, and the policy-adjusted value is what routing reads by default. **Amended by [Phase 1](phase1_measure_the_channel.md) E3 (2026-08-08): the off-diagonal cells are empty (0 of 14) and, more importantly, they are empty BY CONSTRUCTION — the asserted verdict rides in the same envelope key E1 measured as absent on every run where `is_error` is true, so the two rows cannot both be populated for one run at any N.** Adopt the two-name shape and **build nothing else**, and record the *structural* reason rather than the count, so a future reader does not re-take the measurement at a larger N expecting a different answer
- [x] **Who owns the to-do bit.** Kind 1 uses open/closed; a typed record carries a verdict; the corpus's only nearby answer is *neither closes the loop*. **Amended by [Phase 1](phase1_measure_the_channel.md) E3 (2026-08-08) — this is ruled by measurement and the ruling is `open`.** The last typed verdict disagreed with the PR's terminal disposition in **6 of 7** PRs where both existed, and **31 of 38 PRs carry no typed verdict at all**. State the loser's purpose explicitly: the typed `verdict` is a **routing input for the next dispatch decision with a lifetime of one parent invocation**, not a durable record of whether work remains — and add **no machinery to reconcile the two**, which would be the composition engine the row above just declined to build
- [x] **Cross-language schema ownership. RULED: the schema is declared ONCE, in the V2 Python tree, and the ruling is SCOPED to that tree — because there is no second envelope to diverge from.** The question as written assumed two fleets that both emit typed records. They do not, and the assumption was retired after this checklist was written: [`exit-protocol.md` §7](../../standards/exit-protocol.md) freezes the V1 bash fleet — **not migrated and not retired**, kept as the working fallback precisely because V2 is not fully proven, with nothing invested in upgrading it. **The bash fleet is a carve-out, stated, not an omission.** It keeps emitting prose, so it has no envelope, so neither option's failure mode is reachable:
  - **A single language-neutral schema artifact both trees load** — solves a divergence between two producers, and there is one producer.
  - **Two declarations plus a cross-tree conformance test in `testing/suites/`** — **explicitly NOT built.** A conformance test between a tree that will change and a tree nobody will change is a gate that can only ever go red for one reason: someone edited the frozen fleet, which is already forbidden. It would cost a suite, a fixture and a maintenance obligation to restate a freeze.

  **The declaration site is `modules/assistant/exit_record.py`** — dependency-free, sibling of `routing.py`. **The promotion rule is stated as a deviation rather than claimed as conformance:** `§10.1` rule 3 is mechanical — *promoted out of a workflow folder iff more than one workflow uses it, consumer count never taste* — and `routing.py` earned its level with three consumers where this module has **one** today. It is placed here because [Phase 4](phase4_fleet_migration.md) is the migration that supplies the others and moving it now would move it back within one phase. **If Phase 4 does not migrate a second consumer, this module belongs in `review_pr/`** — that is the condition, written down so the deviation expires rather than settling. Both the JSON Schema handed to the CLI and the vocabulary the parent routes on come from that one module; the schema string passed to `--json-schema` is *serialised from* the same object the router validates against, so the two cannot disagree without the module contradicting itself. **The re-scoping is the ruling, not an evasion of it** — and if the freeze is ever lifted, this is the line that has to be re-opened, which is why it says so here rather than only in the roadmap.
- [x] Record all three in the [roadmap's Key Decisions](roadmap.md#key-decisions) so they are readable without opening a phase doc

#### Step 6 — The three rulings, as one table

| Ruling | Answer | Rests on | What was deliberately NOT built |
|---|---|---|---|
| **Disagreement policy** | Two names — `outcome` (asserted, verbatim, never overwritten) and `routed_outcome` (policy-adjusted, what routing reads). Precedence: **computed gates, asserted decides** | [Phase 1](phase1_measure_the_channel.md) E3(a) — the off-diagonal is empty **by construction** | any composition machinery |
| **To-do-bit ownership** | **`open` owns it.** The typed `verdict` is a **routing input for the next dispatch decision, with a lifetime of one parent invocation** — not a durable record of whether work remains | E3(b) — 6 of 7 disagreements; 31 of 38 PRs carry no typed verdict at all | any reconciliation between the two, which is the composition engine in a second guise |
| **Cross-language schema ownership** | **One declaration, scoped to the V2 tree.** The bash fleet is a frozen carve-out with no envelope | [`exit-protocol.md` §7](../../standards/exit-protocol.md) | the cross-tree conformance test |

**On the to-do bit, the sentence that matters is what the loser is FOR.** A typed `verdict` that disagrees with a merged PR is not a defect and not a signal to reconcile: the two answer different questions over different lifetimes. The typed one answers *what should the parent do in the next thirty seconds*; `open` answers *does this work still need something*. **6 of 7 is unsurprising once that is written down, and alarming until it is** — which is precisely why the ruling has to state the loser's purpose rather than only naming the winner.

### 7. Build it once and prove it

- [x] `review-pr` writes the record at exit on the specified channel, and continues to emit the prose VERDICT line unchanged. `disposition.md` Stage 6a instructs the tool call, 6b is the unchanged VERDICT line, and the pair-selection is the V2 tree throughout: `review_pr_workflow` is the record's reader and `build_workflow` the routing consumer. **`children/review-pr.sh` is untouched** — frozen fleet
- [x] One parent reads the record and routes on it, with the prose parse retained as a **shadow** — `review_pr_workflow.run_review`. The typed one decides (`verdict_from_record`), the prose is parsed from the same log, and a mismatch **raises**. **The prose is read from the assistant text blocks, not from the console or `.result`** — see the finding below, which is the reason
- [x] Tests, one per absence mode — `tests/unit/test_exit_record.py`. Absent, unparseable (four validator branches), **stale**, unknown `schema_version`, and the valid control. Plus the two rules outside the four: R1's denial and R9's residual. **The stale case is written against a record that is otherwise perfect** — it validates, its version is supported, every field is present, and only the nonce betrays it
- [x] Test: the shadow comparison actually fires — `test_the_shadow_comparison_actually_fires` mutates the typed record to `hold`/`redispatch` against a prose `MERGE` and asserts the raise. **Eleven mutations were run against this suite and all eleven fired**, listed in the PR body; the two that matter most are *"delete the shadow comparison"* and *"let the prose channel decide"*, which each turned four tests red
- [x] **DONE (2026-08-10) — the shadowed pair ran over a real run set, and the agreement count and every disagreement with its cause are recorded** in [the run-set section below](#step-7--the-run-set-nine-live-dispatches-and-the-three-numbers-it-yields). **Nine live dispatches, four real PRs, real budget, none staged for the measurement** — the condition the original text set. It was not produced by a pass that arranged it; it accrued as a byproduct of ordinary work and was read back out of `.claude/logs/review-pr-*.jsonl` afterwards. *Original text, kept so the closure can be checked against what it was asked for:* ~~NOT DONE — This needs live `review-pr` dispatches against real PRs on a build of this branch — order-of-40-minutes and real budget per data point, and the code has to be merged before a dispatch picks it up. A draft pass cannot produce it and should not fake it: **an agreement count of one, from a run this pass staged itself, would be the measuring-an-artifact-you-are-inside-of defect [Phase 1](phase1_measure_the_channel.md) recorded three times.**~~
- [x] Verify the vocabulary is declared per the requirement-8 ruling, and that no second declaration was introduced — `test_the_typed_vocabulary_is_declared_in_exactly_one_module`, which enumerates the members **from the enums** rather than from a list of today's strings, so it catches the *next* member added and not only the ones that exist now. It found a real second declaration while being written: `exit_record.as_prose_verdict` was re-typing `routing.py`'s three prose tokens — the §6 defect, inside the module written to enforce §6 — and it was replaced by a single predicate with the translation at one call site

#### Step 7 — The measurement that changed the build, and would have deleted a live protection

Re-verified on **CLI 2.1.224, 2026-08-09, host `puma-workstation-mint`**, in the real child shape. Phase 1 E1(g) measured `structured_output` against `--output-format json`; the fleet runs `stream-json`, and nobody had checked the combination.

1. **`--json-schema` composes with `--output-format stream-json`** — the `result` event carries a validating `structured_output` under both. This is the addition, not a re-take.
2. **Declaring a schema REPLACES `.result` with the serialised structured output.** On a run that emitted prose *and* called the tool, `.result` was `{"outcome":"merge",…}` and the terminal `VERDICT:` line was **not in it**. `run-claude.sh`'s § *Completion contract* reads `.result`. *(Cited by section: this pass MOVED that read, so the line range the draft wrote here was stale the moment it was typed.)*

**What that means, stated plainly: adding `--json-schema` to this fleet's invocation without moving that read would have silently deleted the fleet's only write-time gate — on every conforming run, in the same change that added the typed record.** Phase 1 E5 established that gate exists (`run-claude.sh` fails the run loud when the token is absent from `.result`), and roadmap candidate 1 / `D-007` both turn on it. A change that removed it while looking like an addition is the exact shape this component exists to catch, and it was one probe from shipping.

**The fix, and the correction a refine pass had to make to it.** When a caller declares a schema, the completion gate reads the stream's **assistant text blocks** — closer to the parent's own parse surface, which is candidate 1's open question. The prose shadow reads the same surface for the same reason: a shadow read from `.result` would report a disagreement that is an artifact of where it looked.

> **The draft read EVERY block, and that gave back the protection it had just saved.** `.result` supplied *finality* for free by being the final message, and the gate exists to catch a run that **ended** early — so grepping the concatenation changed the predicate from *"the run finished with a verdict"* to *"the run ever mentioned a verdict"*, and a model that prints its verdict at turn 20 and then stops on *"let me confirm the comment posted"* passes a gate built to fail exactly that. **The test shipped alongside it could not see the difference**: it asserted the filter *finds* a verdict, which is true of both the correct gate and the widened one. It now takes the **last** block (and excludes sub-agent turns, whose events carry a `parent_tool_use_id`), and the pair of tests is the discriminator — one that the signal is found, one that a non-final signal is **rejected**. **The generalisable form:** a guard whose test asserts only the positive is a guard with no mutation evidence, and §6 asks for the demonstration that it *fails*.

**The shadow keeps the wider surface deliberately, and the two now differ.** `parse_verdict` is last-match-wins across all of this run's text, because a disposition comment may quote an earlier pass's verdict while reaching a different one of its own; the gate wants finality instead. So the gate's surface and the parent's are *closer* than they were and are not identical — candidate 1's gap is narrowed, not closed, and saying so is cheaper than a reader discovering it.

**The fixture is the measurement.** `tests/fixtures/schema_declared_run.jsonl` is that run, reduced to the events under test, and two tests run the **jq programs extracted from the shipped script** against it — one asserting the assistant filter finds the signal, one asserting the `.result` filter does not. The second is the finding as a permanent negative control: if `.result` ever starts carrying prose again, that test goes red and says so.

#### Step 7 — The run set: nine live dispatches, and the three numbers it yields

**Taken 2026-08-09 20:45 → 2026-08-10 09:40 from `.claude/logs/review-pr-*.jsonl`, on merged Phase 3 code (`9fa69ff`, PR #71).** Nine live `review-pr` dispatches against four real PRs (#73, #74, #75, #77) at real budget. `append_parent_route` writes `routed_outcome`, `undetermined_reason`, `hold_kind`, `shadow_verdict`, `shadow_parseable` and `channels_agree` to the run log; the child's own `outcome` is read from the `result` event's `structured_output`. **Every figure below was extracted and reconciled independently of the brief that requested it, and where the two disagreed the logs won** — three of those disagreements are recorded as findings rather than smoothed into the table.

| # | log `review-pr-…` | PR | child `outcome` / `hold_kind` | prose shadow | `routed_outcome` | `channels_agree` |
|---|---|---|---|---|---|---|
| 1 | `20260809-211149` | 73 | `hold` / `redispatch` | **none emitted** | *never computed* | **not recorded** |
| 2 | `20260809-212315` | 74 | `hold` / `redispatch` | `HOLD - redispatch` | `undetermined` / `permission_denied` | **false** |
| 3 | `20260809-212652` | 73 | `hold` / `redispatch` | `HOLD - redispatch` | `hold` / `redispatch` | true |
| 4 | `20260809-221815` | 77 | `hold` / `redispatch` | `HOLD - redispatch` | `hold` / `redispatch` | true |
| 5 | `20260809-223438` | 75 | `hold` / `redispatch` | `HOLD - redispatch` | `hold` / `redispatch` | true |
| 6 | `20260809-233717` | 77 | `hold` / `redispatch` | `HOLD - redispatch` | `hold` / `redispatch` | true |
| 7 | `20260809-234435` | 75 | `merge` / — | `MERGE` | `undetermined` / `permission_denied` | **false** |
| 8 | `20260810-092344` | 73 | `hold` / `redispatch` | `HOLD - redispatch` | `hold` / `redispatch` | true |
| 9 | `20260810-094030` | 73 | `hold` / `needs_ruling` | `HOLD - needs-assistance` | `hold` / `needs_ruling` | true |

**Nine dispatches produced eight rows of instrument, and the three numbers the set yields answer three different questions.** Collapsing them into one figure is what would make this table misleading:

| Question | Figure | Read from |
|---|---|---|
| Did the parent's **route** match what the prose channel would have decided? | **6 of 8** | `channels_agree` |
| Did the two channels' **content** agree, among instrumented runs? | **8 of 8** | child `outcome` vs `shadow_verdict` |
| Did they agree across **every** dispatch? | **8 of 9** | row 1 emitted no prose verdict at all |

**0 of 9 is attributable to the record, the schema or the transport.** All nine records validated, all nine echoed the correct `run_id`, and `shadow_parseable` is **true on all eight** instrumented runs — so no agreement in this count is two fail-safe defaults colliding, which [step 5](#step-5--the-fail-safe-contracts-own-fail-safe-what-happens-when-the-parent-cannot-evaluate-a-rule)'s note names as the case that would be indistinguishable from a real agreement.

**Both `channels_agree: false` rows have one cause and it is not a channel defect.** Both routed `undetermined` / `permission_denied` — **R1 fired**. And on both, **the child's typed `outcome` matched the prose shadow exactly**: `hold`/`redispatch` against `HOLD - redispatch` on PR #74, `merge` against `MERGE` on PR #75. The divergence is entirely between `routed_outcome`, which R1 has already overridden, and the prose channel's raw assertion. **The two channels did not disagree about the review. The parent applied a safety rule the model could not see** — the [roadmap's Key Decision](roadmap.md#key-decisions), *"a non-empty denial list routes to the human arm and never to automatic redispatch"*, working exactly as specified.

> **`channels_agree` compares ACROSS STRATA, and that is correct for requirement 6 rather than a defect — but it is not a channel-fidelity measure and must not be read as one.** `verdict_from_record` reads `routed_outcome`, a *computed, policy-adjusted* value (§2.3); the shadow is an *asserted* one. Requirement 6 asks whether the two **routing paths** agree, and the R1 override is part of the new path, so including it is the question being asked. **The name invites the other reading**, which is why both figures are stated above and neither is reported alone.

**The two denials, and a correction to what this run set was briefed as showing.**

| PR | denied command (abridged) | what it actually was |
|---|---|---|
| #74 | `cd /tmp && rm -rf m6 && mkdir m6 && …` | a mutation sandbox for a negative control |
| #75 | `rm -rf /tmp/pr75-merge && git worktree add --detach /tmp/pr75-merge $C` | pre-cleaning a scratch path before materialising a trial merge |

**Both were briefed as false positives — an over-match in `block-dangerous.sh`, tracked as issue #61, with a fix in flight on PR #77. That is wrong, and it was settled by execution rather than by reading.** Both commands are denied by the hook on `main`, and **both are still denied by PR #77's rewritten hook**. PR #77's own diff carries `# MUST BLOCK: rm -rf /tmp/build` as an explicit intent comment, and issue **#62** — the over-match issue, now **CLOSED** — enumerates four over-matches (`--force-with-lease`, `| shasum`, `./confirm -f`, prose), none of which is this shape. **The hook denied `rm -rf` on a scratch path exactly as designed, and as re-ratified by the very rewrite that was supposed to relax it.**

**The adjudication is unchanged and its ground is firmer.** These are not a defect firing spuriously; they are a control firing **correctly**, and the composition rule routing on it. A run set whose only two divergences are the safety layer doing its job says more for the typed channel than one whose divergences were hook bugs.

**This is the first time anything has ROUTED on `permission_denials[]`, and that is the precise claim.** The array itself is not new: non-empty denial lists appear in **21 run logs going back to 2026-04-10**, including a `review-pr` run on 2026-08-08 (`review-pr-20260808-131356`) that denied the *same* `cd /tmp && rm -rf mtest` sandbox shape and carries **no `parent_route` event, because nothing read it**. `git grep permission_denial 9fa69ff^ -- scripts/` returns nothing. So the fleet recorded denials for months and branched on none of them; R1 is the first consumer, and the roadmap's description of this component as *"the fleet's only scheduled opportunity to observe that control firing"* is borne out — **it fired twice in nine**.

**Row 1 is the dispatch the instrument could not record, and finding it changed what this section can claim.**

Run `e724da01…` (PR #73, 21:11) completed cleanly — `subtype: success`, `is_error: false`, 26 turns — and emitted a valid typed record (`hold`/`redispatch`, correct nonce). It has **no `parent_route` event**. The cause is its final assistant text block, which reads *"Comment posted. Emitting the typed exit record."* and carries **no `VERDICT:` line**. Under a declared schema the completion gate reads the last assistant text block (the finding above), so `run_claude` raised on a failed completion contract — **before `route()` and before `append_parent_route`**.

**So the prose channel failed, the typed channel succeeded, and the comparison that exists to notice exactly that never ran.** The pair's fail-loud contract worked as designed: the run was failed, an operator saw it, the work was redone on the next dispatch. Nothing was lost operationally. **What was lost is the datum** — and it was the most informative one in the set.

**Stated as the limitation it is: `channels_agree` can only be written on runs where the prose channel already succeeded.** The instrumented denominator is conditioned on the incumbent working, so **the instrument is structurally blind to the incumbent's own failure mode** — which is the mode this component exists to weigh. One dispatch in nine hit it, so the condition is not vacuous. [Step 4](#step-4--how-the-two-arms-get-measured-separately-so-the-prediction-becomes-a-number)'s note already argued that *"a machinery failure that leaves no trace is the one most worth counting"* and moved the append **before the shadow comparison** for that reason; this is the same argument one stage further upstream, where the append has no record to write yet. **Recording the pair on the gate-failure path is new capability rather than a repair**, so it is placed as **C-060** and not fixed here — the same routing [C-056](../../standards/architecture/research/candidates.md) took for the identical class.

**What this run set does NOT establish.**

- **It is not a rate, and this phase reports none.** Nine dispatches over four PRs, three of them repeat passes on #73 — the runs are not independent. [Step 4](#step-4--how-the-two-arms-get-measured-separately-so-the-prediction-becomes-a-number) already rules the denominator to be [Phase 4](phase4_fleet_migration.md)'s, and nothing here revises that.
- **The agreed-`MERGE` cell is unobserved.** Six of the eight instrumented agreements are `HOLD - redispatch` and the seventh is `HOLD - needs-assistance`; the set's one `merge` record was overridden by R1. **No dispatch has yet routed MERGE with the prose channel agreeing** — that half of the vocabulary is exercised in tests and not in the field.
- **It gives the first field evidence on a question [§Runtime Verification](#runtime-verification) left open** — whether a 120-turn disposition pass reliably calls the tool. **Nine of nine did**, at real budget, against probes that had run at `--max-turns` 2 and 3. Nine is not a rate either; the useful part is the direction, and that **the one channel failure in the set was on the prose side**.

**The generalisable form, and it is this phase's own pattern one more time: an instrument that can only observe the runs its subject did not break will report the subject as unbroken.** The eight instrumented rows reconciled in minutes. The ninth took noticing a run that *was not there* — and it is the only row that speaks to the question the phase is actually asking. **Checking a denominator against the population it was drawn from is not a refinement of the measurement; it is the measurement.**

### 8. Render the human record from the typed one

Kept in this phase rather than [Phase 4](phase4_fleet_migration.md) because it concerns exactly the child this phase already opens and proves, and its failure mode — an operator loses prose they relied on — is unrelated to the migration's failure mode of an unenumerated call site.

- [x] The `pr_review:` block posted to the PR becomes a copy of the exit-channel record, filtered to the publishable subset — for the fields the two share. `disposition.md` Stage 5 Part 2 now states it in those words: `verdict:` renders `outcome`, and `findings[].id` / `findings[].disposition` are identical in both. **`run_id` is the only field of the record that does not cross**, and it is an opaque nonce
- [x] **CO-AUTHORING PERSISTS, so the write-time invariant is what ships** — `_verify_block_matches_record` in `review_pr_workflow`. Every `(finding id, disposition)` pair in the record appears in the posted block and vice versa; either direction fails loud, and a missing block fails loud on its own. It shipped as `_verify_` rather than `_assert_` because it had a third outcome besides raise-or-nothing: when the thread could not be READ it returned a note the caller must surface, and a name promising only two outcomes is how a later call site drops that note. **Superseded 2026-08-09 by Phase 5** — that phase hoisted the thread read to the caller (its predicate needs the same observation), which took the third outcome out with it into `_thread_unreadable_note`. The function is now `_assert_block_matches_record` and has exactly the two outcomes an `_assert_` prefix promises; the note is still the caller's to surface, at the caller. The full render-from-record was **not** chosen, and the reason is the next checkbox: three of the block's regions have no field to render from, so a render would drop them silently
  > **Two corrections a refine pass made to this check, both of which made it green where it should have been red.** (1) It compared **ids only**, while the prompt promises the child that ids *and dispositions* are both checked — and `findings[].disposition` is the field [Phase 5](phase5_convergence_stopping.md)'s stopping predicate keys on, so a block reading `deferred` against a record reading `fixed` let the operator and the convergence rule disagree about whether work was closed. (2) It read **the latest block on the thread**, not this pass's. `disposition.md`'s INVARIANT 1 *requires* a pass to carry every prior finding forward, so **identical id sets across passes is the norm, not a coincidence** — a pass 2 that produced a valid record and then failed to post its comment was compared against pass 1's still-matching block, matched, and reported the invariant satisfied. It is now bound to the fence-anchored block count, which must rise by one. **The shape worth carrying to Phase 4: an invariant over two copies must first establish that it is looking at the two copies it thinks it is.**
- [x] **DEVIATION, stated: the invariant is enforced AFTER the comment posts, not before, and it blocks the route rather than the post.** The parent has no hook between the child authoring the comment and the child posting it — under arrangement A the author *is* the model. What the requirement is protecting against is a mismatch reaching a consumer, and the check reaches that: a mismatch raises before any verdict is returned to `build_workflow`, so no route is taken on a disagreeing pair. The prompt additionally carries the rule at authoring time, which is the earliest point anything can
- [x] **Declare that the typed region wins** — declared in three places that a reader might arrive at independently: the prompt (*"the typed record is authoritative … the block is its rendering"*), the invariant's own failure message, and [`exit-protocol.md` §3](../../standards/exit-protocol.md)'s precedence rule
- [x] **RULED: the comment stays INDEPENDENTLY AUTHORED alongside the record, and the three prose regions get no field.** The per-finding disposition reasoning, the one-line verdict rationale and the Post-Run Reflection are not modelled. **The reasoning, and it is not "they are hard to type":**
  - **They have exactly one reader, and it is a person.** No rule in §4 branches on them and none ever could — the disposition reasoning's whole job is to let a later actor *re-adjudicate*, which is the opposite of a value a parent routes on.
  - **Modelling them would put multi-KB prose into a routing envelope with a 4096-byte posture**, on a transport whose schema is an argument value. The Post-Run Reflection alone routinely exceeds the entire fixed envelope.
  - **A free-text field would make the record the primary copy of prose whose home is the durable one**, which inverts the two records' lifetimes: the envelope lives one parent invocation, the comment lives forever.

  **The cost is paid in the open and the prompt says so out loud:** *"Write them in full. Do not compress them because a typed record exists — it carries none of them and cannot."* That sentence is the whole mitigation, and it is a prompt instruction, which is a weaker guarantee than a schema. **Named as the weaker half rather than dressed up**: the invariant checks that the finding *sets* agree; nothing checks that the reasoning column was written well, and nothing can.
- [x] Preserve the retrievability convention, **and preserve the EMITTER's shape rather than the readers'** — the prompt now states the fence requirement explicitly, and `count_prior_passes` was **fixed** rather than carried forward: it was `"pr_review:" in body`, matching any comment merely *mentioning* the key, and it is now `helper.PR_REVIEW_BLOCK`, fence-anchored and declared once. `latest_pr_review_block` uses the same declaration with §6.2's last-wins ordering. **`children/review-pr.sh:142` carries the identical defect and is deliberately NOT fixed** — frozen fleet — so issue **#68** stays open on that half
- [x] Verify against Phase 2's consumer list that every reader still reads what it needs — checked against [`memory-model.md` §4.1](../../guide/memory-model.md) rather than assumed:
  - **`/standup` reads `verdict` and `next_steps[]` (`standup.md:56-59`) and block presence.** All three are unchanged: no field is removed, renamed or re-spelled, and `verdict:` keeps its `MERGE`/`HOLD` spelling. **This item previously claimed `/standup` was a fourth declaration of the address, "matching by mention in prose". That is withdrawn — the line does not say that.** `standup.md:56` reads *"find the LATEST comment containing a `pr_review:` **yaml block**"*: it names the block form. And the distinction is not merely that a prompt differs from code — the three code declarations are **executable matchers with a comparable `.pattern`**, which is what makes §6's *"declared once and loaded"* both meaningful and gateable; a prompt has nothing to load. **Phase 4's sweep does not inherit this**, and it was carried unchecked through three passes before anyone opened the file
  - **`replay_pr_review_blocks.py` was already fence-anchored** and is untouched
  - **`review_pr_activities.py` was one of the two over-matchers and is now correct**, which changes a value a consumer reads — `pass:` will be right going forward. That is the defect being fixed, not a break

#### Step 8 — The spellings the two copies do NOT share, and why they stay

**There are TWO, and the draft's ruling named one.** [Phase 4](phase4_fleet_migration.md) migrates nine more call sites reading this section as the complete mapping list, so the omission is the kind that propagates:

| Record (typed) | Block (durable yaml) | Scope | Live consumer of the block's spelling |
|---|---|---|---|
| `outcome: merge \| hold` | `verdict: MERGE \| HOLD` | envelope-level, both | `/standup` classifies on `HOLD` (`standup.md:56-59`) |
| `hold_kind: redispatch \| needs_ruling` | `hold_kind: redispatch \| needs-assistance` | **envelope-level in the record; PER-FINDING in the block** | `next_steps[].kind`, and `routing.py`'s prose token |

**Two spellings of one value is exactly the shape §6 exists to prevent — and unifying them is the wrong call here.** The block's spellings have live consumers, and changing them would break Phase 2's consumer list to make two documents look alike. `needs_ruling` versus `needs-assistance` additionally carries the rename step 4 ruled deliberately: the incumbent token names *who is called*, the new one names *what is missing*, and the wire keeps the old one. So the ruling is: **the record is authoritative and the block is its rendering**, both mappings are stated at the one place that writes both (`disposition.md` Stage 6a and 6b), and the invariant covers the finding ids **and their dispositions** — the fields where drift would actually be silent.

**The scope difference in row 2 is the sharper trap and is called out rather than left in the table.** The block's `hold_kind` is per-finding; the record's is one aggregated value on the envelope. A Phase 4 renderer that mapped them field-for-field would write a per-finding token into an envelope slot, and the aggregation rule (any `needs_ruling` beats every `redispatch`) is the child's to apply — step 1 records why a parent must not re-derive it.

### Close-out

- [x] **Every requirement above is met and its evidence is in this doc — EIGHT OF EIGHT (2026-08-10).** Requirements 1–5, 7 and 8 were met at build and each names the artifact that proves it. **Requirement 6 is now met on both halves**: its first (one parent routes on the typed record end-to-end, prose still emitted, disagreement is a loud failure) at build, and its second by [the run set](#step-7--the-run-set-nine-live-dispatches-and-the-three-numbers-it-yields) — nine live dispatches, the agreement count recorded, every disagreement traced to its cause. **The two limits the run set carries are stated beside the numbers rather than folded into them**: it is a run set and not a rate, and the instrumented denominator is conditioned on the prose channel succeeding. Neither is an unmet part of requirement 6 — the first is [Phase 4](phase4_fleet_migration.md)'s by [step 4](#step-4--how-the-two-arms-get-measured-separately-so-the-prediction-becomes-a-number)'s own ruling, and the second is capability that does not exist, placed as **C-060**. *Original text:* ~~SEVEN OF EIGHT … This box stays unchecked until the run set lands~~
- [x] The prose channel is still live and unchanged — `disposition.md` Stage 6b is byte-identical in its instruction, every parent still parses the same token, and `routing.py` is untouched. **Retiring it is [Phase 4](phase4_fleet_migration.md)'s decision**, and it now has a shadow to make that decision against rather than an argument
- [x] Any standards implication is surfaced in the [roadmap](roadmap.md#standards-amendment-candidates), not written — **with one change of governance this phase operated under and the checklist predates.** The `§ Composition` amendment (candidate 1) is **untouched**: it waits on this phase *proving* the replacement, which requirement 6 has not yet done, and amending it now would be exactly the "standard amended on the strength of a plan" this bullet warns about. What *did* change is that [`standards-governance.md`](../../../config/rules/standards-governance.md) now states a dispatch **opening a PR** against a standards file satisfies human-in-the-loop, so candidates **6 and 7** — both edits to the *draft* Exit Protocol, both fully specified — were applied rather than left waiting on a ratification they had no real dependency on. **The protocol is NOT ratified**: the banner stands, and every `⟨PHASE N⟩` marker this phase did not answer is still there

#### Close-out — what the correction pass changed, and the one pattern behind it

**Eight defects, and seven of the eight were in the guard machinery itself rather than in the design.** Recorded together because the *distribution* is the finding, not any single item:

| What was wrong | Where the argument was, when the check was not |
|---|---|
| R6 routed a `merge` carrying a `hold_kind` — a record whose author said a human must decide — straight to MERGE | [protocol §4](../../standards/exit-protocol.md) argued at length that R9 is reachable because R6–R8 do not exhaust `outcome` × `hold_kind`, and named one of the three unhandled cells |
| `route(None)` reported `permission_denied` where step 5 above rules `record_absent` | step 5 states the rule in its own words; the test asserted the *arm* and not the *reason*, so it was green under both readings |
| The render↔record invariant compared against **any** block on the thread, and on ids only | step 8 explains in four lines why the two copies are *"checked rather than trusted"* |
| The completion gate's schema branch accepted a verdict printed at any turn | step 7 above is a whole section on catching the near-deletion of that gate |
| `--dry-run` raised `TypeError` — the one zero-spend pre-flight, dead | `run_review`'s own docstring records this exact dry-run/real-path drift happening once before |
| The computed arm's rate had nothing to compute over | step 4 defines the predicate and calls the arm the reliable one |
| `_redact` raised on a non-dict denial entry, from inside the routing contract | step 5's own heading is *"the fail-safe contract's own fail-safe"* |
| `ExitRecord.needs_human` was unused, and its only distinguishing behaviour was to be **right** where the used predicate was wrong | it is a second declaration of the routing question, inside the module written to forbid second declarations — the same defect this phase already caught once, at `as_prose_verdict` |

**The pattern, stated so the next phase can be checked against it: this component reasons at a higher altitude than it verifies.** Every entry above is invisible if you read the comment and visible in about ninety seconds if you read only the code, and in three cases a *shipped test* asserted the weaker of two properties the prose claimed. That is not an argument against the thesis — *"put the load-bearing fact in a typed record and check it in code"* is exactly right, and the enumerable schema is why these were findable at all. **It is an argument that the prose is not the check, and [Phase 4](phase4_fleet_migration.md) should treat a well-written justification as the place to look hardest rather than the place to stop.** The concrete habit: **enumerate the product instead of arguing about it** — the `outcome` × `hold_kind` table now in §4 is six rows and would have caught two of the eight.

All eight fixes carry mutation evidence. Two conformance gates §6 requires and the draft left open are now built: the Kind 1 **shared parse** is asserted identical across both Python declarations (the schema half already had a gate; the address half is the *measured* one), and the shipped prompt's field table is asserted against `CHILD_SCHEMA`.

#### Close-out — the review pass's three, and why all three are one finding

**Three more, and the distribution is again the finding: every one is the component's own §6 rule failing inside the component that enforces it.** Recorded separately from the eight above because they were found by a *different* actor reading the same tree — which is the argument for the review stage existing.

| What was wrong | Why it is the same defect three times |
|---|---|
| `review_pr_helper.parse_verdict` was a byte-identical retype of `routing.parse_verdict` — on the **shadow channel's own route** | §6 names `parse_verdict` as the defect it exists to prevent. Issue **#34** named both copies and was **closed on a half-fix**; the survivor was in the module whose comparator exists to notice channel divergence, so **the comparator was the divergence** |
| R1's two branches both reported `permission_denied`, so *"the control fired"* and *"the parent could not read the key"* shared a bin | The reason **is** the payload — the same sentence step 5's own note already recorded at R2, one rule down. [Protocol §4](../../standards/exit-protocol.md) *asserted* the two were distinguishable; the code made them identical |
| The one-declaration gate covered **two of four** shared regexes, and the ungated pair had **already drifted** | The gate was written for the address and the same commit added a second pair it did not name. Those feed Phase 5's stopping predicate and the render↔record invariant, so a disagreement would have both suites green while the convergence rule and the write-time guard disagreed about whether work is closed |

**Each was closed as a CLASS rather than as the instance found**, which is the difference from #34's closure:

- `parse_verdict` is now gated by an **AST scan of the whole tree** — any `def parse_verdict` outside `routing.py` fails, including in a module nobody has written yet — plus an identity assertion catching a runtime wrapper the AST cannot see. Two identity checks would have closed the two known copies and said nothing about the third, and a third copy is precisely what #34's closure produced.
- The address gate is now an **enumerated table** with a completeness assertion: a regex added to either reader fails the gate until it is either paired or explicitly declared one-sided. The previous shape — one assertion per remembered name — is what let the second pair ship ungated.
- R1's split fixes the instance; the class is *"[protocol §4](../../standards/exit-protocol.md) asserts a property the code does not have,"* and **that has now fired twice on this component with every test green** — the R2 row against `route(None)` in the correction pass, the R1 sentence in the review pass. So §4's reason column is now **parsed out of the markdown and compared against `UndeterminedReason`**: a member the protocol does not document, or a documented string the code never writes, fails. Its mutation evidence includes **re-creating the R2 defect's exact shape**, which the gate catches.

**The generalisable form, and it is the one this phase keeps re-learning: a gate whose scope is a hand-kept list retires itself the moment it passes.** It can only ever confirm the instances already found. The three closures above are all the same move — replace the remembered list with an enumeration of the real population (the tree, the module's regexes, the protocol's table).

> **One thing this deliberately does NOT gate: the row-to-rule mapping.** §4's table says which *rule* emits which reason, and the gate compares only the vocabulary. Binding the mapping would mean asserting the routing order out of prose, which is the [step 5 note](#step-5--the-fail-safe-contracts-own-fail-safe-what-happens-when-the-parent-cannot-evaluate-a-rule)'s territory and is already covered by the per-rule tests. **Naming the gate's scope matters because a reader seeing "§4 is gated" would otherwise assume more than is true.**

#### Close-out — which `⟨PHASE 3⟩` markers were resolved, and which were left

Resolved: §1's Kind 1 reference form · §2's field list, per-field consumer and publish classification, and size bound · §3's abstention members, names and emitters, and the precedence rule · §4's rule ordering · §5's versioning mechanism and compatibility window. Plus §1's `⟨PHASE 2⟩` marker (candidate 7) and §6's one-declaration rule (candidate 6).

**Left, deliberately:** §6's `⟨PHASE 4⟩` — *"every child emits a conforming record; every parent routes only on the record"* — which is fleet-wide and is not this phase's to answer on one pair. **The Transport row and §3's precedence conditional were resolved by [Phase 1](phase1_measure_the_channel.md) and were not re-derived here**; the Transport row gained §2.4, which is an addition to that measurement rather than a re-take of it.

---

## Notes and gotchas

- **The case for this phase is robustness and measurement, not a demonstrated defect.** No evidence shows the incumbent producing a wrong route; it is fail-closed, its vocabulary is three closed tokens, and it fails loud on absence. What the evidence establishes is narrower and sufficient: it is the one arrangement the corpus never ships without a write-time gate, and its failure mode is **silent** — a prose format change moves the token and the fail-closed default converts that into a spurious human-assistance route rather than an error. Phase 1 E5 is what turns this from an argument into a number.
- **Three of the incumbent's protections live in the channel, not the format** — `mktemp` freshness, per-invocation isolation, and the anchored URL regex that guarantees the extracted PR number is digits from github.com. Step 3 carries the first two; [Phase 4](phase4_fleet_migration.md) carries the third. A migration that moves the format and leaves these behind is weaker than what ships.
- **Arrangement A has a real cost and this doc must carry it.** Everything the human reads must be expressible in the typed record, or the render loses it. GitHub's SARIF consumer implements a documented subset and ignores the rest — that is the general shape of the loss, and Phase 2's prose enumeration is what keeps it from being discovered by an operator noticing something missing.
- **Do not cite SARIF or Conventional Commits as precedent for arrangement A.** SARIF is arrangement C (sibling fields on one object); Conventional Commits presupposes a rebaseable pre-merge artifact and a posted PR comment is not one.
- **Do not put the machine channel in git notes or commit trailers.** That family is metadata *about* a durable artifact and the corpus never routes a process outcome with it.
