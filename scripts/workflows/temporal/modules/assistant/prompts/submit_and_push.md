- **Update the PR's SELF-DESCRIPTION**: the PR body must describe what the PR NOW contains, and docs/file_structure.txt must reflect any files added
- **RE-READ THE WHOLE BODY AGAINST THE TREE, not just the part you wrote.** The body is the one artifact SEVERAL runs share, and appending your section while leaving theirs unverified is how it goes stale with everybody having followed the rule. **Open every path it names and confirm it exists; check every count and filename against what is on disk now.** A body that describes a file a later run renamed is a body that misleads the reviewer, the operator and the next dispatch — and none of them can tell it is stale without doing the check you skipped.
- **This has now happened twice, on two different families.** `C-hdme5l4k` filed it for `research_refine`, whose corrections traced into the synthesis and not into the body. It then recurred across a four-run planning chain where **every run updated its own artifact correctly** — which is precisely why nobody caught it: there is no run whose job the body is, so it is nobody's until it is everybody's./removed/renamed. A fix that leaves the PR's own description stale mechanically manufactures findings for the next review pass (measured: 1-2 per round, and one pass found ZERO code defects — only self-description drift).
- **MERGE `origin/main` INTO YOUR BRANCH NOW, then re-run the suite — not only check that it moved.** `git fetch -q origin main && git merge origin/main` (your worktree is disposable; resolve any conflict, then run the suite again). **Checking `git log <base>..origin/main` tells you a commit LANDED, not that your work still WORKS beside it** — and the collisions that matter merge cleanly and are wrong: a pinned integer bumped on both sides, a swept-population count, a package's re-export list. Those are invisible to `git log` and to any guard run against your branch alone. **A run that allocated an id, a filename or a number from the tree it read at Stage 1 is allocating from a stale tree by now** — measured: two candidate-id collisions in one day, one of which would have merged silently. If the merge conflicts or the suite goes red, that is the finding; fix it here rather than letting CI find it after the push.
- **AND CHECK THE PR'S OWN HEAD HAS NOT MOVED:** `gh pr view ${PR_NUMBER} --json headRefOid --jq .headRefOid`. If it is not the commit your branch descends from, **another dispatch pushed to this PR while you worked.** Do not force. Fetch it, read what it did, and say so in your report — two runs' work merged blind is the expensive collision, and it is invisible in your own diff.
- Push the branch (this updates PR #${PR_NUMBER}) **CHECK YOU ARE ON A BRANCH FIRST — the worktree may hand you a DETACHED HEAD.** `git rev-parse --abbrev-ref HEAD`; if it returns `HEAD`, you are detached and `git push -u origin HEAD` fails with `refs/heads/HEAD`. Create the branch (`git checkout -b <name>`) or push explicitly to a ref (`git push origin HEAD:<branch>`). **Asked for on five separate reflections across four PRs** — runs keep losing turns rediscovering it, and every wording of the instruction below says "the branch" as though one exists.
- **As your FINAL line, print the PR URL** — run `gh pr view ${PR_NUMBER} --json url --jq .url` and print the result. This is the run's completion signal. On this path you UPDATE an existing PR rather than creating one, so nothing else emits the URL; a run that ends without it is misread as an early-stop failure even though the work succeeded.
- Report a summary of the entire workflow
