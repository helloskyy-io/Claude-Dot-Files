# Fixture metrics specification (synthetic)

This file is a fabricated stand-in for a metrics specification. It exists so
the citation fixture has bytes to store; nothing in it describes a real system.

## fixture.usage.tokens

Counts the fixture tokens consumed by one invocation, split by direction.

## fixture.agent.calls

The number of fixture model calls one agent invocation makes before it returns.
