---
id: C-yq30mgwd
title: A child prompt may hold a drifted near-copy of a shared pool fragment and NO guard can see it at any granularity, so promotion silently stops being the single source of truth the standard sells it as
status: open
count: 1
filed: 2026-08-26
filed_by: triage-candidates
component: workflow-decomposition
---

**Both duplication guards exclude the pool BY CONSTRUCTION**, so the child↔pool axis has no detector: `test_prompt_blocks_are_shared_not_copied.py:118-119` skips it (`if p.parent == SHARED: continue`) and `test_tier_siblings_do_not_DRIFT_by_a_sentence.py:170` defines `SHARED` only to scan children against each other. **Live instance, measured in this branch's tree rather than inherited:** of the 12 blocks in `modules/assistant/prompts/mutation_discipline.md` (counted at the duplication guard's own `MIN_BLOCK = 120`), two survive in `build/build_refine/prompts/stages_2_to_4.md` as drifted copies — *SIZE THE CHANGE, THEN SET THE BAR* at **0.966** and *ASK WHAT EACH GUARD DOES NOT LOOK AT* at **0.925** — while the whole-file ratio is **0.117**, so a whole-file comparison hides both and a verbatim hash sees neither (**zero** blocks are byte-identical). `build_refine` does not load `${MUTATION_DISCIPLINE}` at all — only `build_draft` and `build_draft_minor` do (`build_draft_workflow.py:57`, `build_draft_minor_workflow.py:49`). **This is a PROPOSAL, not a defect this PR introduced:** the duplication predates the branch, and reconciling it is a content ruling rather than a mechanical edit — the two copies have each drifted by a clause, and `build_refine` would share only 2 of the fragment's 12 blocks, so loading the whole fragment is exactly the question **C-at80groo** exists to ask. **Read with C-at80groo:** those two blocks are also two of its named ten, so the rows are distinct mechanisms over the same text. **NOT an expansion of C-uva9dsox**, which covers the pool's OWN content (promotion-time byte fidelity, and post-promotion deletion from a fragment); this is the child→pool DUPLICATION axis — a different detector and a different remedy. **Scored the way this PR's own guard scores** — `difflib`, `autojunk=False`, symmetric max, per `_pair_score` at `test_tier_siblings_do_not_DRIFT_by_a_sentence.py:423-447`. **THE SETTING IS THE WHOLE DISCREPANCY, and an earlier draft of this row got the diagnosis backwards.** It read *"the pass that surfaced this reported 0.925 and 0.117; re-measured here at 0.844 and 0.061"* — as though the earlier figures had failed to reproduce. Both pairs reproduce exactly, in this tree, today: 0.844/0.061 are `difflib`'s DEFAULT, and 0.966/0.925/0.117 are the same comparison with `autojunk` off. That default is the scorer artifact this very PR removed from `_pair_score` after it scored one line at 0.366 and 0.701 depending on argument order — so a row carrying the autojunk-ON numbers would re-import the artifact the same change was written to eliminate. The autojunk-free figures are the ones stated. (The first block's 0.966 is identical under both settings; only the second moves.)

**Source:** PR #100 `review-pr` pass 4

*Migrated from `docs/standards/architecture/research/candidates.md` on 2026-08-26, preserving its id.*
