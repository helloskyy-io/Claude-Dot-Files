---
id: C-rm2g8ope
title: workflow-scripts.md` § *A prompt block with two consumers is promoted* forbids a TEST from ruling on near-duplicates, and does not say whether a surfacing-only detector is a test in that sense — so the same sentence licenses and forbids the guard PR #100 shipped under it
status: open
count: 1
filed: 2026-08-26
filed_by: triage-candidates
component: workflow-decomposition
---

**The sentence, verbatim:** *"That half is a judgement about which differences are deliberate, and it belongs to the fork-vs-parameterize ruling, not to a test."* It is the closing paragraph of the section that created the shared prompt pool, and it is there to stop a test from deciding that two drifted paragraphs must be collapsed. **Why it needs a ruling rather than a reading.** PR #100's second correction pass shipped `test_tier_siblings_do_not_DRIFT_by_a_sentence.py`, which freezes today's seven near-duplicate pairs between a workflow and its own `_minor` sibling, rules on none of them, and fails only when the SET grows — with a failure message that offers reconciliation and *record-it-as-deliberate* as equal exits. `standards-auditor` read § 715 as forbidding a test that DECIDES and permitting one that SURFACES, citing that the same section sanctions exactly this shape two paragraphs earlier for the verbatim half (*"freezes the duplication that existed when the rule landed and ratchets both ways"*). `quality-control` agreed the reading is plausible and flagged that settling a binding standard's ambiguity inside the PR that ships the tooling is the wrong actor. **Both are right, which is the finding.** **The consequence is not this guard, it is the next one.** The near-duplicate half was named as a known blind spot and left unguarded for as long as the sentence has existed; the next author who wants to close it reads the same sentence and gets no answer, so the outcome depends on which agent reads it. **Measured on this PR:** the two drifts that guard now covers were live for the whole life of the branch, ran through a draft pass and a full correction pass, and were found by a review pass reading the two files side by side — which is the manual labour a detector exists to remove. **Done-state today: yes, and it is one sentence.** Either § 715 gains a clause permitting a surfacing ratchet and naming the two-exit failure message as the condition, or it gains one forbidding it and the shipped guard is withdrawn. **PROPOSAL, not a defect:** nothing is wrong today under either reading — what does not exist is the sentence that makes the reading unnecessary. **Not an expansion of C-lg7lpge9**, which proposes a fidelity check on the ACT of promoting; this is about whether a class of guard may exist at all. **This row is placed rather than surfaced because a standards amendment is a proposal and `standards-governance.md` § *research/ is a WORKING SURFACE* names this file as where an autonomous run puts one — the amendment itself is human-in-the-loop and is not made here.**

**Source:** PR #100 `build-refine` (second correction pass), raised by `quality-control

**Routing note, for `triage-candidates`.** This reads as a proposed amendment to the TEXT of a named standard, which [§1](../../docs/standards/documentation/tracked_items_standard.md) routes to `tracked/standards/` rather than here. **It was not moved during the migration**: an id is immutable (§2), a prefix change is an id change, and this id may be cited elsewhere in the planning corpus. Rule on it — if it moves, mint a fresh `S-` id, carry the reasoning, and leave a pointer here.

*Migrated from `docs/standards/architecture/research/candidates.md` on 2026-08-26, preserving its id.*
