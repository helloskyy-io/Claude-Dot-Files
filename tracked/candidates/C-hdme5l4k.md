---
id: C-hdme5l4k
title: Trace a paper's corrections into the PR BODY — `research_verify` Stage 2 names the synthesis as the only dependent, so a merge-time reader can be handed the over-claim the correction rounds just removed
status: open
count: 1
filed: 2026-08-26
filed_by: triage-candidates
---

**Stage 2 of `verify.md` binds the Research Standard's *a corrected fact traces to ALL its dependents*, and then enumerates the dependents as the draft SYNTHESIS only.** The PR body is not in that set and is a real dependent: it is written by the producing run BEFORE any correction round exists, and it is the first artifact a human reads when ruling on the merge. **Two instances survived three correction rounds and a full `review-pr` pass on this PR, in the same body.** (1) A source count of 13 against the paper's §8, which enumerates 15 — `[R10]` and `[R11]` were added by round 1 and the body was never re-counted; the breakdown beside it was stale in two of its five terms. (2) **The consequential one:** the body stated the negative finding as *"no prompt/LLM-engineering literature on divergence between copies of a prompt exists yet"* — the exact does-not-exist over-claim that round 3 removed from the paper's §3.5 heading and §8 after `[R6]` was found to be a vision paper rather than a systematic survey. So the paper says *not found by these methods on this date* and its own PR body said *does not exist*, and the reader ruling from the body carries forward a claim the gate specifically corrected. **Both were fixed in this PR by the verify pass; the row is for the missing trace, not the instances.** Remedy is one clause — name the PR body in Stage 2's dependent set, beside the synthesis, and require the body's derived figures be re-enumerated at the end of the last correction round. **The count half is ALSO a recurrence of C-523klr8n** (a figure restated where nothing derives it) and is reported here rather than added to that row, which has other filers; the over-claim half is not a count and C-523klr8n does not reach it. **Distinct from C-4pr7kq11 and C-iceozlh1**, the two other `research_verify` rows: C-4pr7kq11 is about normalisation of a verbatim span, C-iceozlh1 about which spans a round re-downloads — neither is about which SURFACES a correction has to reach. **This row was placed as C-lg7lpge9 and renumbered to C-hdme5l4k at the merge that found the collision:** `origin/main` landed a different C-lg7lpge9 (the retry-policy row) while this branch was open, and that one is merged, so it keeps the id. Same shape as C-npx1uwgj's renumbering note, and the third recorded instance of the allocation collision C-yi65bngf proposes fixing.

**Source:** PR #102 (research_minor, verify correction pass)

**Routing note, for `triage-candidates`.** This reads as a proposed amendment to the TEXT of a named standard, which [§1](../../docs/standards/documentation/tracked_items_standard.md) routes to `tracked/standards/` rather than here. **It was not moved during the migration**: an id is immutable (§2), a prefix change is an id change, and this id may be cited elsewhere in the planning corpus. Rule on it — if it moves, mint a fresh `S-` id, carry the reasoning, and leave a pointer here.

*Migrated from `docs/standards/architecture/research/candidates.md` on 2026-08-26, preserving its id.*
